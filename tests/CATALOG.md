# Каталог тестов — зоны ответственности + правило «не плодить»

> Зачем: чтобы **развивать существующие** тесты, а не плодить. Каждый тест имеет ЗОНУ ОТВЕТСТВЕННОСТИ (что покрывает только он) и ЗАПАС РАСШИРЕНИЯ (что ещё впитает). Новый тест заводим ТОЛЬКО когда сценарий вне зон ВСЕХ существующих И запас хозяина-кандидата исчерпан.
>
> Полный тест-план (E-матрица, слои, приоритеты) — `docs/roadmap/03_testing_plan.md`. Угрозы для симуляций — `docs/roadmap/06_threat_catalog.md`. Скил — `test-master`.
>
> **Машинерия тестирования** (раннер, фикстуры, харнесс живого сервера, гейт, покрытие) — `docs/roadmap/14_testing_system_plan.md`, этапы T0–T10. **T0 закрыт (S18):** гейт = `pytest -m "not live"`, наборы находит `tests/test_suites.py` сам (новый файл попадает в CI без правки `ci.yml`); напрямую набор по-прежнему запускается скриптом (`python3 tests/<...>/test_<...>.py`, exit 0 = ok).

## Правило «не плодить» (жёстко)

1. **Сначала — расширить существующий** в его зоне ответственности (добавить сценарий/параметр/вектор). Каждый тест ниже указывает свой запас расширения.
2. **Новый тест ТОЛЬКО если оба верны:**
   - (а) сценарий **вне зоны ответственности ВСЕХ** существующих тестов, И
   - (б) **лимит расширения** естественного хозяина исчерпан — впитать сценарий сделало бы тест разнородным/нечитаемым/смешало бы уровни (unit vs симуляция vs e2e).
3. **Перед новым — «не дублирую ли?»** (README §4): найти похожий, проверить, не решалось ли.
4. **Размещение нового** (project-rules §3): быстрый/гипотеза → `tests/quick/` (после прогона удалить); постоянный → `tests/<name>/`.

## Два класса тестов

- **Простые** (`tests/quick/`) — in-process / unit / contract / регрессия. Быстрые, точечные, ассерт на контракт `ToolResult`/`ErrorDetail`/код реакции. Часть постоянные (регрессии), часть — «создал→прогнал→удалил».
- **Симуляции** (`tests/<name>/`, adversarial/system) — сценарные, многовариантные, против `core/firewall` / живого сервера. Первый класс: проверяют, что защита реально ловит И подключена. Развивает их скил `test-master`.

## A. Простые (`tests/quick/`)

| Тест | Зона ответственности (ТОЛЬКО он) | Зачем | Запас расширения (впитывает) | Новый рядом оправдан, если… |
|---|---|---|---|---|
| `test_audit_fixes.py` | **Дом ВСЕХ регрессий закрытых `D#`** (D1–D13) | откат фикса → красный | +регрессия на каждый новый закрытый `D#`/`F#` | никогда — это единый дом D#-регрессий |
| `test_firewall.py` | firewall happy/block контракт (injection/rate/IP) против живого сервера | защита отклоняет атаки, пропускает легит | новые firewall-правила, векторы IN2/IN3/IN5, identity-rate | правило переросло в отдельную СИМУЛЯЦИЮ (→ dir) |
| `test_search.py` | `core/search` coverage+регрессия (FsSearcher/QueryPlanner, D36 traversal) **+ личность файлов из реестра и фильтры owner_id/chain_prefix (F60)** | search-контракт/поведение | relevance-eval (E-I), новые `search_*`, poisoning-outbound | search-качество как отдельный eval-слой перерастёт unit |
| `test_structure.py` | `TemplateEngine` Ф1 (depth-control, PATH_ESCAPE, ID) **+ система ID: таксономия из шаблонов, реестр связей, резолвер цепочки, ручная ветка ФС (S18-g/S18-h)** | структура/шаблоны/глубина/адресация | E-A/E-B/E-C эмуляция, `structure_link/migrate`, F25 reconcile | эмуляция «реальной работы ИИ» станет тяжёлой сценарной (→ dir) |
| `test_tables.py` | table/excel smoke-контракт (Кат.2+3) | инструменты таблиц отвечают контрактом | E-D деструктив, формулы/устойчивость (F30), `table_materializer` (Ф3) | деструктив-над-таблицами станет adversarial-симуляцией |
| `test_tunnel.py` | `core/transport/tunnel` парсер+автомат оффлайн (D11) | регрессия логики туннеля без cloudflared | новые режимы туннеля, форматы логов | — (узкая стабильная зона) |
| `test_providers.py` | **Провайдерский слой целиком**: реестр адаптеров, опись и установка моделей, спеки `model_specs.yaml`, расход, ключи канала, локальные адаптеры (piper/sd-turbo/onnx) | подъём модели и отказ провайдера — оба должны быть слышны | новые адаптеры, новые виды ресурса, спеки моделей | — расширяем; это дом P-оси |
| `test_uniqueness.py` | `core/uniqueness`: n-gram-слой, «тихий столбец» из данных канала, `readiness` full/partial/empty и `fragment_gaps` | «нет данных» не должно выглядеть как «ноль уникальности» | листы сцен, реакции-рекомендации A7 | — |
| `test_tools_inventory.py` | **Контракт инвентаря** `tools/list`: состав (сегодня 68, число живёт в эталоне, а не здесь) + group/title/description/annotations/input_schema против эталона `tools_inventory.golden.json` | структурные правки (A2-распил, переезды групп) не двигают контракт клиента молча | новые инструменты/группы (эталон обновляется `--bless`, diff виден в ревью) | никогда — это единый дом инвентаря; поведение инструмента проверяют тесты его зоны |
| `test_table_materializer.py` | **A1′: материализация книг по `.schema.yaml`** — фаза ТАБЛИЦЫ, типы столбцов, `enum`, связи листов | схема объявлена — книга обязана родиться именно такой | новые схемы `config/templates/tables/`, фаза ФОРМУЛЫ | — расширяем; дом материализации |
| `test_montage_declarations.py` | **декларации монтажа M0**: профили рендера, `SCENE_ELEMENTS`/`SCENE_AUDIO`, обложки | рендеру нечего читать, если декларация противоречит себе | этапы M1–M7 плана `19` | — расширяем; дом деклараций монтажа |
| `test_ffmpeg_filters.py` | **словарь переходов/фильтров против БИНАРЯ `ffmpeg`** | объявленный фильтр может отсутствовать в сборке — узнать об этом на рендере поздно | новые фильтры и переходы | — (узкая зона «объявлено ↔ есть в сборке») |
| `test_ffmpeg_engine.py` | **движок монтажа**: сборка команды, запуск, приёмка результата | команда собрана верно, а файл принят по факту, а не по коду возврата | профили рендера, аудио-дорожка, несколько дорожек | — расширяем; дом движка |
| `test_montage_tools.py` | **инструменты монтажа**: замысел сцены из книги → проверенный файл | инструмент отвечает контрактом, а не «сработало» | новые инструменты плана `19` (6 в MVP) | — расширяем; дом инструментов монтажа |
| `test_invariants.py` | **сам сторож `scripts/guards/invariants.py`** на временных каталогах: каждая проверка ЛОВИТ своё нарушение и МОЛЧИТ на чистом; сюда же ось «объявлено ↔ ждёт сценарий» (`declared_but_unscripted`, `codes_without_emitter`, `scenario_calls_unknown_tool`) и ось «факт ↔ реестр типов ↔ наблюдатель» (`facts_outside_registry`, `facts_without_observer`, `facts_without_emitter`) | сторож без обеих сторон ослабляется незаметно | новые межфайловые инварианты | никогда — это единый дом проверок сторожа |
| `test_trail.py` | **запись и её потребитель**: писатель следа (`core/observability` — удержание файлов, предел, неполное объявление, отказ записи) И производитель (`scripts/guards/reproduce.py` — отбор отказа, форма порождённого объявления, решётка независимых вызовов и её сужение зависимостями; подпись вывода `_stamp` и её разбор по ключу) | обе половины читают ОДНУ форму записи: разъехавшийся формат ломает их сразу, а порознь этого не видно | второй источник записи (протокольные отказы файрвола), новые роды наблюдений | никогда — это единый дом записи; поведение «отказ попал в след» живёт в сценариях `observability.yaml`, а не здесь |
| `test_hooks.py` | **хуки `.claude/hooks/` — решение по СОБЫТИЮ сессии**: гейт фактов (несущий файл, рождение набора, запись через оболочку, обе ложные тревоги), обоснование текста в коде, шимы в сторожей (включая форму того, что правка оставила в дереве: компиляция и линтер проекта), отличие заявления о зелёном от пожелания | вход хука — событие (`tool_name`/`tool_input`/`session_id`) и его состояние с TTL, а не дерево: хозяева `test_invariants`/`test_comment_guard` судят дерево, и впитать это значило бы смешать уровни; объявлением не выражается — переход карты умеет `call` инструмента СЕРВЕРА, а хука в описи нет | новые хуки и новые роды событий, новые рубильники | никогда — это единый дом решений по событию сессии |
| `test_comment_guard.py` | **сам гейт текста `scripts/guards/comment_guard.py`**: что обязан ловить и что обязан пропускать | храповик, который перестал ловить, выглядит как зелёный | новые классы текста в коде | никогда — это единый дом гейта текста |
| `test_what_if.py` | **отчёт сторожа `scripts/guards/what_if.py`**: заявленное отделено от незаявленного, исчезнувшая проверка не выдаётся за «цвет не сменился», ПОЯВЛЕНИЕ и исчезновение объявляемы наравне со сменой цвета (у одного имени исходов бывает несколько — вставка шага рождает новые проверки и уносит старые под новым номером), новый файл доезжает в дерево кандидата, а в базовую линию — НЕТ (иначе сравниваем правку саму с собой), роспись трёх карт полна (у каждого сторожа есть набор-дом) и одна метка на несколько проверок не съедает соседей | сравнение двух деревьев стоит минуты и в быстрый набор не влезает — суть проверяется подставленными вердиктами | новые роды исходов сравнения (появилась/исчезла/сменила цвет) | никогда — это единый дом отчёта сравнения |
| `test_dependencies.py` | **манифест ↔ настоящие импорты**: сторонний импорт мимо `pyproject.toml` и объявленная зависимость, которую не читает ни `import`, ни команда раннера, ни `[build-system]` | лок-файла больше нет, `pyproject` — единственный источник; расхождение всплывает на ЧУЖОЙ машине при установке с нуля, а не у нас | новые роды читателей объявления, дрейф extras | никогда — это единый дом манифеста зависимостей |
| `test_blast_radius.py` | **вторая улика сторожа радиуса `scripts/guards/blast_radius.py`**: статическая ось ЛОВИТ вызывающего без сценария, МОЛЧИТ на покрытом и при отсутствии графа никого не обвиняет | внешний источник улики отваливается молча: пропуск выглядит как «чисто», а ложное обвинение выключает сторожа целиком | новые роды улик о радиусе, разбор ответов графа | никогда — это единый дом сторожа радиуса |
| `test_findings_count.py` | **судья читаемости реестра `scripts/guards/findings_count.py`**: что считается строкой находки и что закрытием, пол ловит ужавшийся разбор, а «улики нет» (нет реестра, нет пола, ноль строк) отделено от «чисто»; сюда же инвариант «у реестра один разбор» | реестр читают два судьи, и разошедшийся вердикт ослепляет `status_off_registry` молча — на замере расходились три находки | новые знаки закрытия, новые роды строк реестра, исходы пола | никогда — это единый дом разбора реестра находок. **Почему не декларацией:** переход карты умеет только `call` инструмента сервера, ни один сценарий не запускает сторожа (замер S25: 0). Предметы судьи — реестр × пол × число строк × флаги — это 54 сочетания, и как только у перехода появится запуск команды, их место в карте, а не здесь |

## B. Симуляции (adversarial / system)

| Набор | Зона ответственности | Зачем | Запас расширения (впитывает векторы каталога) | Новый рядом оправдан, если… |
|---|---|---|---|---|
| `bot_army/` | массовое подключение → rate-limit + ban (**IN3**) | армия ботов реальна | agent-swarm honest+attacker, **identity-rate**, **slowloris** (§H.1), anomaly (IN5) | — обычно расширяем; рой уходит в `agent_swarm/` |
| `cache_injection/` | injection-паттерны в данные/кеш (**IN2/IN8**) | отравление данных | **HTTP-smuggling/malformed/jsonrpc-abuse** (§H.2), cache-key poison (§H.3), deser (IN8) | протокол-атаки перерастут «инъекцию» тематически |
| `cache_overflow/` | устойчивость при переполнении (**IN4/IN7**) | сервер не падает, кеш чистится | **payload-overflow**, cache-**stampede** (§H.3), resource-quotas (DIM-11) | — |
| `config_change/` | адаптация к смене конфига + уведомление (hot-reload, **OUT3 rug-pull**) | конфиг защищён, клиент уведомлён | rug-pull tools/list (OUT3), secret-hygiene (IN10/D31) | — |
| `virus_injection/` | блокировка malware/payload (**IN2/IN6**) | вирус не проходит | **write-allowlist** forbidden-filetype (§F/F34), **no-root** exec-workspace (§G/F35), outbound injection-via-output (OUT1) | — расширяем; это дом «сервер-как-канал-вреда» inbound |
| `conformance/` | **сверка с эталонным набором спеки MCP** сторонним клиентом (`@modelcontextprotocol/conformance`) через прокси, добавляющий ключ | своя схемная регрессия проверяет НАШИ ожидания, чужой набор — спеку; так нашёлся `F103` | новые сценарии спеки, снятие записей из `UNSUPPORTED` по мере реализации | — расширяем; дом сверки со спекой |
| `structure_emulation/` | **E-матрица: сценарии реальной работы ИИ со структурой** целиком через провод (E-A конфиг-матрица ниши · E-B точки входа × глубина · E-C метаморфный инвариант 3≡2≡1) | unit-тест видит единицу, а клиент — сценарий: `F102` (якорь искался по всей области) виден только при нескольких нишах в одной области | E-B/E-C, циклы рекомендаций, `structure_reconcile` в связке с созданием | — расширяем; это дом E-матрицы |
| `test_studio_emulation.py` | **приёмка правки ИИ по дереву студии** (`tests/studio_emulation/`, дерево `app/`): обе стороны четырёх условий, которые судит `scripts/guards/acceptance_studio.py` — компонент не сломан · стиль, шрифты и параметры не сменены · не вышел за рамки задачи и не оставил мёртвого · компонент адресуем маркером. **Естественного хозяина нет по устройству:** у каждого сторожа свой дом (`guard_without_home`), а предмет здесь не питонное дерево, а `.tsx` эмуляции; объявлением это не выражается — переход карты умеет `call` инструмента сервера, сторожа и дерево студии не запускает ни один шаг | студии на диске ещё нет, а правило без исполнителя — тот же класс, что `F213`/`F221`: несудимый сторож ослабляется молча | новые условия приёмки правки ИИ, новые роды поверхности студии (роутер, стор, темы), задания стенда `6dc3` | никогда — это единый дом приёмки правки по студии; поведение сервера остаётся у сценариев |
| `routes/` | **маршрутная таблица потоков данных**: `routes.yaml` объявляет ЧТО течёт, ЧТО выбирает маршрут (`decided_by`), через какие рубежи и ЧТО значит обрыв; вердикт берётся из СЛЕДА прогона сценария-опоры | покрытие по строкам говорит «строка исполнилась», а не «значение доехало до клиента»; закрывшийся разрыв обязан краснеть, иначе реестр находок отстаёт молча | новые маршруты: провайдер→fallback, ID→реестр→путь, конфиг→поведение; наблюдение по покрытию (рубеж не просто существует, а ИСПОЛНЯЛСЯ) | — расширяем; это дом потоков, контракт отдельного вызова остаётся у сценария |
> **Язык шага:** `call` (инструмент) · `rpc` (метод протокола) · `python` (шаг-помощник) · `headers` (подменённый `Origin`, чужой `Host`) · `token` (пустой/неверный ключ) · `repeat: N` (поведение РЯДА вызовов, сводка в `data._repeat`) · `as` (имя для ссылок `${шаг.путь}`). **Совет при провале:** строка провала называет следующий шаг сама — реестр о ПРИШЕДШЕМ коде (класс + `suggested_tool`), развилка «прав сервер / прав сценарий» с готовым грепом, а промах по полю печатает, на каком колене оборвался путь и какие ключи там на самом деле. **Покрытие деклараций** держит `scripts/guards/invariants.py` храповиком: новый код реестра или новый инструмент описи растит долг, и закрывается он строкой YAML, а не новым скриптом. **Ожидание:** `ok` · `code` (включая протокольные `RPC_-32601`) · `class` · `recovery` (`true/false` — есть ли рецепт; ИМЯ инструмента — куда он ведёт) · `facts` · `data` · `data_contains` · `console` · `console_absent` · `http` · `open: F###` (объявлено ЖЕЛАЕМОЕ при открытой находке). **Карта:** `walk.coverage: edges` — каждый переход по разу, `pairs` — каждая ПАРА соседних (порядок вызовов как отдельный источник поведения). **На каждом прибытии спрашиваются ВСЕ предикаты, а не только целевой:** держится чужой — либо состояния неразличимы, либо мы стоим в нём, а связи к нему не объявлено (забытое ребро). Одинаковые предикаты у двух состояний видны статически, до обхода.

| `scenarios/` | **дом ОБЪЯВЛЕННЫХ сценариев и КАРТ** (`*.map.yaml`: состояния-предикаты, переходы, обход по всем переходам): `*.yaml` (`given/when/then/why`) + один исполнитель `tests/harness/scenario.py` против живого сервера; шаг `run:` спрашивает вердикт сторожа кодом возврата, поэтому обе стороны сторожа выражаются объявлением, а не python-набором. Зоны сегодня — **вся опись инструментов по проводу (72 из 72)**: E-D деструктив над таблицами, провенанс вывода, живучесть, файловые операции, отложенная запись и очередь (`tables_ops`), правки книг (`excel_ops`), осмотр данных (`data_inspection`), поиск (`search` — три пресета одного `QueryPlan` различаются проводкой, а не движком), структура и память (`structure_ops`), провайдеры (`media_ops` — контракт отказа вместо весов) монтаж (`montage_ops` — цепочка отказов называет недостающее) и **периметр** (`perimeter.yaml` — ключ, манифест и протокольный отказ; шаг объявляет `rpc`/`headers`/`token`, ожидание — `http`; закрыты ключ, манифест, протокольный отказ, чужой `Origin`, чужой `Host` и тип тела) (`filesystem.yaml` — запись ЧЕРЕЗ инструмент, а не фикстурой: иначе путь записи не исполняется вовсе, F141) | одинаковая обвязка «поднять → позвать → сверить» живёт в одном месте, а различия объявляются; журнал прогона на каждый шаг | E-E/E-F, любые сценарии «вызов → код реакции»; новые состояния и переходы карты (сценарии растут без кода); сложная подготовка — шагом в `steps.py` | — расширяем; новый ФАЙЛ сценариев = новая тема (свой сервер, своя область) |

## C. Рой (декларация + раннер, S24)

| Артефакт | Зона ответственности | Статус |
|---|---|---|
| `agent_swarm/patterns.yaml` | **дом мульти-вариантных/эмерджентных сценариев**: N честных клиентов + N злоумышленников (inbound+outbound) в одном прогоне; 36 паттернов со `status` | ✅ реестр (до S24 не парсился вовсе — F85) |
| `agent_swarm/test_agent_swarm.py` | раннер: исполняет паттерны против ЖИВОГО сервера (харнесс T2), 4 фазы = 4 процесса (файрвол считает по IP — один прогон банил бы сам себя) | ✅ F32 закрыт S24. 28 защищено · 5 известных дыр · 1 «не достали» · 2 отложены |

**Правило роя (иначе он превращается в театр):** паттерн без исполнителя красит гейт, исполнитель без
паттерна — тоже; `xfail-spec` со СРАБОТАВШЕЙ защитой красит («статус устарел» — так нашлись 10 штук);
проба, не достающая до поверхности (калитка провайдера), — отдельный вердикт «НЕ ДОСТАЛИ», не зелёный.

## D. Маршрут «куда класть новый сценарий» (сначала — в существующий)

| Хочу протестировать | Хозяин (расширяем ЕГО) | Новый только если… |
|---|---|---|
| закрытый дефект `D#`/`F#` | `test_audit_fixes` | — никогда |
| firewall-правило/вектор | `test_firewall` (unit) / `bot_army`+`virus_injection`+`cache_*` (сим) | вектор — отдельная угроза вне их тем |
| структура: единица (движок/реестр/инструмент) | `test_structure` | — |
| структура: СЦЕНАРИЙ работы ИИ (E-A/B/C) | `structure_emulation/` (живой сервер) | — это его дом |
| деструктив/формулы таблиц (E-D/E-F) | `test_tables` → сим при утяжелении | нужен живой .xlsx с формулами (Ф3) |
| поиск/relevance (E-I) | `test_search` | eval-качество — отдельный слой |
| протокол/DDoS/пакеты (§H) | `cache_injection`/`cache_overflow`/`bot_army` | — |
| allowlist/no-root/outbound (§F/§G/OUT) | `virus_injection` | — |
| рой honest+attacker (F32) | `agent_swarm/` (раннер) | это и есть дом роя |

**Итог:** почти всё расширяет существующий тест. Реально НОВЫЕ постоянные наборы на горизонте — только `agent_swarm/test_agent_swarm.py` (раннер роя) и, при утяжелении, `tests/structure_emulation/` (E-матрица структуры). Всё прочее — сценарии внутри уже имеющихся зон.

---

## E. Реестр подтверждения находок (F# → тест → статус) — git-tracked

> Зачем: чтобы ИИ видел, какая находка обмера `02` чем подтверждается и в каком статусе. **Статусы —
> в git** (полный прогон тестов в лимит контекста не влезает → трекаем инкрементально, как и историю).
> Метод: **static** = подтверждено чтением/grep/ls (тест не нужен — закрывается lint I4 или постройкой);
> **behavioral** = нужен C1-тест против кода/живого сервера (§6 ступень C1 = код-пруф теории).
>
> Статусы: ✅ подтверждён · ⬜ нужен C1-тест · 🟡 OPEN-CONFIRMED (C1 красный = находка доказана, ждёт фикса) · 🔨 пишется · 🟢 регрессия зелёная после фикса.
>
> **Механика strict-xfail** (`test_audit_fixes.py` `xcheck`): открытая находка подтверждается «ожидаемо
> красным» (`[OPEN-CONFIRMED F#]`), но baseline остаётся зелёным (exit-код держат только регрессии). Если
> находка внезапно «проходит» (`[UNEXPECTED-PASS F#]`) → сигнал: фикс применён → обнови §E ⬜/🟡→🟢 и перенеси в регрессию.

| F# | Что | Метод | Тест-хозяин (не плодить) | Статус |
|---|---|---|---|---|
| **F106** | `Origin` не проверялся по умолчанию (`D12`): чужая вкладка доходила до инструментов, если у неё есть ключ | behavioral | `quick/test_firewall.py` (§2c) + `conformance` (`dns-rebinding-protection`) | 🟢 **ЗАКРЫТ S24** (мутация M120 → красный) |
| **F107** | тип тела не проверялся: `text/plain` — «простой» запрос браузера БЕЗ preflight, им и делается CSRF | behavioral | `quick/test_firewall.py` (§2c) | 🟢 **ЗАКРЫТ S24** (мутация M121 → красный) |
| **F108** | разрешительный разбор `Host`: склейка `свой, evil.com` принималась по первому значению | behavioral | `quick/test_firewall.py` (§2c) | 🟢 **ЗАКРЫТ S24** (мутация M123 → красный) |
| **F109** | ответ шёл без заголовков безопасности и объявлял точную версию aiohttp | behavioral | `quick/test_firewall.py` (§2c) | 🟢 **ЗАКРЫТ S24** (мутация M122 → красный) |
| **F110** | защита ослепила чужую проверку: 415 отвечал раньше парсера, и бомба вложенности (F36) перестала доходить до механизма | behavioral | `agent_swarm` (`atk_payload_overflow`) | 🟢 **ЗАКРЫТ S24** (сырой запрос объявляет тип тела) |
| **F93** | обратный проход целостности не доставал до сгруппированной ветки: `F25` был зелёным только на неветвящемся дереве | behavioral | `quick/test_structure.py` (§40) | 🟢 **ЗАКРЫТ S24** (мутации M110/M110a → красный) |
| **F98** | эталон инвентаря фиксировал контракт «как есть»: свойство без типа и два без описания прошли молча | behavioral | `quick/test_tools_inventory.py` (8 инвариантов схемы) | 🟢 **ЗАКРЫТ S24** (мутация M109 → красный) |
| **F97** | `structuredContent: null` в проводе против схемы спеки — внешнего арбитра сериализации у нас нет | behavioral | `quick/test_firewall.py` (§4, живой сервер) | 🟢 **ЗАКРЫТ S24** (мутация M108 → красный) |
| **F96** | названный ребёнок чужого уровня исчезал из ответа: ИИ считал, что создал дерево | behavioral | `quick/test_structure.py` (§39) | 🟢 **ЗАКРЫТ S24** (мутация M107 → красный) |
| **F95** | объявленный `DUPLICATE_PATH` был недостижим: клиент получал `INTERNAL_ERROR` и «нужен человек» | behavioral | `quick/test_structure.py` (§38, через `engine.call`) | 🟢 **ЗАКРЫТ S24** (мутация M106 → красный) |
| **F94** | `parent_path` без слэша склеивался с именем: каталог-двойник рядом с контейнером, целостность зелёная | behavioral | `quick/test_structure.py` (§37) | 🟢 **ЗАКРЫТ S24** (мутация M105 → красный) |
| **F92** | `structure_migrate` уносил с диска поддерево, а в реестре правил путь только самой сущности | behavioral | `quick/test_structure.py` (§36) | 🟢 **ЗАКРЫТ S24** (мутация M104 → красный) |
| **F26** | на пустом реестре сервер молчал двумя пустыми списками вместо онбординга | behavioral | `quick/test_structure.py` (§17b) | 🟢 **ЗАКРЫТ S24** (мутация M103 → красный) |
| **F25** | целостность смотрела только реестр: каталог мимо сервера был невидим | behavioral | `quick/test_structure.py` (§17, обратный проход) | 🟢 **ЗАКРЫТ S24** (мутация M102 → красный) |
| **F55** | слепые зоны: `bot_army` не читал боевые лимиты, `test_search` не проверял фильтры/сортировку | behavioral | `bot_army/test_bot_army.py` (`test_shipped_config_is_enforced`) + `quick/test_search.py` (раздел фильтров) | 🟢 **ЗАКРЫТ S24** (мутации M99/M100 → красный) |
| **F90** | при `order: desc` нечисловые значения всплывали наверх — «топ» начинался с заглушек | behavioral | `quick/test_search.py` (сортировка в обе стороны) | 🟢 **ЗАКРЫТ S24** (мутация M101 → красный) |
| **F91** | набор провайдеров зависел от весов вне git — зелёным был только на одной машине | behavioral | `quick/test_providers.py` (пропуск с причиной) | 🟢 **ЗАКРЫТ S24** (пруф — красный CI-прогон) |
| **F32** | симуляции одновариантны — нет агентного роя (честные+атакующие в одном прогоне, outbound) | behavioral | `agent_swarm/test_agent_swarm.py` (34 исполнителя против живого сервера) | 🟢 **ЗАКРЫТ S24** |
| **F85** | `patterns.yaml` не парсился — «декларация», которую машина не читала | behavioral | `agent_swarm/test_agent_swarm.py` (грузит реестр первым действием; падение = красный гейт) | 🟢 **ЗАКРЫТ S24** |
| **F86** | сигнал аномалии немой: `reason` выбрасывался на ALLOW, `log_suspicious` — мёртвый knob | behavioral | `agent_swarm/test_agent_swarm.py` → `atk_anomaly_sequence` (след в консоли живого сервера) | 🟢 **ЗАКРЫТ S24** (мутация M98: `if False and …` → красный) |
| **F87** | провенанс не доехал до результатов поиска (остаток OUT8) | behavioral | `agent_swarm/test_agent_swarm.py` → `atk_search_poison` (объявлен `xfail-spec`) | 🟡 **OPEN-CONFIRMED S24** — дыра доказана живьём, ждёт фикса |
| **F43** | реестр обходится хендлерами → error без `reaction_class` из реестра | behavioral | `test_audit_fixes` (регрессия: `table_get_row` на нет-таблице → `reaction_class` И `recovery.reason` из реестра) | 🟢 **ЗАКРЫТ S16 (A6)** — `_err`→`get_error` + 24 `fs_*`-сайта; регрессия (2 проверки) зелёная |
| **F5** | DEFAULT-fallback игнорит `DEFAULT.message_template` | behavioral | `test_audit_fixes` (`get_error(unknown)` → assert message==template) | 🟢 **ЗАКРЫТ S16 (A6)** — DEFAULT-ветка тянет class/template/recovery из реестра; регрессия зелёная |
| **F40** | search-коды `QUERY_NOT_FOUND`/`PATH_NOT_FOUND` НЕ в реестре | behavioral | `test_audit_fixes` (assert коды ⊂ реестр) | 🟢 **ЗАКРЫТ S16 (A6)** — коды добавлены в `server_reactions.yaml`; регрессия зелёная. Остаток дубль-классов → A5 |
| **F42** | `_match_filter`/`_apply_sort` на разнотипном → TypeError | behavioral | `test_audit_fixes` (фильтр str vs int gt + сортировка разнотипного) | 🟢 **ЗАКРЫТ S16 (A5)** — type-safe фильтр+сортировка; регрессия зелёная (2 проверки). Остаток relevance/overclaim → A5 (F31) |
| **F29** | `validate_formulas`=grep токенов без пересчёта (театр) | behavioral | `test_audit_fixes` (`=1/0` → `validate_formulas` ловит через LO recalc; гардед по soffice) | 🟢 **ЗАКРЫТ S16 (A-tables)** — LibreOffice headless recalc; регрессия зелёная. F30 loader / F28 → отдельно |
| **F28** | delete/move_column ломает формулы молча (сырой `delete_cols`) | static | — (эвидентно из чтения `excel_core.py:242/253`; фикс A-tables) | ✅ подтверждён (чтение); одно корневое с F29 (нет пересчёта/зависимостей) |
| **F37** | `_safe` ловит голый ValueError → всегда PATH_ESCAPE | static | — (тригер контрив; `server.py:513` эвидентен; фикс A2/A6 = типизир. PathEscapeError) | ✅ подтверждён (чтение) |
| **F11** | raw_response митигирован (D23-санитайзер) | behavioral | `test_audit_fixes` (ErrorDetail с секретом → замаскирован) | 🟢 **регрессия ЗЕЛЁНАЯ** (api_key/nested token → `***REDACTED***`) |
| **F38** | мёртвый `_lock` в обоих search-классах | static | — (закрывается lint/vulture, I4) | ✅ подтверждён (grep) |
| **F39** | `QueryPlanner` лезет в приватный `table_engine._load` | static | — (архитектура, фикс A5) | ✅ подтверждён (чтение) |
| **F41/F46** | таксономия entity_type захардкожена 3× (search+schema+templates) | static→behavioral | `test_structure` (parity: три источника совпадают — metamorphic) | ✅ static; ⬜ parity-тест опционально |
| **F44** | повторные function-local импорты в ~15 хендлерах | static | — (lint, I4/A2) | ✅ подтверждён (grep) |
| **F45** | inline Python-skeleton (hardcode) | static | — (A2, вынести в template) | ✅ подтверждён (чтение) |
| **F10** | stt `device="cuda"` хардкод | static | — (config/anti-hardcode) | ✅ подтверждён (grep) |
| **F30** | `table_materializer` не построен (loader формул) | static | — (постройка A-tables/Ф3) | ✅ подтверждён (ls ∅) |
| **F3** | провайдеры = честные стабы (G16) | behavioral | `scenarios/media_ops.yaml` | 🟢 покрыт ЧЕРЕЗ ПРОВОД: отсутствие провайдера даёт громкий `PROVIDER_NOT_CONFIGURED`, а не пустой успех; установка без подтверждения — `CONFIRM_REQUIRED`. Прежний хозяин `render_draft_final` звал ноль провайдерских инструментов |
| **F68** | обход allowlist переименованием + исполняемое содержимое под «безопасным» расширением | behavioral | `test_structure` §32 (сигнатуры под чужим расширением, `.txt`→`.sh`/`.exe`, каркас скрипта, фрагменты) | 🟢 **регрессия ЗЕЛЁНАЯ** (мутации M44/M45/M46 краснеют) |
| **F72/F73** | шаблон проекта пишет мимо allowlist; `kind: config` тянет файл из-за пределов `config/` | behavioral | `test_structure` §34 (враждебный `evil.tpl.yaml`: `.sh`, shebang под `.md`, `source: ../.env`) | 🟢 **регрессия ЗЕЛЁНАЯ** (мутации M50/M51/M52 краснеют) |
| **F69** | `cryptography` не объявлена → подпись S9 молча выключена в чистом окружении | static | — (объявлена в `pyproject.toml`; `test_structure` §28 краснел в `.venv` без неё) | ✅ подтверждён (`ModuleNotFoundError` + grep по манифестам) |

**Статус C1 (S15) → фиксы (S16) — ВСЕ behavioral-находки ЗАКРЫТЫ:** A6 (F43·F5·F40) + A5-TypeError (F42) + A-tables (F29) переведены strict-xfail→регрессия. **`test_audit_fixes` 49/49, 0 OPEN** (S18: +5 проверок F54 «выключатель выключает»). F11 🟢 (D23). **static-находки** F38/F44 закрыты (I4); остаток F28/F37/F39/F41/F45/F10/F30 — эвидентны из чтения, закрываются A2/A-tables(F30)/anti-hardcode. Дальше — C2 симуляции+консоль (накоплены фиксы).

> **Правило статусов (git-native):** этот реестр — единственный источник «что подтверждено». Обновлять после
> каждого C1-теста (⬜→🟢), коммитить. Прогон целиком не нужен — гоняем зону находки, статус фиксируем в git.

---

## F. Мутационная проверка тестов (S18, 2026-08-11) — «а тест вообще ловит?»

> Зачем: зелёный тест доказывает что-то только если он **краснеет на сломанном коде**. Метод: откатываю фикс
> (или ослабляю защиту), гоняю тест-хозяина, восстанавливаю (`git checkout --`). Дерево после прогона чисто.
> Скрипт одноразовый (правило `quick`: создал → прогнал → удалил), результаты — здесь. Находки → `02` F54/F55.
>
> ⚠️ **Реестр отстал (сверено S24).** Таблица останавливается на `M57`, а мутации `M58`–`M98` (F20,
> F56, F28, F30, F59, A7.2, F77, F78, ключи провайдеров, F86) закрыты и описаны в `_sessions.md` и
> `02_findings.md`. То есть **источник истины по мутациям — сессии и реестр находок**, а этот раздел
> — исторический срез S18. Либо его дописывают в ту же сессию, что делает мутацию, либо с него
> снимается звание канона: реестр, который догоняют раз в 40 мутаций, вводит в заблуждение сильнее,
> чем его отсутствие.

| # | Что сломано | Файл | Тест-хозяин | Результат |
|---|---|---|---|---|
| M177 | П7 не видит стирающего алиаса (`if False`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — `import { Card as C }` стирал бы имя на входе, и по разметке компонент не нашёлся бы |
| M176 | необъявленный каталог молча судится по корню | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — **первая попытка была неинформативной** (набор падал `AttributeError`): файл вне дерева получал бы чужую зону вместо отказа |
| M175 | П5 не сверяет роды файла с зоной каталога (`лишние = []`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — сетевой вызов в примитиве считался бы нормой, и дверь наружу перестала бы быть одна |
| M174 | кадры анимации не попадают в снимок (`pass` вместо сбора) | `scripts/guards/_studio_surface.py` | `test_studio_emulation` | 🔴 ловит 2 ассерта (S26) — подменённые кадры вернуть было бы нечем: описание словами не даёт ни `from`, ни `to` |
| M173 | рядом с восстановленной строкой нет ЗНАЧЕНИЯ токена (`return ""`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 2 ассерта (S26) — «было `tokens.duration.enter`» без `160ms` не даёт сверить возврат глазами |
| M172 | П8 смотрит только на СОХРАНИВШИЕСЯ свойства | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — стёртая анимация была бы не «потеряна», а просто отсутствующей, и о ней никто бы не сказал |
| M171 | `--задача` не подставляет зону (`зоны += []`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — **сперва ВЫЖИЛА**: проверка смотрела на печатную строку «зона взята из объявления», а печать идёт РАНЬШЕ подстановки; переписана на следствие — критерий границ обязан быть суждён |
| M170 | ось эксперта не сверяет метку исполнителя с наборами (`elif False`) | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — сценарий звал бы несуществующую проверку и выглядел покрытым |
| M169 | род улики не выводится из тронутых файлов (пустой цикл) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 2 ассерта (S26) — совет пришлось бы спрашивать у самого правщика, то есть верить ему на слово |
| M168 | П4 не требует уникальности маркера (`if len(files) > 1` → `if False`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — один `data-component` на двух файлах перестал бы быть уликой, и «какой компонент» снова отвечалось бы наугад |
| M167 | П2 не сверяет форму со снимком (ранний `return notes`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 2 ассерта (S26) — смена пропсов и подменённое значение токена проезжали бы зелёными: «стиль не менял» держалось бы словом ИИ |
| M166 | П1 перестаёт разрешать импорт (`цель = None`) | `scripts/guards/acceptance_studio.py` | `test_studio_emulation` | 🔴 ловит 1 ассерт (S26) — переименованный экспорт при целой строке импорта снова считался бы рабочим кодом |
| M165 | битый потолок памяти пропускается молча (`return []`) | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — нечитаемый потолок выглядел бы пройденным, и храповик не судил бы ничего |
| M164 | ось объёма памяти судит ЧИСЛО ФАЙЛОВ вместо символов | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — нарезка на разделы меняла бы счёт, не трогая объёма, и перекладывание засчитывалось бы победой |
| M163 | читатель трения слеп (`return []`) | `scripts/guards/_stamp.py` | `test_trail` | 🔴 ловит 1 ассерт (S26) — счёт отказов гейта снова умирал бы в сессии, и «сторож кричит» не отличалось бы от «сторож молчит» |
| M162 | ключ сессии снова схлопывается (хвост-хеш снят) | `.claude/hooks/vpm-fact-gate.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — состояние одной сессии течёт в другую, и гейт молчит там, где должен отказать |
| M161 | взгляд не запоминает ИМЯ файла | `.claude/hooks/vpm-fact-gate.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — возврат к пошлине: предъявить факты ДО правки снова стало бы невозможно |
| M160 | цикл не записывает свою длительность (`секунд: 0`) | `scripts/guards/what_if.py` | `test_what_if` | 🔴 ловит 1 ассерт (S26) — цена цикла снова стала бы константой в комментарии, а не замером |
| M159 | отчёт цены считает по ПРОГОНАМ, а не по намерениям (`key` вместо `intent`) | `scripts/guards/_stamp.py` | `test_trail` | 🔴 ловит 2 ассерта (S26) — повтор после фикса считался бы отдельным наблюдением, и выигрыш цикла завышался бы (4 из 15 против 3 из 12) |
| M158 | распознаватель литералов снова привязан к heredoc | `.claude/hooks/vpm-fact-gate.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — литерал в `python3 -c` уходил бы в постфактум, и объявленная граница «по литералам» была бы ложью |
| M157 | ось границ слепа: порог близости недостижим (`БЛИЗОСТЬ = 999`) | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — путаемая пара не называется вовсе, и долг зон выглядит нулевым |
| M156 | ось границ не отбрасывает фоновые слова (`фон = set()`) | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — `python`/`server` считались бы признаком сходства, и ось обвинила бы ВСЕ пары: сторожа выключают целиком |
| M155 | ось границ не требует взаимности в `description` | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — починенная пара оставалась бы в долге навсегда, и потолок нельзя было бы опустить |
| M154 | читатель цикла судит ВХОЖДЕНИЕ слов, а не позицию команды | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — блокируется подпись, лишь ОПИСЫВАЮЩАЯ ошибку: механизм нельзя ни починить, ни описать |
| M153 | читатель цикла не снимает тело heredoc | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — команда в начале строки ВНУТРИ heredoc читается как запуск, хотя это данные |
| M152 | зона цикла сужена до сервера (машинерия выпала) | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — замер 09-02: все 5 коммитов в зоне были по сторожам и хукам, читатель молчал бы 10 раз из 10 |
| M151 | читатель цикла не судит свежесть (годится любой вердикт) | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — один прогон закрывал бы правило навсегда, вчерашний цикл покрывал бы сегодняшнюю правку |
| M150 | проверка целей замера снова зовёт `git ls-files` без `-z` | `tests/quick/test_comment_guard.py` | `test_comment_guard` | 🔴 ловит 1 ассерт (S26) — git экранирует не-ASCII путь, и проверка краснеет на первом кириллическом имени |
| M149 | признание снова ЗАКРЫВАЕТ хвост (`ПРИЗНАНИЯ = ()`) | `scripts/guards/_stamp.py` | `test_hooks` | 🔴 ловит 2 ассерта (S26) — «беру» и «отложил» тихо равнялись бы «закрыл», хвост исчезал бы из показа |
| M148 | роль «В РАБОТЕ» не снимает запрет (`признан` → `отложен`) | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — возврат к тупику F214: взяться за хвост можно лишь объявив, что им не занимаешься |
| M147 | ось зон не требует взаимности границы (`if False`) | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — сосед, о котором сказали, а он промолчал, считает зону своей, и оба растут в одну область |
| M146 | «отложить» снова значит «закрыть» (`role != ОТЛОЖЕН` снято) | `scripts/guards/_stamp.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — отложенный хвост исчезал бы из показа вместе с запретом, то есть откладывание равнялось бы забыванию |
| M145 | сторож намерений пускает запись при непризнанном хвосте | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — возврат к варианту 1: работа ложится на диск, а хвост называется после (или никогда) |
| M144 | сторож дисциплины судит без отсрочки на возраст следа (`if False`) | `.claude/hooks/_trace.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — в первый день пылящимися оказались бы ВСЕ сторожа сразу, и сторож приучил бы себя выключать |
| M143 | сторож намерений слеп к обещанию (`найдено = None`) | `.claude/hooks/vpm-intent-guard.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — сессия кончалась бы обещанием, а хвост не переживал бы её |
| M142 | подпись отказа гейта снова пишется в БОЕВОЙ корень (`root=root` → умолчание) | `.claude/hooks/vpm-delivery-gate.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — каждый прогон набора дописывал бы в журнал улик отказ, которого не было: 28 из 29 записей рода `гейт` были именно такими |
| M141 | правило «повторить — команда» распространено на ВСЕ роли (`if not runnable(cmd)`) | `scripts/guards/_stamp.py` | `test_trail` | 🔴 ломает обоих живых производителей: отказ гейта несёт прозу законно, и набор падает `ValueError` на нём |
| M140 | третий источник правки снят: гейт смотрит только `diff`/`log` | `.claude/hooks/vpm-delivery-gate.py` | `test_hooks` | 🔴 ловит 1 ассерт (S26) — реестр, заведённый в этой же сессии, ещё не отслеживается, и правка без улики прошла бы мимо |
| M139 | указатель журнала сверяется только именем файла, но не СТРОКОЙ (`rows[n-1] != f"### {head}"` → `False`) | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S26) — дописанный выше абзац сдвигает адреса, и читатель попадает в середину чужой записи молча |
| M138 | разбор реестра не видит зачёркнутый идентификатор (`(~~)?` → `(XX)?`) | `scripts/guards/findings_count.py` | `test_findings_count` | 🔴 ловит 1 ассерт (S25) — сломанный разбор ужимает реестр со 150 строк до 55, и это ровно то, ради чего заведён пол |
| M137 | храповик не судит (`if len(rows) < floor` → `if False and …`) | `scripts/guards/findings_count.py` | `test_findings_count` | 🔴 ловит 3 ассерта (S25) — ужавшийся реестр проезжает с кодом 0 |
| M136 | пола нет — молча считаем нулём (снята проверка `FLOOR.exists()`) | `scripts/guards/findings_count.py` | `test_findings_count` | 🔴 ловит 2 ассерта (S25) — **до фикса ВЫЖИВАЛА**: удалённый файл пола выключал храповик молча и давал код 0 на любом числе строк |
| M135 | в повторе побеждает последняя строка, а не каноническая (`setdefault` → присваивание) | `scripts/guards/findings_count.py` | `test_findings_count` | 🔴 ловит 2 ассерта (S25) — таблица переформулировок внизу закрывала бы находку за канон |
| M134 | 🟢 перестаёт быть знаком закрытия (`CLOSED` без него) | `scripts/guards/findings_count.py` | `test_findings_count` | 🔴 ловит 2 ассерта (S25) — **до фикса ВЫЖИВАЛА**: два читателя одного реестра расходились на F18/F40/F42, и печатаемый список открытых врал на три находки (F159) |
| M133 | обход карты по ПАРАМ переходов вместо каждого по разу | `tests/scenarios/structure_growth.map.yaml` | `scenarios` (карта) | ⚪ порядок вызовов проверен: 3 пути → 6, проверок 95, все зелёные — сервер выдерживает все порядки; механизм заведён под карты, где порядок значим |
| M132 | предел частоты выключен в конфиге (`rate_limit.enabled: false`) | `config/firewall.yaml` | `scenarios` (`rate_limit.yaml`) | 🔴 ловит 1 ассерт (S25): шквал из 120 вызовов проезжает вместо `RPC_-32000` |
| M131 | «создано ноль» снова объявляется успехом (откат F145) | `tools/filesystem/__init__.py` | `scenarios` (`structure_ops.yaml`) | 🔴 ловит 3 ассерта (S25): код, класс реакции и рецепт |
| M130 | проверка `Origin` снята — запрос из чужой вкладки снова исполняется | `server.py` | `scenarios` (`perimeter.yaml`) | 🔴 ловит 1 ассерт (S25): «пришло УСПЕХ» вместо `RPC_-32002`. Родня `M120`, но ловится теперь ОБЪЯВЛЕНИЕМ, а не питонным набором |
| M129 | проверка ключа снята (`if not ALLOW_NO_AUTH` → `if False`) | `server.py` | `scenarios` (`perimeter.yaml`) | 🔴 ловит 6 ассертов (S25) — среди них «манифест отдаётся без ключа» и «инструмент исполняется без ключа»; до расширения языка объявлений периметр был декларации недоступен вовсе (F147) |
| M128 | отбор точечного прогона не совпадает ни с одним сценарием (опечатка в `VPM_SCENARIO`) | `tests/scenarios/test_scenarios.py` | `scenarios` | 🔴 ловит 2 (S25) — **до фикса ВЫЖИВАЛА**: `0/0 прошло · ВСЁ ЗЕЛЁНОЕ ✅`, exit 0, то есть точечный прогон был ритуалом (F144) |
| M127 | `fs_create_file` пишет пустое: сервер отвечает «создано», содержимого нет | `tools/filesystem/__init__.py` | `scenarios` (`filesystem.yaml`) | 🔴 ловит 2 ассерта (S25) — **до `filesystem.yaml` выживала**: 156 сценариев и `structure_emulation` 54/54 зелёные, слепая зона на записи (F141) |
| M126 | `fs_read_file` обрезает содержимое до пяти символов | `tools/filesystem/__init__.py` | `scenarios` (`provenance.yaml`) | 🔴 ловит 1 ассерт (S25) — точным значением, а не трейсом |
| M125 | селектор наблюдения промахивается мимо поля (`data.results.0.trust` → `data.rezults…`) | `tests/routes/routes.yaml` | `routes` | 🔴 ловит 1 ассерт (S25) — **до починки ВЫЖИВАЛ**: ветка `arrives: false` зеленела и рапортовала «разрыв F87 подтверждён» по несуществующему полю |
| M124 | зона набора ищется по всему тексту каталога, а не первой ячейкой строки таблицы | `scripts/guards/invariants.py` | `test_invariants` | 🔴 ловит 1 ассерт (S25) — упоминание в прозе снова засчитывалось бы зоной |
| M120 | `Origin` снова проверяется только при непустом allowlist (откат F106, повторное открытие D12) | `server.py` | `test_firewall` §2c | 🔴 ловит 5 ассертов (S24) |
| M121 | тип тела не проверяется — простой запрос из чужой вкладки снова исполняется (откат F107) | `server.py` | `test_firewall` §2c | 🔴 ловит 5 ассертов (S24) — среди них РЕАЛЬНАЯ запись файла, а не только код ответа |
| M122 | заголовки ответа сняты (откат F109) | `server.py` | `test_firewall` §2c | 🔴 ловит 4 ассерта (S24) |
| M123 | `Host` снова разбирается по первому значению до запятой (откат F108) | `server.py` | `test_firewall` §2c | 🔴 ловит 1 ассерт (S24) |
| M117 | проверка `Host` снята — сервер снова отвечает чужому имени хоста (откат F103) | `server.py` | `conformance` + `test_firewall` §2b | 🔴 ловит 4 ассерта (S24) |
| M117a | набор пропускается вместо прогона при отсутствии node | `tests/conformance/test_conformance.py` | `conformance` (с `VPM_CONFORMANCE_REQUIRED=1`) | 🔴 ловит (S24) — иначе сломанная установка node дала бы зелёную джобу |
| M118 | `0.0.0.0` возвращён в список разрешённых хостов (имя браузерного обхода localhost) | `server.py` | `test_firewall` §2b | 🔴 ловит 1 ассерт (S24) |
| M119 | перечень «чем исполнять можно» собран из всех объявленных, включая безадаптерных | `core/providers/adapters.py` | `test_providers` §12 | 🔴 ловит 3 ассерта (S24) |
| M116 | сведение связывает, но НЕ переносит — маршруты 1/2/3 прохода расходятся | `tools/structure/__init__.py` | `structure_emulation` E-C | 🔴 ловит 2 ассерта (S24) |
| M116a | сведение переносит, но НЕ связывает — путь верный, реестр врёт | `tools/structure/__init__.py` | `structure_emulation` E-C | 🔴 ловит 2 ассерта (S24) |
| M115 | отложенные дети не сообщаются клиенту | `core/engine/template_engine.py` | `structure_emulation` E-B | 🔴 ловит 12 ассертов (S24) |
| M115a | не названный тип разворачивается сам — контроль глубины снят | `core/engine/template_engine.py` | `structure_emulation` E-B | 🔴 ловит 9 ассертов (S24) |
| M114 | имя из пути не становится связью — ФС и реестр снова расходятся (откат F101) | `tools/structure/__init__.py` | `structure_emulation` E-A4 | 🔴 ловит 4 ассерта (S24) |
| M114a | якорь ищется по всей области, а не в своей ветке (откат F102) | `tools/structure/__init__.py` | `structure_emulation` E-A3 | 🔴 ловит 4 ассерта (S24) — сначала падал IndexError'ом вместо ассерта, проверка укреплена |
| M113 | компенсация снята: упавший пакет не откатывает уже перенесённое | `tools/structure/__init__.py` | `test_structure` §42 | 🔴 ловит (S24) — **сначала ВЫЖИЛ**: помеха срабатывала до первого переноса, откатывать было нечего; сценарий перестроен на падение ВТОРОЙ сущности |
| M113a | сервер гадает: при нескольких кандидатах берёт первого | `tools/structure/__init__.py` | `test_structure` §42 | 🔴 ловит (S24) |
| M113b | `role: owner_channel` снят с объявления — якорь снова только в коде | `config/templates/workspace/competitor_channel.tpl.yaml` | `test_structure` §42 | 🔴 ловит (S24) |
| M112 | ORPHAN-политика расширена до объявления предков (`channel`→ниша, `video`→канал) | `core/ids/link_registry.py` | `test_structure` §41 | 🔴 ловит 2 ассерта (S24) |
| M111 | в описание инструмента вписан `U+200B` — невидимая правка манифеста | `tools/tables/__init__.py` | `test_tools_inventory` | 🔴 ловит (S24) |
| M111a | описание несёт `<!-- … -->` — текст, скрытый от ревью, но видимый модели | `tools/tables/__init__.py` | `test_tools_inventory` | 🔴 ловит (S24) |
| M111b | описание несёт `SYSTEM: ignore previous instructions` | `tools/tables/__init__.py` | `test_tools_inventory` | 🔴 ловит (S24) |
| M111c | описание несёт внешний URL и `curl` — эксфильтрация подсказана манифестом | `tools/tables/__init__.py` | `test_tools_inventory` | 🔴 ловит (S24) |
| M111d | латинская `c` внутри русского слова — confusable-подмена имени/термина | `tools/tables/__init__.py` | `test_tools_inventory` | 🔴 ловит (S24) |
| M110 | токен `{parent:<тип>}` не раскрывается — сгруппированная ветка снова невидима (откат F93) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит (S24) |
| M110a | спуск только по прямым детям — пропущенный уровень иерархии снова невидим | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит (S24) |
| M109 | снять описание у свойства схемы — контракт для клиента-LLM теряется (откат F98) | `tools/search/__init__.py` | `test_tools_inventory` | 🔴 ловит 2 ассерта (S24) |
| M108 | пустой `structuredContent` снова уезжает как `null` (откат F97) | `core/transport/transport.py` | `test_firewall` | 🔴 ловит 2 ассерта (S24) |
| M107 | `children_unfulfilled` всегда пуст — невыполненное снова молчит (откат F96) | `tools/structure/__init__.py` | `test_structure` | 🔴 ловит (S24) |
| M106 | регистрация узлов мимо `ctx.safe` — код реакции обезличивается (откат F95) | `tools/structure/__init__.py` | `test_structure` | 🔴 ловит 3 ассерта (S24) |
| M105 | нормализация контейнера снята: путь без слэша снова склеивается (откат F94) | `core/engine/template_engine.py` | `test_structure` | 🔴 ловит 3 ассерта (S24) |
| M104 | перенос правит путь только самой сущности, потомков в реестре не трогает (откат F92) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит 7 ассертов (S24) |
| M103 | холодный старт не срабатывает (откат F26) | `tools/structure/__init__.py` | `test_structure` | 🔴 ловит (S24) |
| M102 | обратный проход диск→реестр отключён (откат F25) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит (S24) |
| M99 | файрвол игнорирует объявленный порог (`max_requests=10⁹`) | `core/firewall/firewall.py` | `bot_army` | 🔴 ловит (S24) |
| M100 | `_match_filter` пропускает всё | `core/search/query_planner.py` | `test_search` | 🔴 ловит (S24) |
| M101 | чужеродные значения снова участвуют в развороте сортировки (откат F90) | `core/search/query_planner.py` | `test_search` | 🔴 ловит (S24) |
| M98 | сигнал аномалии снова выбрасывается на ALLOW-пути (откат F86) | `core/firewall/firewall.py` | `agent_swarm` | 🔴 ловит (S24) |
| M1 | `ToolContext.err` мимо реестра реакций (откат F43) | `tools/_context.py` | `test_audit_fixes` | 🔴 ловит |
| M2 | DEFAULT-fallback хардкодит message (откат F5) | `core/reactions/reactions.py` | `test_audit_fixes` | 🔴 ловит |
| M3 | `QUERY_NOT_FOUND` убран из реестра (откат F40) | `config/server_reactions.yaml` | `test_audit_fixes` | 🔴 ловит |
| M4 | типонебезопасный `_match_filter` (откат F42) | `core/search/query_planner.py` | `test_audit_fixes` 🔴 / **`test_search` 🟢** | частично — **слепа зона search** |
| M5 | `validate_formulas` без LO-пересчёта (откат F29) | `core/excel/excel_core.py` | `test_audit_fixes` | 🔴 ловит |
| M6 | санитайзер секретов выключен (откат D23/F11) | `core/contracts/error_detail.py` | `test_audit_fixes` | 🔴 ловит |
| M7 | containment путей снят (откат D1/G17) | `core/paths.py` | `test_audit_fixes` 🔴, `test_search` 🔴 / **`test_structure` 🟢** | **слепа зона structure** |
| M8 | детектор prompt-injection обнулён (IN2) | `core/firewall/rules/injection_detector.py` | `virus_injection` 🔴, `cache_injection` 🔴, `test_audit_fixes` 🔴 | ловят |
| M9 | дефолты rate-limit сняты (IN3, проводка) | `core/firewall/rules/rate_limiter.py` | **`bot_army` 🟢 22/22** | **слепа боевая конфигурация** |
| M9b | сам механизм rate-limit сломан | `core/firewall/rules/rate_limiter.py` | `bot_army` 🔴 15/22, `test_audit_fixes` 🔴 | ловят (механизм покрыт) |
| M10 | тихо изменён `description` инструмента | `tools/filesystem/__init__.py` | `test_tools_inventory` | 🔴 ловит (эталон+`--bless`) |
| M11 | `_valid_name` → `True` (обход через имя) | `core/engine/template_engine.py` | `test_structure` | 🔴 ловит |
| M12 | боевой `max_requests_per_minute: 10⁹` | `config/firewall.yaml` | 12 наборов → **только `test_audit_fixes` 🔴** | частично (D2-загрузка конфига) |
| M13 | боевой `injection_detection.enabled: false` | `config/firewall.yaml` | 12 наборов → **никто 🟢** | **было:** ключ `enabled` код не читал (F54). **S18: исправлено** — см. M14 |
| M14 | откат фикса F54 (`enabled` снова игнорируется) | `core/firewall/firewall.py` | `test_audit_fixes` (5 новых проверок) | 🔴 ловит ✅ |
| M15 | детектор injection обнулён — проверка САМОГО ГЕЙТА `pytest -m "not live"` | `core/firewall/rules/injection_detector.py` | `pytest` → 3 failed, 11 passed | 🔴 ловит ✅ (гейт T0 живой) |

| M16 | инвариант пути снят (второй ID на занятый каталог) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит ✅ |
| M17 | `check_integrity` не сверяет путь с диском (откат F65) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит ✅ |
| M18 | резолвер не берёт готовых предков (`find_by_path` → None) | `core/ids/chain_resolver.py` | `test_structure` | 🔴 ловит ✅ |
| M19 | `structure_create` снова с `parent_ids=None` (откат F63) | `tools/structure/__init__.py` | `test_structure` | 🔴 ловит ✅ (71/82) |
| M20 | молчаливое усыновление (снята блокировка `unresolved`) | `tools/structure/__init__.py` | `test_structure` | 🔴 ловит ✅ |
| M21 | перенос мимо реестра (откат F65) | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ (77/82) |
| M22 | ручное создание без владельца (откат F61) | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ |

| M23 | повторный `assign_id` плодит второй ID на путь | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ |
| M24 | префикс файла мимо объявленного класса | `core/ids/taxonomy.py` | `test_structure` | 🔴 ловит ✅ (92/95) |

| M25 | подстановка `{parent:channel}` снята (откат F62) | `core/engine/template_engine.py` | `test_structure` | 🔴 ловит ✅ (103/107) |
| M26 | неоднозначность без списка кандидатов (откат F64) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит ✅ |

| M27 | личность файла не из реестра (откат F60) | `core/search/fs_searcher.py` | `test_search` | 🔴 ловит ✅ |
| M28 | `chain_prefix` игнорируется | `core/search/fs_searcher.py` | `test_search` | 🔴 ловит ✅ (32/35) |

| M29 | `structure_find` не фильтрует по тексту | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит ✅ (117/122) |
| M30 | гигиена ярлыка снята (длина/переводы строк) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит ✅ |

| M31 | конверт провенанса снят (ярлык голой строкой) | `tools/structure/__init__.py` | `test_structure` | 🔴 ловит ✅ |
| M32 | пометка `instruction_like` отключена | `core/contracts/untrusted.py` | `test_structure` | 🔴 ловит ✅ (128/129) |
| M33 | детектор берёт свою копию паттернов вместо `firewall.yaml` | `tools/_context.py` | `test_structure` | 🔴 ловит ✅ (127/129) |

| M34 | пустой ожидаемый токен снова «открывает» сервер (откат F14 fail-open) | `core/auth.py` | `test_audit_fixes` | 🔴 ловит ✅ (62/63) |
| M35 | права `0600` на файле секрета не выставляются | `core/auth.py` | `test_audit_fixes` | 🔴 ловит ✅ (61/63) |
| M36 | `compare_digest` по строкам (не-ASCII токен → 500 вместо 401) | `core/auth.py` | `test_audit_fixes` | 🔴 ловит ✅ |

| M37 | защита записи снята: чужой инстанс пишет в реестр (откат S9) | `core/ids/link_registry.py` | `test_structure` | 🔴 ловит ✅ (144/147) |
| M38 | отпечаток машины не проверяется | `core/integrity.py` | `test_structure` | 🔴 ловит ✅ (146/147) |
| M39 | разрушающие инструменты выпали из `dangerous_tools` | `config/firewall.yaml` | `test_structure` | 🔴 ловит ✅ (146/147) |

| M40 | allowlist типов выключен (пишем `.sh`/`.exe`) | `core/write_policy.py` | `test_structure` | 🔴 ловит ✅ |
| M41 | удаление без подтверждения снова молчит | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ |
| M42 | конверт содержимого без провенанса и пометки | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ (170/172) |
| M43 | журнал пишет сырой `Fact.data` | `core/state/state_manager.py` | `test_structure` | 🔴 ловит ✅ (170/172) |

| M44 | проверка СОДЕРЖИМОГО снята (скрипт под видом .md проходит) | `core/write_policy.py` | `test_structure` | 🔴 ловит ✅ (10 провалов) |
| M45 | `fs_rename`/`fs_move` не проверяют тип цели (обход allowlist) | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ (4 провала) |
| M46 | каркас скрипта пишет мимо единой двери (`description` от ИИ не проверяется) | `tools/filesystem/__init__.py` | `test_structure` | 🔴 ловит ✅ (2 провала) |

| M47 | `kind: config` игнорируется — копии конфига проекта нет | `core/engine/template_engine.py` | `test_structure` | 🔴 ловит ✅ (3 провала) |
| M48 | копия затирается серверным дефолтом при каждом проходе (правка проекта теряется) | `core/engine/template_engine.py` | `test_structure` | 🔴 ловит ✅ |
| M49 | нет дефолта на диске → выдуманный файл вместо честного пропуска | `core/engine/template_engine.py` | `test_structure` | 🔴 ловит ✅ (падением) |

| M50 | шаблон пишет `.sh` мимо allowlist (проверка типа снята) | `core/engine/template_engine.py` | `test_structure` §34 | 🔴 ловит ✅ (5 провалов) |
| M51 | shebang под `.md` из шаблона проходит (проверка содержимого снята) | `core/engine/template_engine.py` | `test_structure` §34 | 🔴 ловит ✅ (в тех же 5) |
| M52 | `kind: config` без containment — `source: ../.env` вычерпывает секрет сервера | `core/engine/template_engine.py` | `test_structure` §34 | 🔴 ловит ✅ (3 провала) |

| M53 | ротация снова печатает значение ключа (секрет оседает в логах) | `server.py` | `test_audit_fixes` | 🔴 ловит ✅ (static) |
| M54 | `.env` выпал из `.gitignore` (класс F70) | `.gitignore` | `test_audit_fixes` | 🔴 ловит ✅ |
| M55 | сравнение по значению, а не по хэшу (откат хранения-отпечатка) | `core/auth.py` | `test_audit_fixes` | 🔴 ловит ✅ (4 провала) |
| M56 | миграция оставляет открытое значение соседом с хэшем | `core/auth.py` | `test_audit_fixes` | 🔴 ловит ✅ |
| M57 | значение ключа печатается вне терминала (осядет в логе) | `server.py` | `test_audit_fixes` | 🔴 ловит ✅ (static) |

| M130 | `offload: false` для класса `cpu_heavy` в БОЕВОМ `config/resources.yaml` | `config/resources.yaml` | `scenarios/res_heavy_call_does_not_freeze_server` | 🔴 ловит ✅ — `within_budget` и `overlapped` оба `False`: выключатель действительно выключает, а не украшает конфиг |
| M128 | переход карты не меняет наблюдаемое (вместо создания — чтение) | `structure_growth.map.yaml` | `scenarios/карта` | 🔴 ловит ✅ — «предикат with_channel держится и после перехода: состояния неразличимы». Без этого правила карта описывала бы вымысел |
| M129 | состояние без входящих переходов | `structure_growth.map.yaml` | разбор карты ДО прогона | 🔴 ловит ✅ — ⚠ «недостижимо из empty» + красный прогон; так же ловится тупик без `terminal` |
| M126 | провенанс ПОЯВИЛСЯ в выдаче поиска (имитация закрытия F87) | `tools/filesystem/__init__.py` | `routes/provenance_of_search_results` | 🔴 ловит ✅ — **сценарий остался зелёным (3/3)**, покраснела маршрутная таблица: «разрыв закрыт, обнови F87 и переверни `arrives`». Уникальная для маршрутов ловля |
| M127 | рецепт восстановления теряется на рубеже конверта | `core/transport/transport.py` | `scenarios/ed1`, `routes/*` | 🔴 ловит ✅ (3 маршрута через «опора не отработала»; первым сработал сценарий — он объявляет `recovery: true`) |
| M124 | guard столбца снят: удаление/перенос под формулой проходит молча (откат F28) | `core/excel/excel_core.py` | `scenarios/ed5,ed6` | 🔴 ловит ✅ (6 провалов; отказ печатает пришедший УСПЕХ и то, что класс с рецептом не доехали) |
| M125 | защита вычисляемого столбца снята (откат E-D1) | `core/tables/tables_core.py` | **теневой слой:** `test_tables` (in-process) **и** `scenarios/ed1` | 🔴 ловит ОБА ✅ — старый 2 провала (один из них в соседней секции: отвергнутая запись попала в очередь), сценарий 3, и только сценарий видит, что класс реакции и recovery до клиента не дошли |

**Итог (обновлено S21): 53 мутации, поймано 39+.** первый прогон S18 дал 11/14. Три пробоя (M4-search, M7-structure, M9-конфиг) + мёртвый выключатель (M13)
заведены как **F55** и **F54**. Зоны в §A/§B выше описывают ЖЕЛАЕМОЕ покрытие — расширять хозяев по F55:
`test_search` (+фильтры/сортировка), `test_structure` (+containment через `safe_resolve`, не только имя),
`bot_army` (+проверка боевых лимитов из `config/firewall.yaml`), любой (+auth `MCP_AUTH_TOKEN`).

---

## G. Реестр перевода в сценарии (`T12`) — доказательство, что проверка не потеряна

> Заводится вместе с движком (`docs/roadmap/14_testing_system_plan.md` §T11–T12). До первого перевода
> таблица пуста — это нормально; пустой её делает отсутствие переводов, а не отсутствие правила.

Набор переводится **целиком и по одному**. Удаление старого файла — ПОСЛЕДНЕЕ действие и только после
трёх доказательств; ни одно не заменяется чтением диффа:

1. **Покрытие зоны не упало** — `VPM_COVERAGE=1` до и после, сравнение по файлам зоны (не по общему проценту).
2. **Мутации зоны из §F по-прежнему красные** — прогон тех же мутаций против нового сценария.
3. **В таблице ниже нет строки без адреса или без причины снятия.**

| Старая проверка (файл · метка) | Стало | Причина, если снята |
|---|---|---|
| `test_tables` · COMPUTED_READONLY on set | `tables_destructive.yaml#ed1_computed_readonly` | **не снята** — теневой слой на время `T11`: хозяин остаётся `test_tables`, оба вердикта сверены мутацией M125 |
| `test_tables` · ENUM_VIOLATION on set | `tables_destructive.yaml#ed2_enum_violation` | **не снята** — теневой слой, тот же порядок |
| _(дальше заполняется при переводе)_ | `scenarios/<файл>.yaml#<id>` | дубль хозяина `<набор>` · мёртвый код · эквивалент `<id>` |

**Причины снятия, которые принимаются:** проверка дублирует хозяина зоны · механизм удалён (проверять
нечего) · проверка эквивалентна другой (одна и та же ветка, разные данные). **Не принимается:**
«выглядит лишним», «давно не краснела», «покрыто соседним набором» без указания адреса.

## H. Форма теста (что не должно попадать в исходник)

Обмер 2026-08-24: 85 упоминаний `D#/F#/M#` в коде тестов, 27 строк процессного шума, 37 многострочных
docstring при правиле «одна строка» с 2026-07-05. Правило без механики не держится — поэтому оно здесь,
рядом с зонами, а проверяется сторожем (`scripts/guards/invariants.py`) по мере `T11`.

- **Статус находки хранит реестр, не тест.** `⬜→🟢`, `OPEN-CONFIRMED`, «обнови CATALOG» — это состояние
  процесса; в тесте `D#`/`F#` допустим одним тегом в однострочном docstring как АДРЕС эксплойта.
- **Причина отказа парсится из ответа и печатается** (`ErrorDetail.code` + сообщение), а не предполагается.
- **Ожидание объявляется намерением**, а не вычисляется из конфига, по которому отвечает сервер.
- История «прежде было X», планы, номера сессий — в commit и `docs/roadmap/_sessions.md`.
