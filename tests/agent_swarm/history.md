# История — tests/agent_swarm (агентный adversarial-рой)

## 2026-07-05 — Реестр паттернов заведён (planning, скрипт TODO)

**Решение → факт:** все паттерны атак/практик из `docs/roadmap/06_threat_catalog.md` (IN1–IN10, OUT1–OUT8, §F allowlist, §G no-root, §H кэш/DDoS/пакеты) зафиксированы как декларативные behavior-модели в `patterns.yaml`. Источник истины угроз — каталог; здесь они превращены в исполняемые сценарии с `expect`/`mitigation`/`status`.

**Состав:** 5 моделей честных клиентов (baseline доступности) + 10 inbound + 8 outbound + 5 escalation (§F/§G) + 8 cache/ddos/packet (§H) злоумышленников. Каждый ссылается на запись каталога, surface, ожидаемый исход (код реакции/провенанс/блок), практику-митигацию, наследуемый сим-набор и `status` (implemented | xfail-spec).

**Почему xfail-spec у части:** защита ещё не построена (провенанс OUT1/F33, allowlist F34, slowloris/payload-лимиты F36, media IN6) — паттерн честно фиксирует ТРЕБОВАНИЕ и покраснеет как «не реализовано», не фейкая success (G16).

**Что дальше (не в этой сессии):** `test_agent_swarm.py` — раннер: поднять живой сервер, исполнить модели (честные+атакующие в одном прогоне), ассерт по 4 критериям (`meta.success_criteria`), сброс firewall между прогонами. Наследует существующие сим-наборы (`bot_army`/`virus_injection`/`cache_*`). Порядок реализации — `03_testing_plan.md` (agent-swarm, после I1).

**Baseline:** кода сервера не трогали → сим-наборы держатся; этот набор пока только декларация (yaml + history).
