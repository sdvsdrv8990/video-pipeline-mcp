# История файла core/transport/tunnel.py

> **Роль:** управление cloudflared-туннелем к Claude AI Web.
> **Последнее обновление:** 2026-07-03

---

## v1.3 — 2026-07-03 — D31: гигиена секретов

### Решение
- `config/tunnel.yaml` добавлен в `.gitignore` (не попадает в git)
- Warning в `tunnel.yaml` (D31, .gitignore)
- Env-приоритет: `MCP_TUNNEL_TOKEN` > yaml

### Регрессия
- `--token` в argv остаётся (ограничение cloudflared)
- Токен в yaml теперь не попадёт в git

### Связь
- D31 частично закрыт, G11 (cloudflared)

---

## v1.2 — 2026-07-01 — Событийная готовность

### Решение
- Готовность по событию соединения, а не таймеру
- Супервизор с backoff+jitter
- Исправлен баг: "Unregistered..." ложно матчился как "connected"
- Дренаж stdout (иначе буфер пайпа → зависание)

### Регрессия
- Named не тест-прогнан вживую (нет домена)

### Связь
- D11 закрыт, SESSIONS.md §Сессия 5

---

## v1.1 — 2026-07-01 — Named режим

### Решение
- Два пути: connector-токен ИЛИ credentials+UUID
- Env-приоритет для токена

### Регрессия
- Токен в argv (виден в ps) — D31

### Связь
- G11

---

## v1.0 — 2026-07-01 — Инициализация

### Решение
- cloudflared quick/named
- Таймаут-готовность (позже заменена на событийную)

### Регрессия
- Туннель не поднят без --tunnel

### Связь
- G11, SESSIONS.md §Сессия 5
