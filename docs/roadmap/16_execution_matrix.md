# 16 — Матрица исполнения: фикс + тест + защита в одном блоке

> Зачем: в `_sessions.md` накопился хвост «что ещё надо», находки живут в `02`, тест-программа в `14`,
> защита в `15` — четыре списка, которые легко исполнять вразнобой. Этот документ **сшивает их в блоки**:
> один блок = одна-две сессии = **проблема закрыта + тест на неё написан + защита продвинута**.
>
> **Директива владельца (2026-08-11): сначала фиксы.** Внутри блока порядок жёсткий:
> **(1) фикс проблемы → (2) тест, который краснеет при откате фикса → (3) защитный/инфра-инвариант.**
> Тест-система и защита развиваются **параллельно** с фиксами, а не «когда-нибудь потом».

## Состояние блоков на S23 (2026-08-14)

| Блок | Статус | Что осталось |
|---|---|---|
| B0 станок и правда | ✅ закрыт S18 | — |
| B1 харнесс и auth | ✅ S24 | T1 ✅ · S1 ✅ · **T2 ✅ — `tests/harness` поднимает сервер сам; `test_firewall` в гейте (17/17), F51 закрыт** |
| B2 слепые зоны и запрет файлов | 🔨 S2 ✅ (F34/F68) | F55 — слепые зоны хозяев (T10) |
| B3 outbound | 🔨 S3 ✅ (F33) | T6/S7 — раннер роя `agent_swarm` (F32 🔴) |
| B4 табличный слой | ✅ закрыт S22 | — (плюс сверх плана: конфиг канала переехал в листы, три режима создания) |
| B4′ система ID | ✅ закрыт S19 | — |
| B5 структура | 🔲 не начат | F25 реконсиляция ФС↔реестр · F26 cold-start · F27 авто-перенос |
| B6 периметр под нагрузкой | 🔲 не начат | F35/F36 · S4 origin-лимиты · S5 deploy-hardening |
| B7 качество поиска и контракт | 🔲 не начат | F31 eval · F40/F41/F42/F45/F46 · T7 conformance · S6 анти-rug-pull |
| B8 зависимости/наблюдаемость/продукт | 🔨 S8 ✅ (pip-audit) · F47 закрыт (S20) | **I5 observability** · P2–P4 облачные провайдеры · P1 FFmpeg · OAuth `S1′` |

**Сверх матрицы, вне блоков (S22–S23):** слой уникальности A7.2 · три режима создания (план `13`) ·
секреты провайдеров в канале под Fernet · каталог и установка локальных моделей ·
подъём моделей **на видеокарте** без root (F79–F82).

## Как читать

| Колонка | Что значит |
|---|---|
| **Фикс** | находка `F#` из [`02_findings.md`](02_findings.md) или шаг воркстрима из [`01_master_roadmap.md`](01_master_roadmap.md) |
| **Тест** | этап `T#` из [`14_testing_system_plan.md`](14_testing_system_plan.md) + конкретный тест-хозяин (правило «не плодить» — `tests/CATALOG.md`) |
| **Защита** | этап `S#` из [`15_security_system_plan.md`](15_security_system_plan.md) |
| **Готово, когда** | доказуемая приёмка блока (не «сделано», а «проверено») |

---

## Блоки

### B0 — Станок и правда (разблокирует всё остальное) · ✅ ЗАКРЫТ S18

| Фикс | Тест | Защита |
|---|---|---|
| **F50** pytest = ложный зелёный · **F53** тест-доки врут | **T0** канонический раннер (мост `assert main()==0`, CI → `pytest -m "not live"`) · **T3** правда доков | **S0** `enabled:` в `firewall.yaml` начинает работать (**F54**) |

**Готово, когда:** ✅ всё выполнено — `pytest -m "not live"` = 14 passed и краснеет от поломки (мутация M15:
3 failed); `test_tunnel` (20 проверок) вернулся в гейт; `enabled:` в `firewall.yaml` живой + 5 регрессий
(`test_audit_fixes` 49/49), откат ловится мутацией M14; тест-доки переписаны по факту, мёртвый
`scenarios.yaml` и пустые каркасы `tests/{filesystem,pipeline,tables}/` удалены. **F50/F53/F54 закрыты.**

---

### B1 — Харнесс и auth · ✅ ЗАКРЫТ S24 (T1 ✅ S18 · S1 ✅ S19 · T2 ✅ S24 — F51 закрыт)

| Фикс | Тест | Защита |
|---|---|---|
| **F51** нет харнесса живого сервера · **F52** покрытие не измерялось · **F14** auth fail-open | **T2** `tests/harness/` (сервер на свободном порту + tmp-workspace + JSON-RPC + **чтение консоли**) · **T1** coverage-ratchet | **S1** auth fail-CLOSED: без токена не стартуем, `.env.example`, identity как ключ |

**Готово, когда:** `test_firewall` переехал на фикстуру и **вошёл в CI**; C2 (`08 §6`) впервые физически
исполним — симуляция ассертит эффект в консоли; тесты auth: 401/401/200 + отказ старта без токена.

---

### B2 — Слепые зоны и запрет опасных файлов · 🔨 **S2 ✅ S19+S20** (F34/F68 закрыты, мутации M40/M44/M45/M46 краснеют) — остался F55 (слепые зоны хозяев)

| Фикс | Тест | Защита |
|---|---|---|
| **F55** слепые зоны (bot_army не видит боевые лимиты, structure — containment, search — фильтры) | **T10** расширяем ХОЗЯЕВ + мутационная приёмка как регламент | **S2** write-type allowlist default-deny (**F34**), код `FILE_TYPE_FORBIDDEN` |

**Готово, когда:** каждая 🟢-мутация из `CATALOG §F` стала 🔴; `.sh`/`.html`/`.exe` не материализуются,
`.json/.md/.xlsx/.yaml/.py` проходят; allowlist живёт в конфиге, не в коде.

---

### B3 — Outbound: сервер перестаёт быть оружием · 🔨 **S3 ✅ S19** (F33 закрыт: провенанс + force-confirm + чистый журнал; остаток — табличные чтения) — остался T6=S7 рой (F32 🔴)

| Фикс | Тест | Защита |
|---|---|---|
| **F33** outbound-gap (OUT1/5/6/7) | **T6**=**S7** раннер роя `agent_swarm` (36 паттернов, **F32**) — красная команда против своих защит | **S3** провенанс вывода · `force`-confirm на деструктиве · санитизация `Fact.data` в `_SESSION_LOG` |

**Готово, когда:** инъекция из файла возвращается помеченной как данные (не инструкция); `fs_delete` без
`force` отказывает; лог без сырых данных; рой: 4 критерия (inbound блокирован · outbound не пробит ·
честные без ложных банов · эмерджентность).

---

### B4 — Табличный слой: доделать A1′ · ✅ **ЗАКРЫТ S22** (F20 схемы+конфиг в листах · F56 атомарная правка · F30 деградация формул · F28 зависимости столбцов)

| Фикс | Тест | Защита |
|---|---|---|
| **F56** `table_update` (мульти-поле, одна атомарная операция в очереди) + `table_find_row` (значение → row_id) · **F30** декларативная деградация формул · авторинг схем (**F20/F21**, ждёт решения владельца: интроспектор vs руками) · **F28** dependency-aware delete/move столбцов | **T5** E-D деструктив над таблицами · E-E тайминг ID · E-F устойчивость формул (что не построено — `xfail(strict)` со ссылкой на F#) | — (данные, не периметр) |

**Готово, когда:** правка строки атомарна (5 полей = ОДНА операция в очереди, частичного применения
нет), `table_find_row` отдаёт `row_id` по значению; неполные данные → пусто/`PENDING`/0, а не `#DIV/0!`; удаление столбца под формулой даёт
реакцию, а не молчаливый коррапт; каждая ячейка E-D…E-F либо зелёная, либо честный `xfail`.

---

### B4′ — Система ID: иерархия и адресация · ✅ **ЗАКРЫТ S19** (F60–F67 все закрыты; остаток вынесен в B5 как F25)

| Фикс | Тест | Защита |
|---|---|---|
| **F60–F67 ✅ ВСЕ ЗАКРЫТЫ (S19):** F63 создание теряло предков · F65 перенос мимо реестра · F61 ручное создание без ID/владельца · F64 адресация по ID (`find_by_path`, `DUPLICATE_PATH`, `resolve_ref`) · F62 подстановка `{parent:channel}` · F60 поиск по ID в ФС (личность из реестра, не из имени) · F66 индекс реестра (найти ID, не зная ID) · F67 ревизия `dangerous_tools` | `test_structure` §13–§21 (40 → 82 проверки) + мутации **M16–M22** в `CATALOG §F` | инвариант «один путь = одна сущность» (`DUPLICATE_PATH`), `check_integrity` сверяет путь с диском, усыновление только явное (`adopt=true`) |

**Готово, когда:** цепочка одинакова независимо от числа вызовов · перенос не рвёт личность (ID неизменны, адрес пересчитан) · ни один каталог не получает второй ID · поиск находит файлы по `owner_id`/`chain_prefix` (F60 — остаток).

---

### B5 — Структура: рекомендации и целостность · 1–2 сессии

| Фикс | Тест | Защита |
|---|---|---|
| **F26** cold-start advisory · **F25** reconcile ФС↔реестр · **F27** авто-оркестрация переноса | **T4** E-матрица (E-A конфиг-матрица ниши, E-B точки входа, E-C метаморфный инвариант проходов) | — |

**Готово, когда:** пустая ниша → сервер выдаёт онбординг-совет; `check_integrity` видит незарегистрированное
дерево на диске; `end_state(3-pass) == end_state(2-pass) == end_state(1-pass)`.

---

### B6 — Периметр под нагрузкой и изоляция хоста · 1 сессия

| Фикс | Тест | Защита |
|---|---|---|
| **F36** identity-rate / slowloris / payload-limit · **F35** нет регрессии no-root | `bot_army` расширяется (смена IP при одном принципале не обходит лимит) + инвариант-тест «не root, нет exec-sinks» | **S4** origin-лимиты + edge-runbook Cloudflare · **S5** deploy-hardening (NoNewPrivileges/cap-drop/read-only) |

**Готово, когда:** обход лимита сменой IP не проходит; тело сверх лимита и медленный клиент отбиваются;
инвариант-тест краснеет на добавленный `shell=True`.

---

### B7 — Качество поиска и контракт протокола · 1–2 сессии

| Фикс | Тест | Защита |
|---|---|---|
| **F31** нет eval-слоя · остаток **F40/F42** (дубль exception-классов, overclaim «умного» поиска) · **F41/F46** таксономия в 4 источниках · **F45** inline-skeleton | **T8** eval (E-I relevance → E-G фикстуры → E-H промпт-vs-данные, opt-in) · **T7** conformance в CI + parity G14/D30 | **S6** анти-rug-pull: описания только из git, reload не меняет `tools/list` |

**Готово, когда:** precision/recall считается на синтетике (без облачных LLM); conformance-джоба обязательна;
таксономия сущностей — один источник; reload не двигает инвентарь ни на байт.

---

### B8 — Зависимости, наблюдаемость, продукт · далее по roadmap

| Фикс | Тест | Защита |
|---|---|---|
| **F47** CVE в зависимостях · **I5** observability/audit-trail · **F3/F10** провайдеры-стабы → продукт (P2–P4, FFmpeg последним) | **T9** sec-инварианты как регрессии · продуктовые unit+contract+honest-stub | **S8** pip-audit из advisory в хард-гейт · **S1′** OAuth 2.1 |

---

## Сводка «одна проблема → где она закрывается»

| F# | Sev | Блок | Чем закрывается |
|---|---|---|---|
| F50, F53 | 🔴🟡 | B0 | T0, T3 |
| F54 | 🟠 | B0 | S0 |
| ~~F51~~ | ✅ S24 | B1 | T2 ✅ (T1 ✅ закрыл F52) |
| F14 | 🔴 | B1 | S1 |
| F55 | 🟠 | B2 | T10 |
| F34 | 🟠 | B2 | S2 |
| F33 | 🔴 | B3 | S3 |
| F32 | 🔴 | B3 | T6/S7 |
| F56, F30, F20, F21, F28 | 🟠 | B4 | A1′ + T5 |
| F25, F26, F27 | 🔴🟠 | B5 | A3 + T4 |
| F35, F36 | 🟠 | B6 | S4, S5 |
| F31, F41, F45, F46, остаток F40/F42 | 🟠🟡 | B7 | A5 + T7/T8 + S6 |
| F47, F3, F10 | 🟠🟡 | B8 | S8, P-ось |
| F8, F24 (остаток), F15, F16, F17, F53 | 🟡 | попутно | закрываются внутри своих блоков |

**Закрыты/сняты верификацией S18:** F1, F2, F4, F5, F6, F7, F9, F11, F12, F13, F18, F19, F22, F23, F29,
F37, F38, F39, F43, F44, F48, F49 — статусы и пруфы в `02 §6`.

---

## Регламент блока (чтобы не разъезжалось)

1. **Открыть блок:** `engineering-questions` по типу задачи → перечитать соответствующие `F#` в `02` и зону
   теста в `tests/CATALOG.md`.
2. **Фикс первым.** Правка — по домену (`mcp-developer` / `reactions-errors` / `anti-hardcode`).
3. **Тест сразу за фиксом**, в ХОЗЯИНА зоны (не плодить набор). Обязательна **мутационная проверка**: откатить
   фикс → тест обязан покраснеть → вернуть. Результат — строкой в `CATALOG §F`.
4. **Защита/инвариант** — этап `S#` блока; для security-этапа приёмка = симуляция с чтением консоли (C2).
5. **Закрыть блок:** обновить `02` (статус F# + пруф), `CATALOG §E/§F`, `_sessions.md`, коммит с отчётом
   (что/почему/регрессии/F#), убедиться, что CI зелёный (`gh run list` — урок F48).
