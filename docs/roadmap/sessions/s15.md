# Сессия 15 — записи журнала

> Перенесено из `../_sessions.md` 2026-09-02: закрытое уходит из
> активного журнала. Записи побайтово те же; адрес каждой — в указателе
> `../_sessions.md` §«Указатель журнала».

### Сессия 15 (доп. 4) — переход к тестам (ступень C1): реестр подтверждения находок

**Выбор владельца:** сперва **тестами подтвердить найденные проблемы** (§6 C1 = код-пруф), а не Блок 0. Статусы — git-tracked (полный прогон в лимит не влезает).
- **`tests/CATALOG.md §E` — реестр подтверждения находок** (F#→метод static/behavioral→тест-хозяин→статус ✅/⬜/🔨/🟢). Развёл: **static** (F38/F39/F41/F44/F45/F10/F30 — подтверждены grep/ls, закрываются lint I4 или постройкой) vs **behavioral** (F43/F5/F40/F42/F28-29/F37/F11 — нужен C1-тест). «Не плодить»: каждая находка привязана к тесту-хозяину (test_audit_fixes/test_search/test_tables/render_draft_final).
- **Приоритет C1:** F43→F5 (ядро реакций B2) · F40→F42 (search) · F28/F29 (формулы) · F37 · F11.
- **Скил `test-master` привязан к реестру** (reading-order + стратегия-ссылка → `tests/CATALOG.md §E`), чтобы ИИ видел покрытие и статусы. (Скил — глобальный конфиг, вне git репо; реестр CATALOG.md — в git.)

### Сессия 15 (доп. 5) — C1 F43 подтверждён (strict-xfail, код НЕ трогаем)

- **F43 C1 в `test_audit_fixes`** (strict-xfail механика `xcheck`): `table_get_row` на нет-таблице → `reaction_class='unknown'` vs реестр `'ai_recoverable'` → **OPEN-CONFIRMED** (находка доказана красным). Baseline **31/31 зелёный**, exit=0 (открытая находка не ломает базу; внезапный pass → сигнал обновить §E).
- **Бонус-инсайт (B2 вживую):** `recovery.suggested_tool` СОВПАЛ с реестром — не фикс, а **дубль код↔yaml** (движок хардкодит ту же строку). Убрал вводящую в заблуждение проверку, оставил чистый пруф по `reaction_class`. Записано в F43.
- Реестр §E: F43 ⬜→🟡 OPEN-CONFIRMED; добавлена механика strict-xfail в легенду.

### Сессия 15 (доп. 6) — C1 F5/F40/F42 подтверждены (strict-xfail, НЕ чиним)

- **F5** OPEN-CONFIRMED: `get_error(unknown).message='Непредвиденная ошибка'` vs `DEFAULT.message_template='Непредвиденная ошибка.'` — шаблон реестра игнорится. NB: `reaction_class='unknown'` совпал с `DEFAULT.class` (как suggested_tool у F43 — не пруф, меряли шаблон).
- **F40** OPEN-CONFIRMED: `PATH_NOT_FOUND`/`QUERY_NOT_FOUND` (коды core/search) отсутствуют в `server_reactions.yaml`.
- **F42** OPEN-CONFIRMED: `QueryPlanner._match_filter({"x":"abc"},{"x":{"gt":5}})` → `TypeError` (str vs int), не деградация.
- Все в hub `test_audit_fixes` (единый xcheck-механизм, не плодим). Baseline **31/31 зелёный**, 4 OPEN-CONFIRMED, 0 unexpected, exit=0. §E: F5/F40/F42 ⬜→🟡.

### Сессия 15 (доп. 7) — C1 F29/F11 + behavioral-набор ЗАКРЫТ

- **F29** OPEN-CONFIRMED: `insert_formula("=1/0")` → `validate_formulas` `ok=True` (греп токенов без пересчёта = театр).
- **F11** 🟢 регрессия ЗЕЛЁНАЯ: D23-санитайзер маскирует `api_key`/вложенный `token` → `***REDACTED***`, несекретное не тронуто.
- **F28/F37 → static** (эвидентны из чтения: F28 сырой `delete_cols`, F37 `_safe:513`; фикс A-tables/A2). Baseline **34/34**, 5 OPEN-CONFIRMED, exit=0.
- **Behavioral C1-набор ЗАКРЫТ:** все находки с наблюдаемым эффектом подтверждены (F43·F5·F40·F42·F29 🟡 + F11 🟢). static-находки (F28/F37/F38/F39/F41/F44/F45/F10/F30) — эвидентны, закрываются lint I4/A2/постройкой.

### Сессия 15 (доп. 9) — статус закрытия прохода 1 + MVP-пул готовых решений

**Что осталось для прохода 1 (обмер-цикл):** ✅ ЗАКРЫТ как groundwork:
- ✅ Ступень A (обмер) — 7 подов, «есть vs должно», F# засеяны.
- ✅ C1 — behavioral находки подтверждены (strict-xfail), static эвидентны, реестр `CATALOG §E` живой.
- ✅ Переоценка зрелости `07`; спорные B1–B4 решены; требования `11` (R-#); тест-реестр.
- ⬜ Остаток до ПОЛНОГО §6-прохода = **ступень B (адаптация) + фиксы + C2 (симуляции+консоль)** — это уже **сборка MVP**, начинается сейчас.

**MVP-пул (`04` новая секция) — консолидация кандидатов под первый зрелый MVP БЕЗ ffmpeg:** 12 позиций
по воркстримам (I4 линт→I3 CI→A2 распил→A3 ops→A6 реакции→A5 search→A-tables формулы→A7 уникальность→
P media→I5 obs→I6 auth→conformance), каждая: R-# + готовое решение + DIM + в-MVP?. **FFmpeg ВНЕ MVP**
(P1, продакшен-уровень, последним). Безопасность — из `06`, GitHub не ищем. Порядок = путь к L3 из `11`.

**План владельца (проходы 2-3):** повторить обмер-цикл на более высоком уровне зрелости + усиливать
зрелость репо и качество продукта — это их цель.

### Сессия 15 (доп. 8) — конечный фид требований через инженерные вопросы (`11`)

**Указание владельца:** рано и фиксить, и фундамент — сперва прогнать накопленное через `engineering-questions`, чтобы сформировать **конечные требования всей системы** (для прохода 2 + адаптации готовых решений ступени B).
- **Новый `11_system_requirements.md`:** находки F# + зрелость DIM-1…16 + патерны качества/стиля/анти-хардкод/безопасность + дизайны (A7/реакции/B1-B4) сведены в **R-# требования** по 7 осям банка вопросов (границы/контракт/безопасность-обе-стороны/хардкод/тесты/конфиг/продукт+долгосрок). Каждое R-#: *что нужно* → *откуда* (F#/DIM/OQ) → *приёмка* → *GitHub-кандидат* (кроме sec — из `06`).
- Ключевые R-#: R-ARCH1/2/3 (tools/ + ops.yaml + read.json↔.xlsx синк) · R-CONTRACT1/2/3 (единое ядро реакций, коды⊂реестр, parity) · R-SEC-IN/OUT/AUTH (из `06`) · R-DECL1/2/3 (таксономия 1 источник, per-project override, B2-принцип) · R-TEST1/2/3 (strict-xfail→регрессия, C2+консоль, honest-stub) · R-PROD1/2/3 (провайдеры, формульный слой, A7).
- **Как использовать:** проход 2 = per-воркстрим R-#+F#+DIM → §6 A→B(GitHub-кандидат)→C1→C2; поиск решений = R-# с непустым «GitHub-кандидат» → `04`/`08 §4`.
- README пропатчен (doc 11).

**RESUME:** требования сформированы. Дальше проход 2 по воркстримам через §6 (R-#→B адаптация→C1✅→C2 консоль→🟡→🟢), приоритет A6 реакции (R-CONTRACT1, F43/F5/F40). ИЛИ Блок 0 фундамент. C2 — когда фиксы применены. По-прежнему НЕ чиним до решения владельца о старте фиксов.

**Итог обмера (7 подов) — БЛОК 1 ступень A ЗАВЕРШЁН:** инфраструктура зрелее памяти (G17-choke-point/контракты-Literal/D23-санитайзер/D30-транспорт/wiring/честные-стабы), незрелы точечно: реакции-эмиссия (F43/B2 — реестр обходится хендлерами), data-формульный слой (F28–30, loader ∅), search-quality (F38–42), продукт (стабы). Новые F37–F46. Спорные B1–B4 решены. **Переоценка зрелости в `07`.** Дальше по плану: Блок 0 (фундамент I4→I3) ИЛИ ступени B/C по воркстримам (не «весь проход» — обмер был только A).

### Сессия 15 — Блок 1 обмер, широкий проход (state-vs-expectations)

**Сделано:** первый заход обмера (ступень A §6) — общая карта состояния через ЧТЕНИЕ кода (не память), чтобы размерить поды. Лупа `code-quality`.
- **✅ Состояние ЛУЧШЕ ожиданий:** G17 choke-point реально консолидирован (`core/paths.safe_resolve` — единая реализация, импортит все 6 модулей с путями); D29 закрыт в state_manager; `create_server` wiring чист (D2/D4/D24); хендлеры ТОНКИЕ (`_safe(lambda: engine.method())`, логика в core, ошибки→коды реакций). **Следствие: A2 = механическая экстракция хендлеров из одной функции `register_basic_tools` (1120 стр) в `tools/<group>/`, риск НИЗКИЙ** (движки не трогать). Пересматривает F1: дефект = размещение, не логика-в-обёртке.
- **🟠 Подтверждено:** F1/F3/F4/F5/F13/F14/F24/F25/F26/F28–F30 держатся (глубокая проверка — в подах).
- **Новое:** F37 (`_safe` ловит голый ValueError→всегда PATH_ESCAPE).
- **Реестр спорных моментов (OQ-B1…B4)** — для решения владельцем: B1 `_SESSION_LOG.md` в корне репо (нарушение 2 вселенных + OUT6 + дубль памяти — убрать/в workspace/заменить audit-trail); B2 A3 ops-слой vs уже-декларативный inline `engine.register` (пересмотреть F23: заменить, не поверх); B3 два движка на одни .xlsx (`table_engine` read+queue vs `excel_engine` openpyxl — CQRS или случайность? где источник правды); B4 `_safe` широкий catch.

**Тесты:** кода не трогал (только `02_findings.md`) → baseline держится.

**Коммит:** `docs(roadmap): Block-1 obmer pass 1 — state map + disputable ledger (session 15)`.

### Сессия 15 (доп.) — спорные моменты OQ-B1…B4 решены владельцем (проверено на диске)

- **B1 (`_SESSION_LOG`):** корень репо = **намеренно** (техн. инфа, не клиентские данные → не нарушение 2 вселенных). **Дубля НЕТ** (пруф: один файл 47KB, один write-путь `state_manager.py:184`). Остаётся OUT6-exfil сырого `Fact.data` → санитизировать (security F33), файл не двигать.
- **B2 (дубль ошибок код↔yaml):** корень — **у ошибок нет ядра**; движки хардкодят message-строки И есть `server_reactions.yaml` → расхождение. Решение: **yaml=единственный источник**, движки эмитят только код, ядро подставляет message/recovery. Принцип: есть `.yaml` → обязано ядро-SoT, без параллельного хардкода (то же к A3: ops.yaml ЗАМЕНЯЕТ inline-register). → A6. F5 обновлён.
- **B3 (два движка):** **дубля нет, но и не CQRS** — ДВА ХРАНИЛИЩА: `read.json` (данные+оба поиска читают его) vs `.xlsx` (структура/формулы), синк НЕ построен (G16). Источник правды данных = `read.json`. ⚠️ **A7-скрипт уникальности читает/пишет `read.json`**, не `.xlsx`-формулы (doc 10 §2 обновлён).
- **B4 (`_safe` ValueError):** моё решение — типизир. `PathEscapeError(ValueError)` в `core/paths.py`; `_safe` ловит его→`PATH_ESCAPE`, голый `ValueError`→`INTERNAL_ERROR`. Back-compat цел. → A2/A6 с §6-тестом.

### Сессия 15 (доп. 2) — обмер под-2: `core/search` (оба модуля прочитаны, НЕ чиним)

- **❗ F4 НЕВЕРНА → понижена:** search НЕ «0 тестов/не ревьюен». `test_search.py` содержателен (happy+контракт+**adversarial D36 traversal**+edge+QueryPlanner+`_detect_entity_type`); в коде следы **D36/D37** (уже находили+чинили). Незрелость была переоценена; остаётся relevance-eval (F31) + quality-находки.
- **Новые находки F38–F42:** F38 мёртвый `_lock` в обоих классах (D28); F39 `QueryPlanner` лезет в приватный `table_engine._load` (DIM-13, хрупко); F40 дубль exception-классов + коды `QUERY_NOT_FOUND`/`PATH_NOT_FOUND` мимо реестра + `_safe` их не ловит (B2 в search); F41 захардкожена иерархия workspace+ID-формат (DIM-14, дубль `templates/workspace`, ломается при кастомной структуре S14); F42 overclaim «умного» поиска (`analyze_dependencies`=group-by, не граф; фильтрация≠семантика F31) + разнотипные сравнения → TypeError (F30).
- **Консистентно с B3:** табличный поиск читает `read.json` через `table_engine._load` (данные-SoT).

### Сессия 15 (доп. 3) — обмер под-3: реакции/ошибки (B2-корень доказан). Коммиты держим до конца обмера.

- **B2-корень ДОКАЗАН:** две ветки эмиссии ошибок. Хендлеры (`_safe`→`_err`, все data-инструменты) строят `ErrorDetail` из полей исключения, **реестр не трогают** → теряют `reaction_class`/`message_template`/`recovery.suggested_params`. Только `engine.execute` (engine.py:64) идёт через `get_error`, но `raw_message` перекрывает template. **F43** (новое): реестр обходится на доминирующей ветке (G14/D30-наоборот — реестр богаче эмитируемого). **F5** уточнён.
- **✅ Паритет кодов ЧИСТ:** 9 кодов движков ⊂ реестр (34). Орфан только search-коды `QUERY_NOT_FOUND`/`PATH_NOT_FOUND` (F40 подтверждён).
- Что должно: единое ядро — любой путь через `get_error(code, params)`, движки эмитят код, реестр даёт class/message/recovery.

**RESUME:** обмер продолжается (комиты держим по указанию владельца до конца проверки фактов): монолит `server.py` карта распила A2 · формулы `excel_core` (F28–30) · провайдеры-стабы (F3/F10/F11) · контракты/state/transport. Потом — противопоставить «есть vs должно» на карте зрелости `07`, переоценить уровень. НЕ чиним. Метод §6 ступень A.
