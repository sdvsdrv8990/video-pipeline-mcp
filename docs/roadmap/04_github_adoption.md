# 04 — GitHub-решения под нереализованные системы

> Кандидаты для «не изобретать заново». **Иерархия доверия** (из skill-curator): официальная дока →
> зрелые/звёздные репо → случайный gist. **Подсмотреть ≠ вставить**: берём паттерн, переписываем под наш
> стиль/безопасность (Pydantic v2, ToolResult-контракт, Russian-comments).
> Сессия 1 = **скелет-список кандидатов**; глубокая per-repo оценка + план адаптации — отдельным проходом (по воркстриму).

## Продукт (ось P)

| Система | Кандидат-направление | Что взять | → |
|---|---|---|---|
| P1 FFmpeg | внешний FFmpeg MCP-сервер (проект уже проектировался как адаптер к нему) | протокол trigger/poll/download; НЕ тащить рендер к себе | P1 |
| P1 FFmpeg (fallback) | `ffmpeg-python` / прямой subprocess-обёртка | если внешний MCP не готов — локальный рендер за адаптером | P1 |
| P2/P4 TTS/IMG | **LiteLLM** (уже в дизайне) — единый прокси к облачным провайдерам | routing/fallback-цепочки, retry, стоимость | P2, P4, A4 |
| P3 STT | **stable-ts** / faster-whisper (локально) | локальная транскрибация; device-выбор в конфиг (не хардкод cuda, F10) | P3 |
| P5/P6 Pipeline | паттерны step-оркестрации (напр. лёгкие DAG-раннеры) — идея, не тяжёлый фреймворк | контракт «шаг = чистая функция над ToolResult» | P5, P6 |

## Инфра (ось I) — тут решения зрелые и стандартные

| Система | Кандидат | Что взять | → |
|---|---|---|---|
| I2 Packaging | `pyproject.toml` (PEP 621) + `uv`/`pip-tools` | пиннинг, extras, entry-points | I2 |
| I3 CI/CD | GitHub Actions matrix | lint+mypy+pytest+coverage; security: `bandit`, `gitleaks`, `pip-audit` | I3 |
| I4 Lint/типы | `ruff` (линт+формат) + `mypy` + `pre-commit` | конфиги под наш стиль | I4 |
| I5 Observability | `structlog` + Prometheus client / OpenTelemetry | структурные логи, метрики, `/health`, trace-id по запросу | I5 |
| I6 Auth/secrets | app-level токен (за туннелем) + `python-dotenv`/vault-паттерн | заголовок-аутентификация MCP; секреты вне репо | I6 |

## Скилы (инструментарий разработки)

| Потребность | Кандидат-репо (подсмотреть) | Решено |
|---|---|---|
| Навигация по коду | — | нативные Grep/Glob/Explore/LSP; внешний скил = дубль (см. сессию про чистку) |
| Реакции/ошибки как система | наш `server_reactions.yaml`-подход + MCP error-handling best-practices | создаётся выделенный скил (Сессия 1) |
| Code hygiene с ИИ | наш guard «таски в код» + code-quality/project-conventions | покрыто |

## MVP-пул готовых решений (после обмера — ступень B; БЕЗ ffmpeg)

> Консолидирует кандидатов из этого файла + `08 §4` + `11` (R-#-кандидаты) в **один пул под первый зрелый
> MVP**. Цель MVP: продукт-петля видео-препродакшена работает e2e на зрелом уровне **до рендера** — структура/
> таблицы/патерны-уникальность + генерация media-ассетов (tts/stt/img). **FFmpeg-рендер ВНЕ MVP** (P1 — на
> серьёзном продакшен-уровне, тогда же в оценку зрелости). Порядок = путь к L3 из `11`.
>
> Правило: **подсмотреть паттерн → переписать под наш контракт/стиль → тесты** (§ниже). Deep per-repo research
> (WebFetch офиц.дока→репо) — в момент адаптации воркстрима, не заранее.

| # | Воркстрим | R-# | Готовое решение (адаптировать) | Поднимает | В MVP? |
|---|---|---|---|---|---|
| 1 | **I4** линт/типы | R-DECL2/3 | `ruff`+`mypy`+`pre-commit` (конфиг под стиль) — закрывает static F38/F44/F45/F10 | DIM-13/14 | ✅ |
| 2 | **I3** CI | R-TEST | GitHub Actions matrix: ruff+mypy+pytest+`bandit`+`gitleaks`+`pip-audit`; firewall/tunnel=integration→skip | DIM-6/8 | ✅ (гейт под C-тесты) |
| 3 | **A2** распил монолита | R-ARCH1 | layout `github/github-mcp-server` (структура групп `tools/<g>/`) — паттерн, не копия | DIM-13/15 | ✅ |
| 4 | ~~**A3** ops-слой~~ ❌ снят S22 | R-ARCH2 | ops-реестр упразднён как слой; декларативность держат `config/providers.yaml` / `server_reactions.yaml` / `firewall.yaml` / `config/templates/` | DIM-14 | ❌ |
| 5 | **A6** ядро реакций | R-CONTRACT1/2 | НЕТ внешнего (наш `server_reactions.yaml`); паттерн «errors-as-data» + MCP error-handling — все пути через `get_error` | DIM-10 | ✅ (закрывает F43/F5/F40) |
| 6 | **A5** харден search | R-DECL1 | `rapidfuzz`/`rank-bm25` для relevance (F31); фикс F38–F42 — наш код | DIM-6 | ✅ |
| 7 | **A-tables** формулы | R-PROD2 | `formulas`/`pycel` (реальный пересчёт → убить театр F29) + `table_materializer` (loader F30) | DIM-12 | ✅ (ядро продукта) |
| 8 | **A7** уникальность | R-PROD3 | `datasketch`(MinHash)/`rapidfuzz` — n-gram/shingling локально (скрипт по `read.json`, `10 §2`) | DIM-12 | ✅ |
| 9 | **P3/P2/P4** media | R-PROD1 | **stable-ts**/faster-whisper (STT локально, device в конфиг) · **LiteLLM** (TTS/IMG прокси, routing/fallback) | DIM-12 | ✅ (генерация ассетов) |
| 10 | **I5** observability | R-CFG/sec | `structlog`+`/health`+OpenTelemetry + audit-trail | DIM-7 | ✅ (для «зрелого») |
| 11 | **I6** auth | R-AUTH | MCP SDK OAuth 2.1 Resource Server (из `06`/M3) → `core/auth/` | DIM-2 | ✅ (за туннелем app-auth) |
| 12 | **DIM-1** conformance | R-CONTRACT3 | `modelcontextprotocol/conformance` как CI-гейт | DIM-1 | 🟡 желательно |
| — | **P1** FFmpeg | R-PROD1 | внешний FFmpeg-MCP / `ffmpeg-python` | DIM-12 | ❌ **ВНЕ MVP** (продакшен-уровень, последним) |

**Безопасность (R-SEC-IN/OUT):** решения и паттерны фиксов — **из `06`**, GitHub повторно НЕ ищем (правило `08 §6 B`).

## Правило адаптации

1. Официальная дока библиотеки (Context7/сайт) → 2. звёздный референс-репо → 3. переписать под наш контракт/стиль → 4. тесты (honest-stub → реальные) → 5. цитировать источник в `history_*.md`. **Никакого слепого копипаста и скрытого поведения** (principle of least surprise).

## Вердикты карантина (программа код-ревью, S25+)

> **Куда складывается найденное.** Проблемы копятся в `02_findings.md` (у него есть машинный
> читатель — `findings_count.py` и `invariants.status_off_registry`); РЕШЕНИЯ — здесь. Кандидат
> попадает в таблицу только с вердиктом, а вердикт выносится прогоном: отдельное дерево
> (`git worktree`) и окружение, кандидат подсажен НА МЕСТО нашего решения, судят его наши карты и
> наши сторожа. Рейтинг, звёзды и README отвечают на «стоит ли смотреть», а не на «лучше ли нашего».
> **Отказ записывается наравне с принятием** — иначе отвергнутый кандидат возвращается через полгода
> на новый разбор.

| Кандидат | Куда метил | Замер | Вердикт |
|---|---|---|---|
| **Свой тонкий слой на `sqlite3` FTS5** (СТАНДАРТНАЯ библиотека, ноль новых зависимостей) — индекс по разделам `##` каталога памяти | «сколько токенов стоил ВЕРНЫЙ ответ» — критерий владельца №1 (выхлоп) | **Замер ПОСЛЕ нарезки (2026-09-02), 136 разделов, 8 реальных вопросов проекта, улика у каждого — проверяемая строка; промах доплачивает чтением файла целиком, иначе дешевизну получает тот, кто не ответил.** Чтение файла целиком: 36 533 токена на восемь ответов (4566 на ответ), попадание 8/8. FTS5 top-1: 19 808 (2476), 6/8. FTS5 top-3: 20 332 (2541), 7/8. `grep` по разделам: 193 553 (24 194), 8/8 — то есть **без ранжирования поиск в ПЯТЬ РАЗ ХУЖЕ, чем просто открыть файл**. Стенд целиком — 90 строк | ✅ **ГОДЕН, и потолок выхлопа теперь известен: 1,8×.** Не 25×, как считалось: те 25× мерились ДО нарезки и заниженной прикидкой `слова × 1.3` (`F208`). Нарезка забрала выигрыш себе — движок добирает остаток и платит за него двумя промахами из восьми |
| `agentscope-ai/ReMe` (3385★, форков 292, Apache-2.0, push 2026-09-01, не архивирован) — markdown + frontmatter + wikilinks, BM25 без LLM | тот же выхлоп, что у тонкого слоя, но готовым решением | **Гейт владельца пройден: жив И рейтинг** — значит режим «источник готового решения», смотреть можно. Судит второй критерий: **17 рантайм-зависимостей**, среди них `fastapi`, `uvicorn`, `fastmcp`, `numpy`, `openai` — это отдельный сервер, а не библиотека поиска. Потолок выхлопа при этом ОБЩИЙ с тонким слоем и уже измерен: 1,8×, потому что упирается он в корпус, а не в качество ранжирования. **Побочно: репозиторий ПЕРЕЕХАЛ** `modelscope/ReMe` → `agentscope-ai/ReMe` — наша запись S25 звала старый адрес, `api.github.com` отвечает `Moved Permanently` | ❌ **не берём** (2026-09-02): за тот же измеренный выхлоп 17 зависимостей и второй сервер против 90 строк стандартной библиотеки. Ценен как источник ИДЕЙ формата (markdown+frontmatter+wikilinks — у нас он уже такой) |
| `basicmachines-co/basic-memory` (3838★, форков 268, **AGPL-3.0**, push 2026-09-02, 100 коммитов за 4 недели) — SQLite FTS + `sqlite-vec` | то же место | **Гейт владельца пройден: жив И рейтинг.** Отсекает третий факт, и он не про вкус: **40+ рантайм-зависимостей** (`sqlalchemy`, `alembic`, `asyncpg`, `psycopg`, `fastembed`, `openai`, `logfire`, `pillow`, `pyright`, `pytest-asyncio` — тестовые пакеты в рантайме), `fastmcp==4.0.0b1` — **пин на бету**, и `litellm>=1.60.0,<1.92.0`. Последнее — прямой конфликт: у нас объявлен `litellm>=1.84.0`, в `.venv` стоит **1.96.2**, то есть подсадка требует ОТКАТИТЬ нашу зависимость на четыре минорных версии. Их собственный комментарий называет причину: `litellm` 1.92 принёс Rust-мост, чей PyO3 отвергает Python 3.14 — **а мы на 3.14.7**. Директива владельца 2026-08-20 закрывает вопрос прямо: библиотека, требующая откатить чужую версию, вне поверхности поддержки — ищем альтернативу, а не костыль | ❌ **не берём** (2026-09-02). Отдельное окружение сняло бы конфликт версий, но завело бы второй сервис под обслуживание — то есть проиграло бы по второму критерию владельца ровно там, где выигрывать было нечего: выхлоп у всех троих один и тот же 1,8× |
| `typing.assert_never` + `mypy` (СТАНДАРТНАЯ библиотека и линтер, уже стоящий в джобе `lint`) | опасная половина класса 1 «ветвление по значению»: не «новый случай правкой кода», а «о забытой ветке никто не узнаёт» | **Замер до кандидата:** 4 цепочки при потолке 4; ни одна не проверялась на полноту разбора, и потребитель, молча роняющий вид, был невидим. **Поиск на GitHub не понадобился — механизм оказался в стандартной библиотеке**, а судья уже стоял в гейте: цена усыновления = ноль пакетов, ноль строк конфигурации. **Проверено прогоном:** объявлен `LineKind` (5 видов), оба потребителя туннеля заканчиваются `assert_never` → `mypy` зелёный; мутация «снять ветку `disconnected`» → `exit=1` и вид НАЗВАН значением (`Literal['disconnected']`), а не «где-то неполно». **Честный предел:** работает только там, где вид приходит типом — на `op.get("action")` из данных очереди (тип `Any`) исчерпываемость не выражается, там кричит `else`. **Побочная улика:** первый же прогон нашёл живую дыру в готовности туннеля (`F151`) | ✅ **ГОДЕН, усыновлён S25.** Из библиотеки — механизм, наше — политика: какие словари объявлены закрытыми. Своей оси под этот класс НЕ пишем |
| `ruff` правила `PLR2004`/`S105`/`S106` (уже в `.venv`, выключены нашей политикой) | класс 2 «магические литералы», класс 4 «секреты в коде» | `PLR2004` — 172 находки, в серверном коде 25: HTTP-статусы, размерности тензоров, эпсилоны сравнения. `S105`/`S106` — 9, ВСЕ ложные (имя переменной окружения `MCP_AUTH_TOKEN`, фикстуры наборов) | ❌ **отказ как сторожу** (S25): включённый врал бы чаще, чем находил, а врущего сторожа выключают целиком — вместе со всеми его настоящими проверками. Годен разовой линзой под присмотром |
| `ast-grep` 0.45.2 (15,7k★, MIT, push 2026-08-27) — структурный матчер по дереву, правила объявлением в YAML | класс 1 «ветвление по значению» как замена нашей оси; классы 2/5 как линза | Класс 1: правило в 8 строк нашло на живом дереве ТЕ ЖЕ 4 места и те же строки, что 46 строк нашего кода. Семь синтетических образцов: совпали на пяти, разошлись на двух — и в обоих прав кандидат (наш `_chain` видит только строковые значения, F172). Но из девяти храповиков **восемь — стык кода с декларацией**, а матчер видит только исходник: заменить он способен ОДНУ ось из девяти. Цена — бинарь 47,9 МБ в гейте и новая цепочка поставки | ⚖️ **годен как ЛИНЗА, не годен как механизм** (S25). Линзой он окупился сразу: правило `$CFG.get($KEY,$DEF)` вскрыло `F170` — 29 копий объявлений в форме, невидимой нашей оси. Но саму находку закрывают **15 строк stdlib `ast`** и ноль зависимостей, а прототип на них нашёл 29 против 24 у кандидата и сверил каждую с адресом декларации, а не с именем ключа |
| `import-linter` 2.14 (1151★, BSD-2, push 2026-08-28) — архитектурные контракты ОБЪЯВЛЕНИЕМ (`.importlinter`), граф через `grimp` | ось 1 «архитектура-адеренс» (этажность) и ось 6 «Bounded Context» — у обеих машинного читателя не было ВОВСЕ | На живом дереве: 102 файла, 214 зависимостей, оба контракта KEPT — независимое подтверждение нашего замера (0 перекрёстных импортов между девятью группами) плюс НОВЫЙ факт: `core` не импортирует `tools` ни разу, этажность цела. Обратная сторона доказана внесением двух нарушений в карантинное дерево: `exit=1`, оба контракта BROKEN, назван контракт И цепочка распространения (`tools.memory → tools._context → core.ids → core.ids.link_registry → tools.tables`); после снятия — снова 2 kept, `exit=0` | ✅ **ГОДЕН** (S25). Правила — декларация, а не наш код; ловит то, чего мы не видели; обе стороны красные там, где надо. Цена усыновления: строка в группе `[dev]` манифеста, файл `.importlinter` и вызов в гейте. **Ставит два «нуля» под охрану вместо того, чтобы писать их самим** |
| `import-linter` 2.14 — **УСЫНОВЛЁН S25 (2026-08-29)** | ось 1 «архитектура-адеренс» (этажность) и ось 6 «Bounded Context» | **Замер до кандидата:** 9 групп `tools/`, 0 перекрёстных импортов; `core` не импортирует ни `tools`, ни `server`; своего читателя у обеих осей не было. **Цена:** 2 пакета (`grimp` + сам), строка в `[dev]`, контракты в `[tool.importlinter]`, шаг в джобе `lint` и хук pre-commit. **Проверено прогоном:** 102 файла, 214 зависимостей, 2 контракта KEPT; мутация «`tools/media` импортирует `tools.excel`» → `exit=1` и имя сломанного контракта. **Честный предел:** контракты судят ПАКЕТЫ — `server.py` это модуль, `grimp` его в корни не берёт, поэтому этаж сервера контрактом не выражается и остаётся за нашей осью. **Формула допуска дала спор:** 1151★ при поле 1000 ✓, но форков 87 при поле 100 ✗ — по букве регламента рейтинг НЕ высокий; усыновлено решением владельца, а не подгонкой замера | ✅ **ГОДЕН, усыновлён** |
| `semgrep` 1.175 (16434★, форков 1040, LGPL-2.1, push 2026-08-28) — правила объявлением, межфайловый анализ | классы 1 и 2 как ЗАМЕНА нашим осям `dispatch_by_value` и `mirrored_declaration` | **Замер до кандидата:** наши 4 находки класса 1 (потолок 4) и 34 класса 2 (потолок 34). **Класс 1 — паритет по охвату, хуже по точности:** правило в 12 строк нашло ТЕ ЖЕ четыре цепочки, но выдало 7 срабатываний — по одному на каждый `elif`, то есть свёртку цепочки пришлось бы дописывать нам. Обобщение на нестроковые значения (`if code == 404`) ничего не прибавило: на этом дереве таких цепочек нет. **Класс 2 — НЕ ВЫРАЖАЕТСЯ:** уликой у нас служит связь ДВУХ ИМЕНОВАННЫХ источников (ключ `config/*.yaml` ↔ литерал в `.py`), а единственный механизм semgrep для такой связи — `mode: join` — экспериментальный, не развивается и схемой 1.175 уже ОТВЕРГАЕТСЯ («One of these properties is missing: 'languages'»); рекомендованная замена (cross-file analysis) — поток данных между файлами КОДА и платная, к декларациям отношения не имеет. **Цена:** 360 МБ окружения против 46 строк нашей оси (`_chain` 16 + `dispatch_by_value` 30); прогон быстрый — 1,2 с. **Побочная улика карантина:** шим `venv/bin/semgrep` — sh-скрипт, ушедший в чужой `~/.local/bin/pysemgrep` (сентябрь 2025) и вернувший `exit=0` при `ModuleNotFoundError`; карантин обязан проверять версию СВОИМ путём | ❌ **НЕ ГОДЕН как механизм** (класс 2 не выражается вовсе, класс 1 — паритет ценой 360 МБ). Как линза дублирует уже разобранный `ast-grep`, поэтому и линзой не берём |
| `vulture` 2.16 (4780★, MIT, push 2026-04-30) — мёртвый код глубже модуля (методы, атрибуты, переменные, недостижимый код) | ось 4 «мёртвое» ниже уровня модуля: наш `module_without_reader` судит только модуль целиком и стоит на 0 | При `--min-confidence 100` — 2 находки: `core/ids/link_registry.py:352` (`return ""` СРАЗУ после `return {…}`, настоящий недостижимый код) и ложная `__context` из хука Pydantic. При 60% — 41; поштучная проверка вскрыла ТРИ класса лжи, каждый специфичен нашей архитектуре: методы, которые зовёт Pydantic (9); классы, которых зовёт ДЕКЛАРАЦИЯ (`config/providers.yaml: tts.piper_local:PiperLocalTTS` — 2); присваивание атрибута ЧУЖОГО объекта, где запись и есть действие (`openpyxl` `c.font`, `piper` `cfg.length_scale` — 2). Плюс намеренный резерв с записанной причиной (`ANNOTATIONS_DESTRUCTIVE`). Подтверждённых настоящих — 4 | ⚖️ **годен как ЛИНЗА, сторожем — только с тремя белыми списками** (S25). Первый из них наш `module_without_reader` уже умеет («считать объявление чтением обязательно, иначе вся декларативная архитектура числилась бы мёртвой») — то есть кандидат слабее нашего там, где у нас есть своё, и сильнее там, где нет |
