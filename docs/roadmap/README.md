# Enterprise Roadmap — video_pipeline_mcp

> Программа доведения проекта до **корпоративного уровня**. Ведётся мультисессионно:
> каждая рабочая сессия закрывает 1–2 воркстрима и коммитится в git с полным отчётом.
> Этот каталог **git-tracked** (в отличие от `docs/dev/`, который в `.gitignore`).
> Когда работы завершатся — тех-документацию можно будет отозвать из git.

## Как читать

| Файл | Что внутри |
|---|---|
| [`00_reality_check.md`](00_reality_check.md) | **Читать первым.** Реальное состояние на диске vs заявленное в README/памяти. Фундамент плана. |
| [`01_master_roadmap.md`](01_master_roadmap.md) | Фазовый план по 3 осям (Продукт / Архитектура / Инфра). Воркстримы P#/A#/I#, разбивка на сессии. |
| [`02_findings.md`](02_findings.md) | Реестр проблем (F#). Пополняется каждым прогоном. Severity + ось + воркстрим. |
| [`03_testing_plan.md`](03_testing_plan.md) | План тестирования под специфику проекта: симуляции, безопасность, реакции/ошибки. |
| [`04_github_adoption.md`](04_github_adoption.md) | Готовые решения с GitHub под нереализованные системы + план адаптации. |
| [`06_threat_catalog.md`](06_threat_catalog.md) | **Каталог угроз + митигаций** (inbound IN1–IN10 / outbound OUT1–OUT8), восстанавливает `threat_landscape` (F16), питает agent-swarm (F32). Приоритет фиксов защиты. |
| [`07_maturity_rubric.md`](07_maturity_rubric.md) | **Рубрика зрелости** (12 измерений × L0–L4) + честная само-оценка (сегодня L1 с карманами L2) + карта развития. «Линейка» для вопроса «на какой стадии репо». Сильные MCP-репо-эталоны. |
| [`08_pass_closure.md`](08_pass_closure.md) | **Закрытие проходов**: ремап удалённого `docs/dev/` (source-of-truth), список недостающих файлов (LICENSE/CI/Docker/…) по гейтам, exit-чеклисты, сильные репо→уровень/стиль/структура, реконсиляция скилов. |
| [`09_agent_org.md`](09_agent_org.md) | **Оргструктура агентов**: 3 отдела (Разработка/Тестирование/Безопасность) + оркестратор, роли/скилы/база знаний, git-native воркфлоу, разрешение конфликтов, назначение ВСЕХ задач по проходам/сессиям. ⚠️ Агент-типы `.claude/agents/` **удалены** (S17, работа идёт через Claude AI Web) — роли читать как чек-листы ответственности. |
| [`10_pattern_uniqueness_system.md`](10_pattern_uniqueness_system.md) | **Патерн-слой и слой уникальности** (воркстрим A7): патерны конкурентов vs наши + порядок чтения, отслеживание структуры + повторяемых слов/предложений, фоновый расчёт %+сигнал через петлю памяти, реакции-рекомендации (сервер через Claude AI Web не навязывает поток), 2 критерия уникальности сцены, конфиг-шаблоны ФС/таблиц и поиска через инструменты. Расширяет `UNIQUENESS`/`SCENES`/`*_PATTERNS_USED` из `05`. |
| [`11_system_requirements.md`](11_system_requirements.md) | **Конечный фид требований** (через `engineering-questions`): накопленное (находки F#, зрелость DIM-1…16, патерны качества/безопасности, дизайны) сведено в R-# требования по осям (границы/контракт/безопасность/хардкод/тесты/конфиг/продукт) — что система обязана стать, приёмка, GitHub-кандидат для адаптации. Опора прохода 2 и ступени B. |
| [`12_a2_split_plan.md`](12_a2_split_plan.md) | **План распила монолита A2** (готов к исполнению): `ToolContext` + `tools/<group>/`, 8 шагов = 8 зелёных коммитов, критерий приёмки = 52 инструмента + все тест-наборы зелёные + реал-пруф. Крупная задача №1 — не начата (осознанно, свежий контекст). |
| [`13_a1_table_loader_plan.md`](13_a1_table_loader_plan.md) | **План табличного loader A1′** (`table_materializer`, готов к исполнению): по `tables_pending` материализует `.schema.yaml` → .xlsx; фаза ТАБЛИЦЫ + декларативная деградация неполных данных (F30) + авторинг 6 схем (F20/F28). Крупная задача №2 — не начата. |
| [`14_testing_system_plan.md`](14_testing_system_plan.md) | **Программа развития СИСТЕМЫ тестирования** (воркстрим I7): `03` = что покрывать, `tests/CATALOG.md` = кто хозяин сценария, этот = **чем и в каком порядке строим станок** — pytest-раннер, фикстуры, харнесс живого сервера + чтение консоли (условие C2), покрытие, гейт. Обмер тест-системы (F50–F53) + этапы T0–T9 с приёмкой + подъём DIM-6 до L3. |
| [`spec/`](spec/README.md) | **Канон намерения** — импортированные спек-файлы владельца (инструкции + схемы данных). Функц. улучшения — `spec/IMPROVEMENTS.md`. |
| [`_sessions.md`](_sessions.md) | Журнал сессий: что сделано, коммит, что дальше. Один RESUME-указатель. |

## Механика программы

- **Единица работы** — воркстрим (`P1`, `A2`, `I3`…) из мастер-roadmap. Сессия берёт 1–2.
- **3 прогона** (оси ревью, не разовые): `quality` (code-quality), `style/structure` (project-conventions + anti-hardcode), `systems/security` (security-reviewer + test-master). Каждый прогон сеет находки в `02_findings.md`, привязан к измерениям зрелости (`08 §3.1`) и **честно закрывается по `08 §6`** (обмер всех файлов → адаптация GitHub кроме sec → простые тесты + симуляции с чтением консоли).
- **Каждая сессия:** делает работу → обновляет `02_findings.md` + `_sessions.md` → коммит в git с отчётом → обновляет память проекта.
- **Правило истории (обновлено 2026-07-05):** `docs/dev/` удалён → решения→факты пишем в `_sessions.md` + commit-сообщение + память (НЕ в `docs/dev/history_*.md`). Полный ремап — `08_pass_closure.md §1`.
- **Тесты зелёные** до и после каждого изменения (baseline см. `_sessions.md`).

## Статус

**Сессия 18 (2026-08-11) — обмер тест-СИСТЕМЫ и программа I7 ([`14`](14_testing_system_plan.md)):** baseline зелёный
(**288 проверок в гейте**, 24 вне), но сама машинерия незрелая. **F50** 🔴 — `pytest` объявлен и не используется:
запуск `pytest` даёт **ложный зелёный** (`test_audit_fixes` с 44 регрессиями не собирается; сим-функции проходят
всегда — `check()` не ассертит; script-наборы исполняются на коллекции), а гейт держится на ручном списке 12 файлов
в `ci.yml` (так выпал оффлайн-способный `test_tunnel`, 20 проверок). **F51** 🔴 — нет харнесса живого сервера и
чтения консоли ⇒ ступень **C2 из `08 §6` структурно незакрываема**, E-матрица и раннер роя заблокированы.
**F52** 🟠 покрытие ни разу не измерено · **F53** 🟡 тест-доки описывают несуществующее. План: T0 раннер ∥ T3 доки →
T1 покрытие → **T2 харнесс** (снимает блокеры) → T4 E-матрица ∥ T6 рой → T5 таблицы → T9 sec-инварианты → T7
conformance → T8 eval. ≈6–8 сессий, DIM-6 L1–L2 → L3.

**Сессия 18 (доп.) — верификация реестра F1–F53 + мутационная проверка тестов:** 14 мутаций (откат фикса →
тест обязан покраснеть) — **11/14 пойманы**, ядро регрессий не театр (`tests/CATALOG.md §F`). Исправлены
разошедшиеся с диском статусы: **F13** 🔴→✅ (CI есть и зелёный), **F14** переформулирован (bearer-auth есть
с первого коммита; дефект = **fail-open по умолчанию** + статический токен + 0 тестов), **F6 опровергнута**,
F9 неактуальна, F15/F8/F24/F17 понижены, F47 расширен. Новое: **F54** 🟠 мёртвые `enabled:`-выключатели в
боевом `config/firewall.yaml` (код ключ не читает) · **F55** 🟠 слепые зоны тестов (bot_army не проверяет
боевые лимиты, structure — containment, search — фильтры, auth не покрыт) → этап **T10** в `14`.

**Сессия 17 (2026-08-11) — уборка репо + починка CI:** `.claude/agents/` (7 агент-типов) удалены — мёртвый
груз при работе через Claude AI Web (см. `09`). **F48 ЗАКРЫТ:** CI-гейт I3 был красным с рождения (7/7
прогонов `failure` за 12 с — `pip install -e .[dev]` падал на PEP 639: SPDX-выражение + классификатор
`License :: *`; gitleaks-action фейлился без платной лицензии). Роадмап при этом рапортовал «I3 ✅» —
ложный зелёный. Сейчас: классификатор убран, gitleaks = пиннутый бинарь (v8.30.1, `git`-скан истории).

**Сессия 16 (2026-07-06) — фаза фиксов: 7 воркстримов закрыты, все тесты зелёные, 0 открытых behavioral-находок.**
Закрыто и на GitHub: A6 реакции (F43/F5/F40) · I4 линт/типы (F38/F44) · I3 CI-гейт · A5 F42 · A-tables F29
(LibreOffice recalc) · tunnel-fix · C2 6 adversarial-наборов (реконсиляция под модель угроз). Тесты: C1
audit 41/41·structure 35/35·tables 33/33·search 24/24 · интеграция firewall 4/4(live)·tunnel 20/20 · C2 6/6.

### Крупные запланированные задачи (не начаты — статус на GitHub)

| # | Задача | План | Статус |
|---|---|---|---|
| 1 | **A2 распил монолита** `server.py` → `tools/<group>/` | [`12_a2_split_plan.md`](12_a2_split_plan.md) | ✅ **ЗАКРЫТО (S17)** — 8 шагов = 8 зелёных коммитов; 1477 → 402 строки, 52 инструмента целы (реал-пруф на живом сервере), F1 закрыт |
| 2 | **A1′ табличный loader** (`table_materializer`, F20/F30/F28) | [`13_a1_table_loader_plan.md`](13_a1_table_loader_plan.md) | 🔨 **В РАБОТЕ (S17)** — шаги 1–2 готовы (loader + фаза `structure_materialize_tables`, 24 теста); дальше F30-деградация, авторинг схем (ждёт ответа владельца), F28 |

**Хвост прохода 2** (меньше объёмом): A5-relevance (F31 rapidfuzz/bm25) · dep-hardening (F47 CVE) · A3 ops-слой ·
A7 уникальность ([`10_pattern_uniqueness_system.md`](10_pattern_uniqueness_system.md)) · media P2–P4 ([`05_data_template_media_system.md`](05_data_template_media_system.md)). Детали и RESUME — `_sessions.md`.
