# Enterprise Roadmap — video_pipeline_mcp

> Программа доведения проекта до **корпоративного уровня**. Ведётся мультисессионно:
> каждая рабочая сессия закрывает 1–2 воркстрима и коммитится в git с полным отчётом.
> Этот каталог — **вся документация проекта**: `docs/dev/` удалён владельцем (2026-07-05),
> история файлов и решений = git (`git log --follow`, `git blame`) + журнал [`_sessions.md`](_sessions.md).

## Три способа войти в программу

| Хочу… | Иду в |
|---|---|
| понять, **что реально есть на диске** | [`00_reality_check.md`](00_reality_check.md) — замер, актуализируется каждый раз при расхождении |
| **взять следующую задачу** | [`_sessions.md` → RESUME](_sessions.md) (что закрыто, что дальше) → [`16_execution_matrix.md`](16_execution_matrix.md) (блок = фикс + тест + защита) |
| **работать по плану, а не по сессии** | [`01_master_roadmap.md`](01_master_roadmap.md) (воркстримы P/A/I со статусами) → тематический план: [`13`](13_a1_table_loader_plan.md) таблицы · [`14`](14_testing_system_plan.md) тесты · [`15`](15_security_system_plan.md) защита · [`10`](10_pattern_uniqueness_system.md) уникальность · [`05`](05_data_template_media_system.md) данные/медиа |

Часть планов **не привязана к сессиям** — их можно брать напрямую из таблицы ниже. Обратная связь
обязательна: закрыл шаг плана → отметил его в плане, в [`02_findings.md`](02_findings.md) и в журнале.

## Как читать

| Файл | Что внутри |
|---|---|
| [`00_reality_check.md`](00_reality_check.md) | **Читать первым.** Реальное состояние на диске vs заявленное. Фундамент плана; при расхождении прав диск. |
| [`01_master_roadmap.md`](01_master_roadmap.md) | Фазовый план по 3 осям (Продукт / Архитектура / Инфра). Воркстримы P#/A#/I#, разбивка на сессии. |
| [`02_findings.md`](02_findings.md) | Реестр проблем (F#). Пополняется каждым прогоном. Severity + ось + воркстрим. |
| [`03_testing_plan.md`](03_testing_plan.md) | План тестирования под специфику проекта: симуляции, безопасность, реакции/ошибки. |
| [`04_github_adoption.md`](04_github_adoption.md) | Готовые решения с GitHub под нереализованные системы + план адаптации. |
| [`05_data_template_media_system.md`](05_data_template_media_system.md) | **Данные/шаблоны/медиа** по спекам владельца: почему `config/ops` и `model_routing` упразднены (§0), конфиг канала (с S22 — листы книги `channel_data`), выбор media-модели через `RESOURCE_LIMITS`. |
| [`06_threat_catalog.md`](06_threat_catalog.md) | **Каталог угроз + митигаций** (inbound IN1–IN10 / outbound OUT1–OUT8), восстанавливает `threat_landscape` (F16), питает agent-swarm (F32). Приоритет фиксов защиты. |
| [`07_maturity_rubric.md`](07_maturity_rubric.md) | **Рубрика зрелости** (16 измерений × L0–L4) + само-оценка. Актуальный вердикт — раздел «Переоценка S23»: **L2 с карманами L3**; на нуле остались observability (DIM-7) и эконом-контейнмент (DIM-11). Сильные MCP-репо-эталоны. |
| [`08_pass_closure.md`](08_pass_closure.md) | **Закрытие проходов**: ремап удалённого `docs/dev/` (source-of-truth), список недостающих файлов (LICENSE/CI/Docker/…) по гейтам, exit-чеклисты, сильные репо→уровень/стиль/структура, реконсиляция скилов. |
| [`09_agent_org.md`](09_agent_org.md) | **Оргструктура агентов**: 3 отдела (Разработка/Тестирование/Безопасность) + оркестратор, роли/скилы/база знаний, git-native воркфлоу, разрешение конфликтов, назначение ВСЕХ задач по проходам/сессиям. ⚠️ Агент-типы `.claude/agents/` **удалены** (S17, работа идёт через Claude AI Web) — роли читать как чек-листы ответственности. |
| [`10_pattern_uniqueness_system.md`](10_pattern_uniqueness_system.md) | **Патерн-слой и слой уникальности** (воркстрим A7): патерны конкурентов vs наши + порядок чтения, отслеживание структуры + повторяемых слов/предложений, фоновый расчёт %+сигнал через петлю памяти, реакции-рекомендации (сервер через Claude AI Web не навязывает поток), 2 критерия уникальности сцены, конфиг-шаблоны ФС/таблиц и поиска через инструменты. Расширяет `UNIQUENESS`/`SCENES`/`*_PATTERNS_USED` из `05`. |
| [`11_system_requirements.md`](11_system_requirements.md) | **Конечный фид требований** (через `engineering-questions`): накопленное (находки F#, зрелость DIM-1…16, патерны качества/безопасности, дизайны) сведено в R-# требования по осям (границы/контракт/безопасность/хардкод/тесты/конфиг/продукт) — что система обязана стать, приёмка, GitHub-кандидат для адаптации. Опора прохода 2 и ступени B. |
| [`12_a2_split_plan.md`](12_a2_split_plan.md) | **План распила монолита A2** — ✅ **исполнен (S17)**: `ToolContext` + `tools/<group>/`, 8 шагов = 8 зелёных коммитов. Хранится как образец метода «крупная задача = цепочка зелёных коммитов». |
| [`13_a1_table_loader_plan.md`](13_a1_table_loader_plan.md) | **План табличного слоя A1′** — ✅ **исполнен (S17–S22)**: материализация `.schema.yaml` → .xlsx, фаза ТАБЛИЦЫ, деградация формул (F30), зависимости столбцов (F28), три режима создания. |
| [`14_testing_system_plan.md`](14_testing_system_plan.md) | **Программа развития СИСТЕМЫ тестирования** (воркстрим I7): `03` = что покрывать, `tests/CATALOG.md` = кто хозяин сценария, этот = **чем и в каком порядке строим станок** — pytest-раннер, фикстуры, харнесс живого сервера + чтение консоли (условие C2), покрытие, гейт. Обмер тест-системы (F50–F53) + этапы T0–T9 с приёмкой + подъём DIM-6 до L3. |
| [`15_security_system_plan.md`](15_security_system_plan.md) | **Программа развития СИСТЕМЫ защиты** (воркстрим I6 + `core/firewall`): `06` = какие угрозы, `14` = чем доказываем, этот = **что и в каком порядке строим в защите** — этапы S0–S8 (конфиг-не-лжёт → auth fail-closed → write-allowlist → outbound-провенанс → origin-лимиты → no-root/deploy → анти-rug-pull → рой → supply-chain), у каждого тест-хозяин и приёмка. Фиксирует факт защиты, доказанный мутациями. |
| [`16_execution_matrix.md`](16_execution_matrix.md) | **Матрица исполнения — сшивка всего**: блоки B0–B8, где один блок = одна-две сессии = **фикс `F#` + тест `T#` + защита `S#`**. Директива владельца «сначала фиксы»: внутри блока фикс → тест (с мутационной приёмкой) → инвариант. Сводка «одна проблема → где закрывается» + регламент блока. **Точка входа для планирования сессии.** |
| [`spec/`](spec/README.md) | **Канон намерения** — импортированные спек-файлы владельца (инструкции + схемы данных). Функц. улучшения — `spec/IMPROVEMENTS.md`. |
| [`_sessions.md`](_sessions.md) | Журнал сессий: что сделано, коммит, что дальше. Один RESUME-указатель. |

## Механика программы

- **Единица работы** — воркстрим (`P1`, `A2`, `I3`…) из мастер-roadmap. Сессия берёт 1–2.
- **3 прогона** (оси ревью, не разовые): `quality` (code-quality), `style/structure` (project-conventions + anti-hardcode), `systems/security` (security-reviewer + test-master). Каждый прогон сеет находки в `02_findings.md`, привязан к измерениям зрелости (`08 §3.1`) и **честно закрывается по `08 §6`** (обмер всех файлов → адаптация GitHub кроме sec → простые тесты + симуляции с чтением консоли).
- **Каждая сессия:** делает работу → обновляет `02_findings.md` + `_sessions.md` → коммит в git с отчётом → обновляет память проекта.
- **Правило истории (обновлено 2026-07-05):** `docs/dev/` удалён → решения→факты пишем в `_sessions.md` + commit-сообщение + память (НЕ в `docs/dev/history_*.md`). Полный ремап — `08_pass_closure.md §1`.
- **Тесты зелёные** до и после каждого изменения (baseline см. `_sessions.md`).

## Статус на S23 (2026-08-14)

**Общий вердикт: L2 (Beta) с карманами L3** — см. [`07` → «Переоценка S23»](07_maturity_rubric.md).
Замер: `server.py` 465 строк · 67 инструментов · 8 групп `tools/` · 66 модулей `core/` ·
58 кодов реакций · 31 тест-файл в git · CI с 5 хард-гейтами · 144 коммита.

**Закрыто:** Фаза 0 целиком (I1/I2/I3/I4) · A1′ таблицы · A2 распил монолита · A6 реакции ·
блоки B0/B4/B4′ · план `13` (три режима создания) · A7.2 слой уникальности ·
защита S0/S1/S2/S3/S8 · ключи провайдеров в канале · подъём локальных моделей **на видеокарте** без root.

**Оставшиеся долги, по порядку блокирования:**

| # | Долг | Почему блокирует | Где |
|---|---|---|---|
| 0б | **F83 🟡 — 6 пустых каталогов-реликтов + 3 ссылки в коде на снятые слои** | дёшево и убирает ложный сигнал «слой есть»; движка реакций/ошибок у нас нет и не нужно — единый источник держат реестр и две точки маппинга | [`02`](02_findings.md) F83 |
| 0 | **F76 🟠 — `structure_materialize_tables` всегда `INTERNAL_ERROR`** | инструмент обещан в `tools/list` и не работает; мелкий фикс, но сломанное обещание клиенту | [`02`](02_findings.md) F76 |
| 1 | **T2 — харнесс живого сервера** (F51 🔴) | без него C2-симуляции и приёмка периметра структурно неисполнимы | [`14`](14_testing_system_plan.md), блок B1 в [`16`](16_execution_matrix.md) |
| 2 | **I5 — observability + audit-trail + quotas** | единственные измерения, оставшиеся на L0 (DIM-7/11) | [`01`](01_master_roadmap.md) ось I |
| 3 | **P-ось — продукт** | `pipeline/` и `tools/video/` пусты, облачные провайдеры = честные стабы; это и есть «видео-пайплайн», ради которого проект назван | [`01`](01_master_roadmap.md) ось P |

**Снято осознанно:** `A3` (`config/ops`) — ops-слой упразднён вместе с `model_routing`;
`config/paths.yaml` — пути сведены в `core/paths.py`; `X1` — аудит-словарь слит в `02_findings.md`.

Понарративно по каждой сессии — [`_sessions.md`](_sessions.md) (журнал; порядок записей смешанный,
ищи по заголовку `### Сессия N`). Прежний блок «Статус», застрявший на Сессии 18, снят: его содержимое
целиком есть в журнале.
