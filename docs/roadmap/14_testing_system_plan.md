# 14 — Программа развития СИСТЕМЫ тестирования (воркстрим I7)

> Разделение ответственности: [`03_testing_plan.md`](03_testing_plan.md) = **ЧТО** покрывать (слои, E-матрица
> E-A…E-I, eval, рой) · [`../../tests/CATALOG.md`](../../tests/CATALOG.md) = **КТО хозяин** сценария (зоны, «не плодить», реестр F#→тест) ·
> **этот документ = ЧЕМ и В КАКОМ ПОРЯДКЕ строим сам инструмент тестирования** (раннер, фикстуры, харнесс
> живого сервера, гейт, метрики покрытия) + приёмка каждого этапа.
>
> **Исполняется не сам по себе:** этапы `T#` разложены по блокам в [`16_execution_matrix.md`](16_execution_matrix.md) — в одном блоке с
> фиксом `F#` и этапом защиты `S#` из [`15_security_system_plan.md`](15_security_system_plan.md). Планирование сессии начинается с `16`.
>
> Причина отдельного документа: `03` описывает богатую программу сценариев, но она **упирается не в сценарии,
> а в отсутствующую машинерию** — без раннера, харнесса и чтения консоли ни E-матрица, ни рой, ни C2 (`08 §6`)
> физически не пишутся. Сначала станок, потом детали.

---

## 1. Факт на 2026-08-11 (измерено прогоном, не по памяти)

Все in-process наборы **зелёные**:

| Набор | Проверок | Как гоняется | В CI-гейте |
|---|---|---|---|
| `quick/test_tools_inventory.py` | 60/60 | скрипт | ✅ |
| `quick/test_audit_fixes.py` (дом D#/F#-регрессий) | 44/44, 0 OPEN | скрипт (async `main()`) | ✅ |
| `quick/test_search.py` | 24/24 | скрипт | ✅ |
| `quick/test_structure.py` | 35/35 | скрипт | ✅ |
| `quick/test_tables.py` | 33/33 | скрипт | ✅ |
| `quick/test_table_materializer.py` | 30/30 | скрипт | ✅ |
| `virus_injection` · `cache_injection` · `bot_army` · `cache_overflow` · `config_change` · `render_draft_final` | 5·5·22·4·11·15 = 62 | скрипты (in-process `Firewall`/`create_server`) | ✅ |
| `quick/test_tunnel.py` | 20/20 | скрипт, **работает оффлайн** | ❌ исключён как «нужен туннель» |
| `quick/test_firewall.py` | 4 теста | нужен живой сервер `:8080` | ❌ |

**Итого в гейте: 288 проверок. Вне гейта: 24** (tunnel оффлайн-способен, firewall — единственный live-набор).

Декларации без исполнителя: `agent_swarm/patterns.yaml` (36 паттернов, раннера нет — F32) ·
`tests/config/scenarios.yaml` (ссылается на несуществующие `tests/runner.py` / `tests/scenarios/`).

---

## 1a. Обмер ФОРМЫ тестов (2026-08-24, прогоном) — почему дальше идут T11–T14

Машинерия построена (T0–T2), а форма самих наборов осталась прежней и стала главной статьёй расхода:

| Мера | Факт | Чем плохо |
|---|---|---|
| Наборы · строки | 24 · **8747** | — |
| Вызовы `check()` вручную | **1283** | одинаковая обвязка «позвать → сверить» переписана 1283 раза |
| Упоминания `D#/F#/M#` в исходниках | **85** | статус находки имеет второго хозяина помимо реестра |
| Строки процессного шума (`OPEN-CONFIRMED`, «обнови `CATALOG §E`») | **27** | тест хранит состояние процесса и устаревает молча |
| Многострочные docstring | **37** (до 12 строк) | правило «одна строка» есть в `tests/README.md` и в скиле `test-master` с 2026-07-05 |

Вывод: правило без механики не держится (тот же класс, что `F48`). Форму лечит не вычитка, а
**объявление сценария + один исполнитель** (`T11`) и перевод наборов с доказательством непотери (`T12`).

## 2. Обмер тест-системы (ступень A по `08 §6`) — что есть vs что должно

| # | Есть сейчас | Должно быть | Находка |
|---|---|---|---|
| 1 | 12 самописных скриптов, каждый со своим счётчиком `results`/`_checks` и `sys.exit` в `__main__`. `pytest` в dev-deps **объявлен, но не используется** | один раннер, один отчёт, селекция по маркерам, автоподхват нового теста | **F50** 🔴 |
| 2 | Гейт = **ручной список 12 файлов** в `ci.yml`. Новый набор не попадает в CI, пока человек не допишет строку; `test_tunnel` (20 проверок) так и выпал | `pytest -m "not live"` — гейт видит всё, что лежит в `tests/` | **F50** 🔴 |
| 3 | Нет фикстур: `sys.path`-инъекция и tmpdir копируются в каждом файле; состояние firewall сбрасывается вручную | `conftest.py`: `repo_root`, `tmp_workspace`, `firewall_reset` | **F50** 🔴 |
| 4 | Нет харнесса живого сервера (поднять на свободном порту, свежий `workspace/`, JSON-RPC клиент) и **нет чтения консоли** | `tests/harness/` + маркер `live`; C2 наблюдает stdout/логи | **F51** 🔴 |
| 5 | Покрытие **никогда не измерялось**; «дыры» в `03` — экспертные, не посчитанные; порога нет | `--cov` baseline + ratchet-порог в CI | **F52** 🟠 |
| 6 | Тест-доки описывают несуществующее и противоречат директиве владельца 2026-07-05 | доки = факт | **F53** 🟡 |
| 7 | **Зоны `CATALOG` шире фактического покрытия** (вскрыто мутациями S18): `bot_army` не проверяет боевые лимиты, `test_structure` не бьёт по containment, `test_search` без фильтров, auth не покрыт вообще | каждая заявленная зона подтверждена мутацией | **F55** 🟠 |

### F50 — `pytest` даёт ЛОЖНЫЙ ЗЕЛЁНЫЙ (пруфы)

```
$ pytest tests/quick/test_audit_fixes.py -q     → no tests ran        # 44 регрессии НЕВИДИМЫ
$ pytest tests/virus_injection tests/bot_army   → 7 passed in 0.01s   # 27 проверок «прошли» за 10 мс
$ pytest tests/quick/test_structure.py -q --collect-only → no tests collected in 0.71s
```

Механика: `check(name, cond)` кладёт `bool` в список и **никогда не ассертит** — гейт живёт в `main()` под
`if __name__ == "__main__"`. Под pytest функция `test_*` отрабатывает без единого `assert` → PASS при любом
исходе проверок. Script-наборы (`structure`/`tables`/`search`/`inventory`/`materializer`) вообще не отдают
тест-функций: их тело исполняется **в момент коллекции**, результат к отчёту не привязан.

Это класс F48 («гейт добавлен ≠ гейт зелёный»), только наоборот: **зелёный, который ничего не проверил**.
Пока раннер не канонизирован, любой `pytest` в CI/у контрибьютора — обман.

---

## 3. Целевая архитектура тест-системы

```
pytest (канонический раннер, xfail_strict=true)
├── conftest.py            общие фикстуры: repo_root · tmp_workspace · firewall_reset
├── harness/               ЖИВОЙ сервер: спавн на свободном порту · JSON-RPC клиент /mcp · захват консоли
├── quick/                 C1 — unit/contract/регрессии D#/F# (in-process, быстро)
├── <sim>/                 C2 — adversarial-симуляции против живого сервера + ассерты по консоли
├── structure_emulation/   E-A…E-C (реальная работа ИИ со structure_*)
├── agent_swarm/           рой: patterns.yaml (36) + раннер, honest+attacker в одном прогоне
└── eval/                  качество (relevance/поток данных) — opt-in, вне гейта
```

Маркеры: `live` (нужен поднятый сервер) · `sim` (адверсариал) · `slow` · `eval` (opt-in).
CI: `pytest -m "not eval"` — харнесс поднимает сервер сам, cloudflared не требуется; `eval` — отдельная ручная джоба.

---

## 4. Этапы

| # | Этап | Что делаем | Приёмка (доказуемая) | Зависит | Сессий |
|---|---|---|---|---|---|
| **T0** ✅ | **Канонизация раннера** — СДЕЛАНО S18 | `[tool.pytest.ini_options]` (testpaths, markers, `xfail_strict=true`) + `tests/conftest.py`. **Мост, не переписывание:** каждому скрипту — `def test_<name>_suite(): assert main() == 0`, `__main__` сохраняется (baseline не двигаем). CI: 12 строк → `pytest -m "not live"` | `pytest -q` красный, если сломать любую проверку (проверяется намеренной поломкой и откатом); `test_audit_fixes` виден раннеру; удаление строки из `ci.yml` больше не прячет тест | — | 1 |
| **T1** ✅ | **Покрытие** — СДЕЛАНО S18 (факт 61%, ratchet-порог 60 в CI) | `pytest --cov=core --cov=tools --cov=server`; зафиксировать baseline-число как порог (ratchet, не аспирация); отчёт — артефакт CI | порог в `ci.yml`, падение покрытия = красный гейт; в `03` вписаны ПОСЧИТАННЫЕ дыры вместо экспертных | T0 | 0.5 |
| **T2** ✅ | **Харнесс живого сервера + консоль — СДЕЛАНО S24** | `tests/harness/`: `live_server.py` (свободный порт, свежий tmp-`workspace/`, готовность, гарантированный teardown) · `jsonrpc.py` (`tools/list`/`tools/call`) · `console.py` (захват stdout/stderr + ассерты по логам — требование C2 из `08 §6`) | ✅ Приёмка выполнена: `test_firewall` переписан на харнесс (22 проверки, C2 — ассерты по консоли сервера) и **вошёл в гейт** — `pytest` без `-m 'not live'` даёт 17/17, маркер `live` пуст, CI больше не отсекает периметр. Побочно закрыта дыра вне формулировки: рабочая область сервера была прибита гвоздём (`MCP_WORKSPACE` не существовало), и живой тест правил бы боевые данные | T0 | ✅ |
| **T3** ✅ | **Правда тест-доков** — СДЕЛАНО S18 | `tests/README.md` (снять несуществующие `filesystem/`/`pipeline/`/`tables/`, `test_import.py`; «5 вопросов в docstring» → директива 2026-07-05: docstring в одну строку, рацио в commit; запуск через pytest; `history.md` → git-native) · `scenarios.yaml` — свернуть в `CATALOG.md` и удалить (мёртвая декларация) · `CATALOG §E` счётчики (audit 44/44, inventory 60/60, materializer 30/30) · память `project-rules §3.3` | grep: ни один тест-док не ссылается на несуществующий файл/каталог; ни один не требует «5 вопросов в docstring» | — (парал. T0) | 0.5 |
| **T4** ✅ | **E-матрица структуры — СДЕЛАНО, отмечено 2026-08-24** (E-A/E-B/E-C) | `tests/structure_emulation/`: E-A конфиг-матрица ниши (orphan/childless), E-B 4 точки входа × depth-control, E-C метаморфный инвариант 3≡2≡1 прохода. E-A1 cold-start = `xfail(strict)` на F26 | E-A2/A3/A4 зелёные, E-A1 xfail-spec; `end_state(3-pass)==end_state(2-pass)` по `structure_check_integrity`; расхождение = находка | T2 | 1–1.5 |
| **T5** 🟠 | **Таблицы: ~~E-D~~ ✅ сценариями S25 · E-E/E-F открыты** | Расширяем **хозяев** (`test_tables`, `test_table_materializer`) — новый набор только при утяжелении (правило CATALOG). E-D деструктив→коды реакций (F28 = xfail-spec), E-E тайминг ID (вкл. out-of-order), E-F деградация формул (xfail-spec до F30) | каждая ячейка E-D…E-F либо зелёная, либо `xfail(strict)` со ссылкой на F# — «молчаливых» нет | T2, хвост A1′ (Ф3) | 1 |
| **T6** | **Раннер роя** (F32) | `agent_swarm/test_agent_swarm.py`: исполняет `patterns.yaml` (36 паттернов, поле `status` → xfail-spec), смесь honest+attacker в одном прогоне против живого сервера | 4 критерия `03`: inbound блокирован с кодом · outbound не пробит · честные без ложных банов · эмерджентность (чередование honest/attacker с одного IP) | T2 | 1–1.5 |
| **T7** | **Conformance** (DIM-1) | `modelcontextprotocol/conformance` в CI + strict-schema-регрессия; ~~закрыть parity G14/D30~~ ✅ **паритет закрыт и проверен живьём S24** (`test_firewall` §3): класс реакции и recovery доживают до `CallToolResult` в `structuredContent`. Остаётся сам conformance-гейт | conformance-джоба зелёная и обязательная; parity-тест в `test_tools_inventory` | T0 | 0.5–1 |
| **T8** | **Eval-слой качества** (F31) | E-I relevance по `core/search` (слой существует **сейчас**) → E-G синтетические фикстуры → E-H разделение «промпт хромает» vs «поток данных плох». Синтетика + локальные модели, облачные LLM в тестах запрещены | precision/recall на фикстурах, метрика в отчёте; opt-in джоба, гейт не блокирует | T2, T4 | 1–2 |
| **T9** | **Инварианты безопасности как регрессии** | В дом D#/F#-регрессий: no-root/no-exec-sinks (F35 — сегодня baseline держится «на честном слове»), write-allowlist `FILE_TYPE_FORBIDDEN` (F34, при постройке), провенанс-маркировка вывода (F33/OUT1), **auth** (F14 — сейчас 0 тестов: fail-open по умолчанию, 401 при неверном токене), **«выключатель выключает»** (F54 — `enabled: false` в боевом конфиге обязан менять поведение) | каждый закрытый инвариант краснеет при откате; открытый = `xfail(strict)` `[OPEN-CONFIRMED F#]` | T0; по мере I6 | сквозной |
| **T10** ✅ | **Слепые зоны F55 — СДЕЛАНО S24, отмечено 2026-08-24** | Расширить ХОЗЯЕВ (не плодить): `test_search` +фильтры/сортировка разнотипного · `test_structure` +containment через `safe_resolve` (не только имя-сегмент) · `bot_army` +боевые лимиты из `config/firewall.yaml` · любой +auth. Мутационный прогон — часть приёмки этапа, а не разовая акция | ✅ Приёмка выполнена и перепроверена прогоном 2026-08-24: `bot_army` **26/26** (пороги читаются из `config/firewall.yaml`, ожидаемое считается из них же — мутация M99), `test_search` **53/53** (фильтры + разворот сортировки — M100/M101), `test_firewall` **59/59** (containment живьём через `fs_read_file` — три формы побега → `PATH_ESCAPE`, то есть choke-point `safe_resolve`, а не имя-сегмент; auth: без ключа 401, чужой ключ 401, ключ не течёт в консоль). Регламент остаётся: тест зоны безопасности принимается только после мутационной проверки | T0 | ✅ |

| **T11** ✅ | **Сценарный движок + журнал прогона — СДЕЛАНО S25 (2026-08-24)** | `tests/scenarios/*.yaml`: `given` (область/конфиг/env/предыстория) · `when` (инструмент+параметры, последовательность) · `then` (наблюдения: `status`, код реакции, класс, поля `facts`, строка консоли) · `why` (одна строка — какое требование утверждается). Исполнитель один — `tests/harness/scenario.py`, против ЖИВОГО сервера (харнесс T2). Хитрая подготовка (гонка, падение на второй операции, порча файла) остаётся Python-шагом, вызываемым из сценария. Журнал прогона — jsonl `запрос→ответ→код→консоль`, один файл на прогон | ✅ **Приёмка выполнена, все четыре пункта доказаны прогоном.** Движок — `tests/harness/scenario.py` (≈250 строк), объявления — `tests/scenarios/*.yaml`, раннер — `tests/scenarios/test_scenarios.py` (в гейте сам, `pytest` 27/27). E-D: **60 проверок с первого прогона**, поведение выяснялось пробой, а не угадывалось. (1) Кривое объявление падает на ЗАГРУЗКЕ — проверены шесть классов: неизвестный ключ, код мимо реестра, класс против реестра, отказ без кода, сценарий без `why`, пустой файл. (2) Валидный, но НЕ ТОТ код в ожидании красит прогон (`LAST_SHEET`→`SHEET_NOT_FOUND` → красный с печатью пришедшего). (3) Мутация продукта красит сценарий: **M124** (guard столбца) — 6 провалов. (4) **Теневой слой** — **M125** красит И `test_tables` (2), И `scenarios/ed1` (3), причём только сценарий видит, что класс реакции и recovery до клиента не доехали. Журнал: строка JSON на шаг с запросом, кодом, сообщением и строками консоли. Прежняя формулировка приёмки: **Пилот — НОВЫЕ сценарии, а не перевод: рабочие наборы не трогаются до `T12`** (директива владельца 2026-08-24 — «пока нет тестов, которым можно доверять, рабочие трогать нельзя»). Доверие набирается теневым слоем: горсть существующих проверок переписывается сценарием ПАРАЛЛЕЛЬНО (старый набор жив), и оба вердикта сравниваются под одной мутацией. (1) движок исполняет новые сценарии (`E-B`/`E-C` из `T4`) против живого сервера; (2) на теневой горсти мутация зоны красит и старый набор, и сценарий — одинаково; (3) намеренно испорченное ожидание в yaml красит прогон — движок умеет краснеть; (4) сценарий с неизвестным шагом = **ошибка загрузки**, не молчаливый пропуск (иначе повторим мёртвую `scenarios.yaml` из `T3`) | T2 | 1–1.5 |
| **T12** | **Перевод наборов с ДОКАЗАТЕЛЬСТВОМ непотери** | Опись всех 1283 площадок (метка → механизм → ожидание) → таблица `CATALOG §G`: старая проверка → адрес сценария **либо** «снята, причина». Набор переводится целиком и по одному; старый файл удаляется последним действием | Три доказательства на КАЖДЫЙ набор, иначе удаление запрещено: (а) покрытие строк зоны не упало (`coverage` до/после, ratchet в CI); (б) каждая мутация зоны из §F по-прежнему красная; (в) в §G нет строки без адреса или без причины снятия (дубль хозяина / мёртвый код / эквивалент). Причина «выглядит лишним» не принимается | T11 | 0.5 на набор (≈4–5) |
| **T13** ✅ | **What-if: карта радиуса + диффер поведения — СДЕЛАНО S25 (2026-08-24)** | (а) `scripts/blast_radius.py` — из по-сценарного покрытия (`coverage json`) строит карту `файл:строка → сценарии+ожидания`; запрос ДО правки отвечает «что висит на этом месте». (б) `scripts/what_if.py` — намерение (`intent.yaml`: что меняем / где / зачем / ожидаемое изменение поведения) + кандидатный патч → матрица гоняется на `HEAD` и в отдельном worktree → отчёт в ТРИ колонки: заявленное сбылось · **изменилось незаявленное (регрессия)** · заявленное не сбылось | ✅ **Приёмка выполнена прогоном.** Карта радиуса: покрытие снимается с СЕРВЕРНОГО процесса, по одному сценарию на сервер (13 сценариев × 98 файлов). Точность: `core/excel/excel_core.py:306` (строка guard'а) → ровно `ed5`+`ed6`; `core/firewall/firewall.py` → все 13 (через файрвол идёт каждый запрос); неисполняемая строка названа честно; файл вне области замера отличён от непокрытого. **`--check-routes` закрыл известный предел `T15`:** все рубежи-`.py` подтверждены исполнением опоры (0 мимо потока), декларации помечены как неизмеримые покрытием, а не выданы за пройденные; попутно вскрыт баг обеих проверок — фильтр «путь = есть косая черта» выбрасывал `server.py`. Диффер: откат закрытого `F28` кандидатным патчем при намерении, объявившем ТОЛЬКО `ed5`, дал ровно три колонки — сбылось `ed5 → red`, **скрытый риск `ed6 → red`**, не сбылось пусто; код возврата 1 при сюрпризе, 0 без него | T11 | ✅ |
| **T14** | **Симуляция локальными ИИ-командами** (opt-in, вне гейта; требования владельца 2026-08-24 ниже) | Две роли на ОДНОЙ локальной модели (на машине `ollama qwen3.5:9b`; карта одна — две модели разом дают `CUDA OOM`, замер S24): «пользователь» идёт по лестнице зрелости (новичок без каналов → владелец сетки каналов) и ставит задачи в чат, «исполнитель» ходит инструментами сервера через харнесс. **Многопользовательская нагрузка — НЕ через LLM:** одновременность и «где упадёт под нагрузкой» дают `bot_army`/`agent_swarm` детерминированно и дёшево; модель нужна только там, где проверяется КАЧЕСТВО решения (понятны ли описания, следует ли из ответа следующий шаг). Итог прогона — не pass/fail, а журнал + кандидаты в `F#` | Прогон воспроизводим (сид + пин модели + журнал); ни одна ветка не идёт в гейт (`-m eval`); каждый прогон кончается либо «расхождений нет», либо строками-кандидатами в `02_findings`; нет модели → пропуск с причиной, не провал | T11, T6 | 2+ |

| **T15** ✅ | **Маршрутная таблица потоков данных — СДЕЛАНО S25 (2026-08-24)** | `tests/routes/routes.yaml`: маршрут объявляет ЧТО течёт · ЧТО выбирает путь (`decided_by`) · рубежи (`hops`) · ЧТО значит обрыв (`means`) · опору (`proof`: сценарий + наблюдаемые поля + `arrives`). Вердикт берётся из СЛЕДА прогона сценария, а не из прозы. `arrives: false` = разрыв, обязан нести адрес `F#` | ✅ 6 маршрутов, 24 проверки. Ловит три класса, каждый доказан: (1) **закрывшийся разрыв** — `M126` (провенанс появился в поиске): сценарий зелёный 3/3, таблица красная с инструкцией обновить `F87`; (2) **обрыв в середине** — `M127` (рецепт теряется на конверте) красит 3 маршрута; (3) **отставшее объявление** — рубеж `core/contracts/results.py` не существовал, поймано первым же прогоном. **Известный предел:** существование рубежа ≠ его участие в потоке (маршрут поиска называл `tools/search/`, тогда как `fs_smart_search` живёт в `tools/filesystem/`) — закрывается наблюдением по покрытию, это работа `T13` | T11 | ✅ |

| **T16** ✅ | **Карта сценария — СДЕЛАНО S25 (2026-08-24)** | `tests/scenarios/*.map.yaml`: состояния (каждое — ПРЕДИКАТ над живым сервером), переходы (`via` + `carries` маршрутов), обход, покрывающий каждый переход. Путь по карте и есть сценарий, поэтому новое состояние/переход дают новые сценарии без строчки кода. Разбор карты печатается ДО прогона: недостижимые состояния, тупики без `terminal`, число путей и шагов | ✅ 7 состояний, 7 переходов → 3 пути, 12 шагов, **65 проверок из объявления**; набор целиком 125/125. Доказано мутациями: **M128** — переход, ничего не меняющий, красит («предикат прежнего состояния держится — состояния неразличимы»); **M129** — недостижимое состояние и тупик без `terminal` ловятся РАЗБОРОМ до прогона. Обход разнесён по процессам (свой сервер на путь) — файрвол считает частоту по IP, и это тот же вывод, что у роя, вместо ослабления боевого порога | T11 | ✅ |


### Требования владельца к симуляции (2026-08-24) — условия приёмки `T14`

1. **Только живой сервер.** Симуляция работает через поднятый и рабочий сервер; без него она
   НЕ ВОЗМОЖНА. Отсюда правило раннера: нет сервера → набор **падает** с причиной, а не пропускается
   (пропуск здесь был бы ложным зелёным: «симуляция прошла», не состоявшись).
2. **Состав ролей.** Модель-исполнитель ходит инструментами; модель-клиент играет владельца канала
   по лестнице зрелости; отдельные модели создают КОНТЕНТ по требованию исполнителя, имитируя
   реальную работу. Всё локально (`ollama`), облачные LLM в тестах запрещены.
3. **Неработающий инструмент фиксируется ТОЧНО.** Не «что-то пошло не так», а: инструмент, параметры,
   пришедший код и сообщение, шаг сценария, строки консоли — то есть запись журнала прогона,
   пригодная к заведению `F#` без переспрашивания.
4. **Сервер не падает.** Отказ API в любом виде — не повод закрывать сервер; так же обрывы сети и
   прочие сбои. Падение допустимо только на очень серьёзном: зависание, гибель процесса ядра.
   Проверяется уже сейчас — `tests/scenarios/resilience.yaml` (батарея отказов подряд → сервер
   продолжает обслуживать, и ни один шаг не оставляет трейса в консоли).
5. **Многопользовательская нагрузка — не через LLM** (см. описание этапа): одновременность даёт
   `bot_army`/`agent_swarm`; модель нужна там, где проверяется КАЧЕСТВО решения.

**Блокер `T14` — `F137`:** сервер обслуживает строго один запрос за раз (замер: тяжёлый вызов 37,2 с,
`tools/list` рядом ждал 36,8 с). Симуляция нескольких клиентов на таком сервере измерит не продукт,
а очередь в одном потоке. Порядок: сначала исполнение (`17 §Ресурсы`), потом симуляция.

**Сверка статусов с диском (2026-08-24, прогоном).** Таблица выше отставала от репозитория — это
третья категория находок метода S24 («сделано, но нигде не записано»):

- `T10` — закрыт ещё S24: `bot_army` 26/26 читает боевой `firewall.yaml`, `test_search` 53/53 держит фильтры
  и разворот сортировки, `test_firewall` 59/59 бьёт containment живьём и покрывает auth. Отмечен.
- `T4` — E-A/E-B/E-C написаны целиком (`tests/structure_emulation`, 39 проверок, в гейте). Отмечен ниже.
- `T6`/`T7` — артефакты на месте и идут в гейте (раннер роя `agent_swarm`, conformance-джоба); **критерии
  приёмки поштучно не пересверялись** — сверить при следующем касании, статус ставить тогда.
- `T5` — открыт частично: **E-D закрыт сценариями `T11`**, `E-E` (тайминг ID) и `E-F` (деградация формул)
  остаются. F28/F30, на которые ссылались клетки E-D5/E-D6 как на GAP, давно закрыты — `03` поправлен.

**Порядок:** ~~`T0 ∥ T3`~~ ✅ → ~~`T1`~~ ✅ → ~~`T2`~~ ✅ S24 → ~~`T10`~~ ✅ S24 → ~~`T11`~~ ✅ S25 → ~~`T13`~~ ✅ S25 → **`T12`** (по набору) → `T4 ∥ T6` → `T5` → `T9` → `T7` → `T8` → `T14`.
Перестановка против прежнего порядка объяснима одним доводом: `T4`/`T6` ещё НЕ написаны, и писать их
скриптами старой формы — значит заводить работу для `T12`. После `T11` они пишутся сразу сценариями.
Суммарно **≈12–15 сессий**; `T11+T12` окупаются тем, что снимают 1283 рукописные площадки и правило,
которое сегодня держится дисциплиной.

---

## 5. Что НЕ меняем (правила остаются)

- **Не плодить.** Новый постоянный набор — только когда сценарий вне зон ВСЕХ существующих И запас хозяина
  исчерпан (`CATALOG` §правило). Реально новых на горизонте: `harness/`, `structure_emulation/`,
  `agent_swarm/test_agent_swarm.py`, `eval/`. Всё прочее — сценарии внутри существующих зон.
- **Тест = сценарий реального поведения**, не проверка кода. Поведенческий → против ЖИВОГО сервера.
  Мок — только истинно внешние провайдеры (litellm/stable-ts), никогда как замена запуску сервера.
- **Ассерт на контракт:** `ToolResult.status/facts`, `ErrorDetail` + конкретный код `server_reactions` — не truthy.
- **Docstring теста — одна строка** (+`D#`/`F#` для регрессии). Рацио и «5 вопросов» — в commit-сообщение
  и `_sessions.md`, не в исходник (директива владельца 2026-07-05).
- **Честный стаб обязан кричать** (G16): `NotImplementedError`-код реакции, не фейковый `success`.
- **Открытая находка = `xfail(strict)`** `[OPEN-CONFIRMED F#]`, baseline остаётся зелёным; «внезапно прошло» =
  сигнал обновить `CATALOG §E` ⬜/🟡→🟢 и перевести в регрессию.

---

## 6. Definition-of-Closed для I7 и подъём зрелости

I7 закрывается по `08 §6` — не «тесты написаны», а:

| Ступень | Что значит для I7 | Даёт этап |
|---|---|---|
| A — обмер | тест-система обмерена, дыры → F50–F53 | ✅ сделано (§2) |
| B — готовые решения | pytest/coverage/conformance взяты как готовые, уложены под конвенции (безопасность — из `06`, не с GitHub) | T0/T1/T7 |
| C1 — код-пруф | каждая находка/фикс подтверждена простым тестом | T0 (без раннера C1 недоказуем) |
| C2 — реал-пруф | симуляции реальными инструментами против живого сервера **с чтением консоли** | **T2** — сегодня структурно невозможен |

**DIM-6 (тестирование):** сегодня L1–L2. `T0+T1+T2` → честный L2 (гейт реален, покрытие измеримо, C2 возможен);
`+T4+T6+T7` → **L3** (conformance в CI, рой не декларативен, E-матрица есть); `+T8` и продукт → L4.
**DIM-1 (conformance)** поднимается T7. До ступени C уровни в `07` не двигаем.

---

## 7. Риски

| Риск | Митигация |
|---|---|
| Миграция на pytest ломает зелёный baseline | T0 = **мост** (`assert main() == 0`), `__main__` сохранён; идиоматичная переписка — по мере касания набора, по одному, с прогоном до и после |
| Харнесс живого сервера делает тесты флаки (порты, гонки, состояние firewall) | свободный порт из ОС, свежий tmp-`workspace/` на тест, гарантированный teardown, `firewall_reset`-фикстура; флак → карантин + чинить корень, не «прогнать до зелёного» |
| Покрытие превращается в самоцель | порог — ratchet от факта, а не «80% потому что круглое»; дыры трактуем по `03` (слои), не по процентам |
| Рой/eval съедают время и токены | рой — детерминированные паттерны из `patterns.yaml`, без LLM; eval — синтетика + локальные модели, opt-in вне гейта |
| **Ожидание вычислено из того же конфига, по которому отвечает сервер** | Тавтология: набор зелен на сломанном сервере. Из реестра берётся СЛОВАРЬ (код существует, класс такой-то), выбор кода для сценария объявляется в сценарии. Приёмка `T11` п.3 (испорченное ожидание обязано краснить) ловит это механически |
| Движок превращается в язык программирования на YAML | Объявлением выражаются условие и ожидание. Всё, что требует ветвления и вычислений, — Python-шаг, вызываемый из сценария по имени. Порог: появилось `if` в yaml — шаг переезжает в код |
| Перевод набора теряет проверку молча | Удаление старого файла разрешено только после трёх доказательств `T12` (покрытие · мутации · §G без пустых строк). Ни одно из них не заменяется чтением диффа |
| LLM-симуляция мигает и красит гейт | `T14` — `-m eval`, opt-in, вне шести джоб; её выход — находки, а не вердикт. Расхождение оформляется `F#` и проверяется детерминированным тестом |
| E-матрица упирается в непостроенный продукт (Ф3/провайдеры) | всё непостроенное = `xfail(strict)`-spec со ссылкой на F# — фиксируем требование, не блокируемся |
