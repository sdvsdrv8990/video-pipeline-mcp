# 21 — Приёмка правки ИИ: два сторожа, одна программа

> **Почему не «сторож качества кода».** Владелец 2026-09-04: «сторожами качества кода этих двоих
> называть не совсем верно». Они судят не качество вообще, а КОНКРЕТНУЮ правку, сделанную ИИ:
> не сломал ли · не сменил ли того, чего не просили · остался ли в границах задачи · понятно ли
> получившееся без комментариев · туда ли положил. Это приёмка работы, а не оценка кода.
>
> Отсюда имена: `scripts/guards/acceptance_studio.py` (React/TS) и `acceptance_server.py`
> (Python). Общее объявление сценариев — `acceptance_scenarios.yaml`, общий код обеих приёмок — `_acceptance.py`.

## Две роли одного сторожа

1. **Судья приёмки** (`--суд`) — вердикт по условиям ниже, exit-кодом. Границы задачи берутся из
   объявления (`--задача <id>` → зона), а не из слов правщика.
2. **Эксперт-советчик** (`--совет`) — по РОДУ УЛИКИ выбирает сценарии и говорит, что здесь
   ломается, почему и чем это доказать. Вердикта не выносит: советчик, а не исправлятель.

## Условия приёмки: требование → механизм → состояние

Состояние честное: **судится** (есть исполнитель), **совет** (исполнителя нет, объявлено, чем
станет судимым), **нет** (не начато). Числа и потолки смотреть прогоном, не отсюда.

| # | Требование владельца | Механизм (ПАРНАЯ улика) | Студия | Сервер |
|---|---|---|---|---|
| П1 | не сломал ни одного компонента | тег ↔ импорт; импорт ↔ экспорт цели | судится | нет |
| П2 | не сменил стили, дизайн, параметры, шрифты, отступы | значение ↔ токен того же рода; форма ↔ снимок | судится | нет |
| П3 | не вышел за рамки задачи, не оставил мёртвого | тронутое ↔ зона задания (механизм общий); проп ↔ читатель; компонент ↔ отрисовщик; на сервере мёртвое судит `ruff` | судится | **судится** |
| П4 | понятно, с каким компонентом работать (описание/скриншот) | `data-component` ↔ имя, уникальность на дерево | судится | — |
| П5 | не кидает код в подвернувшийся файл; зона ответственности файла | студия: роль каталога ↔ роды в файле · сервер: шапка модуля ↔ имя файла или пакета | **судится** | **судится** |
| П6 | не нарушает структуру дерева | объявленная структура ↔ фактические пути (`structure.yaml` / `structure_server.yaml`) | **судится** | **судится** |
| П7 | понятные имена файлов, функций, импортов, компонентов | файл ↔ его главный экспорт; алиас ↔ исходное имя; словарь «имён ни о чём»; на сервере ещё звёздный импорт | **судится** | **судится** |
| П8 | не стирает и не теряет стилизацию и анимации; восстановить ТОЧНО, без бэкапа | снимок хранит ТОЧНЫЕ объявления (цвет, длительность, easing, кадры) → `--восстановить` печатает, что исчезло и чем это было дословно, со значением токена рядом | **судится** | — |
| П9 | понятно без комментариев; комментарий обоснован ЦЕНОЙ | судится: комментарий ↔ слова соседней строки (пересказ — цена нулевая по построению); спрашивается: цена в момент правки, четвёртым вопросом гейта текста. Судить ПОЛЬЗУ машина не берётся — это объявленный предел | **судится + вопрос** | **судится + вопрос** |
| П10 | видит разъехавшуюся вёрстку: наложение, текст за границей, компонент не там | **статическая половина судится**: композиция (кто кого рисует) ↔ снимок — переезд компонента виден до экрана. Наложение и выход текста требуют прогона браузера и ждут развилки владельца о node-джобе (остаток `7a3f`) | **половина** | — |
| П11 | видит явные ошибки логики и синтаксиса | сервер: вердикт `ruff` сводится в приёмку (плюс шим `vpm-invariants`, F195); студия: разбор без node | частично | **судится** |
| П12 | сторож в ТАНДЕМЕ, с журналом и картой потоков | две стороны. Шим `vpm-invariants` называет задетый поток и больные места приёмки по роду улики — своим каналом, не отказом. Сама приёмка закрывает суд общим хвостом (`_acceptance.close`): задетые потоки берёт из источника улики `_routes.py` — того же, что читает шим, — а вердикт подписывает в общий журнал тандема (`tests/.journal/`, род `гейт`, роль `ВЕРДИКТ`/`ОТКАЗ`; `УЛИКА` запрещена — её подписи считает гейт поставки) | **судится** | **судится** |

## Порядок шагов

- **A1** ✅ судья четырёх условий + эмуляция студии как предмет (`20` §4г).
- **A2** ✅ эксперт: сценарии по роду улики, спектр заданий стенда, ось целостности.
- **A3** ✅ П8: снимок стал реставрационной картой — точные объявления, кадры анимации, `--восстановить`.
- **A4** ✅ П5/П6/П7: объявление структуры и ролей (`structure.yaml`), имена как парная улика.
- **A5** ✅ `acceptance_server.py`: те же условия на дереве сервера; чужие механизмы (`ruff`,
  `import-linter`) не переписаны, а сведены — своё только объявление структуры и политика.
- **A6** ✅ П12: тандем знает карту потоков и больные места приёмки (закрывает `b9e4`).
- **A7** ✅ П9: ось «пересказ следующей строки» + вопрос цены в гейте текста.
- **A8** ◐ П10: статическая половина сделана (композиция в снимке). Наложение и выход текста
  за границу упираются в развилку владельца о node-джобе (остаток `7a3f`) — без прогона браузера
  их не судит ничто, и это объявленный предел, а не забытый пункт.
- **A9** ✅ П12 второй стороной: обе приёмки вошли в тандем — вердикт уходит подписью в общий журнал, задетые потоки читаются из общего источника улики (`_routes.py`), и своя копия разбора карты у хука снята.

## Правила, которые здесь не обсуждаются, а действуют

- **Улика только парная.** Одиночный литерал, одиночное имя, «похоже на плохой код» — не улика.
  Счётчик без второго источника даёт шум, а сторожа, который врёт, выключают целиком.
- **Не плодить.** Новый сторож — только когда наблюдение вне зон ВСЕХ существующих И запас хозяина
  исчерпан (`scripts/guards/CATALOG.md`). Механизм берём из библиотеки, себе оставляем политику.
- **Каждая идея проверяется циклом ДО кода** (`what_if.py --intent`), и шаг закрывается тремя
  артефактами: правка · прогон свежее правки · запись решение→факт.
