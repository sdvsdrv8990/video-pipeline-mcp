# 07 — Рубрика зрелости MCP-сервера + честная само-оценка

> Большой список требований, по которому МОЖНО ЧЕСТНО судить, на какой стадии репозиторий, и строить карту развития. Собран из сильных источников (официальный MCP + отраслевые чеклисты, ссылки [M#] внизу), спроецирован на `video_pipeline_mcp`. Это «линейка», а `01_master_roadmap.md` — маршрут; каждое требование привязано к воркстриму/находке.
>
> **Честность — принцип.** Ставим уровень по ДОКАЗАННОМУ состоянию (как аудит: не «есть auth», а «есть OAuth 2.1 Resource Server с per-server scoped token»). Ноль баллов там, где механизма нет, даже если «запланировано». **Измерение поднимается только после `08 §6` (Definition-of-Closed) — обмер всех файлов + простые тесты (код-пруф) + симуляции с чтением консоли (реал-пруф); до ступени C уровень не меняется.**
>
> **Расширение (Сессия 14):** (1) добавлен блок **DIM-13…16 — внутренняя инженерная дисциплина**
> (проектные «паттерны»-скилы: качество кода, анти-хардкод, безопасность-как-паттерн, конвенции/структура).
> Рубрика была externally-sourced (MCP/OWASP) — зрелость ЭТОГО репо меряется и его собственными планками
> (`D#/G#`). (2) **FFmpeg/видео-рендер ИСКЛЮЧЁН из оценки зрелости пока** (указание владельца): у внешней
> MCP-интеграции особенности → честно оценить нельзя, оставляем **на последок**, после работ ранней
> стадии зрелости. `DIM-12` меряем по data/pattern-поверхности, не по видео-рендеру. (3) учтён воркстрим
> **A7** (патерн/уникальность, `10_*`) в карте развития.

## Уровни зрелости (L0–L4)

| L | Имя | Смысл |
|---|---|---|
| **L0** | Прототип/демо | работает у автора, руками; официальные reference-серверы — это L0 by design [M1] |
| **L1** | Alpha | работает локально, контракт есть, но без hardening/auth/CI |
| **L2** | Beta | контракты+тесты+packaging; дыры в auth/observability/security-покрытии |
| **L3** | Production-candidate | OAuth-auth, CI, observability, security-покрытие обеих сторон, экон-контейнмент |
| **L4** | Enterprise | conformance-проходит, полный audit-trail, supply-chain, quotas/budgets, зрелый продукт |

## Требования по измерениям (D-DIM) + наш уровень

> **Строки «Наш уровень» актуализированы S24** (DIM-1, DIM-3, DIM-4, DIM-6, DIM-13, DIM-15, DIM-16 —
> помечены «актуализировано S24»; сплошной проход, волны 1–3). До того — S23 (DIM-2, DIM-5, DIM-9, DIM-12).
> Остальные несут оценку S14/S15. Основания подъёма — сводные таблицы
> [«Переоценка S23»](#переоценка-s23-2026-08-14--что-сдвинулось-делом) и
> [«Переоценка S24»](#переоценка-s24-2026-08-18--итог-сплошного-прохода).
> Чек-листы требований (галочки) актуальны все.

### DIM-1 — Протокол / conformance
- [x] Проходит `modelcontextprotocol/conformance` [M4] — **в применимой части**: прогон 30 сценариев, 7 применимых зелёные, 23 неприменимых объявлены поимённо с причиной
- [ ] Строгое соответствие схем инструментов (MCP Inspector clean) [M6] — Inspector не гоняли; вместо него 8 инвариантов схемы в `test_tools_inventory` (F98)
- [x] Parity: внутренний `ToolResult` не богаче MCP `CallToolResult` без потерь на границе (наш G14/D30)
- **Наш уровень: L2** (актуализировано S24) — джоба `conformance` в `ci.yml`, набор `tests/conformance/test_conformance.py`, сторонний клиент; прогон `19/19`, пропуск без node = провал (`VPM_CONFORMANCE_REQUIRED=1`). Паритет G14/D30 проверен живьём и закрыт. НЕТ: 23 возможности спеки (resources/prompts/logging/completion/sampling/elicitation) — их отсутствие объявлено, а не скрыто; Inspector не гоняли.

### DIM-2 — Аутентификация / авторизация
- [ ] **OAuth 2.1 + PKCE** (обязателен для HTTP-MCP с июня-2025) [M2][M3]
- [ ] Сервер = OAuth Resource Server; отдельный scoped-token на сервер, токены не шарятся [M3]
- [ ] 5 обязательных authz-паттернов спеки [M2]; защита от confused-deputy
- **Наш уровень: L2** (актуализировано S23) — auth есть и **fail-closed**: без токена сервер не стартует, токен хранится хэшем, ротируется, отпечаток сверяется (`core/auth.py`, этап S1). НЕТ: OAuth 2.1 + PKCE, scoped-токенов, 5 authz-паттернов — это остаток `S1′`. За туннелем один клиент, IP по-прежнему бесполезен (G18).

### DIM-3 — Безопасность inbound
- [ ] Firewall подключён, конфиг загружен, нет fail-open (наши D2/D8/D10 закрыты)
- [ ] Traversal/injection/rate/cache покрыты (IN1–IN10 каталога `06`)
- [ ] Секрет-гигиена (gitleaks), нет command-injection (43% серверов уязвимы [M5])
- **Наш уровень: L2 (прочнее, пруф пересобран S24)** — браузерный класс закрыт живыми прогонами: `Host` валидируется первым, до auth (F103/F104/F108), `Origin` — ВСЕГДА, а не при непустом списке (F106), не-`application/json` → `415` до чтения тела (F107), заголовки безопасности на всех ответах (F109), битый конфиг объявляется НЕ применённым вместо 403 на всё (F128). Покрытие проверяется не планом, а роем: **29 защищено / 4 известные дыры** против живого сервера. НЕТ: 4 дыры роя (`F36` slowloris + бомба вложенности, `F87`), identity-rate за туннелем (G18).

### DIM-4 — Безопасность outbound (сервер→клиент)
- [ ] Провенанс недоверенного workspace-контента (OUT1), не эхоить в reason/message
- [ ] Containment на write/move/delete + `destructiveHint` + confirm (OUT5)
- [ ] Нет эксфильтрации через `_SESSION_LOG.md`/параметры (OUT6); error-leak закрыт (OUT7)
- **Наш уровень: L2** (актуализировано S24) — S3 закрыл F33 (провенанс на чтении файла, `force` на деструктиве, санитизация `Fact.data`); проход добавил OUT7 (`F129`: текст внутреннего исключения больше не уезжает клиенту) и отравление манифеста как проверяемый класс — 5 классов, 88 ассертов в `test_tools_inventory` (T2). Остаток прежний и поимённый: **`F87`** — результаты поиска идут БЕЗ пометки провенанса, то есть маркировка сделана на инструмент, а не на класс «чужой текст наружу».

### DIM-5 — Attack-surface reduction / изоляция
- [ ] Write-type allowlist (default-deny) — `06 §F`/F34
- [ ] No-root инвариант: не исполнять workspace, нет shell, не root — `06 §G`/F35
- [ ] Deploy-hardening: seccomp/Landlock/cap-drop/read-only (Anthropic baseline) — `06 §G.1`
- **Наш уровень: L2** (актуализировано S23) — write-type allowlist default-deny построен (F34/F68, код `FILE_TYPE_FORBIDDEN`), запись идёт единой дверью `core/write_policy.py`, no-root baseline чист (F35). НЕ применён deploy-hardening (этап S5), нет регрессии на no-root-периметр.

### DIM-6 — Тестирование (5 ворот demo→prod [M7])
- [ ] Unit+contract на каждый инструмент; strict-schema регрессия [M6]
- [ ] Conformance + capability-discovery в CI [M4]
- [ ] Adversarial/security-симуляции подключены и не «театр»
- [ ] E2E сценарии; honest-stub краснеет, не фейкает success (G16)
- **Наш уровень: L3** (актуализировано S24) — все четыре галочки несут артефакт: strict-схема — 8 инвариантов в `test_tools_inventory` (F98); conformance — своя джоба в CI; adversarial не театр — рой `tests/agent_swarm/` бьёт по живому серверу, а `F110` показал и закрыл способ ложного «защищено»; E2E — `tests/structure_emulation/` (54 проверки, метаморфный инвариант). Блокеры S23 сняты: `F51` (харнесс `tests/harness`) и `F32` (рой). Пруф уровня — прогон: `python -m pytest` → **20/20 наборов**, `ruff` чисто, `mypy` 81 файл без ошибок, `bandit -ll` Medium 0 / High 0, `comment-guard` 0 замечаний в 112 файлах. НЕТ до L4: eval-слоя качества поиска (`F31`), покрытие держит ratchet 60%.

### DIM-7 — Observability / аудит
- [ ] Audit-trail: auth-события, authz-события, tool-invocations с санитизированными params → principal/scope/context [M2]
- [ ] `structlog`/метрики/`/health`/tracing
- **Наш уровень: L0** 🔴 — нет (I5 не начат). `_SESSION_LOG.md` есть, но это не audit-trail и вдобавок утечка (OUT6).

### DIM-8 — Packaging / deploy
- [ ] `pyproject.toml` + пиннинг (наш I2 ✅)
- [ ] Docker-образ (портируемость, −60% support-тикетов [M8]); digest-pin
- [ ] Non-root `USER`, read-only rootfs
- **Наш уровень: L2 — без изменения** (пруф уточнён S24) — pyproject+lock есть (I2). Dockerfile на диске один — `docker/runner.Dockerfile`, и это образ **раннера моделей**, не сервера: образа сервера, non-root `USER`, read-only rootfs и digest-pin нет.

### DIM-9 — Repo-гигиена / supply-chain
- [x] `LICENSE` (F19 закрыт) · [ ] `SECURITY.md`, `CONTRIBUTING.md`, `RELEASING.md` [M8]
- [ ] OIDC trusted publishing (без registry-токенов) [M8]; `pip-audit`/Snyk скан
- **Наш уровень: L2 — без изменения** (пруф пересобран S24) — `LICENSE` на месте (F19), gitleaks (пиннутый бинарь), bandit и `pip-audit` — хард-гейты CI; шапка `ci.yml`, называвшая `pip-audit` advisory, приведена к факту (`F121`). НЕТ по-прежнему (`ls` в корне): `SECURITY.md`, `CONTRIBUTING.md`, `RELEASING.md`, `CHANGELOG.md`, OIDC trusted publishing.

### DIM-10 — Контракты / обработка ошибок
- [ ] Единый `ToolResult`/`ErrorDetail` + реестр реакций (у нас ЕСТЬ)
- [ ] Реакции — публичный контракт, полны и не теряются на границе (D4/D27 gaps, A6)
- **Наш уровень: L2–L3** — контракт+реестр реакций зрелые; D4-fallback/D27-класс частичны (F5).

### DIM-11 — Экономический контейнмент
- [ ] Budgets/quotas/side-effect governors (иначе оркестратор делает всю защиту [M2])
- **Наш уровень: L0** — нет квот/бюджетов/лимитов на дорогие операции (media-провайдеры будут внешними/платными).

### DIM-12 — Продуктовая полнота
- [ ] Заявленная функция реально работает e2e
- **Наш уровень: L1–L2** (актуализировано S23) — data/FS-сервер работает (число инструментов — [`00` §1](00_reality_check.md)), локальные media-провайдеры реально исполняют (озвучка `piper`, кадры `sd-turbo`/`sdxl-turbo` на видеокарте); **A7** (патерн/уникальность) расширяет
  data-продуктовую поверхность (сценарные+сценовые патерны, уникальность, реакции). ВИДЕО-пайплайн не начат: ни кода, ни заглушек — дизайн закрыт S25, план [`19`](19_montage_subsystem_plan.md).
- **FFmpeg/видео-рендер — ВНЕ оценки пока** (владелец S14): внешняя MCP-интеграция с особенностями,
  честно уровень не поставить → **на последок**, после ранней стадии зрелости. Здесь меряем data/pattern-часть.

---

## Внутренняя инженерная дисциплина (проектные «паттерны»-скилы) — DIM-13…16

> Добавлено (Сессия 14). Зрелость меряется и СОБСТВЕННЫМИ планками проекта — кодифицированными в скилах
> и словаре `D#/G#`. Меряем ДОКАЗАННО: не «скил есть», а «код фактически выдержан».
> **Обновление S24:** общий узел этих четырёх — распил монолита (A2) — снят ещё в S17; логика в 8 группах
> `tools/`. Замер на S24: `server.py` = **542 строки** (`wc -l`) — рост от 465 дал не возврат логики, а
> браузерная защита волны 3 (`Host`/`Origin`/`415`/заголовки, F103–F109). DIM-15 поднято до L3, DIM-13
> держится на L2–L3 (см. «Переоценка S24»).

### DIM-13 — Архитектурная адгезия / качество кода (скил `code-quality`)
- [ ] Обёртка → логика группы → `ToolResult/ErrorDetail` → реакции — на КАЖДОМ инструменте *(звено `config/ops` снято S22 вместе с A3)*
- [ ] Нет монолита: логика в `core/*_core`, не в `server.py`; altitude выдержан
- [ ] Contract-parity (G14/D30), нет dead-code/dead-inject (D28), honest-stubs кричат (G16), нет stringly-typed дрейфа (G15)
- **Наш уровень: L2–L3 — без изменения** (пруф пересобран S24) — монолита нет (8 групп, 542 строки транспорта), контракты зрелые, D30-паритет закрыт. Проход снёс мёртвое: **−507 строк** P-оси (4 адаптера + пакеты `ffmpeg/`, `stt/`; `F10` закрыт удалением) и **−446 строк** прополки текста. Почему НЕ L3: сам сторож качества дырявый — `F122` (слеп за пределами фиксированного `TARGETS`), `F123`/`F124` (построчный разбор принимает данные за прозу), `F126` (главный класс мусора regex'ом не ловится). **Цель L3:** сторож на `ast`+`tokenize` + проходы `code-quality` на каждом воркстриме.

### DIM-14 — Декларативность / анти-хардкод (скил `anti-hardcode`)
- [ ] Нет per-entity ветвлений (`if entity==…`), магических литералов (пороги/пути/префиксы) — они в `config/*.yaml`/реестре
- [ ] Конфиг ЗАГРУЖАЕТСЯ, а не игнорируется (нет D2); нет захардоженных секретов (D31)
- [ ] Новая способность = данные+декларации, не ветки в коде — **A7 задаёт эталон** (yaml-конфиг скрипта уникальности + per-project пороги/веса/шаблоны, §5 файла `10`)
- **Наш уровень: L2** (актуализировано S23) — declarative-by-design: конфиг канала = листы книги, провайдеры/модели/устройство/разрядность объявлены в `config/providers.yaml`, реакции и firewall — в своих YAML. Опора на `config/ops` снята (A3 упразднён). Точечный хардкод остаётся: `stt device=cuda` (F10), D2 местами. **Цель L3:** A7 по эталону + дисциплина `anti-hardcode` на каждом воркстриме.

### DIM-15 — Конвенции размещения / структуры / стиль (скил `project-conventions`)
- [ ] Две вселенные (код/конфиг в git; данные ТОЛЬКО в `workspace/`) соблюдены — A7-override подтвердил границу
- [ ] Каждый файл в своём доме (закон §2/§5): `tools/` тонкие, `core/` логика, `config/` декларации
- [ ] Module-headers терсовые; git-native история (решение→факт в commit+`_sessions`, НЕ process-нарратив в коде); naming по Bounded Context
- **Наш уровень: L3** (актуализировано S24) — закон и адгезия сошлись, и адгезия стала **принудительной**:
  третья галочка проверяется машиной в гейте — джоба `comment-guard` (`scripts/comment_guard.py --check`,
  потолок пофайловый в git). Пруф: прополка 93 замечаний в 47 файлах → **0 замечаний в 112 файлах при
  потолке 0**, механизм в репозитории, вторая копия снесена (хук = 15-строчный шим). `tools/` тонкие,
  8 непустых групп. Оговорка: молчание сторожа не аттестат чистоты (`F126`).

### DIM-16 — Безопасность как ПАТТЕРН (secure-by-construction, скил `security-reviewer`)
> Отличие от DIM-3/4/5 (те меряют ПОКРЫТИЕ угроз): здесь — насколько защита это СТРУКТУРНЫЙ паттерн, а не россыпь проверок.
- [ ] Единый choke-point контейнмента (G17: `_safe_resolve`→`core/paths.py`), не per-handler
- [ ] Fail-closed по умолчанию (нет fail-open путей); провенанс-маркировка недоверенного вывода как паттерн
- [ ] Ошибки не «текут» сырьём (raw_response leak закрыт); реакция несёт recovery как контракт (G14)
- **Наш уровень: L2 (прочнее, актуализировано S24)** — choke-point `core/paths.py` выделен (G17), провенанс
  на чтении файла как паттерн (F33). Проход добавил fail-closed там, где его не было: проверка `Host`/`Origin`
  безусловна (защита не зависит от того, заполнил ли владелец переменную — `F106`), отказ hot-reload
  оставляет ПРЕЖНИЕ правила и объявляет «не применён» (`F128`), сырьё исключения наружу не течёт (`F129`),
  заголовки безопасности — middleware на все ответы, а не на успешный путь (`F109`). Почему не L3: провенанс
  всё ещё на инструмент, а не на класс (`F87`), deploy-hardening (S5) не применён. **Цель L3:** `F87` + S5.

## Сводный вердикт (честно)

> **⚠️ Разделы ниже до «Переоценка S23» — снимок S14/S15.** Часть их утверждений устарела: монолит
> распилен (A2 ✅), CI существует, auth-токен есть, провенанс вывода и write-allowlist построены.
> **Актуальная оценка — раздел [«Переоценка S23»](#переоценка-s23-2026-08-14--что-сдвинулось-делом).**
> Старый текст оставлен как точка отсчёта, а не как факт.

**Общий уровень (S15): L1 (Alpha) с карманами L2.** Проект — крепкий data/FS-MCP на L2-контрактах и packaging, но: **product-core на L1** (видео = заглушки; FFmpeg вне оценки пока), а **три enterprise-измерения на L0** — auth (DIM-2), outbound-защита (DIM-4), observability/экон-контейнмент (DIM-7/11). Reference-серверы MCP сами L0 [M1] — мы уже выше среднего demo, но до production-candidate (L3) далеко именно из-за auth+observability+outbound.

> **Обновление S15 (обмер Блока 1):** вердикт держится, но по ДОКАЗАТЕЛЬСТВУ инфраструктура зрелее (DIM-3/10/16 прочнее, A2 де-рискован), а незрелость локализована (F43 реакции · F28–30 формулы · F38–42 search · продукт). См. «Переоценка по обмеру» ниже.

**Внутренняя дисциплина (DIM-13…16):** планки кодифицированы (скилы+`D#/G#`) и держатся в НОВОМ коде, но **доказанная адгезия упёрта в L1** одним узлом — **монолит `server.py` (A2)**: пока `tools/` пусты и логика в монолите, качество (DIM-13), структура-адгезия (DIM-15) и частично анти-хардкод (DIM-14) не поднять. Безопасность-как-паттерн (DIM-16) на L1 отдельно — firewall императивен, choke-point не выделен. **Вывод: A2 — не только архитектурный воркстрим, а разблокировщик четырёх измерений внутренней зрелости.** A7 обязан строиться уже по этим планкам (эталон, не долг).

| Измерение | L0 | L1 | L2 | L3 | L4 |
|---|---|---|---|---|---|
| DIM-1 conformance | | ● | | | |
| DIM-2 auth | ● | | | | |
| DIM-3 sec-inbound | | | ● | | |
| DIM-4 sec-outbound | ●| | | | |
| DIM-5 surface/изоляция | | ● | | | |
| DIM-6 тесты | | ●| | | |
| DIM-7 observability | ● | | | | |
| DIM-8 packaging | | | ● | | |
| DIM-9 repo-гигиена | | ● | | | |
| DIM-10 контракты | | | ● | | |
| DIM-11 экон-контейнмент | ● | | | | |
| DIM-12 продукт (без ffmpeg) | | ● | | | |
| DIM-13 качество кода | | ◐ | ◐ | | |
| DIM-14 анти-хардкод | | | ● | | |
| DIM-15 структура/конвенции | | ◐(адгезия) | ◐(закон) | | |
| DIM-16 безопасность-паттерн | | ● | | | |

> `◐` = split-уровень (закон/дисциплина выше, доказанная адгезия ниже — общий тормоз = монолит A2).

## Переоценка по обмеру Блока 1 (S15, ступень A §6 — доказано чтением всех подсистем)

> Обмер `02` (поды 1–7) сдвинул несколько оценок ПО ДОКАЗАТЕЛЬСТВУ. Общий вердикт **не изменился**
> (L1 с карманами L2), но **баланс уточнён**: инфраструктура зрелее, чем считала память; незрелость
> локализована точечно. Главное следствие — **A2 де-рискован** (распил механический).

| DIM | Было | Обмер-факт | Стало |
|---|---|---|---|
| **DIM-3** sec-inbound | L2 | G17 choke-point ДОКАЗАННО консолидирован (все 6 модулей → `core.paths.safe_resolve`), D29 закрыт в state_manager, D36 traversal в search починен+тест | **L2 (прочнее)** |
| **DIM-16** sec-паттерн | L1 | containment = настоящий структурный choke-point (не россыпь); D23-санитайзер активен; no-root чист. Но fail-closed/провенанс (F33) ещё нет | **L1→L1.5** |
| **DIM-10** контракты | L2–L3 | контракты зрелые (Literal G15, D23), **транспорт D30-парити ЗАКРЫТ** (code/class/recovery в structuredContent). НО F43: хендлеры не заполняют реестр → клиент беднеет | **L2, блокер = F43** |
| **DIM-6** тесты | L1–L2 | **S24: честный L2** — гейт реален (T0), покрытие измеримо (T1), C2 стал возможен (T2: харнесс живого сервера, периметр в CI 17/17). Остаётся до L3: agent-swarm всё ещё декларативен (F32), conformance не в CI (T7) | **L2** |
| **DIM-13** качество | L1–L2 | хендлеры тонкие, спек-списки полу-декларативны → **A2 = механика, риск НИЗКИЙ**; но F44 (повт. импорты), монолит-размещение держатся | **L1–L2, A2 де-рискован** |
| **DIM-14** анти-хардкод | L2 | БОЛЬШЕ хардкода: таксономия entity_type в 3 местах (F41/F46), skeleton inline (F45), device=cuda (F10) | **L1–L2 (честнее вниз)** |
| **DIM-12** продукт | L1 | провайдеры = честные стабы (G16 идеален, позитив); формульный слой незрел (F28–30, loader ∅) | **L1 (без изменений)** |

**Точная локализация незрелости (что чинить, чтобы двигать уровень):** (1) **реакции-эмиссия** — F43/F5 (реестр
обходится хендлерами) = блокер DIM-10; (2) **data-формульный слой** — F28–30 (театр валидации + loader ∅) =
блокер продукта/DIM-12; (3) **search-quality** — F38–42; (4) **продукт** — стабы (P-ось). Инфраструктура
(containment/контракты/транспорт/wiring/честные-стабы) — **зрелая**, не трогать.

## Переоценка S23 (2026-08-14) — что сдвинулось делом

> Оценки ниже подняты **по доказательству** (сессии 16–23: код на диске, CI, прогоны, замеры),
> а не по намерению. Числа-основания — [`00_reality_check.md` §1](00_reality_check.md).

| DIM | Было (S15) | Что сделано | Стало |
|---|---|---|---|
| DIM-2 auth | L0 | `core/auth.py`: токен + отпечаток, старт **fail-closed** без токена, ключ ротируем (S1/S19–S21). OAuth 2.1 всё ещё нет | **L2** |
| DIM-4 sec-outbound | L0 | S3 (S19): провенанс вывода — данные помечаются как данные; `force`-confirm на деструктиве; санитизация `Fact.data` в журнале (F33 закрыт) | **L2** |
| DIM-5 изоляция | L1 | S2 (S19–S20): write-type allowlist default-deny + `FILE_TYPE_FORBIDDEN` (F34/F68); единая дверь записи `core/write_policy.py`, через неё проведён и шаблонный движок (F72/F73). deploy-hardening (S5) — ещё нет | **L2** |
| DIM-6 тесты | L1–L2 | T0 канонический раннер, T1 coverage-ratchet в CI, мутационная приёмка как регламент; **но T2 харнесс живого сервера отсутствует (F51)** | **L2, блокер = F51** |
| DIM-8 packaging | L2 | `pyproject.toml` + `requirements.lock` (пины), extra `dev`/`gpu-amd`, `install.sh` правдив. Docker/non-root/digest-pin — нет | **L2 (прочнее)** |
| DIM-9 repo-гигиена | L1 | LICENSE, тесты и доки в git, gitleaks+bandit в CI, pip-audit advisory; урок F70 (приватный ключ в публичном репо) закрыт ротацией | **L2** |
| DIM-10 контракты | L2, блокер F43 | реестр **58 кодов + `DEFAULT` / 5 классов**, класс и `recovery` доходят до клиента (D30-паритет закрыт); F43/F5 закрыты в S16 | **L3** |
| DIM-13 качество кода | L1/L2 split | A2 ✅ — монолит 1521 → **465 строк**, 8 групп `tools/`; ось 1a («механизм из библиотеки, наш слой — политика») применена и **сократила** код в провайдерах (S23) | **L2–L3** |
| DIM-15 структура | L1/L2 split | размещение по группам реально соблюдается; закон и адгезия сошлись | **L2–L3** |
| DIM-16 sec-паттерн | L1 | choke-point `core/paths.py` выделен (G17), fail-closed на старте, провенанс как паттерн, секреты канала под Fernet с запретом в единой точке путей (S23) | **L2** |
| DIM-12 продукт | L1 | локальные media-провайдеры реально исполняют (озвучка `piper`, кадры `sd-turbo`/`sdxl-turbo` **на видеокарте**); `pipeline/`, `tools/video/`, FFmpeg и облачные — нет | **L1–L2** |
| DIM-7 observability · DIM-11 эконом | L0 | не начинали; частично есть учёт расхода `current_usage` по провайдерам | **L0–L1 — самые красные** |
| DIM-1 conformance · DIM-3 · DIM-14 | L1 / L2 / L2 | без изменений по существу; для DIM-14 отпала опора на `A3` (ops-слой снят) — планку держат `config/providers.yaml`/`server_reactions.yaml`/шаблоны | **без изменений** |

**Вердикт S23: L2 (Beta) с карманами L3 по контрактам и качеству кода.** Проект перестал быть
«L1 с карманами L2»: инфраструктура и периметр вытянуты, внутренняя дисциплина доказана распилом.
До L3 не хватает трёх вещей, и они известны поимённо: **T2 харнесс (F51)**, **observability/quotas (I5)**,
**продукт (P-ось)**.

## Переоценка S24 (2026-08-18) — итог сплошного прохода

> Проход (волны 1–3, журнал — [`18_full_pass.md`](18_full_pass.md)) закрыт. Каждая строка ниже несёт
> **пруф**: файл на диске, прогон или номер находки. Где пруф не появился — уровень не тронут, и это
> написано словом. Замеры сделаны командами в день записи, а не переписаны из прошлой таблицы.

**Гейт на S24 — замер по `.github/workflows/ci.yml`:** **6 джоб** (`lint`, `test`, `conformance`,
`security`, `comment-guard`, `gitleaks`) = **8 хард-проверок** (`ruff`, `mypy`, `pytest`+порог покрытия,
`bandit -ll`, `pip-audit`, `conformance`, `comment-guard`, `gitleaks`). Локальный прогон в день записи:
`pytest` **20/20 наборов** (325 с), `ruff` чисто, `mypy` **81 файл** без ошибок, `bandit -ll` Medium 0 /
High 0, `comment-guard` **0 замечаний в 112 файлах** при потолке 0, `conformance` **19/19**.

| DIM | Было (S23) | Пруф прохода | Стало |
|---|---|---|---|
| DIM-1 conformance | L1 | джоба `conformance` в CI + `tests/conformance/` (152 стр.); прогон: 30 сценариев, **7 применимых зелёные**, 23 неприменимых объявлены поимённо; `VPM_CONFORMANCE_REQUIRED=1` — пропуск без node = провал; паритет G14/D30 проверен живьём | **L1 → L2** |
| DIM-6 тесты | L2, блокер `F51` | оба блокера сняты: `tests/harness/` (живой сервер под тестом) и `tests/agent_swarm/` (34 исполнителя, 29 защищено / 4 дыры); плюс `tests/structure_emulation/` (54 проверки, метаморфный инвариант), `tests/conformance/`, мутационная приёмка M99–M123, `test_tools_inventory` (88 ассертов) | **L2 → L3** |
| DIM-15 структура | L2–L3 split | правило стало исполняемым: джоба `comment-guard` + пофайловый потолок в git; прополка 93 → **0 замечаний в 112 файлах**, механизм в одном файле (вторая копия снесена) | **L2–L3 → L3** |
| DIM-3 sec-inbound | L2 | восемь браузерных угроз воспроизведены на живом сервере; закрыты `F103`, `F104`, `F106`–`F109`, `F128`; покрытие меряет рой, а не план | **L2 (прочнее), уровень тот же** |
| DIM-16 sec-паттерн | L2 | fail-closed перестал зависеть от конфигурации (`F106`), отказ hot-reload оставляет прежние правила (`F128`), сырьё исключения не течёт (`F129`), заголовки — middleware на все ответы (`F109`) | **L2 (прочнее), уровень тот же** |
| DIM-4 sec-outbound | L2 | OUT7 закрыт (`F129`); отравление манифеста стало проверяемым классом (5 классов в `test_tools_inventory`) | **L2, блокер `F87`** |
| DIM-13 качество кода | L2–L3 | −507 строк мёртвой P-оси (`F10` закрыт удалением), −446 строк прополки; НО сам сторож дырявый: `F122`–`F124`, `F126` открыты | **L2–L3, без изменения** |
| DIM-14 анти-хардкод | L2 | `F10` (`device=cuda`) снят вместе с мёртвым кодом; `0.0.0.0` вычищен из списка хостов (`F104`). Нового декларативного слоя проход не строил | **L2, без изменения** |
| DIM-9 repo-гигиена | L2 | `pip-audit` объявлен хард-гейтом и в шапке (`F121`), gitleaks — пиннутый бинарь. `ls` в корне: `SECURITY.md`/`CONTRIBUTING.md`/`RELEASING.md`/`CHANGELOG.md` по-прежнему нет | **L2, без изменения** |
| DIM-8 packaging | L2 | на диске `docker/runner.Dockerfile` — образ раннера, не сервера | **L2, без изменения** |
| DIM-2 auth · DIM-5 изоляция | L2 · L2 | проход не касался: OAuth 2.1/scoped-token и deploy-hardening (S5) не начаты. Косвенно: `Host` теперь проверяется ДО auth | **L2 · L2, без изменения** |
| DIM-10 контракты | L3 | инвариант «код из yaml обязан быть в наборе» (`F100`); реестр — **60 ключей** (59 кодов + `DEFAULT`) | **L3, без изменения** |
| DIM-12 продукт | L1–L2 | инструменты работают (счёт — [`00` §1](00_reality_check.md), эталон `tests/quick/tools_inventory.golden.json`); облачные адаптеры не «стабы», а снесены — `F3` сужен до «облако не реализовано»; `pipeline/`, `tools/video/`, FFmpeg — нет | **L1–L2, без изменения** |
| DIM-7 observability · DIM-11 эконом | L0–L1 · L0 | не начинали. `/health` в проекте есть только у раннера (`core/runner/service.py`), не у сервера; `budget_*` в `core/providers/pool.py` — про VRAM, не про расход | **L0–L1 · L0 — самые красные** |

| Измерение | L0 | L1 | L2 | L3 | L4 |
|---|---|---|---|---|---|
| DIM-1 conformance | | | ● | | |
| DIM-2 auth | | | ● | | |
| DIM-3 sec-inbound | | | ● | | |
| DIM-4 sec-outbound | | | ● | | |
| DIM-5 surface/изоляция | | | ● | | |
| DIM-6 тесты | | | | ● | |
| DIM-7 observability | ◐ | ◐ | | | |
| DIM-8 packaging | | | ● | | |
| DIM-9 repo-гигиена | | | ● | | |
| DIM-10 контракты | | | | ● | |
| DIM-11 экон-контейнмент | ● | | | | |
| DIM-12 продукт (без ffmpeg) | | ◐ | ◐ | | |
| DIM-13 качество кода | | | ◐ | ◐ | |
| DIM-14 анти-хардкод | | | ● | | |
| DIM-15 структура/конвенции | | | | ● | |
| DIM-16 безопасность-паттерн | | | ● | | |

**Вердикт S24: по-прежнему L2 (Beta), но карманов L3 стало три — контракты (DIM-10), тесты (DIM-6),
структура (DIM-15).** Общий уровень не двигается, и это не скромность: L3 по рубрике требует auth
(OAuth), observability и экон-контейнмент, а `DIM-7`/`DIM-11` — единственные L0 в таблице — проход
не трогал вовсе. Проход укрепил то, чем уровень МЕРЯЕТСЯ (тесты, гейт, периметр), а не то, чего
уровню НЕ ХВАТАЕТ.

### Чего именно не хватает до L3 — список проверяемых условий

Условие засчитывается, только когда его можно проверить командой или прогоном (столбец «чем проверить»).

| # | Условие | DIM | Чем проверить |
|---|---|---|---|
| 1 | Audit-trail: auth-события, вызовы инструментов с санитизированными параметрами, `structlog` вместо `print` | DIM-7 | прогон: вызвать инструмент → в журнале появилась запись с principal/tool/результатом, а `Fact.data` в ней санитизирован |
| 2 | `/health` у САМОГО сервера (сегодня он только у раннера) | DIM-7 | `curl http://<порт сервера>/health` → 200 с составом |
| 3 | Квоты/бюджеты на дорогие операции + объявленный отказ | DIM-11 | код `QUOTA_EXCEEDED` в `config/server_reactions.yaml` и регрессия, краснеющая при снятии лимита |
| 4 | OAuth 2.1 Resource Server + PKCE, scoped-token | DIM-2 | живой прогон: без токена 401 с `WWW-Authenticate`, метаданные RS отдаются |
| 5 | Deploy-hardening: non-root, cap-drop, read-only rootfs (или systemd-unit с теми же ограничениями) | DIM-5/8 | файл на диске + прогон сервера под ним; попытка записи вне `workspace/` отбита ОС, а не только нашим кодом |
| 6 | Провенанс на классе «чужой текст наружу», а не на инструменте (`F87`) | DIM-4/16 | ассерт: результат `fs_smart_search` несёт `trust: untrusted`; рой-сценарий `atk_search_poison` перестаёт быть дырой |
| 7 | Четыре известные дыры роя закрыты (`F36`: slowloris, бомба вложенности; identity-rate за туннелем) | DIM-3 | прогон роя: 33/33 защищено, ни одного `xfail-spec` |
| 8 | `SECURITY.md`, `CONTRIBUTING.md`, `RELEASING.md`, `CHANGELOG.md` | DIM-9 | `ls` в корне |
| 9 | Docker-образ СЕРВЕРА с non-root `USER` и digest-pin | DIM-8 | сборка образа + `docker run` с `--read-only` |
| 10 | Продукт: хотя бы один сквозной сценарий `media → pipeline → video` | DIM-12 | прогон против живого сервера, файл на выходе |

Пункты 1–3 блокируют сильнее прочих: пока `DIM-7`/`DIM-11` на L0, общий L3 недостижим арифметически —
рубрика требует их поимённо.

## Карта развития (какой воркстрим поднимает какое измерение)

| Чтобы поднять | Делаем | Целевой уровень |
|---|---|---|
| DIM-2 auth L0→L3 | **I6**: OAuth 2.1+PKCE Resource Server, per-server scoped token | L3 |
| DIM-4 outbound L0→L2 | **I6** P0-митигации `06 §D`: провенанс, containment, destructiveHint | L2→L3 |
| DIM-5 изоляция L1→L3 | **I6**: write-allowlist (F34) + deploy-hardening `06 §G.1` | L3 |
| DIM-7 observability L0→L3 | **I5**: structlog + audit-trail (principal/scope/context) + /health | L3 |
| DIM-6 тесты L1→L3 | **I7**: CI-прогон + conformance + agent-swarm-раннер + E-матрица | L3 |
| DIM-11 экон L0→L2 | **I6/I5**: quotas/budgets на media + side-effect governors | L2 |
| DIM-9 repo L1→L2 | **I8/I2**: LICENSE (F19), SECURITY/CONTRIBUTING/RELEASING | L2 |
| DIM-8 packaging L2→L3 | **I3**: Docker + non-root + digest-pin в CI | L3 |
| DIM-1 conformance L1→L3 | **I7**: `modelcontextprotocol/conformance` в CI; закрыть parity G14/D30 | L3 |
| DIM-12 продукт L1→L3 | **P2–P7** (media→pipeline→video e2e) + **A7** (патерн/уникальность data-поверхность). **P1 FFmpeg — на последок, вне оценки пока** | L3 |
| DIM-10 контракты L2→L3 | **A6**: D4/D27 (F5) — реакции полны | L3 |
| DIM-13 качество кода L1/2→L3 | **A2** (распил монолита) + проходы `code-quality` на каждом воркстриме | L3 |
| DIM-14 анти-хардкод L2→L3 | ~~A3 (config/ops — снят)~~ → **A7** (yaml-конфиг/per-project override — эталон) + дисциплина `anti-hardcode` на каждом воркстриме | L3 |
| DIM-15 структура L1→L3 | **A2** (tools/ тонкие) + `project-conventions` при каждом новом файле | L3 |
| DIM-16 sec-паттерн L1→L3 | **I6/A6**: choke-point (G17), fail-closed, провенанс как структурный паттерн | L3 |

**Порядок к L3 — актуализирован S24.** Пройдено: Фаза 0 ✅ (I1/I2/I4/I3) → **A2 ✅** (разблокировало
DIM-13/15) → **I6 частично ✅** (auth, allowlist, провенанс, choke-point) → **I7 в основном ✅** (T2 харнесс,
рой, E-матрица, conformance — DIM-6 на L3). Осталось до L3, в порядке блокирования: **(1) I5**
(observability + audit-trail + quotas — единственные оставшиеся L0, условия 1–3 списка выше) →
**(2) остаток I6** (deploy-hardening S5, `F87` провенанс поиска, 4 дыры роя, OAuth) → **(3) I8**
(`SECURITY.md`/`CONTRIBUTING.md`/`RELEASING.md`) → **(4) P-ось продукта** (A7 идёт с data-слоем;
**P1 FFmpeg — последним**, тогда же вводим его в оценку). Enterprise (L4) = после продукта: conformance
по всем возможностям спеки + quotas + supply-chain.

> **A7 строится ПО DIM-13/14/15 как эталон** (декларативный per-project override, тонкие обёртки, git-native история) — новый код обязан задавать планку, а не добавлять долг.

## Сильные MCP-репозитории (эталоны для сверки)

| Репо | Чему учит |
|---|---|
| `modelcontextprotocol/servers` [M1] | reference-инструменты/SDK (но сами L0 — не эталон продакшена) |
| `modelcontextprotocol/conformance` [M4] | **conformance-тесты** — DIM-1 gate |
| `github/github-mcp-server` [M9] | официальный ПРОДАКШН-MCP: структура, auth, тесты |
| `appcypher`/`punkpeye`/`wong2` awesome-lists, `best-of-mcp-servers` [M1] | найти сильные репо по категориям для точечной сверки |

**Источники [M#]:** [M1] awesome-mcp-servers (appcypher/punkpeye/wong2) + modelcontextprotocol/servers · [M2] MCP production-readiness (trust-class/auth/param-boundaries/эконом-контейнмент) + security-spec 6 векторов/5 authz-паттернов + observability-события · [M3] OAuth 2.1+PKCE mandatory HTTP-MCP (июнь-2025 spec), server=Resource Server, per-server scoped token · [M4] modelcontextprotocol/conformance · [M5] MCP security checklist (75 пунктов, 30+ CVE, 43% command-injection, 78.3% attack-success @5 серверов) · [M6] strict-schema/MCP Inspector · [M7] «5 Gates demo→production» тестирование · [M8] repo-гигиена (CONTRIBUTING/SECURITY/RELEASING, OIDC-publish, Docker) · [M9] github/github-mcp-server. URL — в ответе сессии.
