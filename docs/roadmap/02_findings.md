# 02 — Реестр находок (F#)

> Пополняется каждым прогоном. Severity: 🔴 блокер enterprise · 🟠 важно · 🟡 желательно · ⚪ мелочь.
> «Пруф» — как проверено. «→» — воркстрим-получатель из `01_master_roadmap.md`.
> Сессия 1: посев тремя прогонами на **скелетной** глубине (структурные находки). Глубина по коду — в фазовых сессиях.

**Счёт на S24 (2026-08-15, сплошной проход): 91 находка, 66 закрыты, 25 открытых.**
Счёт пересчитан по самому реестру, а не унаследован от прошлой записи: четыре строки (`F28`/`F30`/
`F56`/`F59`) были закрыты ещё в S22, но не вычеркнуты и потому числились живыми.
Открытые: F3 · F8 · F10 · F11 · F15 · F16 · F17 · F18 · F20(остаток) · F21 · F24 · F25 · F26 · F27 ·
F31 · F35 · F36 · F40 · F41(сузился) · F42(остаток) · F45 · F46(сузился) · F57 · F58 · F74 · F87.
Куда какая идёт — [`16_execution_matrix.md` §«Состояние блоков»](16_execution_matrix.md);
что брать следующим — [`_sessions.md` → RESUME](_sessions.md).

## Прогон 1 — Качество (skill: code-quality)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F1~~ | ✅ | **ЗАКРЫТ S17 (A2, 8 зелёных коммитов):** монолит распилен — `server.py` 1477 → 402 строки (транспорт/JSON-RPC/auth/firewall/запуск), 52 хендлера в `tools/<group>/` (filesystem 12 · memory 2 · tables 13 · excel 17 · structure 5 · search 3), общие зависимости — `tools/_context.py` (`ToolContext`), общие фрагменты схем — `tools/_schemas.py`. `register_basic_tools` = `build_context` + 6 вызовов. Диффы `core/*` = 0. Контракт клиента доказан механически (`test_tools_inventory` + эталон) и на живом сервере | ранее: `wc -l server.py` = 1521; теперь `wc -l` = 402, `tools/list` = 52 через транспорт | A2 ✔ |
| ~~F2~~ | ⚪ | **ОТОЗВАНО S4:** «config/ops пуст = архитектура сломана» — НЕВЕРНО. ops/model_routing **намеренно упразднены**, консолидированы в `channel_config.yaml → resource_limits` (`media_tools_deployment.md §0`). Не дефект — устаревший README. См. `05` §0 | design-решение владельца | A4 (README), не A1 |
| F3 | 🟠 (сузился) | **АКТУАЛИЗАЦИЯ S24: половина уже не стаб.** TTS и IMG локально ИСПОЛНЯЮТСЯ (`piper_local`, `diffusers_local`, `onnx_bg`/`onnx_upscale` — реальный `generate()`, подъём на видеокарте). Стабами остались ffmpeg и облачная сторона. Отдельный случай — STT: `stable_ts_adapter` не честный стаб, а **осиротевший код**: он не подключён ни к одному провайдеру в `config/providers.yaml` и не реализует нынешний протокол адаптера (`generate`/`poll`/`fetch`), только легаси `trigger_transcription`. **Было:** Провайдеры ffmpeg/tts/stt/img = `NotImplementedError` (честные стабы — G16 соблюдён, но продукта нет) | grep `NotImplementedError`; `ls core/providers/{tts,img}/*_local.py` | P1–P4 |
| ~~F4~~ | 🟡 | **ПОНИЖЕНО S15 (обмер под-2):** «0 тестов, не ревьюен» — НЕВЕРНО. `tests/quick/test_search.py` содержателен (happy+контракт+adversarial D36 traversal+edge+QueryPlanner+`_detect_entity_type`); в коде следы D36/D37 (находили+чинили). Остаётся: relevance-eval (F31) + quality-находки F38–F42. Незрелость переоценена | `test_search.py`; D36/D37 в коде | A5, I7 |
| ~~F5~~ | ✅ | **ЗАКРЫТ S16 (A6/R-CONTRACT1):** DEFAULT-fallback в `reactions.py` теперь единообразен с обычной веткой — тянет `class`/`message_template`/`recovery` из записи `DEFAULT` реестра (было: хардкод `'Непредвиденная ошибка'` без точки, `reaction_class` не ставился). Регрессия `test_audit_fixes` (`get_error(unknown).message == DEFAULT.message_template`) 🟡→🟢. Осталось (не F5): `raw_message` для ИЗВЕСТНЫХ кодов перекрывает template — это НАМЕРЕННО (специфика важнее шаблона; template = дефолт при пустом raw) | `reactions.py` get_error DEFAULT-ветка; `test_audit_fixes` PASS | A6 ✔ |
| ~~F6~~ | ⚪ | **ОПРОВЕРГНУТА S18 (чтением кода):** `id_generator` — не мёртвый inject-параметр. `TemplateEngine.ids.generate_simple` дёргается на создании узла (`template_engine.py:129`) и файла-таблицы (`:157`); `TableEngine.ids.generate_simple` — на генерации `row_id` (`tables_core.py:273`); инъекция идёт через `tools/_context.build_context`. D28 в этой точке закрыт | `grep -n "self\.ids\." core/engine/template_engine.py core/tables/tables_core.py` → 3 живых вызова | — |

## Прогон 2 — Стиль / структура (skills: project-conventions + anti-hardcode)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F7~~ | ✅ | **РЕШЕНО S6 (A4/OQ1):** README переписан под фактическую консолидированную архитектуру — убраны несуществующие `config/ops`/`paths.yaml`/`model_routing.yaml`/`.xlsx`; добавлены реальные `core/{firewall,search,excel,tables,paths}` + `config/{firewall,channel_config,tunnel}` + templates/workspace; маркеры ✅/🟠/🔲; ссылка на `docs/roadmap/`. «Ловить ложные сигналы» больше не с чего | README ↔ диск | A4 ✔ |
| F30′ | — →🔨 | **Остаток F30 (выбор слагаемых) закрыт в части сцены — S22, A7.2.** Требование «`scene_profile.enabled=false` → тип не входит в формулу уникальности» исполнено НЕ формулой Excel, а локальным расчётом (решение владельца S15, `10 §2`): `core/uniqueness` читает профиль из данных канала, выключенный тип не входит в композицию вовсе, веса — `niche_weight` оттуда же. Проверено эмпирически: тот же набор данных при включённом тумблере даёт другой результат (анти-D2). **Плюс контракт готовности, которого не было в дизайне:** ответ несёт `readiness` full/partial/empty + поимённый список отсутствующих входов + `fragment_gaps` — включённый тип БЕЗ применений это отсутствие данных, а не нулевая уникальность. Пустой вход даёт объявленный ноль, а не «100% уникально». **Остаётся A7.1/A7.5:** проектируемые листы сцен и петля сигнал→решение→память | `test_uniqueness` 34/34, мутации M72 (игнорировать тумблер), M73 (пробел = ноль), M74 (неполнота как полнота) краснеют | **A7** |
| ~~F59~~ | 🟠→✅ | **ЗАКРЫТ S22. Сервер сам объясняет ИИ выбор, о котором тот не знал.** Заведён второй канал объяснения — `recommendations[]` в УСПЕШНОМ ответе (реестр реакций объясняет только ошибки, а тут промаха нет: есть незнание). Тексты и маршруты — в `config/recommendations.yaml`, загрузчик `core/advice.py`; в коде ни одной фразы. `structure_create` теперь из ОДНОГО ответа называет все три режима с инструментом и параметрами вызова. Побочно в S22 закрыт кусок F59 по ошибкам: `ROW_NOT_FOUND` ведёт `suggested_tool: table_find_row` вместо совета сканировать лист прозой | `test_structure §35` 236/236: ответ несёт `mode_default/mode_custom/mode_manual`, у каждого есть текст И инструмент. Мутация M70 (замолчать канал) краснеет | **B4** |
| ~~F28~~ | 🟠→✅ | **ЗАКРЫТ S22. Деструктив над столбцом больше не ломает формулы молча.** `delete_column`/`move_column` сперва ищут формулы, ссылающиеся на столбец — по ВСЕЙ книге, включая межлистовые (`META!A2:C9`) и диапазоны (диапазон считается ссылкой на каждый столбец внутри). Есть ссылки → отказ `COLUMN_HAS_DEPENDENTS` с перечислением конкретных ячеек; `force=true` ломает осознанно и ОТЧИТЫВАЕТСЯ, что сломал. Новый читающий инструмент `excel_find_dependents` даёт спросить ДО операции — отказ actionable, а не тупиковый. Границы в разборе ссылок обязательны: без них имя `Q1_total` читается как адрес ячейки и даёт ложную зависимость | прогон: удаление столбца под `=B2/A2` и `=SUM(META!A2:C9)` → отказ с «META!D2, CALC!B2»; `force=true` → удалено, `broken_formulas=1`. `test_tables` 58/58, мутации M66 (снять пред-проверку) и M67 (убрать границы регулярки) краснеют | **B4** |
| ~~F30~~ | 🟠→✅ | **ЗАКРЫТ S22. Формулы деградируют на неполных данных, а не ломаются.** Материализатор оборачивает КАЖДУЮ объявленную формулу в `IFERROR(<формула>, <запасное>)`. Запасное значение — из декларации, не из кода: `on_empty` у столбца → `degrade:` у книги → общий `config/templates/tables/_defaults.yaml` (float/integer → 0, string → PENDING, enum → пусто, чтобы не нарушать дропдаун). В движке нет ни одного зашитого значения — регрессия это проверяет грепом по исходнику. **Доказано РЕАЛЬНЫМ пересчётом LibreOffice** (урок F29: греп токенов = театр): деление на пустую ячейку даёт `0`/`PENDING`, контрольный прогон той же формулы без обёртки — `#DIV/0!`. **Остаток вне F30:** выбор СЛАГАЕМЫХ формулы по `scene_profile.enabled` («тихий столбец») — это фаза формул Ф3, её нет | `test_table_materializer §7c` 56/56, мутация M68 (отключить деградацию) краснеет 4 проверками | **B4** |
| ~~F56~~ | 🟠→✅ | **ЗАКРЫТ S22. Правка строки атомарна, и ID ищется по значению.** `table_update` кладёт НЕСКОЛЬКО полей ОДНОЙ операцией очереди (`action: update`): пять `set` применялись по одной, и отказ на третьей оставлял строку наполовину изменённой. Проверка всех полей идёт ДО записи первого — и при постановке, и на применении (схема могла измениться между очередью и `execute`). `table_find_row(where={столбец: значение})` закрывает разрыв «ИИ знает значение, а писать надо по ID»: раньше единственным путём был скан листа. Побочно закрыт кусок F59 — `ROW_NOT_FOUND` теперь ведёт `suggested_tool: table_find_row`, а не советует ручной скан прозой | прогон: очередь после `update` двух полей = ОДНА операция; при изменении enum между очередью и `execute` — `applied=0`, `skipped=[ENUM_VIOLATION]`, ни одно поле не применилось. `test_tables` 49/49, мутации M64 (писать по мере проверки) и M65 (не проверять имена столбцов) краснеют | **B4** |
| F20 | 🔨→🟡 | Таблично-схемный слой. **Loader построен** (`table_materializer`, фаза ТАБЛИЦЫ). **Авторинг схем закрыт машинно (S21):** конвертер `scripts/spec_to_schema.py` научен прозовой форме, одиночным столбцам вне списка и развороту ГРУПП листов (составное имя `VISUAL_/SCRIPT_/AUDIO_PATTERNS` разворачивается потому, что блок «Дельты» называет листы поимённо: лист = общие столбцы + своя дельта). 6 книг = **67 листов, 813 столбцов** против 43/477 до правки; все шесть материализуются в `.xlsx` живым прогоном. **Остаток — там, где молчит СПЕКА, а не конвертер:** имена `ACT_2…ACT_6` (2 книги × 7 листов) не объявлены нигде; два `ANALYTICS` названы только секциями; `color_primary/secondary` — составной столбец. Тип в прозовой форме спекой не объявлен → принят `string`, помечено строкой в самой схеме | `--verify` на ручном эталоне зелёный; предупреждений 18 → 7, каждое называет книгу+лист+причину; `test_table_materializer` 44/44, мутации M58/M59/M60 краснеют | A1′ |
| ~~F75~~ | 🟠→✅ | **ЗАКРЫТ S21 в момент обнаружения. Конвертер схлопывал диапазон листов в один МОЛЧА** — из-за того, как записана спека, а не что в ней сказано. Детектор диапазона смотрел только на имя из первой пары кавычек: в `competitor_video_data` заголовок `` `ACT_1_HOOK … ACT_7_TRUTH` `` (одна пара) давал предупреждение и пропуск, а в `video_data` тот же диапазон записан `` `ACT_1_HOOK` … `ACT_7_TRUTH` `` (две пары) — многоточие оставалось в хвосте заголовка, и 7 листов становились ОДНИМ листом `ACT_1_HOOK` без единого слова. Тот же класс, что F72/F73: отказ обязан быть слышен. Побочно там же: `\s*` в разборе заголовка перешагивал перевод строки и затягивал в описание листа первую строку тела (`# \| Столбец \| Тип \|` в готовых схемах) | до: `video_data.schema.yaml:49` = один лист `ACT_1_HOOK`, предупреждений ноль; после: предупреждение «спека не называет их поимённо» и лист не создаётся. Мутация M58 краснеет | A1′ |
| F21 | 🟠→🟡 | **Переформулирована S23.** Исходная формулировка («`scripts/` пуст, интроспектора нет») устарела: схемы авторятся машинно из спек владельца — `scripts/spec_to_schema.py` + `scripts/config_to_schema.py`, 67 листов получены (см. F20). Остаток находки: интроспекции **готовых `.xlsx`** (обратный ход «книга → схема») по-прежнему нет, поэтому книги, у которых нет спеки, схему не отдают | `ls scripts/` → 4 утилиты; `find introspect_tables.py` ∅ | A1′ (остаток) |
| ~~F22~~ | ✅ | **РЕШЕНО S5 (OQ4):** 15 спек-файлов импортированы в трекаемый `docs/roadmap/spec/` (schemas/ + instructions/ + Бриф/project_memory.spec/media_tools_deployment) + индекс `spec/README.md` + ledger улучшений `spec/IMPROVEMENTS.md`. Оригиналы в `/home/admin/projects/` оставлены | `find docs/roadmap/spec` = 17 файлов | OQ4 ✔ |
| F8 | 🟠→🟡 | **ЧАСТИЧНО ЗАКРЫТ (A2), проверено S18:** 6 групп `tools/{filesystem,memory,tables,excel,structure,search}` наполнены. Остаются пустыми: `tools/media/`, `tools/video/`, **`tools/excel_engine/`** (0 файлов, даже без `__init__.py`; при этом рядом наполненный `tools/excel/` — каркас-дубль, кандидат на удаление), `pipeline/{entry_points,steps}`, `scripts/` | `ls -A` по каталогам: media/video/excel_engine/pipeline*/scripts = 0 элементов | P4′/P7 (media/video), P5/P6, A1′ (scripts) |
| ~~F9~~ | ⚪ | **НЕАКТУАЛЬНА S18 (ремап):** `docs/dev/` целиком удалён владельцем 2026-07-05 → «отсутствует `docs/dev/audit/`» больше не дефект. Словарь `D#/G#/F#` живёт в этом файле (`02_findings.md`) + git-история; это и есть принятое решение X1 | `ls docs/dev` → нет каталога (удалён намеренно) | X1 ✔ (ремап) |
| F10 | 🟡 (сузился) | Хардкод-хуки: stt `device=cuda` захардкожен — ЖИВ. Вторая половина снята: `ffmpeg.render_full_pipeline` с busy-loop удалён целиком (S24, F88) — 75 строк оркестрации против четырёх `NotImplementedError`-примитивов и без единого вызывающего | `stable_ts_adapter.py:20`; `grep render_full_pipeline` → только честный отказ | P1, P3, anti-hardcode |
| F11 | 🟡 | Провайдеры: api_key-гигиена tts/img; `raw_response` может течь в `_map_error` (D23) | память audit-v2 deferred | I6, P2/P4 |
| ~~F19~~ | ✅ | **РЕШЕНО S14:** `LICENSE` = **PolyForm Noncommercial 1.0.0** (владелец выбрал — защита IP: код виден, коммерц-использование запрещено). pyproject классификатор обновлён. DIM-9 repo-гигиена ↑ | ранее: Нет файла `LICENSE` — публичный репо без лицензии = юридически «all rights reserved», блокирует внешний вклад/использование. Владелец не выбрал лицензию (в pyproject помечено `Private :: Do Not Upload`) | `ls LICENSE*` → нет (S3) | I2/I8 (ждёт решения владельца) |

## Прогон 3 — Системы / безопасность (skills: security-reviewer + test-master)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F12~~ | ✅ | **РЕШЕНО S2 (I1):** `tests/` был свален в секцию «ДАННЫЕ» с `workspace/`. Убран из `.gitignore` (тесты = код); 23 файла (6 quick + 6 симуляций + scenarios.yaml) в git; `__pycache__`/`.pytest_cache` остаются игнорированы. Секретов в tests/ нет (проверено) | `git diff --cached` чист | I1 ✔ |
| ~~F13~~ | ✅ | **ЗАКРЫТ S16 (I3) + починен S17 (F48); статус был протухшим — снят при верификации S18.** `.github/workflows/ci.yml` существует: lint (ruff+mypy ×2 версии Python) · test (6 quick + 6 симуляций) · security (bandit -ll хард-гейт, pip-audit advisory) · gitleaks (пиннутый бинарь). Прогоны зелёные | `ls .github/workflows` → `ci.yml`; `gh run list` → success | I3 ✔ |
| ~~F14~~ | ✅ | **ЗАКРЫТ S19 (S1).** `core/auth.py`: сервер выпускает ключ сам при первом старте (`token_urlsafe(32)` → `.env`, права `0600`, печать один раз), повторный старт ключ не меняет, `--rotate-key` перевыпускает не трогая соседние переменные; пустой токен больше НЕ означает «auth отключена» — fail-closed, калитка только явная (`MCP_ALLOW_NO_AUTH=1`, с громким предупреждением при старте); принимаются оба заголовка из allowlist коннектора (`Authorization: Bearer`, `X-Api-Key`). Побочно найден и исправлен реальный дефект: `compare_digest` по строкам падал `TypeError` на не-ASCII токене → обработчик отвечал 500 вместо 401. Пруфы: 14 регрессий в `test_audit_fixes` (63/63), мутации M34–M36. Прежняя формулировка ниже. |  **ПЕРЕФОРМУЛИРОВАНО S18 (прежняя формулировка «нет app-level auth» — НЕВЕРНА).** Auth есть **с первого коммита**: D3 bearer-токен, `secrets.compare_digest`, проверка ДО файрвола, 401 `AUTH_REQUIRED`/`AUTH_FAILED` (`server.py:67,181`). Реальный дефект другой и остаётся 🔴: (1) **fail-open по умолчанию** — `MCP_AUTH_TOKEN` не задан ⇒ auth молча выключена, сервер открыт; в `run.sh`/`install.sh`/доках токен не выставляется; (2) статический общий секрет ≠ OAuth 2.1 Resource Server со scoped-токеном (DIM-2); (3) **ни одного теста на auth** — `grep MCP_AUTH_TOKEN tests/` ∅, регрессии на «включённую auth» нет. Совпадает с честной оценкой `07` DIM-2 L0 | `server.py:67/181` (bearer есть); `git log -S"Bearer "` → initial commit; `grep MCP_AUTH_TOKEN run.sh install.sh docs/` ∅; `grep -rn MCP_AUTH_TOKEN tests/` ∅ | I6 (OAuth+fail-closed), I7 (тест) | **Дополнено S19 (директива владельца + практики):** ключ должен **генерироваться сервером при установке** (`secrets.token_urlsafe(32)` → `.env` с правами `0600`), а не придумываться человеком; ротация — флагом; имя заголовка берём из allowlist коннектора Клода (`authorization`/`x-api-key`), поэтому фикс совместим с Claude AI Web уже сегодня (Request headers). Самодельное шифрование секрета не делаем — ключ расшифровки пришлось бы держать рядом; правильные меры — права файла + OS-keyring. Нормативно (MCP Security Best Practices): локальный HTTP-сервер **обязан** требовать токен, а владение хэндлом (`row_id`/`entity_id`) **не является** аутентификацией. План — `15 §S1`.
| F15 | 🟠→🟡 | **УТОЧНЕНО S18:** D29 **закрыт** — `safe_resolve` стоит на всех entity-путях `state_manager` (7 вызовов), containment подтверждён мутацией M7 (снял проверку → `test_audit_fixes` и `test_search` краснеют). D3 — не «открыт», а переформулирован в F14 (auth есть, но fail-open). Остаётся: fail-closed-режим + OAuth | `grep -c safe_resolve core/state/state_manager.py` = 7; мутация M7 | I6 |
| F16 | 🟠→🔨 | `threat_landscape.md` отсутствует на диске (заявлен в скиле security-reviewer как источник угроз). **Частично закрыт S8:** каталог угроз восстановлен в git-tracked `06_threat_catalog.md` (IN1–IN10 + OUT1–OUT8 + митигации + приоритет). Осталось: при желании зеркалить в `docs/dev/threat_landscape.md` | `find threat_landscape*` → ∅; `06_threat_catalog.md` создан | I8, security |
| F36 | 🟠 | **DDoS/пакетная защита не задокументирована как двухуровневая + origin-gaps.** Сервер за cloudflared → edge держит L3/4+L7 DDoS/WAF/rate/bot **бесплатно always-on** (origin не выставлен) [C4] — но: (1) edge-настройки не зафиксированы (DDoS=High, rate-rules, Bot-Fight `Definitely Automated=Allow` иначе туннель падает `websocket: bad handshake`); (2) origin-gaps: per-IP rate бесполезен за туннелем (G18) → нужен identity-rate; нет slowloris-таймаутов/conn-лимита; нет явного payload-size-лимита; строгий JSON-RPC/reject-CL+TE **подтверждён живьём** (рой S24: CL+TE и дубль `Content-Length` → 400, неизвестный метод → -32601), а вот **slowloris и бомба вложенности пробивают**: полуоткрытое соединение живёт ≥9 с, JSON глубиной 400 принимается с HTTP 200. Кэша польз.данных НЕТ (только `template_engine._cache` git-данные, ⚪) — практики §H.3 forward. Все практики §H [C1–C7] | `06_threat_catalog.md §H`; `template_engine.py:73` | I6 (P0 edge+identity-rate), A5 (кэш) |
| F35 | 🔴→✅baseline | **No-root инвариант (эскалация привилегий через сервер).** Требование владельца: физически невозможно получить root на машине клиента через сервер. **Эмпирика 2026-07-05 ЧИСТО:** нет `os.system`/`eval`/`exec`/`pickle`/`shell=True` в коде инструментов; сервер не от root (uid 1000); единственный subprocess = cloudflared arg-списком (`tunnel.py:270`, без shell). Задача = НЕ регрессировать: инварианты G-1..G-6 (`06_threat_catalog.md §G`) — не исполнять контент workspace (делает `.py`-allowlist безопасным), нет shell, не root/least-priv, containment блокирует persistence-файлы, safe_load, media-subprocess arg-list. Регрессия = bandit-gate в CI + тест-не-root + рой-агент. **Deploy-hardening §G.1** (проверенные практики, не выдумка): non-root+`NoNewPrivileges`+cap-drop ALL, Landlock (writable=workspace), seccomp-профиль, контейнер по Anthropic Agent SDK baseline (`--cap-drop ALL`/`no-new-privileges`/`--read-only`/`--tmpfs`/`--user 1000`), gVisor/Kata для media. Плюс: транспорт HTTP не STDIO → мимо CVE-2025-49596 | grep exec-sinks ∅; `id -u`=1000; `tunnel.py:270` arg-list; §G.1 [S1–S7] | I6 (P0), I3 (CI bandit), agent-swarm |
| ~~F34~~ | ✅ | **ЗАКРЫТ S19 (S2).**  **Нет write-type allowlist — материализуем любой тип файла.** `fs_create_file`/`fs_write_file` берут произвольный `content` и путь без проверки расширения → можно записать `.sh`/`.html`/`.js`/`.exe`/малварь (усиливает OUT5, цель рой-атакующего «create virus»). Легитимный словарь узок: `.json/.md/.xlsx` (шаблоны), `.yaml` (конфиг), `.py` (скрипты); медиа-ассеты по фазам. Fix = **default-deny allowlist** декларативно в конфиге + единый choke-point на всех путях записи + код `FILE_TYPE_FORBIDDEN` (`06_threat_catalog.md §F`). React/css/веб-типы серверу не нужны вообще | `server.py:169/180` пишут без type-guard | I6 (P0), anti-hardcode, reactions-errors |
| ~~F33~~ | ✅ | **ЗАКРЫТ S19 (S3).**  **Outbound-защита клиента = сплошной gap.** Каталог `06_threat_catalog.md` §B: OUT1 (нет провенанс-маркировки workspace-вывода) / OUT5 (containment на write/move/delete + destructiveHint не подтверждены сплошняком) / OUT6 (`_SESSION_LOG.md` пишет сырой `Fact.data` → exfil) / OUT8 (search-poisoning) — ни один не закрыт. Плюс IN: app-level auth отсутствует (D3/G18) = фундамент под IN3/IN9. Приоритет фиксов — `06 §D` | `06_threat_catalog.md` §B/§D | I6 (P0-митигации), security-reviewer, agent-swarm |
| F17 | 🟠→🟡 | **ПРОВЕРЕНО S18 — утечек нет.** В трекнутых файлах совпадения только на **имена переменных** (`tunnel_token` в `core/transport/tunnel.py:92` — чтение из env/конфига; `t2.tunnel_token = "TOK"` в тесте — фикстура), не секреты. `config/tunnel.yaml` в `.gitignore`; gitleaks сканирует всю историю в CI (S17, 0 утечек). Остаётся: `.env`-стратегия не описана (нет `.env.example`), auth-токен нигде не выставляется (→ F14) | `git ls-files \| xargs grep -l` → только имена переменных; gitleaks job зелёный | I6 (secrets-mgmt) |
| F18 | 🟡→🟢 | Симуляционные тесты **существуют на диске и теперь в git** (S2): `virus_injection`, `bot_army`, `cache_injection`, `cache_overflow`, `config_change`, `render_draft_final`. Остаётся: подтвердить, что зелёные в CI (часть требует живого сервера, как firewall 1/4) | `find tests/` после I1 | I7 |

## Прогон 4 — Конформность структуре (skill: project-conventions)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F23~~ | ✅ | **ПЕРЕРЕШЁН S22 — ops-слой снят.** Решение S8 («строим `config/ops/{filesystem,tables,excel}.ops.yaml`», A3) отменено: реестр операций упразднён как слой вместе с `model_routing`. Логика групп живёт в `tools/<группа>/`, декларативность держат `config/providers.yaml`, `server_reactions.yaml`, `firewall.yaml`, `config/templates/`; `config/ops/` остаётся пустым осознанно. Конфиг канала — листы книги `channel_data` (не `channel_config.yaml`) | `ls config/ops` → 0 файлов; решение владельца S22 | ~~A3~~ снят |
| F24 | 🟠→🟡 | **ЧАСТИЧНО ЗАКРЫТ (A2), проверено S18:** инструменты переехали в `tools/<group>/` (52 шт., `server.py` 406 строк) — главная часть нарушения снята. Остаётся: `pipeline/{entry_points,steps}` пуст (P5/P6) и доки не зеркалят код (`docs/dev/` удалён → зеркало должно строиться в `docs/github/`, I8) | `wc -l server.py`=406; `ls -A pipeline/*` = 0 | P5/P6, I8 |
| ~~F32~~ | 🔴→✅ | **ЗАКРЫТ S24 (волна 2.1 сплошного прохода). Построен раннер `tests/agent_swarm/test_agent_swarm.py`: 34 исполнителя бьют по ЖИВОМУ серверу через харнесс T2 — 5 честных клиентов, inbound, outbound, эскалация, частота/транспорт.** Раннер устроен против театра: паттерн без исполнителя и исполнитель без паттерна красят гейт (реестр и код сходятся в ОБЕ стороны), а `xfail-spec`, у которого защита СРАБОТАЛА, тоже красит — иначе закрытая дыра числится дырой годами (так и нашлись 10 устаревших статусов). Фазы разнесены по отдельным процессам: файрвол считает частоту по IP, и один длинный прогон банил бы сам себя, выдавая ложные «блокировки». Итог живого прогона: 28 защищено, 5 известных дыр (F36 slowloris/бомба вложенности, D12 Origin, F87 провенанс поиска), 1 «не достали» (media за калиткой провайдера), 2 отложены (кэша нет). **Было:** **Симуляции одновариантны — нет агентного adversarial-харнесса.** `bot_army`/`virus_injection`/`cache_*` бьют по `Firewall` в один поток одинаковыми payload'ами; нет (а) множества честных клиентов с РАЗНЫМИ моделями поведения, (б) злоумышленников, (в) СМЕСИ честные+атакующие в одном прогоне, (г) **outbound-атак** (сервер-как-оружие против Claude AI Web: обход защиты Claude, кража данных, удаление файлов пользователя, генерация вредоносного ПО). Симуляции — штатный движок многовариантных тестов, но используются на 10% | `tests/bot_army/*` один поток, нет agent-swarm; outbound-слой в таблице слоёв = 🔴 gap | I7, security-reviewer (обе стороны), test-master |
| F31 | 🟠 | **Нет eval-слоя качества пайплайна (сверх контракта).** Контрактные тесты проверяют «валидный ToolResult», но не КАЧЕСТВО: насколько хорош выбор информации для сценария/аудио/картинок, релевантность умного поиска (`core/search`), поток данных. Ключевой диагностический разрыв: не отделено «хромает ПРОМПТ» от «плох ПОТОК ДАННЫХ» (лишний/ненужный контекст vs недостаток). Поисковый слой (`FsSearcher`/`QueryPlanner`, `search_quick/multi/tables`) существует и тестируем сейчас (quick 24/24, но не ревьюен — F4, без relevance-eval); генерация данных/асетов и eval выбора-контекста — forward к P1–P7. Экономия токенов = синтетика+локальные модели, не облачные LLM в тестах | `core/search/*` без relevance-eval; eval-харнесса нет | A5 (search), P1–P7, test-master (E-G/H/I) |
| ~~F30~~ | ✅ | **ЗАКРЫТ S22 — см. строку `F30` выше; здесь исходная формулировка.** **Устойчивость формул к неполным данным не заложена в loader (Ф3).** Требование владельца: при отсутствии части данных (нет/часть асетов, нет конкурентов, частичные данные канала) формулы НЕ должны ломаться — деградировать (пусто/PENDING/0), не `#DIV/0!`/`#REF!`. Дизайн для этого В СПЕКЕ есть: `scene_profile.enabled=false` → формула уникальности пропускает тип («тихий столбец»), `niche_weight` — вес, `automation_rules.condition` — пороги, `signal_on_reuse/reuse_threshold` — сигналы уникальности + вариации асетов `AST_x`. Но table-loader/фаза формул (Ф3) не построены → механизма нет. Обязан быть ДЕКЛАРАТИВНЫМ (флаги конфига), не `if type missing` в коде (anti-hardcode) | `channel_config.schema.md §6 scene_profile`; Ф3 loader ∅ | A-tables (Ф3), anti-hardcode, test-master (E-F) |
| ~~F28~~ | ✅ | **ЗАКРЫТ S22 — см. строку `F28` выше; здесь исходная формулировка.** **Деструктив над столбцами молча ломает формулы.** `excel_delete_column`/`excel_move_column` работают по сырым ячейкам openpyxl (`delete_cols`/`insert_cols`/копия `.value`) — формулы, ссылающиеся на затронутый столбец, съезжают/ломаются БЕЗ реакции в момент операции. Нет пред-проверки «на этот столбец ссылается формула». Сервер должен предупреждать/отклонять или авто-валидировать после | `excel_core.py:242/253` не трогают формульные ссылки | A-tables, reactions-errors |
| ~~F29~~ | ✅ | **ЗАКРЫТ S16 (A-tables, выбор владельца — LibreOffice headless recalc).** `validate_formulas` теперь пересчитывает книгу реальным движком LO (`--headless --calc --convert-to`, уникальный профиль+timeout), читает вычисленные значения (`data_only=True`) → `=1/0`→`#DIV/0!` ловится (был греп несчитанных токенов = театр). LO недоступен → честный `RECALC_UNAVAILABLE` (не фейк-ok, G16). Регрессия `test_audit_fixes` 🟡→🟢 (гардед по soffice; CI ставит libreoffice-calc). Остаётся отд. **F30** (`table_materializer` loader → A1′) и **F28** (delete/move ломает формулы — тот же корень «нет зависимостей», → A-tables/anti-hardcode) | `excel_core._recalc_via_lo`; `test_audit_fixes` PASS | A-tables ✔ (F30/F28 отдельно) |
| F26 | 🔴 | **Cold-start advisory отсутствует.** На пустой нише (0 объектов) сервер обязан вести онбординг: уведомить «данных для анализа нет» + рекомендованный путь (изучить конкурентов — скриншоты каналов + 2-3 транскрибации сценариев) + fallback (начать с личного канала). В коде НЕТ: `structure_status` (server.py:624) выдаёт лишь orphans+childless → на пустом реестре молчит. Спека-требование владельца (2026-07-05), поведение продукта, не мелочь. Тест = xfail-spec (E-A1) | grep advisory/cold-start в core/ ∅; `structure_status` возвращает пустые списки | A3/structure, honest-stub G16 |
| F27 | 🟠 | **Reconcile-оркестрация ручная.** `structure_migrate` физически переносит (`shutil.move` server.py:616) + правит реестр, но ОДНУ сущность с явным `new_path` от ИИ. Сценарий владельца «конкурент появился → сервер сам связал конкурента↔наш канал И фоном переместил ОБА в `competitors/{наш_канал}/`» требует авто-оркестрации (вычислить целевой путь §4 + переместить обе сущности + link одним шагом) — её нет | `structure_migrate` сигнатура `(entity_id, new_path)`; нет авто-вычисления пути §4 | A3/reconcile (пара к F25/F26) |
| ~~F25~~ | 🟠→✅ | **ЗАКРЫТ S24 (волна 2.3). Целостность проверяется в ОБЕ стороны.** `check_integrity(taxonomy)` кроме прямого прохода (запись есть, каталога нет — `missing_path`, F65) делает обратный: каталоги-сущности на диске, которых нет в реестре, дают `unregistered_path` с типом узла ИЗ объявления шаблонов, а не из имени каталога. Что контейнер, а что сущность, знает таксономия; без неё обратный проход честно НЕ выполняется и пишет об этом в `disk_scan` — молчаливого зелёного нет ни в одном случае. Приёмка мутацией M102. **Было:** **Integrity-слепое пятно ФС ↔ реестр.** `structure_check_integrity` аудирует ТОЛЬКО `LinkRegistry` (`_id_registry.json`), не файловую систему. Реестр наполняет лишь `structure_create`; `fs_create_project_structure`/`fs_write`/тесты создают те же папки МИМО реестра. Итог: инструмент рапортует `total_entities:0, issues_count:0`, пока на диске лежит незарегистрированное дерево (`workspace/niches/_TEST_finance`, `workspace/channels/test` — реестра нет). Персист-бага НЕТ (эмпирически: реальный `create_node`+`register` в venv → `total_entities:1`, файл пишется) — дыра в ПОКРЫТИИ проверки, а не в записи. Fix = reconcile-скан ФС↔реестр (найти on-disk узлы без записи и наоборот) в `check_integrity` | эмпирика: venv-прогон `create_node`+`register`=1 сущность; live `check_integrity`=0 при непустом `workspace/` | A3/reconcile (пара к F23), project-conventions |

## Прогон 5 — Обмер S15 (ступень A §6: состояние vs ожидания, широкий проход)

> Первый заход Блока 1. Цель — общая карта «что есть vs что ожидалось» + спорные места, чтобы
> размерить глубокие поды-проходы. Всё проверено ЧТЕНИЕМ кода на диске (не по памяти).

**✅ Лучше, чем подразумевала память (снять тревогу):**
- **G17 choke-point РЕАЛЬНО консолидирован:** `core/paths.safe_resolve` — единственная реализация
  containment, импортируется ВСЕМИ 6 модулями с путями (`server.py:47`, `state_manager:15`,
  `excel_core:28`, `template_engine:32`, `fs_searcher:100`, `query_planner:65`). `server.py:_safe_resolve`
  = тонкая back-compat обёртка-делегат. Безопасность-паттерн (DIM-16) по путям — не россыпь, а choke-point.
- **D29 закрыт в state_manager:** `safe_resolve(...)` на каждом entity-path (`state_manager.py:64/83/100/131/161/205`).
- **Wiring чист (`create_server` server.py:96):** D2 (firewall-config грузится), D4 (реакции→engine),
  D24 (state_manager→engine). Не «мёртвые объекты».
- **Хендлеры ТОНКИЕ:** каждый = `_safe(lambda: <engine>.method(...))` → логика в `core/*` движках
  (`table_engine`/`excel_engine`/`template_engine`/`link_registry`), НЕ в обёртке. `_safe` мапит
  исключения в коды реакций; типизированные `TableError/ExcelError/TemplateError/LinkError` несут `.code`.
- **Следствие для A2:** монолит `server.py` = все хендлеры в ОДНОЙ функции `register_basic_tools`
  (стр. 127→1248, ~1120 стр), но логики в них нет. **A2 = механическая экстракция в `tools/<group>/`,
  риск НИЗКИЙ** (движки трогать не надо; `engine.register(group=...)` уже группирует). Пересматривает F1: дефект = размещение, не «логика в обёртке».

**🟠 Подтверждено на диске (как и ожидалось):** F1/F24 (монолит-размещение), F3 (провайдеры-стабы),
F4 (search 0 тестов), F5 (реакции fallback частичен), F13/F14 (нет CI/auth), F25/F26 (integrity/cold-start),
F28–F30 (формулы-театр). Глубокая проверка каждого — в подах Блока 1.

**Новая находка:**
| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F37~~ | ✅ | **ЗАКРЫТ S16 (B4).** `core/paths.PathEscapeError(ValueError)` — `safe_resolve` кидает его; `_safe` мапит `PathEscapeError`→PATH_ESCAPE, а прочий `ValueError`→INTERNAL_ERROR (не вводит в заблуждение). Подтип ValueError → back-compat с `except ValueError` у fs_*/state_manager. Регрессия `test_audit_fixes` (typed+подтип+distinct); D1 traversal→PATH_ESCAPE держится | `core/paths.py`; `server.py:_safe`; `test_audit_fixes` PASS | B4 ✔ |

### Под-2 обмера — `core/search` (S15, прочитаны оба модуля целиком)

**❗ F4 НЕВЕРНА (правка):** `core/search` НЕ «0 тестов, не ревьюен». `tests/quick/test_search.py` содержателен:
happy-path (ext/name/content/limit/subdir), контракт `FileResult`, `_extract_id`, **adversarial D36
path-traversal** (`PATH_ESCAPE` на `/etc`, `../../../../etc`, `docs/../../..`), edge `PATH_NOT_FOUND`,
`load_query` YAML, `QueryPlanner` containment, `_detect_entity_type` по всей иерархии. В коде следы
**D36** (traversal-containment) и **D37** (`_detect_entity_type` FIXED) — находки уже находили и чинили.
→ **F4 понизить: search тестирован+ревьюен; остаётся relevance-eval (F31), не «сырьё».**

**Новые находки (обмер, НЕ чиним — фиксация):**
| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F38~~ | ✅ | **ЗАКРЫТ S16 (I4):** мёртвый `self._lock = threading.Lock()` + `import threading` удалены из обоих search-классов (0 `.acquire`/`with`). Найдено и снято при адаптации линта | `fs_searcher.py`/`query_planner.py` — lock+import убраны | I4 ✔ |
| ~~F39~~ | ✅ | **ЗАКРЫТ S16 (A5/DIM-13).** Добавлен публичный `TableEngine.load_snapshot(table)` (тонкая обёртка над `_load`); `QueryPlanner` использует его вместо приватного `_load`. Кросс-модульного доступа к приватному API больше нет (grep чист) | `tables_core.load_snapshot`; `query_planner.py`; search 24/24 | A5 ✔ |
| F40 | 🟢 | **ЧАСТИЧНО ЗАКРЫТ S16 (A6):** коды `QUERY_NOT_FOUND`/`PATH_NOT_FOUND` добавлены в `server_reactions.yaml` (class `ai_recoverable` + recovery) → search-хендлеры `_err(e.code,…)` теперь мапят их через реестр (регрессия `test_audit_fixes` 🟡→🟢). Остаётся (→ A5): дубль exception-классов `FsSearchError`/`SearchError` (одинаковая форма) — консолидировать при хардене search | реестр: `QUERY_NOT_FOUND`/`PATH_NOT_FOUND` есть; `test_audit_fixes` PASS | A5 (дубль-классы), ~~A6~~ |
| F41 | 🟡 (сузился) | **АКТУАЛИЗАЦИЯ S24: главного пруфа больше нет.** `ENTITY_PATH_PATTERNS` в `core/search/fs_searcher.py` НЕ существует — таксономия вынесена в `core/ids/taxonomy.py` (199 строк), который читает `id:`/`children:` из `config/templates/workspace/*.tpl.yaml` и честно отказывает `TEMPLATE_INVALID` без блока `id:`. Остаток хардкода сузился до двух мест: JSON-schema enum `tools/filesystem/__init__.py` и `FILE_TYPE_MAP`. Находку дозакрывать точечно, а не переоткрывать целиком. **Было:** **Захардкожена иерархия workspace + формат ID (DIM-14, дубль конфига).** `ENTITY_PATH_PATTERNS`, маркеры `_detect_entity_type` (`niches/networks/channels/videos/competitors`), `FILE_TYPE_MAP`, ID-regex `[A-Z]+_[0-9a-f]{32}` зашиты в код — та же структура живёт в `config/templates/workspace/*.tpl.yaml` + `IDGenerator`. Два источника правды → дрейф; **ломается при кастомной структуре канала (решение S14 — шаблоны настраиваемы)** | `fs_searcher.py:145/158/324`, дубль `templates/workspace` | A5, A7 (per-project override), anti-hardcode |
| F42 | 🟢 | **ЧАСТИЧНО ЗАКРЫТ S16 (A5):** TypeError-часть устранена — `_match_filter` gt/lt (try/except→не совпало) + `_apply_sort` (типобезопасный ключ: числа/строки в разных группах) больше не роняют поиск на разнотипном (str vs num). Регрессия `test_audit_fixes` 🟡→🟢 (2 проверки: фильтр+сортировка). Остаётся (→ A5 relevance): overclaim «умного» поиска (`analyze_dependencies` = group-by, не граф; фильтрация≠семантика, F31 relevance-eval нет) | `_match_filter`/`_apply_sort` type-safe; `test_audit_fixes` PASS | A5 relevance (F31), ~~TypeError~~ |

### Под-3 обмера — реакции/ошибки (S15, `reactions.py`+`server_reactions.yaml`+движки прочитаны)

**B2-корень ДОКАЗАН — две расходящиеся ветки эмиссии ошибок:**
- **Ветка хендлеров** (`server.py:_safe`→`_err`, ВСЕ fs/table/excel/structure): `_err(code, message, reason, suggested_tool)` строит `ErrorDetail` **напрямую из полей исключения** (движки хардкодят message: `TableError("INVALID_ACTION","В action отсутствует 'sheet'.",…)`), **реестр `server_reactions.yaml` НЕ трогает**. → теряются `reaction_class` (не ставится), `message_template`, `recovery.suggested_params` (реестр их определяет, клиент не получает).
- **Ветка `engine.execute`** (`engine.py:64`): единственная, кто **использует** реестр (`get_error`), но `raw_message` перекрывает `message_template` (F5).

**✅ Паритет кодов ЧИСТ (позитив):** все 9 кодов движков (COLUMN_EXISTS/…/VALIDATION_ERROR) есть в реестре (34 кода) — орфан-дрейфа нет. **Но** search-коды `QUERY_NOT_FOUND`/`PATH_NOT_FOUND` (F40) в реестре ОТСУТСТВУЮТ — подтверждено.

**Что есть vs что должно:** есть = пассивный маппер + доминирующая ветка мимо него; должно = **единое ядро** — любой путь через `get_error(code, params)`, движки эмитят ТОЛЬКО код (+динам. параметры), реестр даёт class/message/recovery; DEFAULT — тоже через реестр; `message_template` с подстановкой параметров.

**Новое:**
| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F43~~ | ✅ | **ЗАКРЫТ S16 (A6/R-CONTRACT1, B2-корень).** `_err` теперь роутит через `engine.reactions.get_error()` (yaml = единственный источник `class`/`recovery`; raw message сохраняет специфику; fallback лишь для кодов вне реестра — как `engine.execute`). Плюс 24 прямых `ErrorDetail(...)` в `fs_*`-хендлерах сведены к `_err(code, msg)` → **все пути ошибок через реестр**, дубль recovery-строк код↔yaml устранён (анти-хардкод). Регрессия `test_audit_fixes`: error несёт `reaction_class` И `recovery.reason` из реестра (🟡→🟢, 2 проверки). Baseline держится: 33/33 tables + 35/35 structure + 24/24 search | `server.py` `_err`+24 сайта; `test_audit_fixes` PASS | A6 ✔, reactions-errors |

**F5 уточнён:** к DEFAULT-хардкоду (UNKNOWN_ERROR + класс не ставится + message_template игнорится) добавить: **`raw_message` перекрывает `message_template` и для ИЗВЕСТНЫХ кодов** → template реестра почти не используется.

### Под-4 обмера — монолит `server.py` (карта распила A2, S15)

**Структура (доказано чтением):** `register_basic_tools` (127→1248, ~1120 стр) = **11 `engine.register`**, но 44+
инструмента → регистрация **циклами по спек-спискам**: `fs_tools`/`memory_tools`/… = `list[tuple(name,
title,desc,schema,handler,annot)]` + `for …: engine.register(…, group=…)`. Стили: **loop-driven** (filesystem/
memory/excel/tables/search) + **явные** (structure ×5, часть tables). Хендлеры тонкие (под-1).

**Есть vs должно:**
- **Есть:** декларативные метаданные инструментов УЖЕ живут как Python-спек-списки + inline JSON-схемы в `server.py`.
- **Должно (A2/A3):** `tools/<group>/` на группу (filesystem/tables/excel/search/structure/memory) с хендлерами +
  `config/ops/<group>.ops.yaml` (вынесенный спек-список); `server.py` = `create_server`/`run_server`/`main`.
  **Подтверждает OQ-B2:** A3 = ВЫНЕСТИ спек-списки в yaml (единый источник), НЕ слой поверх. **Риск НИЗКИЙ** (механика).

**Новое:**
| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F44~~ | ✅ | **ЗАКРЫТ S16 (I4):** 53 function-local `from core.contracts import …` сведены к одному module-top импорту (устранил заодно 52 ruff F821 на строковых аннотациях `-> "ToolResult"`). Единичные локальные `re`/`shutil`/`yaml`/`datetime` (по одному месту использования) оставлены — не smell, ruff чист | `server.py` module-top import; ruff `All checks passed` | I4 ✔ (остаток стиля → A2) |
| F45 | 🟡 | **Захардкожен Python-skeleton (DIM-14).** `fs_create_python_script` держит codegen-шаблон `.py` как inline f-string в хендлере (`server.py:440`) — шаблон должен быть файлом в `config/templates/`, не в коде | `server.py:440 skeleton = f'…'` | A2, anti-hardcode |
| F46 | 🟡 (сузился) | **АКТУАЛИЗАЦИЯ S24: из трёх копий осталась одна.** Декларация и `fs_searcher` больше не спорят — обе стороны берут таксономию из `core/ids/taxonomy.py`; третьей копией остался только JSON-schema enum в `tools/filesystem/__init__.py` (его и закрывать — генерацией enum из той же таксономии). **Было:** **Таксономия entity_type захардкожена ТРЕТИЙ раз (расширяет F41).** JSON-schema enum `["niche","network","channel","video","competitor_channel","competitor_video","asset","scene","render"]` в `server.py:897` = та же в `fs_searcher.ENTITY_PATH_PATTERNS` + `templates/workspace` → три источника правды типов сущностей. Дрейф; ломается при кастомной структуре (S14) | `server.py:897` ↔ `fs_searcher.py:145` ↔ templates | A2/A3, A7, anti-hardcode |
| ~~F47~~ | ✅ | **ЗАКРЫТ S20 (S8).** Полы подняты выше всех известных CVE: `aiohttp 3.13.5→3.14.3` (9 advisory, публичный транспорт), `litellm 1.83.7→1.96.2` (7; заодно снял точечные пины `click==8.1.8`/`python-dotenv==1.0.1`, мешавшие бампу), `python-dotenv→1.2.2`, `click→8.4.2`, `setuptools→84.0.0`, `torch→2.13.0` (он же требовал `setuptools<82`). `torchaudio` оставлен `>=2.11.0` — версии `2.13` на PyPI не существует, он отстаёт от torch на минор. `pip-audit`: 21 advisory → **0**. В CI переведён из `continue-on-error` в хард-гейт (без `--strict`: тот падает на неаудируемом `-e .` и красил бы гейт всегда — проверено по exit-коду). `requirements.lock` пересобран из чистого окружения. Живой пруф: сервер поднят на новом aiohttp, 401 без токена / 200 с токеном / 57 инструментов / `FILE_TYPE_FORBIDDEN` через реальный JSON-RPC. Было: **ПОДТВЕРЖДЁН и РАСШИРЕН S18 (перепрогон `pip-audit`):** список вырос — к `aiohttp 3.13.5` (fix 3.14.3), `litellm 1.83.7` (7 записей, fix 1.84.0), `python-dotenv 1.0.1` (fix 1.2.2) добавились **`click 8.1.8`** (fix 8.3.3), **`setuptools 81.0.0`** (fix 83.0.0), **`torch 2.12.1`** (PYSEC-2025-194, fix 2.13.0). В CI pip-audit **advisory** (не бумкает гейт) — бамп прод-деп требует регрессии на живом сервере (aiohttp=транспорт), поэтому отдельным воркстримом (I5/sec) | `pip-audit` на `requirements.lock`; `.github/workflows/ci.yml` | I5/sec, dep-hardening |
| ~~F48~~ | ✅ | **ЗАКРЫТ S17 (владелец заметил на GitHub): CI-гейт I3 был красным С РОЖДЕНИЯ — 7/7 прогонов `failure` за 12–15 с, роадмап рапортовал «I3 ✅».** (1) `pip install -e ".[dev]"` падал на ВСЕХ джобах: PEP 639 — `license = "LicenseRef-PolyForm…"` (SPDX, S14/F19) вместе с классификатором `License :: Other/Proprietary License`, setuptools≥77 → `InvalidConfigError`. Ruff/mypy/тесты/bandit **не запускались ни разу**. (2) `gitleaks-action@v2` fail-closed без `GITLEAKS_LICENSE` (проверка лицензии упёрлась в rate-limit). Фикс: классификатор убран; gitleaks = пиннутый бинарь v8.30.1 `git .`-скан (56 коммитов, 0 утечек). **Урок: «гейт добавлен» ≠ «гейт зелёный» — статус воркстрима подтверждать `gh run list`, не фактом коммита** | локальный репро `pip install -e . --dry-run` (setuptools 81 = раннер) → `Would install`; `gh run list` 7×failure | I3 ✔ |
| ~~F49~~ | 🔴 | **ЗАКРЫТ S17 (нашёл первый же живой прогон CI после F48): 6 шаблонов структуры никогда не были в git — репо не работал из клона.** `.gitignore:21` = `workspace/` **без ведущего слэша** ⇒ git игнорирует папку с таким именем на ЛЮБОЙ глубине, под раздачу попал `config/templates/workspace/` (`niche/network/channel/video/competitor_*.tpl.yaml` = ИСХОДНИКИ, не данные). На чистом клоне `structure_create` падает `TemplateError: Шаблон не найден: niche` для любого типа. Локально не ловилось никогда — файлы лежат на диске владельца; вдобавок они нигде не забэкаплены. Фикс: `/workspace/` (якорь корня) + шаблоны закоммичены; рантайм-`workspace/` по-прежнему игнорится (`git check-ignore` подтверждает). **Урок: «тесты зелёные локально» ≠ «зелёные из клона» — CI ловит ровно этот класс** | CI job `test (3.11)`: `TemplateError: Шаблон не найден: niche` на `tests/quick/test_structure.py:52`; `git check-ignore -v` | I3 ✔ / I2 |

### Под-5/6/7 обмера — формулы / провайдеры / контракты-state-transport-engine (S15, подтверждения)

**Под-5 формулы (`excel_core`) — F28/F29/F30 ПОДТВЕРЖДЕНЫ на диске:** `delete_column`/`move_column` = сырой
openpyxl (`delete_cols`/`insert_cols`) без проверки формульных ссылок (F28); `validate_formulas` = grep токенов
`#REF!/#VALUE!/…` без пересчёта (F29, театр); `core/engine/table_materializer.py` **НЕ существует** → loader
формул/устойчивости не построен (F30/F20). **Data-формульный слой незрел** (что должно: loader + пред-проверка ссылок + деградация неполных данных).

**Под-6 провайдеры — подтверждения + позитив:** F3 ✅ — все 4 провайдера (img/tts/stt/ffmpeg) = `NotImplementedError`
с внятным сообщением → **честные стабы (G16 соблюдён идеально)**, не тихий success. F10 ✅ — stt `device="cuda"`
захардкожен дефолтом (`stable_ts_adapter.py:20`). **F11 УТОЧНЁН/митигирован:** `raw_response` имеет активный
санитайзер `ErrorDetail._sanitize_raw_response` (маскирует секреты, D23) → утечка raw_response ЗАКРЫТА; остаётся api_key-гигиена (стабы, не горит).

**Под-7 контракты/state/transport/engine — ЗРЕЛЫЕ (позитив) + уточнения:**
- **Контракты зрелые:** `ToolResult.status`/`TaskStatus.status` = `Literal[…]` (эталон G15); `ErrorDetail` с санитайзером D23.
- **Транспорт D30 ЗАКРЫТ:** error-ветка (`transport.py:125–129`) кладёт `code`/`reaction_class`/`recovery` в `structuredContent`; facts → structuredContent (success). Транспорт **готов** донести данные реестра до клиента.
- **⚠️ Обостряет F43:** раз транспорт готов, единственная причина обеднённого `ErrorDetail` у клиента — **хендлеры (`_err`) не заполняют reaction_class/recovery из реестра**. Фикс F43 даёт полный payoff (данные дойдут).
- **Коррекция D24:** `log_event` **ВЫЗЫВАЕТСЯ** (`engine.py:130`) — «0 callers» устарело; но пишет сырой `fact.data` в `_SESSION_LOG` (OUT6/B1 exfil подтверждён).
- **Engine error-path** идёт через реестр (`engine.py:65 get_error`) — как и в под-3.

## Реестр спорных моментов (обмер S15 — решения владельца, «плохо продумано?»)

> Не дефекты-факты, а **дизайн-споры**: система работает, но продуманность под вопросом. **✅ Все решены владельцем S15** (проверено на диске).

- ~~**OQ-B1 (`_SESSION_LOG.md`):**~~ ✅ **РЕШЕНО.** Владелец: лог = **техническая инфа, не клиентские файлы** →
  корень репо **намеренно** (не нарушение «двух вселенных» — это server-технический артефакт, не данные
  клиента). **Дубля НЕТ** (пруф: на диске один `./_SESSION_LOG.md` 47KB, единственный write-путь
  `state_manager.py:184`; прежний «дубль» ИИ уже схлопнут). Остаётся ОДНА реальная проблема — **OUT6-exfil
  сырого `Fact.data`** в лог → это security (F33/I6), не размещение. Действие: санитизировать запись в лог,
  не переносить файл.
- ~~**OQ-B2 (дубль ошибок код↔yaml):**~~ ✅ **РЕШЕНО** (владелец переформулировал в суть). Проблема: ошибки
  **живут в питон-коде** (движки бросают `TableError(code=, message="...")` с зашитыми строками) **И** есть
  `server_reactions.yaml` — но **своего ЯДРА у ошибок нет** (`reactions.py` = частичный загрузчик, F5), отсюда
  **расхождение** двух источников. **Решение:** дать реакциям настоящее ядро — **yaml = единственный источник**
  сообщений/кодов, движки эмитят ТОЛЬКО код, ядро подставляет message/recovery из реестра. Дубли убрать.
  **Принцип (обобщение):** есть `.yaml` → у него обязано быть ядро-единственный-источник; параллельного
  хардкода в питоне быть не должно. Тот же принцип к A3 (если строим `ops.yaml` — он ЗАМЕНЯЕТ аргументы
  inline `engine.register`, не поверх). → A6 (реакции), reactions-errors.
- ~~**OQ-B3 (два движка):**~~ ✅ **РЕШЕНО — дубля НЕТ, но и не CQRS-над-одним-store.** Пруф: `table_engine`
  (`tables_core.py`) работает на **`read.json`** (снапшот) + write-очереди, **`.xlsx` не трогает**
  (синк отложен, `xlsx_synced=False`, G16); `excel_engine` = openpyxl на **`.xlsx`** (структура/формулы).
  Оба поиска (табличный `query_planner→table_engine._load`, файловый `fs_searcher`) читают **`read.json`**
  (догадка владельца про «второй движок для поиска» — почти: поиск читает read.json через table_engine, не
  excel_engine). Итог: **ДВА ХРАНИЛИЩА** — `read.json` (данные, SoT данных+поиска) vs `.xlsx` (структура+формулы),
  **мост синхры не построен** (G16). **Источник правды ячейки-ДАННЫХ = `read.json`.** ⚠️ **A7-следствие:**
  формулы уникальности живут в `.xlsx`, но данные — в `read.json` → локальный скрипт уникальности (S14)
  **должен читать `read.json`** и туда же писать результат; либо сперва закрыть G16-синк. → A1′/A7, A5, G16.
- ~~**OQ-B4 (`_safe` широкий catch):**~~ ✅ **РЕШЕНО (моё решение, владелец делегировал).** Ввести
  **типизированный `PathEscapeError(ValueError)`** в `core/paths.py`; `safe_resolve` бросает его; `_safe`
  ловит `PathEscapeError` → `PATH_ESCAPE`, а голый `ValueError` из core → общая реакция (`INTERNAL_ERROR`),
  не мислейбл. Back-compat цел (подкласс `ValueError`). Ложится на B2-принцип (ошибки из ядра). → A2/A6 (с §6-тестом), F37.

### Обмер ТЕСТ-СИСТЕМЫ (S18, ступень A по `08 §6`) — машинерия, а не сценарии

Зона обмера: раннер, фикстуры, харнесс, гейт, метрики (сценарное покрытие — `03_testing_plan.md`).
Программа развития и приёмка этапов — **[`14_testing_system_plan.md`](14_testing_system_plan.md)**.
Baseline на 2026-08-11 зелёный: **288 проверок в гейте** (inventory 60 · audit 44 · search 24 · structure 35 ·
tables 33 · materializer 30 · 6 симуляций 62) + **24 вне гейта** (tunnel 20 оффлайн, firewall 4 live).

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F50~~ | ✅ | **ЗАКРЫТ S18 (T0/B0).** Было: **`pytest` = ЛОЖНЫЙ ЗЕЛЁНЫЙ, гейт на ручном списке файлов.** `pytest>=8` объявлен в dev-deps, но раннера нет: наборы — самописные скрипты, гейт живёт в `main()` под `__main__`. Под pytest (а) `test_audit_fixes.py` (44 регрессии, дом всех `D#`/`F#`) **не собирается вовсе**; (б) сим-функции `test_*` проходят ВСЕГДА — `check(name, cond)` кладёт `bool` в список и **не ассертит**; (в) script-наборы (structure/tables/search/inventory/materializer) исполняются в момент КОЛЛЕКЦИИ, тест-функций не отдают. Следствие-2: CI перечисляет 12 файлов руками → новый набор не в гейте, пока человек не допишет строку; так уже выпал `test_tunnel` (20 проверок, **работает оффлайн**, исключён как «нужен туннель»). Класс F48 наоборот: не «красный, о котором не знали», а «зелёный, который ничего не проверил» **Фикс:** `[tool.pytest.ini_options]` (`python_files = ["test_suites.py"]`, `xfail_strict`, маркеры live/sim/slow/eval) + `tests/conftest.py` + `tests/test_suites.py` — гейт гоняет КАЖДЫЙ набор подпроцессом и проверяет exit-код, наборы обнаруживаются автоматически (`rglob`), плюс страховка «список не опустел». CI: 12 строк → `pytest -m "not live"`; `test_tunnel` (20 проверок) вернулся в гейт. Приёмка мутацией M15: обнулил детектор injection → `3 failed, 11 passed` (раньше был бы зелёный). Идиоматичная пофайловая миграция — по мере касания набора | было: `pytest tests/quick/test_audit_fixes.py -q` → `no tests ran`; `pytest tests/virus_injection tests/bot_army -q` → `7 passed in 0.01s` (27 проверок за 10 мс); `pytest tests/quick/test_structure.py --collect-only` → `no tests collected`; `python3 tests/quick/test_tunnel.py` → 20/20 оффлайн; стало: `pytest -m "not live"` → 14 passed, 1 deselected | I7 T0 ✔ |
| ~~F51~~ | 🔴→✅ | **ЗАКРЫТ S24. Построен `tests/harness`: `live_server.py` (свободный порт, свежая tmp-область через новую `MCP_WORKSPACE`, готовность ПО ОТВЕТУ на `tools/list`, гарантированный teardown), `jsonrpc.py`, `console.py` (чтение stdout потоком + `wait_for`). `test_firewall` переписан на харнесс (22 проверки против живого сервера) и ВОШЁЛ в гейт: `pytest` без `-m 'not live'` даёт 17/17, маркер `live` больше никого не отсекает. Побочно закрыта дыра, которой не было в формулировке: рабочая область сервера была прибита гвоздём, и любой тест против живого сервера правил бы боевые данные канала. Было:** `08 §6` требует для закрытия прохода реал-пруф: симуляции реальными инструментами против живого сервера **с доступом к stdout/логам**. Такой capability нет: нет фикстуры «поднять сервер на свободном порту + свежий tmp-`workspace/` + JSON-RPC клиент `/mcp`», нет захвата консоли. Следствия: единственный live-набор (`test_firewall`, 4 теста) гоняется руками и вне CI; E-матрица `03` (E-A…E-F) и раннер роя (F32) заблокированы — их нечем писать | `find tests -name conftest.py` ∅; `tests/harness/` ∅; `ci.yml` исключает firewall/tunnel; `patterns.yaml` 36 паттернов без раннера | **I7 T2** |
| ~~F52~~ | ✅ | **ЗАКРЫТ S18 (T1/B1).** Было: **покрытие никогда не измерялось.** `pytest-cov` не подключён, число не посчитано ни разу → «дыры покрытия» в `03` экспертные, а не измеренные; порог в CI (п.3 плана `03`) невыполним без T0. **Фикс:** `coverage[toml]` в dev-deps, `[tool.coverage.*]` в pyproject; `VPM_COVERAGE=1` заставляет `test_suites.py` гонять наборы под `coverage run --parallel-mode` — без этого цифра была бы **0%**, т.к. вся работа идёт в подпроцессах (обычный `--cov` мерит только родителя; pytest-cov subprocess-хук не сработал — его `.pth` под другой минор Python). **Факт: 61%** (2545 строк, 983 не покрыто). Порог в CI = **ratchet 60** (от факта, не «красивые 80»); проверено обе стороны: `--fail-under=60` → exit 0, `--fail-under=70` → exit 2. Отчёт `coverage.xml` — артефакт CI | было: `grep cov` в `pyproject.toml`/`ci.yml` ∅; стало: `coverage report` = 61% | I7 T1 ✔ |
| ~~F53~~ | ✅ | **ЗАКРЫТ S18 (T3/B0).** Было: **тест-доки описывают несуществующее и противоречат директиве владельца.** (1) `tests/README.md`: в дереве `filesystem/`, `pipeline/`, `tables/` — таких каталогов нет; команда запуска `python3 tests/quick/test_import.py` — файла нет; требует «5 вопросов → в docstring» и формат `history.md` (отменено 2026-07-05: docstring в одну строку, рацио в commit, история git-native). (2) `tests/config/scenarios.yaml` предписывает регистрировать сценарии в `tests/runner.py` / `tests/scenarios/` + `SCENARIOS_MAP` — ничего из этого не существует (мёртвый конфиг, класс D2). (3) `CATALOG §E` рапортует `test_audit_fixes` 41/41 при факте 44/44. (4) память `project-rules §3.3` — та же отменённая «5 вопросов в docstring» **Фикс:** `tests/README.md` переписан по факту (дерево, запуск через pytest, docstring в одну строку, git-native история, мутационный шаг в порядке работы); пустые каркасы `tests/{filesystem,pipeline,tables}/` удалены; `tests/config/scenarios.yaml` удалён (его содержимое — зоны наборов — живёт в `CATALOG §B`); `03` больше не ссылается на него; `CATALOG §E` счётчик 49/49; память `project-rules §3.3/3.4` обновлена | было: `ls tests/` vs дерево в README, `find tests -name runner.py` ∅; стало: ни одной ссылки на несуществующее | I7 T3 ✔ |

## Прогон 6 — ВЕРИФИКАЦИЯ всего реестра (S18, 2026-08-11)

> Задача владельца: проверить статусы ВСЕХ зафиксированных проблем, прогнать их через тесты, подтвердить
> или опровергнуть — **и заодно проверить сами тесты на корректность**. Метод в три слоя:
> **(1) статика** — чтение/grep/ls/перепрогон утилит по каждому F#; **(2) поведение** — 12 in-process наборов
> (288 проверок, все зелёные); **(3) мутационный** — откатываю фикс в коде/конфиге и требую, чтобы
> тест-хозяин **покраснел**. Зелёный тест на сломанном коде = тест-театр, а не подтверждение.
> Полная мутационная таблица — `tests/CATALOG.md §F`. Дерево после всех мутаций восстановлено (`git status` чист).

### 6.1 Закрытые находки — подтверждены МУТАЦИЕЙ (тест реально ловит откат)

| F# / защита | Мутация (что сломал) | Тест-хозяин | Итог |
|---|---|---|---|
| F43 реестр реакций | `ToolContext.err` → ветка мимо `get_error` | `test_audit_fixes` | 🔴 краснеет ✅ |
| F5 DEFAULT-fallback | `message_template` из реестра → хардкод | `test_audit_fixes` | 🔴 краснеет ✅ |
| F40 search-коды ⊂ реестр | `QUERY_NOT_FOUND` переименован в yaml | `test_audit_fixes` | 🔴 краснеет ✅ |
| F42 типобезопасный фильтр | снят `try/except TypeError` в `_match_filter` | `test_audit_fixes` | 🔴 краснеет ✅ |
| F29 пересчёт формул (LO) | `_recalc_via_lo` → чтение без пересчёта | `test_audit_fixes` | 🔴 краснеет ✅ |
| F11/D23 санитайзер секретов | `_sanitize_raw_response` → возврат as-is | `test_audit_fixes` | 🔴 краснеет ✅ |
| D1/G17/F37 containment путей | `safe_resolve` → проверка выключена | `test_audit_fixes`, `test_search` | 🔴 краснеют ✅ |
| IN2 prompt-injection | `InjectionDetector.patterns` → `[]` | `virus_injection`, `cache_injection`, `test_audit_fixes` | 🔴 краснеют ✅ |
| IN3 rate-limit (механизм) | `RateLimiter.check` → всегда allow | `bot_army` (15/22), `test_audit_fixes` | 🔴 краснеют ✅ |
| контракт `tools/list` | тихо изменён `description` инструмента | `test_tools_inventory` | 🔴 краснеет ✅ |
| Ф1 валидатор имени узла | `_valid_name` → `True` | `test_structure` | 🔴 краснеет ✅ |

**Вывод:** ядро регрессий — НЕ театр. 11 из 14 мутаций пойманы штатными наборами; 3 слепые зоны → **F55**.

### 6.2 Статусы, исправленные верификацией (реестр расходился с диском)

| F# | Было | Стало (S18) |
|---|---|---|
| F13 | 🔴 «нет CI/CD» | ✅ CI есть с S16, починен S17, прогоны зелёные — статус протух |
| F14 | 🔴 «нет app-level auth» | 🔴 переформулировано: bearer-auth **есть с первого коммита**, но **fail-open по умолчанию** + статический токен ≠ OAuth 2.1 + **нет теста** |
| F15 | 🟠 «D3+D29 открыты» | 🟡 D29 закрыт (мутация M7 доказывает), D3 → F14 |
| F6 | 🟡 «мёртвый inject-param» | ⚪ **опровергнута** — `self.ids.generate_simple` дёргается в 3 местах |
| F9 | 🟠 «нет `docs/dev/audit/`» | ⚪ неактуальна — `docs/dev/` удалён намеренно, словарь = этот файл |
| F8 / F24 | 🟠 | 🟡 частично закрыты A2; остаток — пустые `tools/{media,video,excel_engine}`, `pipeline/*`, `scripts/`, доки-зеркало |
| F17 | 🟠 | 🟡 утечек в трекнутых файлах нет (совпадения = имена переменных), gitleaks чист; остаётся `.env`-стратегия |
| F47 | 🟠 (3 пакета) | 🟠 расширен: + `click`, `setuptools`, `torch` |

### 6.3 Открытые находки — ПОДТВЕРЖДЕНЫ на диске 2026-08-11 (пруф пересобран)

> ⚠️ **Список датирован 11.08 — актуализация 2026-08-12 (S20):** `F33` (outbound) и `F34` (write-allowlist) с тех пор **ЗАКРЫТЫ** в S19/S20 — см. их строки в главном реестре выше; `F35` держится, но регрессии по-прежнему нет. Остальные строки перепроверены и верны.

| F# | Пруф верификации |
|---|---|
| F3 провайдеры-стабы | `grep NotImplementedError core/providers/` = 12 |
| F10 cuda-хардкод | `stable_ts_adapter.py:20 device: str = "cuda"`; busy-loop `ffmpeg_adapter.py:148/180` |
| F20/F21 табличный слой | *(снимок S18; перепроверено S23)* было: `table_materializer` есть, схем 1 из 7, `scripts/` пуст. Стало: **67 листов** сгенерированы машинно, `scripts/` = 4 утилиты (`spec_to_schema`, `config_to_schema`, `models`, `set_provider_key`); остаток F21 = интроспекция готовых `.xlsx` |
| F25 integrity-слепое пятно | `link_registry.check_integrity` работает только по `entities` реестра, ФС не сканирует. **Перепроверено 2026-08-12:** цикл идёт по `entities` (`link_registry.py:255`); прогон — 7 незарегистрированных каталогов на диске → `issues_count = 0`. Обратное направление (реестр→диск, `missing_path`) закрыто в S19, прямое (диск→реестр) — нет |
| F26 cold-start advisory | `grep -rni "cold.start\|advisory" core/ tools/ config/` → ∅ |
| F27 ручной reconcile | `structure_migrate(entity_id, new_path)` — одна сущность, путь от ИИ |
| F28 формулы при delete/move | `excel_core.py:249 ws.delete_cols(...)` — без анализа ссылок |
| F30 деградация формул | `grep scene_profile\|niche_weight\|PENDING core/tables core/engine` → ∅ |
| F31/F32 eval + рой | `tests/eval/` нет; `tests/agent_swarm/test_*.py` нет (только `patterns.yaml`) |
| F33 outbound | `grep provenance` ∅; `_SESSION_LOG` пишет сырой `data` |
| F34 write-allowlist | `grep FILE_TYPE_FORBIDDEN\|allowlist core/ tools/ config/` → ∅ |
| F35 no-root baseline | exec-sinks (`os.system`/`eval`/`exec`/`shell=True`/`pickle`) → **∅**, `id -u`=1000 — держится; bandit в CI есть, **теста-не-root нет** |
| F36 identity-rate/slowloris | `grep identity\|slowloris\|max_payload` в firewall/server → ∅ |
| F41/F45/F46 хардкод | `ENTITY_PATH_PATTERNS` жив; inline python-skeleton переехал в `tools/filesystem/__init__.py:211`; таксономия `competitor_video` теперь в **4** файлах кода (было 3) — A2 её размножил |
| F50–F53 тест-система | подтверждены (см. предыдущий раздел) |

### 6.4 Новые находки верификации

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F54~~ | ✅ | **ЗАКРЫТ S18 (S0/B0).** Было: **мёртвые выключатели в боевом `config/firewall.yaml` — конфиг обещает то, чего код не читает.** Три секции объявляют `enabled: true` (`ip_blocklist`, `injection_detection`, `anomaly_detection`), но `Firewall._make_rules` **не читает ключ `enabled` вообще** — берёт только `patterns`/`auto_ban`/`ban_duration_hours`/`dangerous_tools`/`max_requests_per_minute`/`ban_after_violations`. Поставить `injection_detection.enabled: false` → **ничего не меняется**, детектор работает. Фактический выключатель — недокументированный `patterns: []` (в комментарии кода, не в конфиге). Класс D2/anti-hardcode: декларация ≠ поведение. Направление безопасное (правило не выключается тайком), но конфиг лжёт владельцу, и любой «выключил, проверил, не сработало» — потерянные часы **Фикс:** `_make_rules` собирает флаги `enabled` для всех 4 секций (отсутствие ключа = включено, back-compat), `check()` их уважает; `rate_limit.enabled` добавлен для симметрии; в `firewall.yaml` — комментарий, что выключатель реальный. Регрессия: 5 проверок в `test_audit_fixes` (49/49), мутация M14 (откат) → красный | было: `grep enabled core/firewall/firewall.py` → ∅, мутация M13 никого не разбудила | S0 ✔ (anti-hardcode) |
| ~~F55~~ | 🟠→✅ | **ЗАКРЫТ S24 (волна 2.2/T10) целиком.** Подпункт (1): `bot_army` получил проверку боевой конфигурации — пороги читаются ИЗ `config/firewall.yaml`, ожидаемое считается из них же (осознанная правка порога тест не красит, разрыв проводки — красит; мутация M99 `max_requests=10⁹` → красный). Подпункт (3): `test_search` получил фильтры (`eq`/`gt`/`contains`/`in`) и сортировку в обе стороны в СВОЕЙ зоне (мутации M100/M101 → красный). Подпункты (2) и (4) закрыты ДРУГИМ хозяином — переписанный на харнесс `tests/quick/test_firewall.py` бьёт containment через живой `fs_read_file` (три формы побега → `PATH_ESCAPE`, то есть choke-point `safe_resolve`, а не имя-сегмент) и покрывает auth (без ключа 401, чужой ключ 401, ключ не течёт в консоль). Живыми остаются (1) `bot_army` не читает боевые лимиты и (3) `test_search` без фильтров/сортировки. **Было:** **Слепые зоны тестов: реестр `CATALOG` шире фактического покрытия хозяев** (вскрыто мутациями). (1) **`bot_army` слеп к боевой конфигурации rate-limit** — строит свой `Firewall({...})` с тестовыми параметрами; подмена дефолтов (`max_requests=10⁹`, `ban_after=10⁹`) → **22/22 зелёные**; поймал только `test_audit_fixes` (D2-проверка загрузки конфига). Механизм покрыт (M9b красный), **проводка боевых лимитов — нет**. (2) **`test_structure` слеп к снятию containment** `safe_resolve` (35/35 зелёные при выключенной проверке): его «PATH_ESCAPE» §5 бьёт по `_valid_name` (имя-сегмент), а не по choke-point G17 — а `CATALOG` записывает PATH_ESCAPE в его зону. (3) **`test_search` слеп к F42** — в наборе нет ни одного теста на фильтры/сортировку, регрессия живёт только в `test_audit_fixes`, хотя зона search — его. (4) **Auth не покрыт вообще** — `grep MCP_AUTH_TOKEN tests/` ∅ (см. F14) | мутации M4/M7/M9/M12/M13 (`tests/CATALOG.md §F`) | I7 (T0/T2 + расширение хозяев), CATALOG (уточнить зоны) |

### Замечание владельца S18 — редактирование строк таблиц (проверено в коде)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| ~~F56~~ | ✅ | **ЗАКРЫТ S22 — см. строку `F56` выше; здесь исходная формулировка.** **Правка строки ЕСТЬ, но асимметрична созданию и адресуется вслепую.** Проверено: `table_set(table, sheet, row_id, column, value)` — полноценный RMW по существующей строке (валидация листа/строки/столбца + computed-readonly + enum **до** очереди, применение в `execute_queue`), плюс `table_append`/`table_delete`/`table_get_row`/`table_get_column`. Ручное управление таблицами для ИИ **не потеряно**. Реальные дыры три: **(1) асимметрия** — создание принимает СЛОВАРЬ (`append(data={...})`), правка только ОДНУ ячейку: обновить 5 полей = 5 вызовов и 5 независимых операций в очереди, атомарности нет (сбой на 3-й → строка полуобновлена); **(2) адресация** — `row_id` добывается лишь из факта `table_append`, ручным сканом `table_get_column` по столбцу ID, либо `search_tables` (YAML-поиск, не «дай ID строки где column=value»); примитива «найти строку по значению → row_id» нет, хотя весь контракт строится на `row_id`; **(3) очередь знает только `set\|append\|delete`** (`push_to_queue` нормализует ровно эти три) → мульти-полевого `update` нет и в декларативном пути, т.е. дыра одинаковая и напрямую, и через конфиг. **Дыра 5 (адресация вслепую, директива S18-b — `13` §S18-b):** `row_id` строится как `ROW_<hex>` с префиксом-параметром **от ИИ** — в нём нет ни книги, ни листа, ни сущности-владельца; ID из чужого листа даёт глухой `ROW_NOT_FOUND`. Нужен квалифицированный ID `<OWNER>.<SHEET>.<SEQ>`, составляемый сервером по схеме листа (форма — на каждый шаблон отдельно) + код реакции `ROW_SHEET_MISMATCH`. **Дыра 4 (проекция, директива владельца S18 — `13` §S18):** `get_row` отдаёт строку ЦЕЛИКОМ (`tables_core.py:122`), выбрать нужные столбцы нельзя → лишние десятки столбцов едут в контекст ИИ, а форму данных приходится держать в промпте вместо схемы. **Фикс:** `table_get_row(..., columns=[…])` — проекция по схеме; `table_update(table, sheet, row_id, data: dict)` — прогон `_validate_write` по каждому полю + **ОДНА** операция `{"action":"update",…}` в очереди (атомарно) + `table_find_row(table, sheet, match: dict) → [row_id]`; оба — тонкие обёртки над `tables_core`, коды реакций в реестр, эталон инвентаря обновить `--bless` | `tools/tables/__init__.py:46` (`table_set` = row_id+column+value) · `tables_core.py:245/264/278` (set/append/delete) · `push_to_queue` принимает только 3 действия · `grep` «найти строку по значению» ∅ | **B4** (A1′/табличный слой), `mcp-developer` + `test-master` (E-D/E-E) |

### Замечание владельца S18-c — «инструмента для поиска по ID тут нет» (проверено прогоном)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| F57 | 🟠 | **Поиск по ID не работает нигде — и молчит нулевым результатом.** `QueryPlanner` подмешивает `_row_id` в **выход** (`query_planner.py:246/249`), но фильтр применяется **раньше** — `_match_filter(row, task.filter)` (`:241`) читает `row.get(key)` из сырой строки снапшота, где ключа `_row_id` ещё нет. Следствие: `filter: {_row_id: {eq: …}}` и `search_quick(filter_col="_row_id")` дают **0 строк для существующей строки** — не ошибка, а «нет данных» (тот же класс маскировки, что дыра-5 F56: `ROW_NOT_FOUND` вместо `ROW_SHEET_MISMATCH`). Второй слой: единственный ID-адресуемый инструмент `table_get_row(table, sheet, row_id)` требует знать лист **заранее** — вопрос «в каком листе живёт этот ID» задать нечем, ни один из 53 инструментов на него не отвечает. Сходится с F55-(3): в `test_search` нет ни одного теста на фильтры, поэтому дыра и не краснела. **Фикс:** инъекцию `_row_id` перенести **до** `_match_filter` (фильтр по служебному ключу становится валидным) + регрессия в `test_search`; после S18-b квалифицированного ID лист резолвится **из самого ID** → `table_find_by_id(row_id)` не требует ни листа, ни скана | `.venv/bin/python` на `QueryPlanner` с фейковым движком: `filter {_row_id: {eq: ROW_abc}}` → **0 строк**, контрольный `filter {video: {eq: V1}}` → 1 строка | **B4** (вместе с S18/S18-b), `test-master` (E-D) |
| — | ✅ | **Косметика того же наблюдения — СДЕЛАНО.** Титулы `table_get_column`/`table_get_row` несли форму ответа в имени (`Таблицы: столбец {id:value}`, `Таблицы: строка {col:value}`) — единственные 2 из 53, и та же информация уже была в `description`; после S18-проекции (`columns=[…]`) стали бы ещё и ложью. Титулы → `Таблицы: столбец` / `Таблицы: строка`, эталон инвентаря `--bless` (diff — ровно 2 строки) | `pytest -m "not live"` → 14 passed; до `--bless`: `title разошлись: ['table_get_column','table_get_row']` | — |

### Замечание владельца S18-d — живой прогон файлового поиска в Claude AI Web (воспроизведено локально)

| F# | Sev | Находка | Пруф | → |
|---|---|---|---|---|
| F58 | 🟠 | **Три обёртки — три произвольные проекции ОДНОЙ модели запроса; ядро ни при чём.** `FsSearchTask` (`fs_searcher.py:113`) — уже единая модель на 11 полей, и все фильтры в `search()` **реализованы и работают** (проверено: `size_min=950` на фикстуре 10×900 Б + 40×1000 Б → ровно 40). Расходятся именно обёртки: **(а) `fs_smart_search`** переименовывает 4 поля из 7 (`root`→`directory`, `extensions`→`extension` **единственного числа**, `content_keywords`→`keyword`, `entity_types`→`entity_type`) и не даёт size/modified вовсе; **(б) `fs_search_multi`** читает 7 полей, а **объявляет в схеме 4** — `id_pattern`/`name_pattern`/`limit` работают, но невидимы для модели; его выход беднее всех (`path/name/entity_type`, теряются `size`, `modified`, `entity_id`, `parent_path`); **(в) `fs_search_yaml`** — единственный вход, дотягивающийся до size/modified, но его грамматика описана ТОЛЬКО в docstring питон-модуля (`fs_searcher.py:8-55`), которого модель не видит: `input_schema` = `{yaml_query: string}`. Форма — **вложенная** (`size: {min, max}`, `modified: {after, before}`), а `load_query` собирает поля через `data.get(...)` без валидации → плоский `size_min: 950` молча проглатывается и возвращаются все 50 файлов. Итог для вызывающей модели: выученный паттерн параметров одного инструмента ломается на другом, а промах даёт не ошибку, а тихий неверный результат. **Отдельный footgun:** `item.suffix` содержит точку → `extension="json"` даёт `count: 0` без ошибки, `".json"` — 5 файлов. **Описания-шаблоны:** `fs_search_yaml` обещает «очередь, многопоточность» — он зовёт `search()` (одна задача, ноль потоков); пул есть только в `search_parallel` у `fs_search_multi`. Та же фраза стоит у `search_tables`, где она **правдива** (`query_planner.py:182` — `ThreadPoolExecutor`) → фраза скопирована из табличного домена в файловый без проверки. **Распил A2 НИ ПРИ ЧЁМ:** `git show a98cf3b:server.py:356` — досплитовая сигнатура `fs_smart_search` побайтово та же; `git log -S"size_min"` = один коммит `a98cf3b` (внешняя search-подсистема, 0 тестов, без ревью — память `core-search-subsystem`). Дыра **врождённая**, не регрессия. **Фикс — директива владельца S18-d (`13` §S18-d): в файловом домене остаются ДВА инструмента** — обычный (по ID и имени) и продвинутый, который **требует фильтры** (мотив: ID в разных проектах могут совпасть → поиск по голому ID — ловушка, продвинутый вход обязан сузить область). Оба — над одной Query-моделью `FsSearchTask` с её же (множественными) именами полей, выход выравнивается; продвинутый получает явную схему вместо `yaml_query: string` + `additionalProperties: false` (неизвестный ключ = `VALIDATION_ERROR`, не тишина) и вбирает пакетность `fs_search_multi` (`search_parallel` наконец оказывается у того инструмента, чьё описание обещает многопоточность). Плюс нормализация расширения (`json`→`.json`) и описания по факту | фикстура + прогон `FsSearcher`: ядро `size_min=950` → **40**; YAML плоский `size_min` → `task.size_min=0`, **50**; YAML вложенный `size:{min:950}` → **40**; мусорный ключ → без ошибки, 50; `extensions=['json']` → **0**, `['.json']` → **5**; `engine.call("fs_search_multi", …id_pattern…)` 61→0 (работает, но не в схеме) | **B4** (вместе с табличным пакетом S18/S18-b), `mcp-developer` + `anti-hardcode` (имена полей) + `test-master` (F55-3: в `test_search` нет тестов на фильтры — потому и не краснело) |
| F58-t | 🟠 | **Табличная половина той же болезни + домен не виден в подписи (замечание владельца S18-e).** Три табличных инструмента поиска — три пресета ОДНОЙ `QueryPlan`: `search_quick` строит запрос из одного чтения (`tools/search/__init__.py:57`), `search_multi` — из N чтений с `join` (`:93`), `search_tables` берёт запрос как есть (`:103`), дальше у всех один `load_query_from_dict` → `execute_plan`. `_parse_plan` (`query_planner.py:128`) собирает поля через `data.get(...)` → неизвестный ключ проглатывается молча, как и в файловом домене; `query_dict: {type: object}` объявлен без внутренней схемы. **Подписи не называли домен** (`Поиск: быстрый`/`Поиск: YAML-запрос`) → в списке коннектора табличный поиск неотличим от файлового. **Умный поиск по таблицам НЕ терялся и распил ни при чём** (третье подтверждение): `git show a98cf3b:server.py:1212-1229` — титулы побайтово те же, что после `1accee1`; родились домен-безымянными во внешней search-подсистеме. **Сделано в S19:** титулы → `Таблицы: быстрый поиск` / `Таблицы: умный поиск (YAML)` / `Таблицы: поиск с объединением (JOIN)`, эталон `--bless`. **Остаётся на B4 (§S18-e):** пара «обычный + продвинутый» и в табличном домене; `search_multi` поглощается продвинутым (JOIN живёт в `QueryPlan.join`/`_merge_results`, не в инструменте — потерь нет); YAML сохраняется ради единого стиля, но разобранный объект валидируется против объявленной грамматики (`additionalProperties: false`); ручные примитивы адресации (`table_get_row`/`table_get_column`/`table_set`) остаются и в счёт «двух» не идут | `tools/search/__init__.py:57/93/103` — три сборки одного `QueryPlan`; `git show a98cf3b:server.py \| sed -n '1212p;1218p;1229p'` = текущие титулы до распила; `pytest -m "not live"` → 14 passed после `--bless` | **B4** (`13` §S18-e) |
| ~~F59~~ | ✅ | **ЗАКРЫТ S22 — см. строку `F59` выше; здесь исходная формулировка.** **Сервер не объясняет ИИ разницу между поисками: активный канал рекомендаций пуст (директива владельца S18-f).** У сервера два канала сказать «какой инструмент когда брать»: пассивный — `description` в `tools/list`, активный — `recovery.{suggested_tool, suggested_params, reason}` из `server_reactions.yaml`, доходящий до ИИ **в момент промаха**. Замер реестра: **38 кодов, `suggested_tool` у 12, `suggested_params` у 0, и НИ ОДИН код не ведёт ни к одному из шести поисковых инструментов**. `ROW_NOT_FOUND` в момент потери строки советует ручной перебор («`table_get_column` отдаст список ID») — прозой в `reason`, без `suggested_tool`; `COLUMN_NOT_FOUND` — так же. Пассивный канал не лучше: описания говорят про механизм («очередь, многопоточность»), из которого выбор не выводится, а у `fs_search_yaml` это ещё и неправда (F58). Итог: ИИ выбирает поиск угадыванием, а после промаха не получает подсказки, куда идти. **Побочно:** `Recovery.suggested_params` объявлено в контракте и не заполнено ни разу (0/38) — D2 «декларация без применения». **Фикс (`13` §S18-f):** описания по формату «что ищет · чем отличается · когда брать · когда НЕ брать (с именем альтернативы)», слова «очередь/многопоточность» из описаний вон; каждый код семейства NOT_FOUND получает `suggested_tool` на поиск + `suggested_params` (таблица маршрутов в §S18-f); примитивы `table_get_row`/`table_get_column` в описаниях объявляются адресацией, а не поиском | `yaml.safe_load(server_reactions.yaml)`: 38 кодов, 12 с `suggested_tool`, **0** с `suggested_params`, пересечение с 6 поисковыми инструментами — **∅**; `config/server_reactions.yaml:148` (`ROW_NOT_FOUND` = ручной скан прозой) | **B4** (`13` §S18-f), `reactions-errors` + `mcp-developer` |
| ~~F60~~ | ✅ | **ЗАКРЫТ S19.** `_extract_id` (разбор имени файла) удалён; личность берётся из `LinkRegistry` по вместимости — у каждого результата есть `entity_id` (если файлу присвоен свой ID), `owner_id` и `chain`. Добавлены фильтры `owner_id` (всё, что принадлежит сущности) и `chain_prefix` (поддерево). Пруф: поиск по реальному `VID`-ID находит файлы видео (было 0), `chain_prefix` сетки отбирает 7 из 11 файлов ниши (`test_search` §F60, мутации M27/M28). Было: **поиск по ID в файловой вселенной мёртв: `entity_id` всегда пустой, фильтр `id_pattern` даёт 0 на реально существующем ID.** `FsSearcher._extract_id` (`fs_searcher.py:320`) ищет ID **в имени файла** регуляркой `([A-Z]+_[0-9a-f]{32})`, но ID присваивает `TemplateEngine` и кладёт их в **facts + `LinkRegistry`**, а имена файлов задают шаблоны (`channel_data.xlsx`, `read.json`, `project_memory.md`) и имена от ИИ — ID в имя не попадает **никогда**. Итог: у всех результатов `entity_id=''`, а `id_pattern` (заявленный в схеме `fs_smart_search` как «Regex паттерн ID (напр. `VID_*`)») отбраковывает **все** файлы, включая файлы того самого видео. Это ровно F57 в файловой половине: там фильтр по `_row_id` отсекался до подмешивания, здесь источник ID вообще не тот. **Побочно:** пример в схеме `VID_*` — glob, а потребляется `re.search` (regex `VID_*` = «VID» + сколько угодно `_`), то есть подсказка учит ИИ неверному синтаксису. **Фикс (`13` §S18-g):** источник `entity_id` — не имя файла, а `LinkRegistry` по **вместимости** (ближайший зарегистрированный предок пути); тогда и рождённые вручную файлы получают владельца | `TemplateEngine.create_node("niche",…)` создал 4 узла с ID (`NICHE_…/NET_…/CH_…/VID_…`), `FsSearcher.search()` по тому же workspace: 8 файлов, `entity_id=''` у **всех**; поиск с `id_pattern=<реальный VID-ID>` → **0 результатов** | **B4** (`13` §S18-g), `mcp-developer` |
| ~~F61~~ | ✅ | **ЗАКРЫТ S19 (b4389bd).** `fs_create_file`/`fs_write_file`/`fs_create_python_script` возвращают `owner_id`/`chain`/`owner_type`/`owner_path` — владелец определяется ПО ВМЕСТИМОСТИ; описания создающих инструментов объявляют это до вызова; `structure_create` объявляет присвоение ID и отдаёт `entities[]`. Пруф: файл, созданный вручную внутри видео, получает владельцем это видео (`test_structure` §21, мутация «создание без владельца» → красный). Было: **две вселенные файлов; рождённые вручную не существовали для сервера.** `structure_create` присваивает `node_id`, регистрирует узел в `LinkRegistry` и возвращает факт `NodeCreated{id,type,name,path,parent_ids}` (`tools/structure/__init__.py:38-44`). Ручная ветка — `fs_create_file`, `fs_write_file`, `fs_create_python_script`, `fs_create_project_structure` — **не присваивает ID и не пишет в реестр вовсе**: возвращает `{"created": path, "size": n}`. Ни одно описание инструмента создания (ни ручного, ни шаблонного) не сообщает ИИ, что **ID присвоит сервер и вернёт его в ответе** — `fs_create_file` описан как «Создание файла». Следствие: ИИ не знает, что адресация по ID вообще существует, а половина файлов workspace недоступна ни поиску по ID (F60), ни `structure_status`, ни целостности реестра. **Фикс (`13` §S18-g):** единый пост-контракт создания — любой создающий инструмент возвращает блок `{id, name, path, parent_id, chain}` на каждый созданный объект и **объявляет это в описании**, независимо от ветки (ручная/шаблонная) и независимо от того, созданы ли уже книги | `grep` по `tools/`: `link_registry`/`generate_simple` встречаются только в `tools/structure` и `core/tables`; `tools/filesystem/__init__.py:57-76` — возврат без `id`; описания `fs_create_file`/`fs_write_file` — «Создание файла»/«Полная перезапись файла» | **B4** (`13` §S18-g), `mcp-developer` + `project-conventions` |
| ~~F62~~ | ✅ | **ЗАКРЫТ S19.** Сделано: блок `id:` + `root_container` в `*.tpl.yaml` — единственный источник префиксов/иерархии (хардкоды `TYPE_PREFIX`/`ROOT_CONTAINER` удалены), цепочка вычисляема (`chain_id`/`qualified_id`), **подстановка `{parent:channel}` реализована** (конкурент ложится в `competitors/<наш_канал>/<конкурент>`; канала нет или их несколько → сегмент опущен и помечен `ungrouped_by`, сервер не гадает), **`IDGenerator.PREFIXES` удалён** — таблица типов жила в двух местах, осталась таксономия. **Закрыт полностью S19:** `ENTITY_PATH_PATTERNS` удалён — тип сущности выводится из объявленной раскладки (`type_for_root_container` + `child_matches` с перебором вариантов токена `{parent:channel}`, потому что сегмент нашего канала бывает и опущен). Было: **иерархия в путях и `parent_ids`, но не в ID.** (1) ID плоский: `VID_<uuid4hex>` — по нему не видно ни сетки, ни канала, ни ниши; цепочка восстанавливается только обходом `parent_ids` в реестре, то есть недоступна ни как фильтр поиска, ни как префикс. (2) `network.tpl.yaml` прямо декларирует «реальный путь `competitors/{наш_канал}/{конкурент}/` — сегмент `{наш_канал}` движок подставляет из `parent_ids`», но `TemplateEngine.create_node` берёт `container` буквально (`template_engine.py:193`) — подстановки нет; конкурент ложится в `networks/net1/competitors/compX`, привязка к нашему каналу живёт **только** как ORPHAN-проверка `REQUIRED_PARENT_TYPE`. (3) Иерархия задана в **четырёх** местах: `children`-ссылки шаблонов, `REQUIRED_PARENT_TYPE` (`link_registry.py:34`), `ENTITY_PATH_PATTERNS` (`fs_searcher.py:143`, описывает как раз нереализованную раскладку `channels/*/competitors/*`) и префиксы в двух копиях (`IDGenerator.PREFIXES` 14 шт. / `TYPE_PREFIX` 6 шт. — F41/F46). **Фикс (`13` §S18-g):** ID = цепочка предков с пропуском отсутствующих уровней, собирается сервером из реестра; блок `id:` в каждом `*.tpl.yaml` (форма проектируется под шаблон) — он же единственный источник префиксов и допустимых родителей | прогон `create_node("niche", children={network,channel,video,competitor_channel,competitor_video})`: `niches/gaming/networks/net1/competitors/compX` (без сегмента канала), ID узлов — `NICHE_…`, `NET_…`, `CH_…`, `VID_…`, `COMP_…`, `CVID_…` без единого общего сегмента | **B4** (`13` §S18-g), `anti-hardcode` + `mcp-developer` |
| ~~F63~~ | ✅ | **ЗАКРЫТ S19 (c8bd4aa).** `structure_create` зовёт `chain_resolver.resolve(parent_path)` и передаёт движку реальные `parent_ids`; появился предпросмотр `structure_resolve`; ответ несёт `entities[]` (id/name/path/chain/qualified_id/owner_id) + факты `EntityRegistered`. Пруф закрытия: видео, созданное ВТОРЫМ вызовом внутрь существующего канала, получает цепочку из 3 предков и владельцем — готовый ID канала (`test_structure` §18, мутация «вернуть parent_ids=None» → 71/72). Было: **создание внутрь существующей структуры теряло предков: цепочка выводима только для greenfield-вызова.** Обёртка `structure_create` зовёт движок как `create_node(type, name, parent_path, None, children)` (`tools/structure/__init__.py:29`) — `parent_ids` **жёстко `None`**, и в `input_schema` инструмента этого параметра нет вовсе, то есть даже добросовестный ИИ передать предков не может. Ancestry заполняется **только** рекурсией внутри одного вызова (`template_engine.py:202` подмешивает `node_id` детям). Прогон ровно по сценарию владельца: сессия 1 создала `ниша→сетка→канал`; сессия 2 создаёт видео внутри этого канала (`parent_path=".../channels/chA/videos/"`) → видео физически ложится верно, а `parent_ids=[]` — сервер **не знает**, что оно принадлежит `CH_…`, `NET_…`, `NICHE_…`. То же при пропущенном уровне (канал без сетки). Следствие для S18-g: квалифицированный ID нечем собрать — «взять готовые ID канала и конкурента» неоткуда, потому что связь с каталогом-родителем не устанавливается в момент создания. **Фикс (`13` §S18-h):** цепочка выводится **из каталога назначения** резолвером, а не передаётся параметром | прогон: `create_node("video","intro", parent_path=".../channels/chA/videos/")` → `path` верный, `parent_ids=[]`; `tools/structure/__init__.py:29` (`None` на месте `parent_ids`), схема инструмента — `type/name/parent_path/children` | **B4** (`13` §S18-h), `mcp-developer` |
| ~~F64~~ | ✅ | **ЗАКРЫТ S19 (469cdac + адресация).** Добавлены `find_by_path`, инвариант `DUPLICATE_PATH`, `resolve_ref` (ID → путь → пара тип/имя) и параметры `child_id`/`parent_id`/`child_path`/`parent_path` у `structure_link`; при повторе имени ошибка перечисляет кандидатов с их ID, а не выбирает наугад. Пруф: два `video:dup` в разных каналах — по имени `VALIDATION_ERROR` со списком, по ID связывание проходит (`test_structure` §23, мутация M26). Было: Появились `LinkRegistry.find_by_path` (обратное отображение путь → сущность) и инвариант «один путь = одна сущность» → `DUPLICATE_PATH` при попытке завести второй ID на занятый каталог. **Остаток:** адресация `structure_link`/`structure_migrate` по паре (тип, имя) по-прежнему неоднозначна в иерархии — перевести на ID/цепочку. Было: **реестр нельзя спросить «кто живёт по этому пути», а адресация по (тип, имя) в иерархии неоднозначна.** Публичный API `LinkRegistry` — `get(id)`, `find(type, name)`, `link`, `migrate`, `check_integrity`: **обратного отображения путь → сущность нет**, хотя `path` в записи хранится. Без него containment-резолюция (S18-g «владелец по вместимости», F60-фикс и §S18-h) не на чем стоять. Второй слой: `_resolve_one` адресует сущность парой (тип, имя) и падает `VALIDATION_ERROR «Неоднозначно»` при повторе имени — а повтор в иерархии **норма**: два видео `intro` в разных каналах ломают `structure_link`/`structure_migrate`. Прогон подтверждает. **Фикс:** индекс `find_by_path` + инвариант «один каталог = максимум одна сущность своего уровня» (сейчас `duplicate_path` ловится только постфактум в `check_integrity`); адресация инструментов — по ID/цепочке, имя остаётся человеческой подписью | `dir(LinkRegistry)` без `find_by_path`; два `video:intro` под разными каналами → `r.link("video","intro","channel","chA")` = `VALIDATION_ERROR — Неоднозначно: video:intro встречается 2 раз` | **B4** (`13` §S18-h), `mcp-developer` |
| ~~F65~~ | ✅ | **ЗАКРЫТ S19 (b4389bd).** `fs_move` считает адрес цели ДО переноса (`new_owner_id`/`new_chain`) и переписывает пути поддерева через `migrate_subtree`; `fs_rename` — то же; `fs_delete` снимает записи (`forget_subtree`, `entities_dropped`); `check_integrity` сверяет путь с диском (`missing_path`). Собственные ID при переносе неизменны. Пруф: целостность 0 issues после создания И после переноса, мутация «перенос мимо реестра» → 77/82 (`test_structure` §21). Побочно закрыт рассинхрон «книга в реестре до материализации». Было: **перенос и переименование шли мимо реестра — путь протухает молча, проверка целостности слепа.** `fs_move`/`fs_rename`/`fs_delete` (`tools/filesystem`) не касаются `LinkRegistry` (grep: реестр используется только в `tools/structure`); `LinkRegistry.migrate(entity_id, new_path)` существует, но вызывается лишь из `structure_migrate` — ручной перенос его не задействует. После `move` запись продолжает указывать на несуществующий каталог, и `check_integrity` этого **не видит**: она проверяет висящие `parent_ids`, дубликаты путей и сирот, но **не сверяет путь с диском** → `issues_count = 0` при заведомо битом реестре. Для S18-g это разрушает основу: цепочка вычисляется из путей, значит протухший путь = неверная цепочка у всего поддерева. Требование владельца («ID цели присвоить ДО переноса, чтобы ИИ проверил корректность») это же и лечит: перенос обязан проходить через резолвер + `migrate` одной операцией | прогон: `register(VID_1, path=.../chA/videos/intro)` → `shutil.move` → `get("VID_1")["path"]` по-прежнему старый, каталога на диске нет, `check_integrity()["issues_count"] = 0` | **B4** (`13` §S18-h), `mcp-developer` + `test-master` |
| ~~F66~~ | ✅ | **ЗАКРЫТ S19 (замечание владельца).** Было: **адресация по ID построена, но узнать НУЖНЫЙ ID неоткуда.** После F60 поиск по ID работает — при условии, что ID известен; иначе ИИ либо обходит дерево, либо перечитывает `project_memory.md` и переписку, и вся экономия контекста съедается на входе. Память проекта хранит ID (`memory_read` их даже парсит регуляркой `PREFIX_<32hex>`), но в реестр они не попадали — связи память→реестр не существовало. **Фикс:** реестр стал индексом. `LinkRegistry.annotate/search` + три инструмента: `structure_find` (по куску имени/ярлыка/метки/пути → id + путь + цепочка, один вызов), `structure_remember` (знание ИЗ ЧАТА в реестр — по запросу ИИ), `structure_index_memory` (ID ИЗ ПАМЯТИ проекта в реестр: заголовок записи становится ярлыком; ID, известные памяти и неизвестные реестру, возвращаются как висящие ссылки). Ярлыки — недоверенный текст (чат/чужие файлы), поэтому отдаются **в конверте провенанса** `{value, provenance, trust: untrusted, note, flags}` (`core/contracts/untrusted.py`, первый кусок S3): значение не искажается, пометка `instruction_like` ставится боевым детектором с паттернами из `firewall.yaml`. Обрезка осталась только как **ресурсная** граница (500 симв.) и защитой не называется — «ignore previous instructions» короче любого разумного лимита. Пруф: сущность находится по «сетапы» без знания ID, её файлы — сразу `fs_smart_search(owner_id=…)`; мутации M29/M30 (`test_structure` §26) | прогон: `structure_find(text="сетапы")` → 1 сущность с цепочкой; `structure_index_memory` → 1 помечен, 1 висящая ссылка; ярлык 400 симв. + перевод строки → 200 симв. без `\n` | **B4′** |
| ~~F67~~ | ✅ | **ЗАКРЫТ S19 (ревизия по указанию владельца «посмотреть, что нам не грозит»).** Было: **наблюдение за деструктивом смотрело на призраков и не видело реальных инструментов.** `anomaly_detection.dangerous_tools` содержал 4 имени, из которых **3 не существуют** (`config_delete`, `system_shutdown`, `system_restart` — наследство generic-шаблона, таких инструментов у сервера нет и не было), а **10 реально разрушающих/пишущих не отслеживались**: `fs_move`, `fs_rename`, `fs_write_file`, `table_delete`, `excel_delete_sheet`, `excel_delete_column`, `excel_move_column`, `excel_rename_sheet`, `json_clear_queue`, `memory_write`. Класс тот же, что D8/D33: правило существует, но сигнал по нему невозможен. **Фикс:** список сверен с живым инвентарём (57 инструментов) и переписан в конфиге — код не трогали. Тест держит инвариант: в списке нет имён вне эталона инвентаря И все разрушающие в нём есть (мутация M39). Попутно подтверждено, что SQL/Windows-паттернов в `injection_detection` нет — у сервера нет БД (D33 закрыт фактом) | прогон: `config_delete`/`system_shutdown`/`system_restart` — «НЕТ ТАКОГО ИНСТРУМЕНТА»; после фикса счётчик аномалий растёт на `fs_write_file`/`table_delete`/`excel_delete_sheet`/`json_clear_queue`/`memory_write` и молчит на `fs_read_file`/`structure_find`/`table_get_row` | **S9/B2** |
| ~~F68~~ | ✅ | **ЗАКРЫТ S20 (регрессии написаны, мутации краснеют).** `test_structure` §32 держит обе половины находки: содержимое (shebang под `.md`, ELF под `.json`, HTML под `.txt`, batch под `.csv`, PHP под `.md` → `FILE_TYPE_FORBIDDEN` и НИЧЕГО на диске; честная заметка проходит) и обход именем (`.txt`→`.sh` и перенос в `.exe` отклонены, диск не тронут, `.txt`→`.md` проходит, каталог переименовывается). Мутационный пруф: M44 (снять `check_content`) → 10 красных, M45 (снять проверку цели в `fs_rename`/`fs_move`) → 4 красных. **Попутно закрыта третья дыра, которую заявка о «единой двери» переоценивала:** `fs_create_python_script` писал файл МИМО `_check_write` — `description` приходит от ИИ и попадает в тело файла, так что `<?php system($_GET[0]); ?>` материализовался (`success`, файл на диске). Теперь каркас скрипта и фрагменты `fs_create_project_structure` идут через ту же дверь; мутация M46 → 2 красных. Было: **Обход allowlist сменой имени и «безопасное» расширение поверх исполняемого содержимого (замечание владельца S19).** Первая дыра: allowlist проверялся ТОЛЬКО при создании/перезаписи, поэтому `fs_create_file("data.txt")` + `fs_rename("data.sh")` (или `fs_move` в `.exe`) обходили правило целиком. Вторая: расширение вообще не свидетельствует о природе файла — `sh payload.txt` исполняется, и переименование «безопасно» не делает. **Фикс (S19):** единая дверь `_check_write(path, content)` — тип + содержимое; проверка типа добавлена в `fs_move`/`fs_rename` (цель), проверка содержимого — во все пишущие пути; 10 сигнатур (shebang, ELF, PE/DOS, Mach-O, Java class, HTML, PHP, batch, PowerShell-загрузчик) объявлены в `config/firewall.yaml → write_allowlist.forbidden_content`, не в коде. **Остаток: регрессии не написаны** (лимит сессии) — мутации M44/M45 стоят в `CATALOG §F` как ⬜; проверка сделана ручным прогоном | прогон: shebang под `.md`, HTML под `.txt`, ELF под `.json`, batch под `.csv` → `FILE_TYPE_FORBIDDEN`; честная заметка `.md` проходит; `.txt`→`.sh` и `.txt`→`.exe` отклонены, `.txt`→`.md` разрешён | **S2** |
| ~~F69~~ | ✅ | **ЗАКРЫТ S20 в момент обнаружения. Подпись артефактов держалась на НЕобъявленной зависимости.** `core/integrity.py` импортирует `cryptography` под `try/except ImportError` → `SIGNING_AVAILABLE = False`, но пакета не было ни в `pyproject.toml`, ни в `requirements.txt`, ни в `requirements.lock`. В системном Python он оказался транзитивно (49.0.0) — поэтому гейт был зелёный и дыра не проявлялась; в `.venv` проекта его не было, и вся защита S9 (подпись + отпечаток машины) молча выключалась, а `test_structure` §28 падал `FileNotFoundError` на `instance_key.pem`. Класс — «отложенный эффект»: у меня зелено ≠ зелено из чистого клона. **Фикс:** `cryptography>=42.0` объявлена в `pyproject.toml` (runtime, не dev — это боевая защита, а не инструмент разработки); попутно `InstanceIdentity._private: Any` — с установленной библиотекой mypy увидел 2 реальные ошибки типов, которых раньше просто не мог заметить | до фикса: `.venv/bin/python -c "import cryptography"` → `ModuleNotFoundError`, `test_structure` §28 «✗ подпись доступна», exit 1; `grep -i cryptography pyproject.toml requirements.txt requirements.lock` → пусто. После: 195/195, гейт 14 passed, mypy чист на 50 файлах | **S9** |
| ~~F70~~ | 🔴→✅ | **ЗАКРЫТ S20 в момент обнаружения (ротация). ПРИВАТНЫЙ КЛЮЧ ПОДПИСИ УШЁЛ В ПУБЛИЧНЫЙ РЕПОЗИТОРИЙ.** `instance_key.pem` — Ed25519-ключ инстанса, выпускаемый S9, — попал в git коммитом `e6c3732` и был запушен на `github.com/sdvsdrv8990/video-pipeline-mcp` (публичный). Причина — щель в `.gitignore`: секция «Секреты» ловила `*.key`, а ключ называется `.pem`. Последствие: **вся защита S9 обнулялась** — кто угодно, прочитав репозиторий, мог подписать записи реестра/очереди/журнала как этот инстанс, и `FOREIGN_WRITE` их бы принял. Нашлось не аудитом кода, а живым прогоном: сервер при старте выпустил ключ, и проверка гигиены секретов показала его в индексе. **Фикс:** `*.pem` + `instance_key.pem` в `.gitignore`, `git rm --cached`, ключ **ротирован** (старый считается скомпрометированным; подписанных записей в `workspace/` не было, поэтому ротация ничего не сломала). **История вычищена (решение владельца S20: «на GitHub должен быть чистый реп»):** `git filter-repo --invert-paths --path instance_key.pem` переписал все 105 коммитов, force-push заменил публичную ветку. Пруф: `git grep <тело_ключа> $(git rev-list --all)` → exit 1, свежий клон с GitHub чист (контроль: в бэкап-бандле тот же поиск НАХОДИТ — значит тест не холостой). Бэкап прежней истории — bundle вне репозитория. **⚠️ Урок: force-push НЕ удаляет данные с GitHub** — коммит становится недостижим из веток, но висящий объект продолжает отдаваться по прямому SHA до сборки мусора на их стороне (проверено: `raw/e6c3732/instance_key.pem` → HTTP 200 с телом ключа уже ПОСЛЕ force-push). Единственный способ убрать немедленно своими руками — пересоздание репозитория; альтернатива — обращение в GitHub Support. **Сделано (S20):** репозиторий удалён и создан заново (`gh repo delete` + `create` + push чистых 107 коммитов; терять было нечего — 0 звёзд/форков/issues/подписчиков). **Финальный пруф:** `raw/e6c3732/instance_key.pem` → **HTTP 404** (был 200), API коммита → 422, свежий клон = 107 коммитов без файла и без тела ключа; контроль на бэкап-бандле находит — проверка не холостая | `git log -- instance_key.pem` → `e6c3732`; `git show HEAD:instance_key.pem` → `-----BEGIN PRIVATE KEY-----`; `git check-ignore .env` → игнорируется, `instance_key.pem` → НЕ игнорировался; после фикса — игнорируется, в индексе нет, права `0600` | **S9** |
| ~~F71~~ | 🟠→✅ | **ЗАКРЫТ S20. Тесты и сервер жили в РАЗНЫХ окружениях — поэтому CVE транспорта не был виден.** Гейт `pytest` исполнялся системным Python (`aiohttp 3.14.1`), а сервер запускается из `.venv` (`run.sh:13`) с `aiohttp 3.13.5` — тем самым, на котором 9 advisory. Зелёный гейт ничего не говорил о боевом окружении. Тот же класс, что F69 (`cryptography` мимо манифеста). **Фикс:** `.venv` приведён к манифесту (`pip install -e ".[dev]"`), полный гейт прогнан ИЗ `.venv`; `requirements.lock` пересобран из ЧИСТОГО окружения, поднятого с нуля по `pyproject.toml` — это заодно пруф, что манифест ставится из чистого клона | до: `python -c 'import aiohttp'` = 3.14.1 (гейт) vs `.venv` = 3.13.5 (сервер); после: обе 3.14.3, `pytest -m "not live"` 14 passed из `.venv` | **S8** |
| ~~F72~~ | 🔴→✅ | **ЗАКРЫТ S21. `TemplateEngine` проведён через тот же allowlist, что и `fs_*`.** Движок строит `WritePolicy` из своего `config_dir` и проверяет КАЖДЫЙ пишущий фрагмент — имя (тип) и содержимое (`template_engine.py::_forbidden`). Запрещённый фрагмент уходит в `skipped` с причиной `forbidden`, соседи создаются (закон пофрагментного скипа §1) — отказ виден клиенту, а не проглочен. Шаблон объявляет, ЧТО писать, но не решает, что МОЖНО. Предусловие режима «свой шаблон под проект» снято | тот же прогон `evil.tpl.yaml`: до — `payload.sh` и `notes.md` с shebang на диске, `skipped: []`; после — `created: []`, три отказа с причинами, на диске только легитимный `ok.md`. Мутации M50/M51 краснеют | **S2/B4** |
| ~~F73~~ | 🔴→✅ | **ЗАКРЫТ S21 (найден при закрытии F72). `kind: config` не имел containment для `source` — шаблон вычерпывал файлы ИЗ-ЗА пределов `config/`.** `src = self.config_dir / fr['source']` без проверки: `source: "../.env"` даёт корень проекта, и копия ложится В `workspace/`, откуда `fs_read` отдаёт её клиенту. Это не «тип файла», а **эксфильтрация серверного секрета через объявление шаблона** — соседний слой к F72 и опаснее его. **Фикс:** источник резолвится `safe_resolve(rel_src, self.config_dir)`, выход за корень → `skipped: source escape` | прогон: до — `stolen.yaml` содержит живой `MCP_AUTH_TOKEN` из `.env`; после — фрагмент пропущен, в рабочей области только `ok.md`. Мутация M52 краснеет | **S2/B4** |
| F74 | 🟠→🟡 | **ЧАСТИЧНО ПОГАШЕН S21: ключ больше не хранится в обратимом виде.** В `.env` лежит `MCP_AUTH_TOKEN_SHA256` — отпечаток; проверка сравнивает `sha256` присланного с записанным (`core/auth.check_auth`), значения сервер не знает и для работы оно ему не нужно. Следствия: читать файл бессмысленно, шифровать нечего (нет ключа расшифровки, который пришлось бы прятать рядом), перебрать `sha256` от 32 байт энтропии нечем; сам отпечаток как ключ не проходит. Прежнее открытое значение мигрирует в хэш при старте И СТИРАЕТСЯ. Значение существует один раз — при выпуске, и печатается только в терминал (`sys.stdout.isatty()`), не в лог. Плюс запреты чтения секретов в `.claude/settings.json`. **Остаток долга (снос после цикла):** калитка `MCP_ALLOW_NO_AUTH=1` и сам файловый путь → OS-keyring / OAuth 2.1 (`15 §S1`, DIM-2). **Пост-квантовость к этому месту не относится:** симметричный секрет ломает Гровер, а не Шор; Шор-уязвим Ed25519 в `core/integrity` (S9), но подписи проверяются локально и сейчас, «harvest now — decrypt later» к ним неприменим | живой прогон: верный ключ 200, чужой 401, **хэш в роли ключа 401**, без ключа 401; `.env` после миграции содержит только `…_SHA256`. Мутации M55/M56/M57 краснеют; `test_audit_fixes` 77/77 | **S1 (остаток — финал цикла)** |
| — | ℹ️ | **Сопутствующее (не дефект боевого пути):** `FsSearcher.__init__` кладёт `Path(workspace)` без `.resolve()`, а `search()` делает `item.relative_to(self.workspace)` → при относительном workspace-пути падает `ValueError` в `INTERNAL_ERROR`. В бою путь абсолютный (`_context.build_context`), поэтому не стреляет; при выравнивании поиска добавить `.resolve()` | воспроизведено на `FsSearcher("ws")` c относительным путём | B4 |
| ~~F76~~ | 🟠→✅ | **ЗАКРЫТ S24 (доп. 7). Был: хендлер звал `materializer` — имя, которое создаётся только внутри соседнего хендлера, поэтому инструмент был объявлен в `tools/list`, обещан клиенту и падал `INTERNAL_ERROR` при любом вызове.** Тот самый вопрос «откуда брать `parent_path`/`mode`» решился так: **адрес брать неоткуда и не надо — он уже в каждой записи** `tables_pending` (поле `path`), а режим стал параметром инструмента, потому что при `mode=custom` шаблоны резолвятся ПО АДРЕСУ, и один материализатор на весь список молча подставил бы соседям чужие схемы. Хендлер строит материализатор на каждую книгу (`_materialize_pending`), в ответе появилось `templates: {путь: чьи}` — «сработали серверные вместо проектных» перестало быть молчанием. Регрессия бьёт по ИНСТРУМЕНТУ, а не по движку: закрывает слепую зону, из-за которой дефект и дожил (тесты материализатора были зелёные) | до: `structure_materialize_tables(pending=[…])` → `INTERNAL_ERROR`, книга не создана; после: `success`, книга на диске, факт `TableMaterialized`. Мутация (возврат `materializer.materialize_pending`) красит 6 проверок §9. `ruff check .` → All checks passed | S24 ✔ |
| ~~F77~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (указание владельца). `structure_customize` клал копию шаблонов ПО ЛЮБОМУ адресу — в том числе в корень рабочей области и в несуществующий каталог, который сам же и создавал.** Резолвер ищет `.templates/` ВВЕРХ по дереву, поэтому копия выше канала молча становится законом для ВСЕХ сущностей ниже: правка «под один канал» меняла бы соседние, и это нигде не было видно. **Фикс:** адрес обязан быть зарегистрированной сущностью (`link_registry.find_by_path`), иначе `ENTITY_NOT_FOUND` с объяснением; в успешном ответе теперь названы владелец копии (`owner`) и её область действия (`scope_note`). Ниша остаётся законным адресом — это осознанная адаптация уровня, а не случайный корень | живой прогон до: `structure_customize(path="")` → `success`, файлы в `workspace/.templates/`; `path="что_угодно_несуществующее"` → каталог создан. После: оба → `ENTITY_NOT_FOUND`, на диске пусто. Мутация M91 краснеет | **B4/S20** |
| ~~F78~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (указание владельца). Локальные адаптеры знали раскладку каталога весов и решали параметры генерации за модель.** `SUBDIR = "tts"/"img"` дублировали `local.catalog[].dest` — правка `dest` в декларации молча разошлась бы с местом, куда смотрит код (класс D2); `DEFAULT_SIZE = 512`, `guidance_scale=0.0` и `steps=1` были зашиты, а `guidance=0` верен для дистиллированных пошаговых моделей и портит картинку у обычных. **Фикс:** путь к весам отдаёт реестр (`AdapterRegistry.model_path/require_model`) по декларации (`id → path`), имя вне каталога = путь внутри корня весов, containment на обоих путях; параметры вызова собираются ТОЛЬКО из объявленных столбцов строки (`img_size`/`steps`/`img_n`/`guidance`/`variant`), чего в строке нет — не передаётся, и решает модель | правка `local.models_dir` + `catalog[].path` во временной декларации меняет путь, куда смотрит адаптер; `_call_kwargs({})` == `{}`; мутация M90 (игнорировать декларацию) краснеет | **P/провайдеры** |
| ~~F79~~ | 🟠→✅ | **ЗАКРЫТ S23 (доп. 6). Исполнитель ушёл на карту, и это доказано замером.** Устройство выбирает единая точка `hardware.compute_device(gpu_rules, want)`: авто = карта, если она есть, её память известна и сборка расчёта до неё дотягивается, иначе процессор; столбец `device` строки перекрывает пробу. Имя устройства (`torch_device`) и разрядность (`dtype: {gpu: float16, cpu: float32}`) объявлены в `config/providers.yaml → local.gpu`, не зашиты. `diffusers_local` поднимает пайплайн в этой разрядности и переносит `.to(device)`; **отказ переноса — ошибка, а не тихий откат на CPU** (разница 26×, молчаливое «медленно» неотличимо от поломки). Где считалось, приходит клиенту в `media_generate.data.compute`. Заодно вердикт стал считаться в разрядности ПОДЪЁМА, а не диска (`bytes_for(as_dtype=…)` + `fit.dtype_alias`): веса fp32 на диске, поднятые в fp16, требуют вдвое меньше — прежде вердикт завышал вдвое. Среда: `torch==2.13.0+rocm7.1` объявлена группой `gpu-amd` в `pyproject.toml` (пин ЛОКАЛЬНОЙ версии — с PyPI приедет CUDA-сборка того же номера); системный ROCm не нужен, библиотеки внутри колеса, хватает драйвера ядра и `/dev/kfd` (права `0666` — без root и без групп) | `torch.cuda.is_available()` True, `gfx1101`, `AMD Radeon RX 7800 XT`, 17.2 ГБ; замер sd-turbo 512×512/1 шаг: **карта 0.3 с/кадр, процессор 7.7 с/кадр** (26×), VRAM по sysfs 1033 → 5786 МБ (+4753) на карте и +0 на процессоре; `device=cpu` в строке уводит на процессор при доступной карте; `test_providers` 176/176 с живой генерацией через `media_generate` | **P/провайдеры** |
| ~~F80~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения. `media_models` не отдавал `variant`, и модель с весами только fp16 не поднималась вовсе.** Опись (`installed.yaml`) хранит `variant: fp16`, но `ModelCatalog.installed()` его не возвращал — ИИ, заполняющий строку провайдера по списку моделей, не мог узнать, что на диске лежат файлы `*.fp16.safetensors`, и без столбца `variant` `from_pretrained` искал fp32-веса, которых нет → `LOCAL_MODEL_MISSING` на КАЖДОМ вызове. Нашлось живым прогоном при переводе теста на «модель из описи, а не зашитая». **Фикс:** `variant` в выдаче `installed()` — это свойство того, что лежит на диске, и оно обязано доехать до строки | до: `installed()[…]` без ключа `variant` → `media_generate` с sd-turbo даёт `LOCAL_MODEL_MISSING`; после: `variant: fp16` в выдаче, тот же вызов `success` | **P/провайдеры** |
| ~~F81~~ | 🟡→✅ | **ЗАКРЫТ в момент обнаружения. Ложный КРАСНЫЙ: набор превращал любой `UserWarning` в ошибку, включая чужие.** `tests/quick/test_providers.py:16` ставил `warnings.simplefilter("error", UserWarning)` глобально. ROCm штатно предупреждает, что быстрый бэкенд внимания на этой карте экспериментальный, — предупреждение поднималось исключением внутри `pipe(...)`, ловилось нашим `except Exception` и возвращалось как `LOCAL_INFERENCE_FAILED`. Картинка при этом рисовалась: прямой прогон адаптера тем же кодом проходил. Зеркало F50 («ложный зелёный»): фильтр, поставленный ради своих предупреждений, судит чужие. **Фикс:** ошибкой остаются СВОИ предупреждения, у `torch.*`/`transformers.*`/`diffusers.*`/`huggingface_hub.*` — поведение по умолчанию | тест краснел `LOCAL_INFERENCE_FAILED: Flash Efficient attention … is still experimental`, тот же вызов вне набора — `success` с файлом на диске; после сужения фильтра 176/176 | **тесты** |
| ~~F82~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения. Единственный путь установки моделей качал в 60–100 раз медленнее обычного HTTPS и вставал на середине.** Клиент HF по умолчанию тянет через Xet-протокол (`hf-xet` приезжает транзитивной зависимостью `huggingface_hub`). На 5-гигабайтном `unet` он давал ~80 КБ/с и на 7 минут переставал двигать файл вовсе, при том что `curl` из того же процесса с того же хоста качал 5 МБ/с — то есть «медленно» было не сетью. 7.8 ГБ такой скоростью не приезжают: установка модели превращалась в бесконечную. **Фикс:** протокол объявлен (`local.install.transfer: https \| xet`) и применяется из кода — `ModelCatalog.apply_transfer()` ставит `huggingface_hub.constants.HF_HUB_DISABLE_XET`, которую клиент читает ЖИВЬЁМ на каждой загрузке; переменная среды снаружи больше ни при чём, поведение воспроизводимо из декларации. **Осталось:** сам Xet как транспорт не исследован (может, дело в дедупликации CAS на первой загрузке) — если он когда-то понадобится, переключается строкой | замер на одном и том же файле: Xet 80 КБ/с → 1006 МБ за 106 с (9.5 МБ/с) с `transfer: https`; `curl` тем же каналом 5.1 МБ/с; тест: смена декларации на `xet` снимает константу (значение не зашито и не из среды) | **P/провайдеры** |
| — | ℹ️ | **Замечание владельца (S23, доп. 7): «выделять fp16 — не очень умно, под крупные модели поддерживать будет сложнее». Верно, и лечится готовым.** Проверено, что умеют уже установленные библиотеки (diffusers 0.39 / transformers 5.15 / accelerate 1.14): `from_pretrained` принимает `variant`, `dtype`, `device_map`, `max_memory`, `quantization_config`, `offload_folder`; у пайплайна есть `enable_model_cpu_offload`, `enable_sequential_cpu_offload`, `enable_group_offload`, attention slicing, VAE tiling; diffusers знает 7 движков квантизации (`bitsandbytes_4bit/8bit`, `gguf`, `quanto`, `torchao`, `modelopt`, `auto-round`). То есть `variant` — параметр библиотеки, а не наш слой, и ответ на «модель больше карты» — выгрузка/квантизация, а не второй набор весов. **Сделано:** `variant` уехал в опись (строка — только перекрытие), загрузочные параметры идут объявленной картой `local.gpu.load_from_row` (новый рычаг = строка в YAML), режим при нехватке памяти объявлен в `local.gpu.oversize`, вердикт каталога несёт `fit.oversize` вместо голого «no». **Что НЕ готовое:** `accelerate.commands.estimate` (оценка памяти по репозиторию HF) знает только `timm`/`transformers` — для diffusers-моделей наш `FitEstimator` не заменяется, для будущих whisper/LLM заменяется; `dtype="auto"` в diffusers, в отличие от transformers, не поддержан — слой политики остаётся наш. Квантизацию сознательно НЕ заводили: на ROCm/gfx1101 её пришлось бы проверять живьём каждый раз, а качество картинки падает — это и есть то усложнение, которого просили избежать | замер выгрузки на настоящей модели (порог поднят декларацией, чтобы sdxl-turbo «не влез»): `model_offload` → **1.5 с/кадр при VRAM +451 МБ** против 0.4 с/+9276 МБ целиком и 7.7 с на процессоре; `accelerate --library_name diffusers` → `invalid choice`; `test_providers` 189/189 | **P/провайдеры** |

| ~~F83~~ | 🟡→✅ | **ЗАКРЫТ S24 (доп. 8): каталогов-реликтов на диске нет, 3 ссылки на снятые слои исправлены по факту.** **Было:** **Каталоги-призраки и ссылки на снятые слои. Проверено S23 (доп. 9) по замечанию владельца «движка ошибок и движка реакций нет».** **Подтверждено:** движка реакций нет — `core/reactions/reactions.py` это **102 строки** читалки реестра и маппера (`load`/`get_error`/`get_reaction`/`list_codes`); отдельного модуля ошибок нет вовсе. **И это не дефект:** `R-CONTRACT1` фактически выполнен — `get_error` зовётся ровно из ДВУХ мест (`Engine._error`, `ToolContext.err`), захардкоженных `message="…"` в движках таблиц/книг/поиска ноль. Неверна не архитектура, а слово: «ядро/движок реакций» = «реестр + единая точка маппинга». Так же и `core/engine/engine.py` — не декларативный движок, а **реестр инструментов + валидатор схемы + диспетчер** (`register`/`call`/`_validate`/`list_tools`); звено «декларация → движок» из формулы «обёртка→декларация→движок→контракт→реакция» не существует (`config/ops` снят в S22). **Дефект-остаток (это и чиним):** 6 пустых каталогов-реликтов и 3 ссылки в коде на несуществующее. **Порядок сноса:** (1) удалить пустые каталоги `pipeline/entry_points`, `pipeline/steps` (и сам `pipeline/`), `tools/video`, `tools/excel_engine`, `config/ops`, `core/providers/ffmpeg/wrappers` — **в git их нет** (git пустые каталоги не хранит), значит это мусор рабочей копии, диффа не будет; (2) починить 3 шапки/комментария: `core/ids/__init__.py:9` и `core/providers/ffmpeg/__init__.py:10` («Используется: … `tools/video`» — каталога нет), `core/firewall/rules/anomaly_detector.py:38` (ссылка на `config/ops/*.yaml` как источник настройки — слой снят); (3) причесать слово «движок» там, где им называют реестр: `01` (формула архитектуры), `11` R-CONTRACT1, `04` строка 5, и в скиле `mcp-developer`. **Ловушка:** `excel_engine` — ЖИВОЕ имя (класс `ExcelEngine` в `core/excel/`, поле `ToolContext.excel_engine`, 21 вызов в `tools/excel/`); реликт — только пустой каталог `tools/excel_engine/`. Имя не трогать | `find . -type d -empty` → 6; `git ls-files` по ним → ∅; `grep -c 'get_error(' core tools server.py` → 2 вызова; `grep 'message="' core/tables core/excel core/engine core/search` → ∅; `wc -l core/reactions/reactions.py` → 102 | попутно (дешёвый пункт RESUME) |
| ~~F84~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24, доп. 6). Упавший раннер числился живым вечно — «всё ещё встаёт» вместо причины.** Раннер запускается сервером, то есть он его ребёнок: умерев, он остаётся зомби, пока его не подберут, а `os.kill(pid, 0)` на зомби отвечает успехом. `RunnerSupervisor._alive` верил этому ответу, и `status()` навсегда застревал в фазе `running_silent` («процесс есть, `/health` молчит — ещё встаёт или завис»). Наблюдаемое следствие ровно то, ради устранения которого раннер и выносили из сервера: падение инференса становилось молчанием, а не причиной. Класс F72/F73/F75 — «немая потеря = дефект». Нашлось не рассуждением, а тестом, который бил `kill -9` и ждал фазу `exited`. **Фикс:** `waitpid(WNOHANG)` перед `kill -0` — мёртвого ребёнка сначала подбираем, потом отвечаем; `ChildProcessError` (раннер пережил перезапуск сервера и уже не наш ребёнок) остаётся законным путём к обычной проверке. Заодно снимает накопление зомби | до: после `kill -9` фаза `running_silent`, `log_tail` есть, но «умер» не сказано никогда; после: `exited` + хвост журнала. Мутация: возврат к `kill -0` красит §17 обратно | **P/раннер** |

| ~~F85~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24, волна 2.1). Декларативный реестр, который машина прочитать не могла.** `tests/agent_swarm/patterns.yaml` — 36 паттернов роя — не парсился вовсе: две строки несли `: ` внутри неэкранированного скаляра (`out-of-order: ребёнок…`, `virus_injection (реверс: вывод…)`), и `yaml.safe_load` падал на первой. Полтора месяца файл числился «декларация готова, раннер TODO», хотя готова была только видимость: ни один читатель его ни разу не открыл. Класс — тот же, что D2: объявленное считается работающим, пока его никто не грузит. Фикс = кавычки; регрессия = сам раннер, который грузит реестр первым действием | `python -c "import yaml; yaml.safe_load(open('tests/agent_swarm/patterns.yaml'))"` → `ScannerError: mapping values are not allowed here, line 47` | I7 T-рой |
| ~~F86~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24, волна 2.1). Сигнал аномалии был немым: правило живое, выход мёртвый.** `AnomalyDetector` на деструктивный инструмент увеличивает счётчик и формирует `reason`, но `Firewall.check` на ALLOW-пути эту причину ВЫБРАСЫВАЛ, а счётчик `get_stats()` в проде не читает никто (2 вызова на весь репозиторий — оба в тестах). Плюс `logging.log_suspicious: true` в `firewall.yaml` — мёртвый knob: ни одного читателя в коде (родня F54). Итог: `fs_delete`/`fs_move`/`memory_write` в бою не оставляли следа нигде. Фикс: `log_suspicious` читается при сборке правил, ALLOW несёт причину, сервер её печатает — 6 строк, без нового контракта. Приёмка мутацией: `if False and …` → раннер роя краснеет `atk_anomaly_sequence` | `grep -rn "get_stats\|log_suspicious" --include=*.py` → потребителей в проде ∅; мутация → `[ПРОБОЙ] atk_anomaly_sequence: след в консоли НЕТ` | security-reviewer, I7 |
| F87 | 🟠 | **Провенанс не доехал до результатов поиска (остаток OUT8).** `F33` закрыт по пути чтения файла — `fs_read_file` честно отдаёт `trust: untrusted`. Но `fs_smart_search` возвращает содержимое найденного БЕЗ единой пометки: отравленный документ в рабочей области приезжает клиенту как обычный текст, и ровно этот путь питает выбор контекста. То есть маркировка сделана на инструмент, а не на класс «чужой текст наружу» — единой точки нет. Фикс — там же, где сборка ответа поиска, а не в каждом инструменте отдельно | рой S24: `atk_search_poison` → «результаты поиска отдаются БЕЗ пометки провенанса»; `grep -rn "untrusted" tools/search core/search` → ∅ | I6 (outbound), A5 |
| ~~F88~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24, волна 2.2). Исключение `mypy` не работало и прятало 35 ошибок типов, пока сессии писали «mypy PASS».** `exclude = "core/providers/"` отсекает только обход каталога — а в провайдеров mypy приходит ПО ИМПОРТАМ из `tools/media`, и потому 15 ошибок жили прямо в гейте; ещё 20 всплыли, когда исключение сняли совсем. Среди спрятанного — не косметика: `tools/media:213` звала `write_policy.check()` как значение (проверка типа файла делалась ради `None` в кортеже), `:292` возвращала из инструмента `dict` вместо `ToolResult` при отказе прозвонки, `http_image:287` могла сделать `raise None` вместо причины отказа, `catalog:256`/`onnx_upscale:47` падали бы `AttributeError` на пустом ответе, `resolver:103` клал `None` в цепочку провайдеров как имя. Плюс снесена мёртвая оркестрация `ffmpeg.render_full_pipeline` (75 строк против четырёх `NotImplementedError`-примитивов, ноль вызывающих) — честный отказ вместо обещания работы (G16). Исключение снято: провайдеры давно не стабы | `mypy` до: `Found 15 errors in 9 files (checked 61)`; после снятия исключения: 20 новых в 5 файлах; итог: `Success: no issues found in 87 source files` | I4, code-quality |
| ~~F89~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24, волна 2.2). Веса моделей тянулись без привязки к состоянию репозитория — и это красило `bandit` в CI (B615, 7 сайтов).** Все загрузки шли «из того, что там сейчас»: файлы ОДНОЙ модели могли приехать из РАЗНЫХ состояний репозитория (снимок из 20 файлов не атомарен), а переустановка через месяц — дать другие веса под тем же именем и молча. Фикс: единая точка `ModelCatalog.revision(repo)` — спрашивается один раз на репозиторий, уходит во ВСЕ загрузки (`hf_hub_download`/`snapshot_download`/`model_info`) и записывается в опись, так что поставленное можно повторить и сверить. Объявленная ревизия (`local.install.revisions.<repo>`) сильнее вычисленной — это настоящий пин владельца. Не узнали состояние → `DOWNLOAD_FORBIDDEN`, а не тихая загрузка вслепую. Заодно снят B104 в раннере: bind на все интерфейсы внутри контейнера объяснён и помечен `nosec` (граница там у публикации порта — `127.0.0.1:порт`, а не у bind) | `bandit -r core server.py -ll` до: 8 issues, exit 1; после: exit 0 | dependency-hygiene, I3 |
| ~~F90~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24, волна 2.2). Ответ на «покажи лучшие» открывался мусором.** `_apply_sort` клал признак типа в ключ сортировки, а ключ разворачивался целиком: при `order: desc` нечисловые значения и пустые ячейки всплывали НАВЕРХ — «топ по просмотрам» начинался со строк-заглушек. Фикс: тип столбца определяется по большинству заполненных значений, чужеродные значения всегда в хвосте, в обе стороны; свойство F42 (разнотипное не роняет `sorted`) сохранено. Приёмка мутацией M101 | новый раздел `test_search`: `desc` → `[R1, R2, R3]`, а не `[R3, R1, R2]` | A5, test-master |
| ~~F91~~ | 🟠→✅ | **ЗАКРЫТ в момент обнаружения (S24). «Зелёный» набор провайдеров существовал только на машине владельца.** `test_providers` читал спеку модели из ФАЙЛОВ `vendor/models/sd-turbo` — а весов нет в git и не может быть; в CI проверка падала `KeyError: 'properties'`, то есть гейт был недостижим для всех, кроме одной машины. Фикс — пропуск с причиной (`⚠ веса не поставлены`), как уже сделано в соседних живых проверках bg/upscale: отсутствие артефакта вне git = не провал, но и не молчание | CI-лог `31905957685`: `test_providers exit=1` при локальных 273/273. **Проверено как в CI:** веса временно убраны с диска → набор 237/237 зелёный, с весами 273/273 — разница целиком в честных пропусках, а не в проваленных проверках. Заодно исправлен эталон «моделей больше нуля» → «ровно то, что в описи»: на чистой машине пустой список — правильный ответ, а не поломка | I3, test-master |

## Открытые вопросы (решить в фазах)

- **OQ1:** README переписать под факт (быстро) или догнать код до README (долго)? → влияет на A2/A4 vs пересмотр дизайна.
- **OQ2:** Аудит D#/G# — реконструировать `docs/dev/audit/` или единый источник = этот реестр F#? → X1.
- **OQ3:** `docs/dev` разгитигнорить (версионировать доки) или оставить приватным? → I1/I8 + пожелание владельца «отозвать позже».
