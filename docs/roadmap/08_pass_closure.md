# 08 — Честное закрытие проходов: файлы, требования, ремап, готовые решения

> `docs/dev/` (приватный, gitignored) **удалён** владельцем — потерял ценность. Это не потеря, а **форсирующее событие**: единственный источник истины теперь git-tracked `docs/roadmap/` + память + коммиты. Этот файл: (1) ремап удалённого `docs/dev`, (2) список файлов+требований для честного закрытия проходов, (3) привязка сильных GitHub-репо к уровням зрелости/стилю/структуре/правилам.
>
> «Проход» = гейт зрелости из `07_maturity_rubric.md` ИЛИ ревью-прогон сессии (§3.1). Закрыть проход **честно** = §6 Definition-of-Closed по ДОКАЗАННОМУ состоянию (не «запланировано»): обмер всех файлов → адаптация GitHub (кроме sec) → простые тесты + симуляции с чтением консоли.
>
> **Добавлено (Сессия 15):** §3.1 привязка ревью-проходов к измерениям зрелости · §6 Definition-of-Closed (когда проход честно закрыт).
>
> **Добавлено (Сессия 24):** §7 — закрытие сплошного прохода S24 (что вошло, чем закрыто, что перенесено, уроки метода).

## 0. Принцип: GIT-NATIVE история (владелец 2026-07-05)

**Git И ЕСТЬ история версий — файлов И кода — и это лучше текстовых `history_*.md`.** Используем на полную:
- **История файла** = `git log --follow <file>` · `git blame` · `git show <commit>` · `git diff` (не рукописный `history_*.md`, который гниёт и дублирует).
- **Решение→факт (почему)** = **commit-сообщение** (тело: что/почему/возможные регрессии/`F#`). Атомарные коммиты + дисциплина сообщений = теперь ЧАСТЬ процесса, не опция.
- **Нарратив сессии** (много файлов) = `_sessions.md`. **Кросс-сессия/резюме/решения владельца** (чего в git нет) = память.
- **Обзор/ревью** = PR + `git blame`; **версии** = tags.

Следствие: `history_*.md` больше НЕ ведём. Скилы перепрошиты git-native баннером (все 9). Авторитет процесса = память `project-workflow-canonical`.

## 1. Ремап удалённого `docs/dev/` (source-of-truth)

Всё, что скилы/процесс писали и читали в `docs/dev/`, переезжает в git-tracked дома. **Скилы обязаны обновить пути** (см. §5).

| Было (`docs/dev/`, удалён) | Стало (git-tracked / память) |
|---|---|
| `workflow.md` (канон процесса, 5 вопросов/4 уровня) | память `project-workflow-canonical` + `project-rules` |
| `threat_landscape.md` (угрозы) | **`06_threat_catalog.md`** (IN/OUT/§F/§G/§H) |
| `testing_strategy.md` | **`03_testing_plan.md`** |
| `audit/AUDIT.md`, `audit/global.md` (D#/G#) | **`02_findings.md`** (F#) — единый реестр; X1 закрыт этим |
| `history_*.md` (решение→факт по модулям) | **`_sessions.md`** (журнал сессий) + commit-сообщения + память |
| `architecture.md` | `README.md` + `docs/roadmap/` |
| `docs/dev/tools/<X>` (зеркало кода) | **новый** `docs/<...>` при I8 (публикуемые доки) — пока НЕ существует |
| `session_*_pipeline_tests.md` | `03_testing_plan.md` + `_sessions.md` |

**Следствие для «history-first»:** решение→факт теперь пишем в `_sessions.md` + commit, НЕ в `docs/dev/history_*.md`. Правило процесса переопределено (обновить память `project-workflow-canonical`).

## 2. Файлы, которых НЕТ и которые нужны для честного закрытия

> **Перепроверено по диску S24 (2026-08-15).** Таблица датировалась Сессией 14 и к моменту проверки
> врала на 5 строк из 8: перечисленное как «нет» давно построено. Файл, называющий себя мерой
> честного закрытия, сам был самым устаревшим артефактом среза — это и чинится здесь.
> Строки-«есть» оставлены вычеркнутыми: важно видеть, что гейт пройден, а не что его не было.

### Гейт L2 (Beta — repo стоит сам, воспроизводим)
| Файл | Зачем / DIM | Статус на S24 |
|---|---|---|
| ~~`LICENSE`~~ | публичный репо без лицензии = «all rights reserved» (F19); DIM-9 | ✅ есть |
| `SECURITY.md` | канал репорта уязвимостей [M8]; DIM-9 | ⬜ нет — I8 |
| `CONTRIBUTING.md` | как контрибьютить [M8]; DIM-9 | ⬜ нет — I8 |
| ~~`.github/workflows/ci.yml`~~ | линт+типы+тесты+security-scan на PR (F13); DIM-6/8 | ✅ есть, и вырос: **6 джоб = 8 хард-проверок** (`ruff`, `mypy`, `pytest`+порог покрытия, `bandit -ll`, `pip-audit`, `conformance`, `comment-guard`, `gitleaks`). `pip-audit` — хард-гейт и в шапке файла тоже (`F121`) |
| ~~`.pre-commit-config.yaml`~~ | ruff/mypy/bandit локально до пуша; DIM-6 | ✅ есть |
| ~~`conftest.py` + pytest-раннер~~ | канонический раннер; DIM-6 | ✅ есть: `tests/conftest.py` + `tests/test_suites.py` (наборы находятся сами, F50) |
| ~~`ruff`/`mypy` секции (в `pyproject`)~~ | типы/линт (I4); DIM-6 | ✅ есть; `mypy` зелёный без исключений (`F88`) — на **81 файле** после сноса мёртвой P-оси (было 87 до волны 2.6) |
| `CHANGELOG.md` | история релизов; DIM-9 | ⬜ нет — I8 |

### Гейт L3 (Production-candidate)
| Файл | Зачем / DIM | Воркстрим |
|---|---|---|
| `Dockerfile` + `.dockerignore` | портируемость, non-root USER, read-only (DIM-8/5) [M8] | I3 |
| `RELEASING.md` + OIDC-publish | supply-chain, без registry-токенов [M8]; DIM-9 | I8 |
| OAuth 2.1 модуль (`core/auth/`?) | Resource Server + PKCE (DIM-2, D3) [M3] | I6 |
| ~~write-allowlist-конфиг~~ | default-deny (F34, §F); DIM-5 | ✅ построен, но НЕ отдельным `config/write_policy.yaml`: список объявлен в `config/firewall.yaml → write_allowlist` (имя И содержимое), исполняет `core/write_policy.py`. Файла с таким именем не будет — строка описывала форму, а не требование |
| provenance-обёртка вывода | недоверенный workspace (F33/OUT1); DIM-4 | 🔶 частично: `core/contracts/untrusted.py` метит чужой текст на чтении файла, но результаты поиска идут БЕЗ пометки (F87) |
| observability (`structlog` + `/health` + audit-trail) | DIM-7 [M2] | I5 |
| ~~`tests/agent_swarm/test_agent_swarm.py`~~ | раннер по `patterns.yaml` (F32); DIM-6 | ✅ построен S24: 34 исполнителя против живого сервера, **29 защищено / 4 известные дыры** (счёт исправлен после `F110`: защита `415` показывала ложное «защищено») |
| systemd unit / deploy-hardening (`06 §G.1`) | seccomp/Landlock/cap-drop; DIM-5 | I6 |

### Гейт L4 (Enterprise)
| Файл/условие | Зачем / DIM |
|---|---|
| ~~conformance в CI (`modelcontextprotocol/conformance`)~~ | DIM-1 [M4] — ✅ джоба `conformance` есть (S24, T7); прогон 30 сценариев: 7 применимых зелёные, 23 неприменимых объявлены поимённо. Остаток к L4 — сами возможности спеки (resources/prompts/logging/completion/sampling/elicitation) |
| quotas/budgets (`config/*` + governors) | эконом-контейнмент (DIM-11) [M2] |
| продукт e2e (media→pipeline→video) | DIM-12 (P1–P7) |
| `docs/` публикуемые (architecture/API, зеркало tools) | DIM-9 (I8, замена удалённого docs/dev/tools) |

## 3. Exit-чеклист по проходам (гейтам)

**Проход-0 (Фундамент → repo самодостаточен без docs/dev):** ✅ ремап §1 зафиксирован · LICENSE выбран · `.github/workflows/ci.yml` гоняет линт+тесты · pre-commit · pytest-раннер · README=факт (✅). **Критерий: клон репо собирается и тестируется без единой ссылки на удалённый `docs/dev`.**

**Проход-L2:** DIM-3/8/10 держат L2 · repo-гигиена (LICENSE/SECURITY/CONTRIBUTING) · тесты в CI зелёные · baseline-наборы (structure 35/35 и пр.) в CI.

**Проход-L3:** 3 самых красных измерения подняты — **auth (DIM-2 L0→L3)**, **outbound (DIM-4 L0→L2+)**, **observability (DIM-7 L0→L3)** · write-allowlist+deploy-hardening (DIM-5) · agent-swarm-раннер + E-матрица зелёные · Docker.

**Проход-L4:** conformance-gate · quotas · продукт e2e · публикуемые доки.

### 3.1 Привязка ревью-проходов к измерениям зрелости (Сессия 15)

«Проход» двузначен: **гейт зрелости** (L2/L3/L4 выше) И **ревью-прогон сессии** (3 оси из README «Механика»). Оба теперь явно привязаны к измерениям `07` — прогон закрывается по тем DIM, которые он держит.

| Ревью-прогон (README) | Скилы | Держит измерения | Находки |
|---|---|---|---|
| **`quality`** | `code-quality` | **DIM-13** (адгезия/качество) + DIM-10/1 (контракт-parity G14/D30) | F# в `02` |
| **`style/structure`** | `project-conventions` + `anti-hardcode` | **DIM-14** (анти-хардкод) + **DIM-15** (размещение/структура/стиль) | F# в `02` |
| **`systems/security`** | `security-reviewer` + `test-master` | **DIM-3/4/5** (покрытие) + **DIM-16** (безопасность-паттерн) + DIM-6 (тесты) + DIM-1/10 (протокол/контракты) | F# в `02` |

Прогон **честно закрыт** только когда его DIM подтверждены по §6 (Definition-of-Closed), а не «пробежались глазами».

## 4. Сильные GitHub-репо → уровень / стиль / структура / правила

Готовые решения, привязанные к нашему (Python+Pydantic, thin-wrapper→ops→engine, декларатив-by-design, русские комменты, `tools/<group>`+`core/providers/<p>`, контракт ToolResult/ErrorDetail+реестр реакций). Дополняет `04_github_adoption.md`.

| Репо | Берём | Поднимает | Как ложится на наш стиль/структуру/правила |
|---|---|---|---|
| **`modelcontextprotocol/conformance`** [M4] | conformance-тесты как CI-гейт | DIM-1 | добавить job в `ci.yml`; ассертить наш MCP-boundary (parity G14/D30) |
| **`github/github-mcp-server`** [M9] | эталон структуры продакшн-MCP: auth, layout, тесты, релиз | DIM-2/6/8/9 | сверить наш `tools/<group>` layout (A2) и auth-модуль (I6) с их; адаптировать, не копировать |
| **`modelcontextprotocol/servers`** (filesystem/др.) [M1] | паттерны safe-path, permission-scoping, аннотации | DIM-3/5 | усилить `core/paths.safe_resolve` + `destructiveHint`; наш firewall — сверх их |
| **MCP Python SDK** (official) | OAuth 2.1 Resource Server + PKCE helper'ы [M3] | DIM-2 | обернуть в `core/auth/`; сохранить контракт ToolResult |
| **awesome-lists** (appcypher/punkpeye/wong2), `best-of-mcp-servers` [M1] | точечно найти сильный репо по категории (FS/tables/media) | все | брать 1 эталон на измерение, сверять с нашими правилами размещения |
| **75-point security checklist / MCP-38** [M5] | пункты аудита → тест-паттерны | DIM-3/4 | уже влиты в `06` (IN/OUT) + `patterns.yaml` |

**Правило адаптации (наше):** любое готовое решение проходит через `project-conventions` (размещение/стиль) + `anti-hardcode` (декларатив, не хардкод) + `security-reviewer` (обе стороны) ДО вливания. Не «скопировать репо», а «взять паттерн и уложить в закон размещения §5».

## 5. Реконсиляция скилов (docs/dev-ссылки протухли)

Все 9 скилов ссылались на удалённый `docs/dev/`. **✅ РЕКОНСИЛИРОВАНО 2026-07-05:** во все 9 вставлен **git-native баннер** (перекрывает ссылки ниже: история=git, write-back=commit+`_sessions.md`, угрозы→`06`, тесты→`03`, findings→`02`, процесс→память); «источниковые» пути в описаниях/телах свопнуты (`threat_landscape`→`06`, `testing_strategy`→`03`, `workflow`→память, `audit`→`02`). Остаточные `history_*`/`session_*`/`architecture` упоминания перенаправляет баннер на git. Первый пункт Прохода-0 закрыт.

## 6. Честно закрытый проход — Definition-of-Closed (владелец, Сессия 15)

Проход (гейт зрелости ИЛИ ревью-прогон §3.1) считается **ЗАКРЫТЫМ** только когда пройдены все три
ступени по ДОКАЗАННОМУ состоянию. Порядок жёсткий: обмер → адаптация → двойное подтверждение.

**Ступень A — полный обмер (не выборка).**
- Пройтись по **ВСЕМ файлам** зоны прохода, выявить **ВСЕ возможные проблемы**: архитектурные
  аномалии + прочие нарушения паттернов (словарь `D#/G#`, скилы `DIM-13…16`).
- Для каждой затронутой **системы** зафиксировать **«что есть сейчас» против «что должно быть»**
  на серьёзной зрелости (целевые уровни `07`).
- Всё найденное → `02_findings.md` (F#), с привязкой к DIM.

**Ступень B — готовые решения с GitHub (адаптировать, где возможно и необходимо).**
- Подобрать сильное готовое решение (§4 / `04_github_adoption.md`) и уложить под нас **правилом
  адаптации §4** (через `project-conventions` + `anti-hardcode` + `security-reviewer`, не «скопировать репо»).
- **ИСКЛЮЧЕНИЕ — БЕЗОПАСНОСТЬ:** паттерны угроз и механизмы фиксов **уже собраны** (`06` IN/OUT/§F/§G/§H
  + `patterns.yaml`). Повторно искать/адаптировать с GitHub по безопасности **НЕ нужно** — берём из `06`.

**Ступень C — двухступенчатое тестовое подтверждение (обе обязательны).**
- **C1 — простые тесты** (`tests/quick/`, unit/contract): подтверждение теории **ЧЕРЕЗ КОД** —
  гипотеза находки/фикса воспроизведена и закрыта на уровне кода + контракта/реакции.
- **C2 — тесты-симуляции** (`tests/*_injection`, `render_*`, `agent_swarm`): **ФИНАЛЬНОЕ**
  подтверждение — используют **РЕАЛЬНЫЕ инструменты** против живого сервера.
  - **Требование C2 (владелец):** симуляциям нужен **доступ на ЧТЕНИЕ КОНСОЛИ** (stdout/логи
    сервера) — иначе реальный эффект не наблюдаем и подтверждение неполно. Это capability
    тест-харнесса (стык `03_testing_plan.md`; пересекается с DIM-7, но здесь — как условие закрытия).
- Проход закрыт **ТОЛЬКО** когда C1 **и** C2 зелёные: C1 без C2 = теория без реальности; C2 без
  C1 = эффект без доказанной причины.

> Формула: **обмер всех файлов → now-vs-target → адаптировать GitHub (кроме sec) → простые тесты
> (код-пруф) → симуляции с чтением консоли (реал-пруф) → проход закрыт.**
>
> Связь с зрелостью: проход поднимает свои DIM (§3.1) в `07` **только** после ступени C. До неё —
> уровень остаётся прежним, даже если «работа сделана».


## 7. Сплошной проход S24 — закрытие (2026-08-18)

> Проход шёл по директиве владельца: пройти по **каждому** файлу планов, по всем сессиям и по коду —
> доделать несделанное, снять устаревшие статусы, попутно ловить живые дефекты. Журнал хода —
> [`18_full_pass.md`](18_full_pass.md); здесь только итог: что вошло, чем закрыто, что перенесено.

### 7.1 Что вошло и чем закрыто

| Волна | Что | Чем закрыто (пруф) |
|---|---|---|
| 1 — сверка планов с диском | пять срезов документов против кода (`00`/`01`/`07`/`08` · `02`/`03`/`14`/`16` · планы подсистем · спеки · все сессии) | статусы приведены к диску; вскрыты `F88` (`mypy` прятал 35 ошибок за нерабочим исключением) и `F89` (`bandit` красный). Самая урожайная категория — «сделано, но нигде не записано»: подсистема `core/runner/` не значилась ни в одном верхнеуровневом файле |
| 2.1 | рой adversarial-агентов (`F32`) | `tests/agent_swarm/` — 34 исполнителя против живого сервера, 29 защищено / 4 известные дыры; устроен против театра (паттерн без исполнителя и «сработавший `xfail`» красят гейт) |
| 2.2 | слепые зоны (`T10`, `F55`) | обе зоны закрыты, мутации `M99`–`M101` краснеют; попутно `F90`, `F91` |
| 2.3 | структура `B5` (`F25`/`F26`/`F27`) | адрес считает один `address_for`, якорь берётся из `role`, `structure_reconcile` откатывает пакет целиком; закрыты `F25`, `F26`, `F27`, `F92`–`F96`, `F100` |
| 2.4 | E-матрица (`T4`) | `tests/structure_emulation/` — 54 проверки, метаморфный инвариант «3 прохода ≡ 2 ≡ 1»; в первый же прогон нашла `F101`, `F102` |
| 2.5 | conformance (`T7`) | джоба `conformance` в `ci.yml` + `tests/conformance/`; прогон 30 сценариев спеки — 7 применимых зелёные, 23 неприменимых объявлены поимённо. Сторонний прогон нашёл `F103` (DNS-rebinding) |
| 2.6 | мёртвая P-ось (`F3`/`F10`) | снесено **507 строк** (4 адаптера + пакеты `ffmpeg/`, `stt/`); `F10` закрыт удалением, живой путь отказа закрыт регрессией, попутно `F105` |
| 2.7–2.9 | гейт и пуш | `mypy`/`bandit` починены, 28 коммитов уехали в `origin`, CI зелёный; `T2` переведён в проверяемую форму — 5 классов отравления манифеста, 88 ассертов |
| 3.0 | браузерный класс угроз | восемь угроз, каждая воспроизведена на живом сервере: `F106`, `F107` — настоящие дыры, `F108`, `F109` — слабости, четыре безопасны С ПРУФОМ и закреплены ассертами; `F110` — урок метода |
| 3.0б | храповик против мусора ИИ | `scripts/comment_guard.py` в репозитории, пофайловый потолок в git, джоба `comment-guard` в CI; `F120`, `F121` |
| 3.0в | прополка | 93 замечания в 47 файлах → **0 в 112 файлах при потолке 0**, по коду −446 строк; шесть дыр в самом стороже (`F122`–`F127`) и два дефекта кода (`F128`, `F129`) |
| 3.1 | свод итога | этот раздел + [«Переоценка S24»](07_maturity_rubric.md#переоценка-s24-2026-08-18--итог-сплошного-прохода) |

### 7.2 Находки прохода (`F85`–`F129`): 37 всего, 31 закрыта, 6 открыты

Счёт снят скриптом по строкам реестра [`02`](02_findings.md), а не переписан из прошлой шапки.
Волна 3 отдельно: `F103`–`F129` — **19 находок, 14 закрыто, 5 отложено**.

| Находка | Sev | Почему отложена (осознанно, не забыта) |
|---|---|---|
| `F87` | 🟠 | провенанс не доехал до результатов поиска. Чинится не в инструменте, а в сборке ответа поиска — это класс «чужой текст наружу», а не разовая правка. Адрес: `B3` остаток / `I6` outbound |
| `F122` | 🟠 | храповик слеп ко всему за пределами фиксированного `TARGETS`. Правка меняет **область замера** — если сделать её посреди волны, потолок сдвинется вместе с областью и перестанет что-либо значить. Делать отдельным шагом, с переблагословлением потолка в том же коммите |
| `F123` | 🟠 | строка-данные с закрывающей кавычкой сбивает построчный разбор. Узкой правки нет: корень в самом способе разбора → переход на `ast` (`get_docstring` + `end_lineno`) |
| `F124` | 🟡 | `#` внутри строки-данных считается комментарием. Тот же корень: комментарии надо брать из `tokenize.COMMENT`, а не по `startswith('#')` |
| `F126` | 🟡 | главный класс мусора — «дубль декларации прозой» — regex'ом не механизируется; наивное правило покрасит правильный однострочник. Ловится длиной и ревьюером; записано в `project-conventions`: **молчание сторожа не аттестат чистоты** |
| `F127` | ⚪ | `--scan` по абсолютному пути вне репозитория падает сырым `ValueError` — ломается ровно тот режим, которым сторожа проверяют мутацией. Правка на строку, идёт вместе с `F122` |

`F122`+`F123`+`F124`+`F127` — один пакет работы: перевести сторожа на `ast`+`tokenize` и на цели из
`git ls-files '*.py'`. Это ось 1a `code-quality` (механизм из библиотеки, наш слой — политика) и
**минус строки**, а не плюс.

**Пакет закрыт в тот же день** (`5090441`, 2026-08-18): сторож переведён на `ast`+`tokenize`, цели —
`git ls-files`, пересказ декларации ловит рычаг «>3 строк вида ключ-значение»; регрессия
`tests/quick/test_comment_guard.py` (17 проверок) краснеет на старом стороже. Отложенным из этого
списка остаётся только `F87`. В сам реестр `02_findings.md` строки попали не сразу, а 2026-08-21,
когда владелец снял заморозку доков, — три дня находки жили в одних commit-сообщениях, и это
чуть не стоило выдачи занятого номера новой находке.

### 7.3 Хвосты, которые проход НЕ закрывал — перенесены поимённо

Собраны разведкой волны 1.5 из `_sessions.md` целиком ([`18` §«Хвосты сессий»](18_full_pass.md)).
Проход их не брал: они не про «доделать несделанное по планам», а про новую работу.

| # | Что | Куда перенесено |
|---|---|---|
| Х1 | поиск по ID в таблицах (`table_find_by_id` без `sheet`) ждёт квалифицированного `row_id` формата `<OWNER>.<SHEET>.<SEQ>` → `F57` | [`16`](16_execution_matrix.md) **B7** (качество поиска и контракт), спека — [`13`](13_a1_table_loader_plan.md) §S18-b |
| Х2 | два инструмента поиска вместо шести проекций одной модели запроса → `F58`; директива S18-d записана, `tools/search/` не тронут | [`16`](16_execution_matrix.md) **B7** |
| Х3 | остаток `A7`: листы `SCENE_ASSET_SEQUENCE`/`SCENE_UNIQNESS_ROLLUP` (A7.1) и петля сигнал→решение→память (A7.5) — ни одного имени листа на диске | [`10`](10_pattern_uniqueness_system.md) §A7.1/A7.5, воркстрим `A7` в [`01`](01_master_roadmap.md) |
| Х4 | `F74` — снос временного секрет-механизма (`.env` + калитка `MCP_ALLOW_NO_AUTH`); маркер на месте, снос не начат | **вопрос владельцу** (цикл разработки кончился?) → затем [`15`](15_security_system_plan.md) §S1 / `S1′`, DIM-2 |
| Х5 | `F31` — нет eval-слоя качества поиска; не подбирался предметно 9 сессий | [`16`](16_execution_matrix.md) **B7**, [`03`](03_testing_plan.md) |
| Х6 | связь ассетов с листом `SCENES` (`scene_id` уже в имени файла); риск сделать дважды с Х3 | [`10`](10_pattern_uniqueness_system.md) — **брать вместе с Х3**, не отдельно |
| Х7 | «волна 2 остальное» (T10 · B5 · T4 · T7 · P-ось) | **закрыт проходом** — см. §7.1, волны 2.2–2.6 |
| Х8 | статусы блоков в [`16`](16_execution_matrix.md) датированы S23: `B5` числится «не начат» при закрытых `F25`/`F26`/`F27`, `B7` — `T7` при закрытом conformance | снять при первом же касании `16`; проход это видел, но матрица — не его зона |

### 7.4 Уроки метода — четыре, которые стоили дороже всего

Не «как надо стараться», а проверяемые правила: каждый выведен из случая, где сделанное «правильно»
оказалось нерабочим.

| Урок | Откуда | Правило |
|---|---|---|
| **Выжившая мутация = тест не добивает** | `M113`: вырезали откат `structure_reconcile` — ассерт «каталог вернулся на место» остался ЗЕЛЁНЫМ, потому что сценарий падал ДО первого переноса, и откатывать было нечего | компенсацию проверяет только падение на **второй** сущности. Приёмка фикса — не «тест зелёный», а «мутация краснеет», и мутацию надо ставить туда, где механизм реально работает |
| **Два источника одной правды работают наполовину — ищи ЧИТАТЕЛЯ** | `F99` (мёртвая читалка `required_parent_types` рядом с живым `REQUIRED_PARENT_TYPE`), `F100` (`KNOWN_ERROR_CODES` разошёлся с `server_reactions.yaml` на три живых кода), `F121` (шапка `ci.yml` врала про собственные джобы) | у объявления должен быть ровно один читатель. Нет читателя — это не декларация, а комментарий; два читателя — они разойдутся, вопрос только когда |
| **Утверждение документа проверяется ЗАПУСКОМ** | `F88` (`mypy`-исключение не работало и прятало 35 ошибок при «гейт зелёный»), `F103` (дыру нашёл сторонний прогон, а не наш аудит, писавший «нам это не грозит») | «нам это не грозит» обязано нести пруф и дату, иначе работает как выключатель проверки. Сверять с диском командой, а не чтением соседнего документа |
| **Новая защита на раннем слое молча обесценивает тесты поздних** | `F110`: `415` встал раньше парсера, бомба вложенности в рое стала получать `415` вместо `200`, и набор отрапортовал «защищено» там, где дыра (`F36`) осталась | после каждой новой защиты — перепроверить, не ослепила ли она чужую проверку. Записано в `security-reviewer` как MUST DO |

Пятый, помельче, но повторился трижды: **красный тест на НОВОМ наборе — сначала подозревай сценарий,
а не продукт** (E-C, `Fal:bg_removals`). Оговорка из того же прохода: подозрение снимается **уликой**
(`checks.json` эталонного набора), а не догадкой — в случае `dns-rebinding-protection` виноват был именно
продукт.

### 7.5 Закрыт ли проход по §6 (Definition-of-Closed)

| Ступень | Состояние |
|---|---|
| **A — полный обмер** | ✅ волна 1: все файлы планов, все сессии, код по срезам; найденное — в [`02`](02_findings.md) с привязкой к DIM |
| **B — готовые решения с GitHub** | ✅ взято одно и по правилу §4: `@modelcontextprotocol/conformance` уложен под нас (харнесс поднимает сервер, прокси добавляет ключ, копия конфига с поднятым порогом частоты). По безопасности — исключение §6: берём из `06`, не с GitHub |
| **C1 — код-пруф** | ✅ `tests/quick/` + мутационная приёмка `M99`–`M123` на каждый закрытый дефект |
| **C2 — реал-пруф с чтением консоли** | ✅ `tests/harness/` даёт живой сервер и захват stdout; рой, E-матрица, conformance, `test_firewall`, `test_config_change` идут через него |

Проход закрыт. Уровень зрелости переоценён в [`07`](07_maturity_rubric.md) — **L2 с тремя карманами L3**;
что именно не хватает до L3, там же списком проверяемых условий.

**Источники [M#]** — см. `07_maturity_rubric.md` (M1–M9): MCP conformance/servers/github-mcp-server, OAuth 2.1 spec, 75-point checklist, repo-гигиена best practices.
