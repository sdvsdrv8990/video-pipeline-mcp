# 18 — Сплошной проход по планам, сессиям и коду (глобальная цель)

> **Это ЖУРНАЛ ПРОГРЕССА, а не план.** Его единственная задача — чтобы обрыв сессии не стоил
> начала с нуля. Любая следующая сессия начинается с чтения этого файла и берёт первый пункт со
> статусом ⬜. Правило: **шаг закрыт только после коммита**; незакоммиченный шаг считается не
> сделанным, даже если код на диске лежит.

## Цель (директива владельца S24)

Пройти по **каждому** файлу планов, по **всем** сессиям и по коду: доделать несделанное, снять
устаревшие статусы, актуализировать документы, чтобы не потерять уже сделанное, и попутно ловить
живые дефекты.

## Как это делается (метод прохода)

1. **Разведка — только на чтение и параллельно.** Утверждения документов сверяются с диском
   ЗАПУСКОМ (`ls`/`grep`/живой прогон), а не чтением соседнего документа. Разведка ничего не
   правит: правки в одном дереве несколькими руками = потерянные правки.
2. **Правки — последовательно и мелкими шагами.** Один шаг = один смысл = один коммит.
3. **Три типа находок разведки, и третий важнее первых двух:**
   - заявлено сделанным, а кода нет → доделать либо снять статус;
   - заявлено открытым, а уже закрыто → записать, иначе работа делается дважды;
   - **сделано, но нигде не записано** → это то, что теряется при смене сессии.
4. Каждый закрытый дефект получает регрессию, **краснеющую при откате фикса** (мутация).
5. Метод сессии — из RESUME: `/engineering-questions` → домен-скил → фикс → тест → `F#` в
   [`02`](02_findings.md) → коммит → запись сюда и в [`_sessions`](_sessions.md).

## Ход прохода

Статусы: ⬜ не начато · 🔨 в работе · ✅ закрыто (с коммитом).

### Волна 1 — сверка планов с диском

| # | Срез | Что входит | Статус | Коммит |
|---|---|---|---|---|
| 1.1 | Верхний уровень | `00` reality-check · `01` мастер-роадмап · `07` зрелость · `08` закрытие прохода | ⬜ | — |
| 1.2 | Находки и тесты | `02` реестр `F#/D#/G#` · `03` тест-план · `14` тест-система · `16` матрица | ⬜ | — |
| 1.3 | Продукт | `05` данные/шаблоны/медиа · `10` уникальность · `13` загрузчик таблиц · `17` раннер | ⬜ | — |
| 1.4 | Требования и защита | `06` угрозы · `09` оргмодель · `11` требования · `12` распил A2 · `15` безопасность | ⬜ | — |
| 1.5 | Сессии | `_sessions.md` целиком: заявленное по сессиям против кода | ⬜ | — |

### Волна 2 — доделать несделанное (порядок по риску, уточняется разведкой)

| # | Что | Почему | Статус | Коммит |
|---|---|---|---|---|
| 2.1 | Рой `agent_swarm` (`F32` 🔴) | 36 паттернов в `patterns.yaml` и ни строчки раннера; красная команда против уже построенных защит. Разблокирован харнессом T2 | ⬜ | — |
| 2.2 | `T10` — закрыть слепые зоны `F55` | мутационная приёмка: тест зоны безопасности принимается только после проверки откатом | ⬜ | — |
| 2.3 | `B5` — структура `F25`/`F26`/`F27` | реестр ↔ ФС расходятся молча (класс «немой потери») | ⬜ | — |
| 2.4 | `T4` — E-матрица структуры | метаморфный инвариант «3 прохода ≡ 2 ≡ 1» | ⬜ | — |
| 2.5 | `T7` — conformance в CI | паритет уже закрыт (S24), осталась сама джоба | ⬜ | — |
| 2.6 | P-ось — облачные провайдеры (`F3`/`F10`) | 12 честных `NotImplementedError`; локальная сторона работает | ⬜ | — |

### Волна 3 — закрепить

| # | Что | Статус | Коммит |
|---|---|---|---|
| 3.1 | Свести итог прохода в `07` (уровни зрелости) и `08` (закрытие прохода) | ⬜ | — |
| 3.2 | Обновить память проекта под новое состояние | ⬜ | — |

## Закрыто до начала этого журнала (S24, чтобы не переоткрывать)

`F76` (фаза ТАБЛИЦЫ падала при любом вызове) · `F83` (каталоги-призраки) · `F51`/`T2`/`B1`
(харнесс живого сервера, периметр вошёл в гейт) · план `17` целиком (раннер, R1–R6) ·
паритет `G14`/`D30` — проверен живьём и оказался ЗАКРЫТЫМ (`R-CONTRACT3` выполнен).
