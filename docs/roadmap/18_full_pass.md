# 18 — Сплошной проход по планам, сессиям и коду (глобальная цель)

> **Это ЖУРНАЛ ПРОГРЕССА, а не план.** Его единственная задача — чтобы обрыв сессии не стоил
> начала с нуля. Любая следующая сессия начинается с чтения этого файла и берёт первый пункт со
> статусом ⬜. Правило: **шаг закрыт только после коммита**; незакоммиченный шаг считается не
> сделанным, даже если код на диске лежит.

## Цель (директива владельца S24)

Пройти по **каждому** файлу планов, по **всем** сессиям и по коду: доделать несделанное, снять
устаревшие статусы, актуализировать документы, чтобы не потерять уже сделанное, и попутно ловить
живые дефекты.

## Как это делается (метод прохода)

1. **Разведка — только на чтение и параллельно.** Утверждения документов сверяются с диском
   ЗАПУСКОМ (`ls`/`grep`/живой прогон), а не чтением соседнего документа. Разведка ничего не
   правит: правки в одном дереве несколькими руками = потерянные правки.
2. **Правки — последовательно и мелкими шагами.** Один шаг = один смысл = один коммит.
3. **Три типа находок разведки, и третий важнее первых двух:**
   - заявлено сделанным, а кода нет → доделать либо снять статус;
   - заявлено открытым, а уже закрыто → записать, иначе работа делается дважды;
   - **сделано, но нигде не записано** → это то, что теряется при смене сессии.
4. Каждый закрытый дефект получает регрессию, **краснеющую при откате фикса** (мутация).
5. Метод сессии — из RESUME: `/engineering-questions` → домен-скил → фикс → тест → `F#` в
   [`02`](02_findings.md) → коммит → запись сюда и в [`_sessions`](_sessions.md).

## Ход прохода

Статусы: ⬜ не начато · 🔨 в работе · ✅ закрыто (с коммитом).

### Волна 1 — сверка планов с диском

| # | Срез | Что входит | Статус | Коммит |
|---|---|---|---|---|
| 1.1 | Верхний уровень | `00` reality-check · `01` мастер-роадмап · `07` зрелость · `08` закрытие прохода | ✅ | `9bf6d71` |
| 1.2 | Находки и тесты | `02` реестр `F#/D#/G#` · `03` тест-план · `14` тест-система · `16` матрица | ✅ | `3f094bd` |
| 1.3 | Продукт | `05` данные/шаблоны/медиа · `10` уникальность · `13` загрузчик таблиц · `17` раннер | ✅ | `984fda6` |
| 1.4 | Требования и защита | `06` угрозы · `09` оргмодель · `11` требования · `12` распил A2 · `15` безопасность | ✅ | `0b8fdba` |
| 1.5 | Сессии | `_sessions.md` целиком: заявленное по сессиям против кода | ✅ разведка; хвосты ниже | — |

**Что дала волна 1 (главное).** Категория «сделано, но нигде не записано» оказалась самой
урожайной: целая подсистема `core/runner/` не упоминалась НИ в одном верхнеуровневом файле;
`08_pass_closure` не правился с Сессии 14 и врал на 5 строк из 8; `F28`/`F30`/`F56`/`F59` были
закрыты ещё в S22 и не вычеркнуты; таксономия уехала в декларацию, а `F41`/`F46` описывали код,
которого больше нет. Отдельно: **гейт врал** — `mypy` прятал 35 ошибок (`F88`), `bandit` был
красным на непривязанных загрузках весов (`F89`).

### Волна 2 — доделать несделанное (порядок по риску, уточняется разведкой)

| # | Что | Почему | Статус | Коммит |
|---|---|---|---|---|
| 2.1 | Рой `agent_swarm` (`F32` 🔴) | 36 паттернов в `patterns.yaml` и ни строчки раннера; красная команда против уже построенных защит. Разблокирован харнессом T2 | ✅ 28 защищено / 5 известных дыр / 1 не достали | `9e2401c` |
| 2.2 | `T10` — закрыть слепые зоны `F55` | мутационная приёмка: тест зоны безопасности принимается только после проверки откатом | ✅ обе зоны закрыты, мутации M99–M101 краснеют; попутно `F90`, `F91` | `d8f44bf`, `b47a934` |
| 2.3 | `B5` — структура `F25`/`F26`/`F27` | реестр ↔ ФС расходятся молча (класс «немой потери») | ✅ закрыты `F25`, `F26`, `F92`–`F96` и `F27` целиком (шаги 2–3: адрес считает один `address_for`, якорь из `role`, `structure_reconcile` с откатом пакета) | `158150c`…`2c91254` |
| 2.4 | `T4` — E-матрица структуры | метаморфный инвариант «3 прохода ≡ 2 ≡ 1» | ✅ `tests/structure_emulation/` — 54 проверки (E-A/E-B/E-C) против живого сервера; инвариант держится, попутно найдены и закрыты `F101`, `F102`; мутации `M114`–`M116a` | `995e0f1`, `70b5169` |
| 2.5 | `T7` — conformance в CI | паритет уже закрыт (S24), осталась сама джоба | 🔨 шаг 1 сделан: схемная регрессия своими силами (`F98`) + `F97` на проводе. Осталась джоба; разведка: инструмент жив (`0.1.16`, HTTP — наш транспорт подходит), но 25 из 30 сценариев проверяют не нас | `91a5358`, `6296d9f` |
| 2.7 | **Гейт врал** — `mypy` прятал 35 ошибок (`F88`), `bandit` красный на загрузке весов (`F89`) | ✅ все локальные гейты зелёные | `aa9f972`, `3f1d859` |
| 2.8 | **Работа двух сессий не проверена CI и не запушена** | ✅ запушено (28 коммитов), CI зелёный на `069fb5a` (прогон `31907640977`). Реальный CI вскрыл три вещи, невидимые локально: пин `python_version` ломал mypy на 3.12-раннере, `pip-audit` нашёл уязвимый `setuptools` (лок его не лечит — CI ставит `-e .[dev]`), а `test_providers` зависел от весов вне git (`F91`) | `b47a934` |
| 2.9 | `T2` в проверяемой форме — отравление манифеста и скилов | «Описания статичны» было счётным утверждением: клиент кладёт 68 описаний в контекст с полным доверием, а проверял их только эталон — то есть ловил ИЗМЕНЕНИЕ, но не отравление с первого дня | ✅ пять классов в `test_tools_inventory` (88 ассертов), мутации `M111`–`M111d`; манифест чист | `2454fd6` |
| 2.6 | P-ось — облачные провайдеры (`F3`/`F10`) | ~~12 честных `NotImplementedError`~~ — разведка показала: это не стабы, а **мёртвый код** (4 адаптера, ноль вызывающих, невозможно подключить). Честность держат живые `PROVIDER_ADAPTER_MISSING`/`PROVIDER_KEY_MISSING` | 🔨 разведка сделана: снос ≈300 строк + тест G16 на живом пути; `F10` снимается удалением | — |

### Что дала разведка волны 2.3–2.6 (четыре разведчика на чтение, параллельно)

Каждый шёл со своим паком скилов и доказывал утверждения ЗАПУСКОМ, а не чтением документов.
Улов — шесть новых находок, все в [`02`](02_findings.md): `F93` (обратный проход целостности
слеп к ветке группировки — значит `F25` зелёный только на неветвящемся дереве), **`F94` 🔴**
(`parent_path` без слэша молча создаёт каталог-двойник, целостность при этом зелёная),
`F95` (объявленный `DUPLICATE_PATH` недостижим — клиент получает `INTERNAL_ERROR` и «нужен
человек» вместо готового рецепта), `F96` (ребёнок чужого уровня исчезает без слова), `F97`
(`structuredContent: null` в проводе — схему спеки не проверяет ничто), `F98` (эталон инвентаря
фиксирует контракт «как есть»: три схемы в нём объявлены неполно).

Плюс три устаревших пруфа сняты по факту: `F27` ссылался на строку `server.py`, которой нет
(код с S18 в `tools/structure/`); `F3` называл стабами мёртвый код; `F10` — хардкод в коде,
который никто не исполняет, и снимается он удалением, а не правкой.

**Шесть из шести закрыты в тот же проход** (`F93`–`F98`), каждая с мутационной приёмкой
M104–M110a. Два урока для метода: (1) регрессия соседа поймала мою же ошибку до коммита —
первая версия спуска по объявлению потеряла пропущенный уровень иерархии, и §17 покраснела;
(2) `F98` показал, чем эталон отличается от инварианта: сверка с эталоном ловит ИЗМЕНЕНИЕ
контракта, но эталон, собранный из кривых схем, зелёный. Инвариант считается из самих данных.

### Чем закрылся `F27` (шаги 2–3)

| Шаг | Что сделано | Урок |
|---|---|---|
| 2 | `_fill_container` → публичный `fill_container`, поверх него `TemplateEngine.address_for`; `Taxonomy.anchor_type()` читает `role: owner_channel` | Правило, применённое ровно один раз внутри своего создателя, — не декларация, а частный случай. Мутация `M113b` (снять `role` из шаблона) теперь краснеет: якорь действительно берётся из объявления |
| 3 | `structure_reconcile`: link + перенос на объявленный адрес, `needs_decision` с кандидатами вместо догадки, откат ВСЕГО пакета при падении на любой сущности (`RECONCILE_ROLLED_BACK`, `LinkRegistry.unlink`) | **Главный урок прохода:** мутация `M113` (вырезать откат) сначала ВЫЖИЛА — сценарий подсовывал помеху, которая срабатывала до первого переноса, и откатывать было нечего. Ассерт на компенсацию был зелёным при вырезанной компенсации. Проверять откат можно только падением на ВТОРОЙ сущности |

Попутно `F100`: `KNOWN_ERROR_CODES` разошёлся с `server_reactions.yaml` на три живых кода
(`PATH_NOT_FOUND`, `QUERY_NOT_FOUND`, `RECALC_UNAVAILABLE`) — штатный отказ поиска и пересчёта
поднимал `UserWarning`, а под strict-warning превращался в `INTERNAL_ERROR`. Инвариант повешен
регрессией: код из yaml обязан быть в наборе.

### Что дала E-матрица (волна 2.4)

Набор окупился в первый же прогон — двумя дефектами, которых unit-тесты увидеть не могли.

| Находка | Почему её не видел `test_structure` |
|---|---|
| `F101` — имя, легшее в ПУТЬ, не становилось связью: сервер одновременно сообщал «конкурент висит без канала» и «у канала нет конкурента», при том что каталог конкурента лежал ВНУТРИ каталога канала | unit проверял части по отдельности: путь считался верным, связь считалась верной, а противоречие между ними видно только в ответе клиенту целиком |
| `F102` — `structure_reconcile` искал якорь по ВСЕЙ области и предлагал каналы из чужих ниш; при единственном совпадении связь проставилась бы молча | unit работает с одной нишей в области — вторая ниша, которая и создаёт коллизию, появляется только в сценарии клиента |

Метаморфный инвариант **держится**: 1 проход ≡ 2 ≡ 3 по каноническому снимку ветки (сущности,
пути, висящие, childless). Уравнивает маршруты `structure_reconcile` — обе его половины
защищены мутациями: `M116` (не переносит) и `M116a` (не связывает) краснят по 2 ассерта.

**Ловушка театра, в которую я попал сам:** первые два прогона E-C краснели, и оба раза причиной
был ТЕСТ, а не продукт — незанормализованный путь самой ниши и слишком широкий ассерт целостности
(он ловил намеренных висящих из E-A3/E-B). Красный тест на новом наборе — сначала подозревай
сценарий, а не сервер; иначе «починишь» работающее.

### Волна 3 — закрепить

| # | Что | Статус | Коммит |
|---|---|---|---|
| 3.1 | Свести итог прохода в `07` (уровни зрелости) и `08` (закрытие прохода) | ⬜ | — |
| 3.2 | Обновить память проекта под новое состояние | ⬜ | — |

## Закрыто до начала этого журнала (S24, чтобы не переоткрывать)

`F76` (фаза ТАБЛИЦЫ падала при любом вызове) · `F83` (каталоги-призраки) · `F51`/`T2`/`B1`
(харнесс живого сервера, периметр вошёл в гейт) · план `17` целиком (раннер, R1–R6) ·
паритет `G14`/`D30` — проверен живьём и оказался ЗАКРЫТЫМ (`R-CONTRACT3` выполнен).

## Хвосты сессий (разведка 1.5 — обещания «доделаю в следующей», которые никто не подобрал)

Собраны из `_sessions.md` целиком. Пункты, уже стоящие в волне 2, помечены ссылкой — они не
потеряны, просто длинные. Остальные до этого журнала не были видны нигде одним списком.

| # | Откуда | Что осталось |
|---|---|---|
| Х1 | С19 доп.5 | Поиск по ID в таблицах (`table_find_by_id` без `sheet`) ждёт квалифицированного `row_id` формата `<OWNER>.<SHEET>.<SEQ>` (S18-b спроектирован, не реализован) → `F57` |
| Х2 | С19 (S18-d/e/f) | Два инструмента поиска вместо шести проекций одной модели запроса → `F58`; директива записана, `tools/search/` не тронут |
| Х3 | С22 доп.5/7 | A7 остаток: листы `SCENE_ASSET_SEQUENCE`/`SCENE_UNIQNESS_ROLLUP` (A7.1) и петля сигнал→решение→память (A7.5) — ни одного имени листа на диске |
| Х4 | С20 доп.1 | `F74` — снос временного секрет-механизма (`.env` + калитка `MCP_ALLOW_NO_AUTH`). Маркер на месте, снос не начат: **вопрос владельцу — цикл разработки кончился?** |
| Х5 | С15 обмер | `F31` — нет eval-слоя качества поиска; не подбирался предметно 9 сессий |
| Х6 | С22 доп.11 | Связь ассетов с листом `SCENES` (`scene_id` уже в имени файла) — возможно, частично закрывается A7.1; риск сделать дважды |
| Х7 | этот проход | Волна 2 остальное: [2.2](#) `T10` · [2.3](#) `B5` · [2.4](#) `T4` · [2.5](#) `T7` · [2.6](#) P-ось |
