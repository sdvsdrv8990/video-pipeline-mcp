# 17 — Раннер локальных моделей: план и что из него сделано

> Статус: **R1–R6 сделаны и проверены живьём (S24).** Раннер поднимается инструментом, считает
> модели вне сервера и падает отдельно от него; набор `test_providers` §17 зелёный (263/263).
> Шаги ниже оставлены как есть — они описывают ПОЧЕМУ так, а «что вышло» дописано в конце каждого.

## Зачем (решение владельца S24)

Сейчас локальные модели поднимаются **в процессе MCP-сервера**. Отсюда три беды, и ни одна не
лечится кэшем: падение или OOM в инференсе роняет сервер и рвёт туннель к Claude; рестарт сервера
теряет поднятые модели; тяжёлые зависимости (torch+rocm, ~3 ГБ) лежат в системе пользователя.

Решение: вынести инференс в **отдельный раннер**, который сервер поднимает **по команде через
инструмент**. Падение раннера сервер переживает, зависимости заперты в контейнере.

**Это не нарушает S15 («демона нет»)**: раннер не тикает сам, ничего не подхватывает в фоне и не
планирует задачи. Он поднимается явной командой и отвечает на запросы. Формулировка обязана
остаться в коде и доках, иначе через полгода это прочтут как отмену S15.

## Что берём готовым, а что — нет (решено на фактах, S24)

| Кандидат | Вердикт | Почему |
|---|---|---|
| **LitServe** 0.2.17 (Apache-2.0) | **не берём** | тянет `fastapi` + `uvicorn[standard]` + `pyzmq`; даёт батчинг и GPU-autoscaling — задачи, которой у нас нет (один пользователь, вызовы последовательные) |
| **BentoML** | **не берём** | платформа упаковки/мониторинга; вес несопоставим с задачей |
| **aiohttp** (уже в зависимостях) | **берём** | на нём уже стоит наш транспорт; раннер = ~80 строк без единой новой зависимости |
| `core/transport/tunnel.py` (наш, 421 стр.) | **берём как образец/основу** | это уже супервизор внешнего процесса: `Popen`, ожидание готовности, backoff, `status()`, `stop()`. Раннер — тот же класс задачи |
| `core/providers/installer.py` (наш, 179 стр.) | **берём приём** | фоновая работа + состояние на диске + прозвонка статуса |
| `core/providers/img/http_image.py` (наш) | **берём как клиента** | раннер говорит на том же контракте «файл → файл», что платные провайдеры |

Идеи из LitServe, которые стоит взять **без** самой библиотеки: единая точка загрузки модели
(`setup`) — у нас это уже пул; health-эндпоинт; аккуратное завершение.

## Контракт раннера (фиксируется первым — от него зависят все шаги)

Раннер — **обычный провайдер** в терминах сервера. Тогда он подключается блоком в
`config/providers.yaml → online.http` **без единого нового адаптера**, а переключение
«в процессе → в раннере» становится правкой строки канала (`table_update`).

- `POST /run` → `{"kind": "bg_removals", "model": "...", "source": "<путь>", "target": "<путь>", "params": {...}}`
  → `{"files": ["<путь>"], "compute": {...}}`
- `GET /health` → `{"ok": true, "pool": {...}}` — чем занят и что держит поднятым.
- **Обмен путями, а не байтами.** `workspace` монтируется в контейнер; гонять 4К-кадр в base64 на
  каждый вызов — лишние десятки мегабайт на ровном месте. Пути внутри раннера проходят тот же
  containment (`core/paths.safe_resolve`): раннер доверяет вызывающему не больше, чем сервер клиенту.
- Слушает **только** `127.0.0.1`; авторизация — токен в заголовке, выдаётся сервером при запуске
  через переменную окружения и живёт в памяти. Раннер за туннель не выставляется никогда.

## Кто формирует задачу и кто кладёт файл (проверено S24, инварианты закреплены тестом)

Вопрос владельца: раз работаем с Claude AI Web — не окажется ли, что задачу раннеру формирует
клиент, и не придётся ли потом перекладывать скачанный файл. **Проверено на диске — нет, и то и
другое уже делает сервер.**

- **Задачу собирает сервер.** `media_generate` принимает от клиента ровно пять полей:
  `table`, `resource_type`, `input`, `scene_id`, `video_slug`. **Ни адреса вызова, ни пути
  сохранения среди них нет** — тело запроса провайдеру (а завтра раннеру) сервер собирает сам из
  строки канала. Claude физически не может указать, куда серверу идти и куда писать. Для раннера
  это значит: он висит на loopback, и единственный, кто к нему обращается, — сервер.
- **Файл кладётся сразу в конечное место.** Промежуточного каталога и перемещения нет ни у
  локального адаптера, ни у загрузчика результата: путь вычисляется ДО вызова
  (`{table}/{dir из декларации}/{имя по шаблону}`), проходит `write_policy` и containment, каталог
  создаётся заранее, адаптер пишет прямо туда.
- **Что именно выбирает ИИ:** сущность (`table`) и имя (`scene_id`/`video_slug`) — то есть «в какой
  проект». Раскладку внутри сущности и расширение диктует декларация `assets`, а не вызов. Это и
  даёт «сразу в нужный проект без перемещения», но хозяином раскладки остаётся сервер: путь,
  пришедший от ИИ, пришлось бы каждый раз проверять на побег из рабочей области.
- **Инварианты закреплены тестом** (`test_providers` §16), потому что свойство легко потерять:
  достаточно однажды добавить в схему поле `target_path`. Тест краснеет на само появление
  адресного поля во входе инструмента.
- **Следствие для R2/R6:** раннер получает `source` и `target` **от сервера** и обязан прогнать оба
  через свой containment — доверия к вызывающему у него не больше, чем у сервера к клиенту. В
  контейнере `workspace` монтируется по тому же пути, иначе target из запроса не сойдётся с диском.

## Шаги

### R1. Контракт + провайдер в декларации
Объявить `Local_runner` в `adapters.by_provider` (адаптер — существующий `HttpImageAPI`) и блок в
`online.http` (адрес `http://127.0.0.1:<порт>`, `send_mode`, поля). Здесь же — исключение для
`http://` на loopback: сейчас адаптер требует https, и это правильно для внешней сети, но
запрещать петлевой адрес значит запрещать саму идею раннера. Исключение объявляется данными
(`allow_insecure_host: 127.0.0.1`), а не снятием проверки.
**Готово, когда:** строка канала с `Local_runner` доходит до попытки вызова и честно падает
«раннер не запущен» (кодом реестра), а не ломается на схеме адреса.
**Сделано.** `Local_runner` объявлен адаптером `img.http_image:HttpImageAPI`, блок в `online.http`.
Адаптер получил три объявленных поля вместо трёх веток в коде: `allow_insecure_host` (петля без
https — чужой хост по http остаётся `DOWNLOAD_FORBIDDEN`), `send_mode: paths`, `response: files`.
Сверх плана понадобились ещё два, и оба про честность отказа: `on_unreachable` (молчащий раннер —
не «повтори позже», а `RUNNER_NOT_RUNNING` с подсказкой на инструмент) и `error_fields` (раннер
называет причину НАШИМ кодом реестра, иначе его `LOCAL_MODEL_MISSING` приехало бы к ИИ общим
`PROVIDER_FAILED` с JSON в тексте). Код реакции заведён в `server_reactions.yaml` и в оба реестра
контракта (`KNOWN_ERROR_CODES`, `KNOWN_FACT_TYPES` — `RunnerState`).

### R2. Сам раннер
`core/runner/server.py` — aiohttp-приложение с `/run` и `/health`, внутри — **существующие**
адаптеры (`onnx_bg`, `onnx_upscale`, `piper_local`, `diffusers_local`) и **пул** (`core/providers/pool.py`).
Новой логики инференса не пишем: раннер — транспорт вокруг того, что уже работает.
**Готово, когда:** `python -m core.runner --port N` поднимается и обслуживает `/run` для фона
(проверяется curl'ом на готовой модели), а `/health` показывает содержимое пула.
**Сделано** — `core/runner/service.py` + `__main__.py`, ни одной новой зависимости. Инференс идёт
в потоке (`asyncio.to_thread`), иначе `/health` молчал бы всё время расчёта и «занят» было бы не
отличить от «умер». Уточнение против плана: `/health` отвечает о ЖИВОСТИ без токена (её спрашивают
и супервизор, и HEALTHCHECK контейнера, и это не секрет), а содержимое пула — только с токеном.
Пути раннер разрешает своим `safe_resolve` и проверяет тем же `write_policy`, что и сервер.

### R3. Управление жизненным циклом + инструмент
`media_runner(action=start|stop|status)`. Под капотом — супервизор процесса по образцу
`tunnel.py` (Popen, ожидание готовности по `/health`, backoff, `status`), состояние — на диске,
как у `installer.py`. Сервер **не поднимает раннер сам**: только по команде (S15).
**Готово, когда:** инструмент поднимает раннер, `status` показывает `running`+`pid`+`uptime`,
`stop` останавливает, а падение раннера видно как `stale`/`exited`, а не как молчание.
**Сделано** — `core/runner/supervisor.py` + `media_runner(action, mode)`, инструментов стало 68.
Авто-перезапуска у супервизора НЕТ намеренно, в отличие от `tunnel.py`: связь рвётся сама и сама
чинится, а раннер падает по причине — тихо поднять его значит спрятать причину и уронить снова.
Фаз пять, и молчания среди них нет: `stopped`, `running`, `running_silent` (процесс есть, ответа
нет), `exited` (умер — вместе с хвостом журнала), `foreign` (на порту кто-то чужой).
🟠 **Найдено тестом, а не рассуждением:** убитый раннер числился `running_silent` вечно. Он
ребёнок сервера, и мёртвый остаётся зомби, пока его не подберут, а `kill -0` на зомби отвечает
«жив» — то самое молчание вместо причины, ради которого раннер и выносили. Лечится `waitpid(WNOHANG)`
перед проверкой (`_alive`). Регрессия в §17 краснела до фикса.

### R4. Переключение и деградация
Строка канала выбирает: `Local_onnx_bg` (в процессе) или `Local_runner` (в раннере) — обычный
`table_update`. Раннер не запущен → отказ с кодом реестра и подсказкой поднять его инструментом;
**молча падать обратно в процесс нельзя** — это скрыло бы факт, что изоляции нет.
**Готово, когда:** тест доказывает оба пути и честный отказ между ними.
**Сделано** — и оба пути, и отказ между ними доказаны живьём на `modnet`: одна и та же работа
считается в раннере (`Local_runner`) и в сервере (`Local_onnx_bg`) после правки одной строки, без
перезапуска. Отказ приходит дважды разной дорогой: раннера не поднимали (токена нет — отвечает
`media_generate`) и раннер упал (токен есть, порт молчит — отвечает `on_unreachable` адаптера).

### R5. Тесты
Стенд, как для платных провайдеров (S24 доп.1): поднять раннер на свободном порту, прогнать
`media_generate` через `Local_runner`, проверить containment пути, отказ без токена, `/health`.
**Готово, когда:** набор зелёный и не зависит от того, стоит ли Docker.
**Сделано** — `tests/quick/test_providers.py` §17, 27 проверок, весь набор 263/263. Стенд поднимает
настоящий раннер на свободном порту с копией конфига (боевой `vendor/` не трогается) и гоняет
через него настоящую модель. Контейнерный режим проверяется по СОБРАННОЙ команде — publish только
на петлю, токен именем переменной, устройства ядра, тома вместо слоёв, — поэтому Docker набору не
нужен.

### R6. Dockerfile (последним — намеренно)
Образ раннера: `python:3.14-slim` + `pip install -e ".[local,gpu-amd]" --extra-index-url …`,
проброс `--device=/dev/kfd --device=/dev/dri --group-add video`, монтирование `workspace` и
`vendor/models`. Системный ROCm **не нужен**: библиотеки лежат внутри колеса torch, нужен только
драйвер ядра `amdgpu` и доступ к `/dev/kfd` (у нас `0666` — проверено S23).
**Почему последним:** контракт проверяется бесплатно процессом; ошибка в контракте, найденная
после сборки, стоит пересборки образа на несколько гигабайт.
**Готово, когда:** `media_runner(action=start, mode=docker)` поднимает контейнер, и тот же тест R5
проходит против него.

## Риски

- **Двойная память.** Если модель поднята и в сервере, и в раннере — платим дважды. Лечится тем,
  что строка канала выбирает ОДИН путь; при переключении пул сервера стоит освободить.
- **Кэш весов внутри образа.** Класть веса в образ нельзя — это гигабайты на каждую пересборку;
  `vendor/models` монтируется томом (и page cache ядра общий с хостом, так что скорость не теряется).
- **Токен.** Раннер без авторизации на localhost доступен любому процессу пользователя.
  ✅ Закрыт: токен выдаётся при запуске, лежит в состоянии с правами `0600` и сверяется постоянным
  по времени сравнением; без него `/run` отвечает 401. В контейнер уходит ИМЕНЕМ переменной, не
  значением, — иначе его показал бы `ps` любому.
- **Размер образа.** Колёса ROCm огромные (~5–10 ГБ) — это цена изоляции, и её стоит знать заранее.
- 🟡 **Остаточный: порт мог занять посторонний.** Если наш раннер умер, а на его адрес встал чужой
  процесс, `media_generate` пошлёт ему наш токен и покажет ИИ его текст отказа. Токен при этом
  ничего чужого не открывает (он только для нашего же раннера), `media_runner(action='start')`
  такое состояние видит как `foreign` и отказывается поднимать поверх, а текст режется до 300
  символов — как цитата любого внешнего провайдера. Полностью это лечится не токеном, а проверкой
  собеседника; пока считаем приемлемым: адрес петлевой, и попасть туда может только процесс
  пользователя, у которого и так есть его файлы.

## Что изменилось против плана по ходу работы

- **Пути идут ОТНОСИТЕЛЬНЫЕ, а не абсолютные.** План требовал монтировать `workspace` по тому же
  пути, иначе `target` из запроса не сошёлся бы с диском. Относительный путь снимает это условие
  вовсе: каждая сторона разрешает его своим корнем и своим containment, и раннеру больше незачем
  знать, как рабочая область называется у сервера.
- **Хозяин раскладки — сервер, и это проверяется.** Раннер отчитывается путём, но принимается
  файл только по тому пути, который назначил сервер: разошлись — отказ, а не «успех куда-то».
- **Внутри контейнера раннер слушает не петлю.** Опубликованный порт приходит на интерфейс
  контейнера, а не на его loopback, поэтому там нужен `0.0.0.0` и ключ `--container`. Граница при
  этом не ослабла, а переехала: супервизор публикует порт только на петлю ХОСТА
  (`127.0.0.1:порт:порт`). Без ключа раннер по-прежнему отказывается вставать на непетлевой адрес.
