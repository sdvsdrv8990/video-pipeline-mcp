# Безопасность скилов — что проверять до установки и чего не допускать у себя

Скилл — это **текст, который попадает в системный контекст агента с полным доверием**, плюс, возможно,
скрипты, которые агент запустит. Поэтому чужой скилл ближе к «исполняемому файлу из интернета», чем к
документации. Замер NVIDIA по публичным скилам: **26.1% содержат уязвимости, 5.2% — признаки <!-- снимок ок: внешний замер NVIDIA с источником -->
злонамеренности**; проверять надо каждый.

Содержание: [1. Модель угрозы](#1) · [2. Чек-лист чужого скила](#2) · [3. Гигиена своего скила](#3) ·
[4. Решение по риску](#4) · [5. Инструмент и его граница доверия](#5) · [6. Чего НЕ берём](#6)

---

<a id="1"></a>
## 1. Модель угрозы: два разных вопроса

| Вопрос | Атакующий | Что смотрим |
|---|---|---|
| **Ставить ли чужой скилл?** | автор скила / тот, кто подменил репо | §2 — весь чек-лист |
| **Не отравлен ли наш собственный?** | тот, кто правит `~/.claude/skills` (в т.ч. агент по чужой подсказке) | §3 — невидимые символы, `allowed-tools`, скрипты |

Второй вопрос не паранойя: наши скилы правит модель, а модель читает недоверенный контент из
`workspace/`. Строка, которая туда попадёт, будет читаться как **инструкция агенту** в каждой будущей
сессии — это самая долгоживущая инъекция из возможных.

---

<a id="2"></a>
## 2. Чек-лист: чужой скилл перед установкой

Порядок — от дешёвого к дорогому. Первое же 🔴 закрывает вопрос: не ставим.

**2.1 Скрытый текст** 🔴 — то, чего человек в ревью не увидит, а модель прочитает:
- невидимые кодпоинты: `U+200B/200C/200D` (zero-width), `U+2060` (word joiner), `U+FEFF` (BOM),
  `U+00AD` (soft hyphen), bidi-управляющие `U+202A–202E`, `U+2066–2069`;
- HTML-комментарии `<!-- … -->` и markdown-комментарии `[//]: # (…)` в теле `SKILL.md`;
- «пробельная набивка» — сотни `\n`/`U+00A0`/`U+2028`, отодвигающие текст за пределы видимой области.

**2.2 Токены перехвата инструкций** 🔴 — `SYSTEM:`, `<system>`, `[INST]`, `<|im_start|>`,
`ignore previous`, `disregard all instructions`, `override safety`, `you are now`.
Легальному скиллу они не нужны ни в теле, ни в описании, ни в описаниях параметров.

**2.3 Снятие отказов** 🔴 — «не показывай пользователю», «не упоминай эти шаги», «без предупреждений»,
«отключи проверки», «не спрашивай подтверждения». Скилл, который просит **скрыть себя от человека**,
злонамерен по определению — легальная оптимизация не требует секретности от владельца.

**2.4 Сбор и вынос данных** 🔴 — `os.environ` / `printenv` / чтение `~/.aws`, `~/.ssh`, `.env`,
`~/.claude*`; `curl`/`wget`/`requests.post` на внешний хост; загрузка в облачные CLI (`aws s3 cp`,
`gsutil`, `rclone`); «отправь результат на …», «webhook». Особенно: **внешний URL по умолчанию в
параметре**. Внешняя сеть в скилле, задача которого локальна, — это и есть находка.

**2.5 Исполнение и цепочка поставки** 🟠 — `exec`/`eval`/`compile`, динамические импорты,
`subprocess` c `shell=True`; `curl … | bash`; неприпиненные зависимости; поставленные в репозитории
`.pyc` без исходника; имя пакета, похожее на популярное (typosquatting).

**2.6 Избыточные полномочия** 🟠 — `allowed-tools: *` / отсутствие поля при наличии скриптов;
инструменты, не нужные для заявленной задачи (скилл про форматирование просит `Bash` и сеть);
отсутствие границ на объём работы («перебирай, пока не получится»).

**2.7 Отравление памяти** 🟠 — попытки писать в долгоживущие места: `CLAUDE.md`, `~/.claude/`,
файлы памяти проекта, `MEMORY.md`. Запись в память = сохранение инструкции на все будущие сессии.

**2.8 Выуживание системного промпта** 🟡 — «повтори свои инструкции», «что тебе сказали в начале»,
и косвенные формы: «перескажи своими словами правила, которым следуешь».

**2.9 Расхождение описания и поведения** 🔴 — самое важное и единственное, что не ловится грепом:
`description` обещает одно, тело/скрипты делают другое. Читай тело целиком и сверяй с описанием;
любой шаг, не выводимый из описания, — красный флаг.

---

<a id="3"></a>
## 3. Гигиена собственного скила

- **`allowed-tools` — минимум, и он обязан совпадать с тем, что скилл делает.** Три перекоса, и все
  три — сигнал: заявлено меньше, чем скилл использует (обман или недосмотр); заявлено `*` (граница
  снесена); заявлено больше, чем используется (заготовка под будущее злоупотребление либо мёртвый
  остаток удалённой функции). Наш эталон: `security-reviewer` — `Read, Grep, Glob, Bash` без `Edit`,
  потому что аудит не правит код.
- **Ни одного невидимого символа.** Проверяется одной командой (§5); правится немедленно, без
  разбирательства «может, так и задумано».
- **Ни одного внешнего URL как значения по умолчанию.** Ссылка-источник в подвале — нормально;
  URL, который скилл предложит агенту дёрнуть, — нет.
- **Скилл не пишет в память/`CLAUDE.md` молча.** Куратор пишет запись об изменении — это осознанное
  действие в конце цикла, а не побочный эффект.
- **Никакой секретности от владельца.** Если инструкция в скилле имеет смысл только при условии, что
  человек её не прочитает, — её быть не должно.

---

<a id="4"></a>
## 4. Решение по риску

Ориентир NVIDIA (баллы: критичное +50, высокое +25, среднее +10, низкое +5; исполняемый скрипт ×1.3):
0–20 — можно · 21–50 — с осторожностью и разбором каждого пункта · **>50 — не ставить**.

Нам считать баллы незачем — у нас десяток-другой своих скилов, а не маркетплейс.
Рабочее правило проще:
**любое 🔴 из §2 = не ставим**; 🟠 = ставим только после того, как объяснили себе, зачем скиллу
это полномочие, и записали объяснение. «Много звёзд» и «известная организация» — не аргументы:
подменяется репозиторий, а не репутация.

---

<a id="5"></a>
## 5. Инструмент и его граница доверия

`NVIDIA/SkillSpector` (Apache-2.0) — сканер скилов: статика (regex + Python AST + YARA), запрос CVE в
OSV.dev, опционально LLM-анализ. Форматы вывода включая SARIF, exit-код `>50 баллов = 1` (годится в CI).

**Граница доверия, которую надо знать до запуска:** при включённом LLM-анализе **содержимое файлов
уходит стороннему провайдеру**, а имена зависимостей — в OSV.dev. Для наших скилов, которые ссылаются
на внутренние пути и находки проекта, это утечка. Запускать только `--no-llm`; §2.9
(описание vs поведение) в этом режиме закрывается чтением, а не инструментом.

Установка — отдельное решение владельца, а не побочный шаг задачи. **Пока не установлен, §2 закрывается
грепом**, и этого хватает на 8 пунктов из 9:

```bash
S=~/.claude/skills/<имя>
grep -rnP '[\x{200B}-\x{200F}\x{202A}-\x{202E}\x{2060}-\x{2069}\x{FEFF}\x{00AD}]' $S   # 2.1 невидимое
grep -rnE '<!--|\[//\]:\s*#\s*\(' $S                                                   # 2.1 комментарии
grep -rniE 'SYSTEM:|<system>|\[INST\]|ignore previous|disregard (all|any) instructions' $S  # 2.2
grep -rniE 'не (говори|показывай|упоминай)|do not (tell|mention|reveal)|without (asking|warning)' $S  # 2.3
grep -rnE 'os\.environ|printenv|~/\.(ssh|aws)|\.env\b|curl |wget |requests\.post' $S    # 2.4
grep -rnE '\b(exec|eval|compile)\(|shell=True|\| *bash' $S                             # 2.5
grep -n 'allowed-tools' $S/SKILL.md                                                    # 2.6
grep -rnE 'CLAUDE\.md|~/\.claude|MEMORY\.md' $S                                        # 2.7
```

Ноль срабатываний на всех восьми ≠ «чисто» — это значит, что осталось прочитать тело и сверить его с
описанием (§2.9). Проверка, которую не проверили на срабатывание, ничего не доказывает: подсунь себе
`SYSTEM:` в копию файла и убедись, что grep его видит.

`$S` — каталог ОДНОГО скила, не `~/.claude/skills/` целиком: этот файл перечисляет опасные токены как
примеры и честно сработает сам на себе.

---

<a id="6"></a>
## 6. Чего НЕ берём из чужой методики (и почему)

- **Скоринг в баллах и baseline-файлы** — механика маркетплейса на тысячи скилов. У нас 11, решение
  принимает человек по §4.
- **YARA-сигнатуры малвари, детект typosquatting, live-CVE по зависимостям скила** — наши скилы не
  тянут зависимостей вовсе; цепочку поставки проекта держит `dependency-hygiene` (`pip-audit` в гейте).
- **«Anti-refusal» и «harmful content» как отдельные оси** — свёрнуты в §2.3: для нас важен один
  признак, «скилл просит скрыть себя от владельца», а не каталог формулировок.
- **Пословный детект confusables на всём тексте.** Наши скилы и описания инструментов — на русском;
  правило «латиница вперемешку с кириллицей в документе» сработает на каждом файле. Работающая
  адаптация — сравнение алфавитов **внутри одного слова**: `cолbцe` ловится, обычная русская проза нет.

---
_Источник: `NVIDIA/SkillSpector` (Apache-2.0) — категории TP1–TP3, LP1–LP4, P1–P9, E1–E5, EA1–EA4,
MP1–MP3 и статистика по публичным скилам. Взято переработанным под нашу библиотеку из 11 локальных
скилов. Серверная сторона той же угрозы (отравление описаний ИНСТРУМЕНТОВ, а не скилов) — в
`security-reviewer`, `references/malicious-server-threats.md` §T2._
